{
  "name": "@liiiraa/contract-generation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node src/generate.ts",
    "check": "tsc --noEmit",
    "test": "node src/generate.ts --check-paths && node src/check-corpus.mjs && node src/check-drift.test.ts",
    "contracts:check": "node src/check-drift.ts",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@typespec/openapi": "1.14.0",
    "@typespec/compiler": "1.14.0",
    "@typespec/http": "1.14.0",
    "@typespec/json-schema": "1.14.0",
    "@typespec/openapi3": "1.14.0",
    "json-schema-to-typescript": "15.0.4"
  }
}
