{
  "name": "@liiiraa/contract-generation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node src/generate.ts",
    "check": "tsc --noEmit",
    "test": "node src/generate.ts --check-paths && node src/check-corpus.mjs",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@typespec/compiler": "1.14.0",
    "@typespec/http": "1.14.0",
    "@typespec/json-schema": "1.14.0",
    "@typespec/openapi3": "1.14.0"
  }
}
