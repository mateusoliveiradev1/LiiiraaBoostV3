---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '10'
type: execute
wave: 9
depends_on: ['05-09']
files_modified:
  - tooling/phase5-evidence/src/evaluate.ts
  - tooling/phase5-evidence/src/windows-probe.ps1
  - tooling/phase5-evidence/tests/evaluate.test.ts
  - tooling/phase5-evidence/evidence-manifest.schema.json
  - package.json
  - .planning/phases/05-hardware-intelligence-and-measured-evidence/05-UAT.md
autonomous: false
requirements:
  [
    DIAG-01,
    DIAG-02,
    DIAG-03,
    DIAG-04,
    DIAG-05,
    DIAG-06,
    DIAG-07,
    MEAS-01,
    MEAS-02,
    MEAS-03,
    MEAS-04,
    MEAS-05,
    MEAS-06,
  ]
must_haves:
  truths:
    - 'D-17: CI owns contract, conformance, migration, policy, trace, fault, UI, accessibility, report, tamper and deterministic resource gates.'
    - 'D-18: Automated gates distinguish deterministic CI evidence from packaged physical Windows 10/11 measurements and never record simulation as physical PASS.'
    - 'D-19: Remaining Phase 4 physical gates remain explicit and are not silently inherited before Phase 5 release completion.'
  artifacts:
    - path: tooling/phase5-evidence/src/evaluate.ts
      provides: Phase 5 evidence admission and release-gate evaluator
      min_lines: 260
    - path: tooling/phase5-evidence/evidence-manifest.schema.json
      provides: Machine-verifiable deterministic/physical evidence distinction
      min_lines: 100
  key_links:
    - from: tooling/phase5-evidence/src/windows-probe.ps1
      to: tooling/phase5-evidence/src/evaluate.ts
      via: signed/hashed packaged evidence manifest
---

<objective>Automate the complete Phase 5 evidence gate and isolate only truly physical matrix checks for final confirmation.</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-VALIDATION.md
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-SPEC.md
@tooling/phase4-evidence/src/evaluate.ts
@apps/desktop/tests/browser/accessibility.spec.ts
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build deterministic and packaged evidence admission</name>
  <files>tooling/phase5-evidence/src/evaluate.ts, tooling/phase5-evidence/src/windows-probe.ps1, tooling/phase5-evidence/tests/evaluate.test.ts, tooling/phase5-evidence/evidence-manifest.schema.json, package.json</files>
  <behavior>
    - A simulated manifest can satisfy deterministic gates but never a physical requirement.
    - Missing OS/build/hardware/source/budget/privacy evidence fails with exact blockers.
    - Memory >25 MB, idle CPU >0.5%, polling >1 Hz, cancellation timeout, or raw identifier leakage fails.
    - Phase 4 physical gaps remain separately named per D-19.
  </behavior>
  <action>Following the Phase 4 evaluator pattern, add a schema and evaluator for D-17 through D-19, plus a read-only packaged Windows probe that records exact environment, source coverage, process budget, polling, cancellation, privacy scan, report integrity, and whether the run is physical. Add root `phase5:verify` scripts without Docker.</action>
  <verify><automated>pnpm exec vitest --run tooling/phase5-evidence/tests/evaluate.test.ts</automated></verify>
  <done>Every deterministic gate is automated and the evaluator cannot confuse fixture, local development, packaged, and physical evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Admit the packaged Windows hardware matrix</name>
  <files>.planning/phases/05-hardware-intelligence-and-measured-evidence/05-UAT.md</files>
  <action>First run all automation on the current Windows PC and attach the generated manifest. Only after deterministic checks and current-PC packaged probes pass, request the minimum remaining physical Windows 10/11 matrix confirmation. Record exact OS build, hardware classes, collector version, accessibility walkthrough, and failures; never translate an unrun matrix cell into PASS.</action>
  <verify>
    <automated>pnpm phase5:verify -- --mode final</automated>
    <human-check>Confirm only the remaining physical matrix cells identified by the evaluator; no repeated browser workflow is required.</human-check>
  </verify>
  <done>All automatable work is green, current-PC physical evidence is admitted, and any unavailable second-machine gate is explicitly pending rather than hidden.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                  |
| ------------------------------------- | ------------------------------------------------------------ |
| Test/probe output -> release evidence | Generated files can falsely claim a physical or passing run. |

## STRIDE Threat Register

| Threat ID       | Category    | Component         | Disposition | Mitigation Plan                                                    |
| --------------- | ----------- | ----------------- | ----------- | ------------------------------------------------------------------ |
| T-05-19         | Spoofing    | evidence manifest | mitigate    | environment identity, run-kind field, hashes, strict evaluator     |
| T-05-20         | Repudiation | UAT/release gate  | mitigate    | exact command, timestamps, versions, immutable artifact references |
| </threat_model> |

<verification>Run `pnpm phase5:verify -- --mode final`; it must either pass with admitted evidence or list exact honest blockers.</verification>
<success_criteria>Repetitive Phase 5 verification is automatic; only genuinely unavailable physical hardware validation reaches the user.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-10-SUMMARY.md` when done.</output>
