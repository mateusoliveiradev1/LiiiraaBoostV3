---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '08'
type: execute
wave: 7
depends_on: ['05-07']
files_modified:
  - packages/desktop-client/src/evidence.ts
  - packages/desktop-client/src/evidence.test.ts
  - packages/desktop-client/src/conformance.test.ts
  - packages/desktop-client/src/index.ts
autonomous: true
requirements:
  [
    DIAG-01,
    DIAG-03,
    DIAG-04,
    DIAG-05,
    DIAG-06,
    MEAS-01,
    MEAS-02,
    MEAS-03,
    MEAS-04,
    MEAS-05,
    MEAS-06,
  ]
must_haves:
  truths:
    - 'D-11: Real Tauri and deterministic adapters expose the same immutable projections behind the existing Measure routes and authored UI.'
    - 'D-12: Production recursively refuses fixture provenance while explicit development and test composition remain deterministic.'
    - 'Refresh preserves the last admitted snapshot and capture inputs never reset during revalidation.'
  artifacts:
    - path: packages/desktop-client/src/evidence.ts
      provides: Evidence authority port, Tauri adapter, fixture adapter, subscriptions and commands
      min_lines: 300
  key_links:
    - from: packages/desktop-client/src/evidence.ts
      to: apps/desktop/src-tauri/src/main.rs
      via: typed Tauri invoke command names
---

<objective>Create the frontend authority boundary that isolates native commands from authored product surfaces.</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@packages/desktop-client/src/experience.ts
@packages/desktop-client/src/conformance.test.ts
@apps/desktop/src/composition.tsx
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock adapter conformance and production truth</name>
  <files>packages/desktop-client/src/evidence.test.ts, packages/desktop-client/src/conformance.test.ts</files>
  <behavior>
    - Native and deterministic adapters share read/refresh/start/cancel/compare/export semantics.
    - Production rejects fixture provenance at any nested path.
    - Stale retained data remains visible but non-actionable during refresh.
  </behavior>
  <action>Extend conformance suites for D-11 through D-13 and every generated state. Include race tests for refresh/cancel, subscription cleanup, stable selections, and no input-reset behavior.</action>
  <verify><automated>pnpm --filter @liiiraa/desktop-client exec vitest --run</automated></verify>
  <done>Both adapters pass identical behavior tests and fixture leakage fails with a stable path/code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evidence authority and route projections</name>
  <files>packages/desktop-client/src/evidence.ts, packages/desktop-client/src/index.ts</files>
  <behavior>
    - One authority caches server/native state without duplicating domain truth.
    - Comparison projection is reused unchanged by summary/diff/timeline/report consumers.
  </behavior>
  <action>Implement immutable ports/adapters using generated validators, AbortSignal, subscriptions, and exact command names from plan 07. Keep domain policy in Rust, presentation localization in UI, and deterministic fixtures only behind explicit test composition.</action>
  <verify><automated>pnpm --filter @liiiraa/desktop-client exec vitest --run</automated></verify>
  <done>The frontend has one typed evidence authority with no handwritten DTOs or production fixture fallback.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                         |
| ----------------------------- | ------------------------------------------------------------------- |
| Tauri response -> React state | Native/persisted data must still pass generated runtime validation. |

## STRIDE Threat Register

| Threat ID       | Category  | Component          | Disposition | Mitigation Plan                                |
| --------------- | --------- | ------------------ | ----------- | ---------------------------------------------- |
| T-05-15         | Tampering | adapter payload    | mitigate    | generated validation and immutable projections |
| T-05-16         | Spoofing  | fixture provenance | mitigate    | recursive production fixture refusal           |
| </threat_model> |

<verification>Run client conformance and production-truth tests.</verification>
<success_criteria>Every desktop evidence surface consumes the same validated authority semantics.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-08-SUMMARY.md` when done.</output>
