---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '11'
type: execute
wave: 10
depends_on: ['05-10']
autonomous: true
requirements: [DIAG-03, DIAG-04, DIAG-06, MEAS-01, MEAS-02, MEAS-03]
---

<objective>Replace the remaining desktop demonstration telemetry and empty capture path with truthful, read-only Windows samples.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add a bounded native live telemetry authority</name>
  <files>apps/desktop/src-tauri/src/live_telemetry.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src/native/live-telemetry.ts</files>
  <behavior>CPU and memory are read from documented Windows APIs, collection latency is measured, unavailable GPU evidence remains unavailable, no raw identifier crosses the native boundary, and polling never exceeds one hertz.</behavior>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Connect production home and measurement capture</name>
  <files>apps/desktop/src/features/premium-operations-production.tsx, apps/desktop/src/app.tsx, packages/desktop-client/src/evidence.ts, packages/feature-shell/src/features/measure.tsx</files>
  <behavior>The packaged home renders native inventory and live readings instead of fixture numbers; active captures admit bounded native counter samples and never finalize as a fabricated result.</behavior>
</task>
<task type="auto">
  <name>Task 3: Verify the packaged experience and update phase evidence</name>
  <files>.planning/phases/05-hardware-intelligence-and-measured-evidence/05-UAT.md, .planning/phases/05-hardware-intelligence-and-measured-evidence/05-11-SUMMARY.md</files>
  <behavior>Tests, build, packaged probe, and source scans prove that the four hard-coded telemetry values and demonstration label are absent from the production path.</behavior>
</task>
</tasks>

<verification>Run desktop TypeScript/Rust tests, build the production desktop, run the current-PC probe, and execute the Phase 5 evidence evaluator without Docker.</verification>

