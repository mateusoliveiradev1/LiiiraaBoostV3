---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts-source/src/hardware-evidence.tsp
  - packages/contracts-source/src/main.tsp
  - packages/contracts-ts/src/generated/models.ts
  - packages/contracts-ts/src/generated/standalone-validators.js
  - crates/contracts-rust/src/generated.rs
  - packages/contracts-ts/src/hardware-evidence.test.ts
autonomous: true
requirements:
  [
    DIAG-01,
    DIAG-02,
    DIAG-03,
    DIAG-04,
    DIAG-05,
    DIAG-06,
    DIAG-07,
    MEAS-01,
    MEAS-02,
    MEAS-03,
    MEAS-04,
    MEAS-05,
    MEAS-06,
  ]
must_haves:
  truths:
    - 'D-02: Every inventory, capture, comparison, report, and claim boundary has one generated schema with typed source capability, deadline, cancellation, health, unavailable and quality states.'
    - 'D-10: Invalid, fabricated numeric, or fixture-shaped production evidence is rejected by runtime validation.'
  artifacts:
    - path: packages/contracts-source/src/hardware-evidence.tsp
      provides: Canonical Phase 5 transport and durable-document contracts
      min_lines: 220
    - path: packages/contracts-ts/src/hardware-evidence.test.ts
      provides: Cross-language schema and invalid-corpus assertions
      min_lines: 100
  key_links:
    - from: packages/contracts-source/src/main.tsp
      to: packages/contracts-source/src/hardware-evidence.tsp
      via: TypeSpec import
    - from: packages/contracts-source/src/hardware-evidence.tsp
      to: packages/contracts-ts/src/generated/models.ts
      via: checked contract generation
---

<objective>
Define the complete language-neutral evidence vocabulary before native or UI implementation.

Purpose: All later plans consume one generated, runtime-validated authority instead of duplicating DTOs.
Output: TypeSpec models, generated TypeScript/Rust transports, validators, and a shared invalid corpus.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-SPEC.md
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-CONTEXT.md
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-RESEARCH.md
@packages/contracts-source/src/desktop-inspection.tsp
@packages/contracts-source/src/provenance.tsp
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock evidence contract behavior with valid and invalid corpora</name>
  <files>packages/contracts-source/src/hardware-evidence.tsp, packages/contracts-ts/src/hardware-evidence.test.ts</files>
  <behavior>
    - Inventory keeps all required classes and represents absence with a typed reason.
    - Sessions distinguish incomplete, completed, degraded, and invalid evidence without numeric placeholders.
    - Comparisons carry all blocker codes or an immutable accepted projection, never both.
    - Reports and claims reference versioned evidence identities, hashes, provenance, and limitations.
  </behavior>
  <action>Create bounded TypeSpec scalars/enums/models/envelopes for inventory observations and snapshots, lifecycle/capability verdicts, collector health/overhead, baselines, metric chunks, sessions, comparisons, reports, and claim admissions. Model D-02, D-03, D-09, D-10, D-14, D-15, and D-16 explicitly. Follow `desktop-inspection.tsp`; do not handwrite TypeScript/Rust transport mirrors.</action>
  <verify><automated>pnpm --filter @liiiraa/contracts-ts exec vitest --run src/hardware-evidence.test.ts</automated></verify>
  <done>The corpus proves required categories cannot disappear, raw identifiers are absent, invalid states fail validation, and accepted/rejected comparison shapes are mutually exclusive.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify TypeScript and Rust contract artifacts</name>
  <files>packages/contracts-source/src/main.tsp, packages/contracts-ts/src/generated/models.ts, packages/contracts-ts/src/generated/standalone-validators.js, crates/contracts-rust/src/generated.rs</files>
  <action>Import the new domain from `main.tsp`, regenerate checked artifacts with existing tooling, export validators, and add contract compatibility coverage. Preserve generator ownership and production fixture refusal per D-12.</action>
  <verify><automated>pnpm test:contracts</automated></verify>
  <done>Regeneration is clean, TS/Rust models agree, validators reject the invalid corpus, and no handwritten duplicate DTO exists.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                            |
| --------------------------------------------------- | ------------------------------------------------------ |
| Native/persisted/exported bytes -> generated models | Untrusted evidence must be validated before admission. |

## STRIDE Threat Register

| Threat ID       | Category               | Component           | Disposition | Mitigation Plan                                                 |
| --------------- | ---------------------- | ------------------- | ----------- | --------------------------------------------------------------- |
| T-05-01         | Tampering              | evidence documents  | mitigate    | bounded schemas, deny unknown fields, hashes and invalid corpus |
| T-05-02         | Information disclosure | inventory transport | mitigate    | contract has no raw stable identifier fields                    |
| T-05-SC         | Tampering              | Cargo packages      | mitigate    | only VERIFIED packages from the research audit may be installed |
| </threat_model> |

<verification>Run contract generation twice and confirm the second run produces no diff.</verification>
<success_criteria>All 13 requirement families have generated, validated transport coverage used by downstream plans.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-01-SUMMARY.md` when done.</output>
