---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '07'
type: execute
wave: 6
depends_on: ['05-03', '05-05', '05-06']
files_modified:
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/tests/evidence_commands.rs
  - apps/desktop/src-tauri/capabilities/default.json
autonomous: true
requirements: [DIAG-01, DIAG-07, MEAS-01, MEAS-02, MEAS-05]
must_haves:
  truths:
    - 'The non-elevated desktop can invoke only operation-specific inventory, capture, comparison, and export commands.'
    - 'Malformed requests and unavailable authority return typed recovery states without crashing or hanging the app.'
  artifacts:
    - path: apps/desktop/src-tauri/tests/evidence_commands.rs
      provides: Tauri command boundary and cancellation tests
      min_lines: 160
  key_links:
    - from: apps/desktop/src-tauri/src/main.rs
      to: apps/desktop/src-tauri/src/hardware_inventory.rs
      via: refresh command
    - from: apps/desktop/src-tauri/src/main.rs
      to: apps/desktop/src-tauri/src/measurement.rs
      via: start/cancel capture commands
---

<objective>Expose the native evidence authority through a narrow non-elevated Tauri boundary.</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@apps/desktop/src-tauri/src/main.rs
@apps/desktop/src-tauri/src/device_identity.rs
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify command admission and cancellation behavior</name>
  <files>apps/desktop/src-tauri/tests/evidence_commands.rs</files>
  <behavior>
    - Read/refresh/list/open operations validate generated request envelopes.
    - Start/cancel is idempotent and cancellation remains reachable during source failure.
    - Export writes only an admitted report to an explicitly selected local path.
  </behavior>
  <action>Write command-level tests for inventory refresh, snapshot read, baseline/session lifecycle, comparison request, report export, health/overhead read, malformed requests, missing authority, and cancellation. Assert D-01/D-02 narrow authority and D-03 privacy at serialization.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test evidence_commands</automated></verify>
  <done>The boundary rejects generic filesystem/registry/script/elevation behavior and exposes stable typed results.</done>
</task>

<task type="auto">
  <name>Task 2: Register least-privilege evidence commands</name>
  <files>apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/capabilities/default.json</files>
  <action>Register operation-specific commands and managed authority state following existing `generate_handler!` patterns. Validate all envelopes, map errors to generated codes, keep the renderer non-elevated, and grant only required Tauri capabilities. Do not expose raw SQL, generic queries, raw stable identifiers, shell execution, or arbitrary report paths.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test evidence_commands</automated></verify>
  <done>Every Phase 5 native workflow is reachable through a tested allowlisted command and no broader capability exists.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                      |
| ------------------------------- | ------------------------------------------------ |
| React WebView -> Tauri commands | Renderer input is untrusted despite being local. |

## STRIDE Threat Register

| Threat ID       | Category               | Component        | Disposition | Mitigation Plan                                               |
| --------------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------- |
| T-05-13         | Elevation of privilege | command surface  | mitigate    | explicit allowlist, validated envelopes, non-elevated process |
| T-05-14         | Information disclosure | command response | mitigate    | generated redacted projections only                           |
| </threat_model> |

<verification>Command tests pass and capability diff contains no shell or broad filesystem permission.</verification>
<success_criteria>The renderer has the smallest useful evidence API and cannot bypass persistence or policy authority.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-07-SUMMARY.md` when done.</output>
