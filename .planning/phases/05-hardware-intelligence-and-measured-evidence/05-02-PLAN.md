---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '02'
type: tdd
wave: 2
depends_on: ['05-01']
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/evidence_store.rs
  - apps/desktop/src-tauri/src/evidence_store/migrations.rs
  - apps/desktop/src-tauri/tests/evidence_store.rs
autonomous: true
requirements: [DIAG-05, MEAS-01, MEAS-03, MEAS-04, MEAS-05, MEAS-06]
must_haves:
  truths:
    - 'D-04: SQLite is the append-oriented local authority and completed evidence survives restart with the same schema version and content hash.'
    - 'D-05: Session metadata commits transactionally and interrupted or corrupt evidence remains inspectable but cannot be admitted.'
    - 'D-06: Versioned sample chunks are bounded, hashed, ordered monotonically, and retained while referenced.'
  artifacts:
    - path: apps/desktop/src-tauri/src/evidence_store.rs
      provides: Append-oriented SQLite evidence authority
      min_lines: 300
    - path: apps/desktop/src-tauri/tests/evidence_store.rs
      provides: Migration, interruption, integrity, and retention tests
      min_lines: 220
  key_links:
    - from: apps/desktop/src-tauri/src/evidence_store.rs
      to: liiiraa_contracts_rust
      via: generated durable document validation
---

<objective>
Create the durable local evidence authority.

Purpose: Evidence must be transactional, inspectable after restart, tamper-detecting, and safe under interruption.
Output: SQLite migrations, repository API, integrity/retention policy, and failure tests.
</objective>

<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-RESEARCH.md
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-CONTEXT.md
@apps/desktop/src-tauri/src/offline_entitlement.rs
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify transactional lifecycle and migration invariants</name>
  <files>apps/desktop/src-tauri/tests/evidence_store.rs, apps/desktop/src-tauri/src/evidence_store/migrations.rs, apps/desktop/src-tauri/Cargo.toml</files>
  <behavior>
    - A completed baseline/session reopens byte-for-byte with its content hash.
    - Cancellation/crash leaves an inspectable incomplete session that comparison queries exclude.
    - Hash mismatch, foreign-key failure, or malformed generated document fails closed.
    - Retention never removes evidence referenced by a retained comparison/report/claim.
  </behavior>
  <action>Add the VERIFIED `rusqlite` and `rusqlite_migration` pins from the research audit. Write failing temporary-database tests and ordered embedded migrations for D-04 through D-06, WAL, foreign keys, bounded busy timeout, chunks, health, comparisons, reports, claims, and retention leases.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test evidence_store</automated></verify>
  <done>Tests cover clean migration, every supported upgrade, restart, interruption, corruption, busy database, and reference-protected retention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement append-oriented repository transactions</name>
  <files>apps/desktop/src-tauri/src/evidence_store.rs</files>
  <behavior>
    - Snapshot/session/chunk writes are atomic at their defined boundaries.
    - Completion changes state only after metadata, chunks, health, and hashes validate.
    - Reads return generated validated documents and stable error codes.
  </behavior>
  <action>Implement an `EvidenceStore` API following the fail-closed authority pattern in `offline_entitlement.rs`. Canonicalize JSON before SHA-256 hashing, keep dense samples in bounded versioned chunks, preserve append-only history, expose explicit incomplete/corrupt/unavailable results, and run quick integrity validation at open.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test evidence_store</automated></verify>
  <done>All repository invariants pass and no browser/Tauri Store/flat JSON path can become evidence authority.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                      |
| ---------------------- | ---------------------------------------------------------------- |
| Process -> SQLite file | Local corruption, tamper, partial writes, and concurrent access. |

## STRIDE Threat Register

| Threat ID       | Category          | Component             | Disposition | Mitigation Plan                                                |
| --------------- | ----------------- | --------------------- | ----------- | -------------------------------------------------------------- |
| T-05-03         | Tampering         | SQLite evidence       | mitigate    | transactions, foreign keys, canonical hashes, integrity checks |
| T-05-04         | Denial of service | store locks/retention | mitigate    | busy timeout, bounded chunks, cancellation, reference leases   |
| T-05-SC         | Tampering         | Cargo packages        | mitigate    | exact VERIFIED pins from `05-RESEARCH.md`                      |
| </threat_model> |

<verification>`cargo test -p liiiraa-desktop --test evidence_store` passes from a clean temporary directory.</verification>
<success_criteria>SQLite is the sole local authority and every invalid durable state remains non-actionable.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-02-SUMMARY.md` when done.</output>
