---
phase: 05-hardware-intelligence-and-measured-evidence
plan: '03'
type: tdd
wave: 3
depends_on: ['05-01', '05-02']
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/hardware_inventory.rs
  - apps/desktop/src-tauri/src/windows_lifecycle.rs
  - apps/desktop/src-tauri/tests/hardware_inventory.rs
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-07]
must_haves:
  truths:
    - 'D-01: The collector is unprivileged and read-only; elevation is introduced only behind a narrow authenticated source-specific broker when proven necessary.'
    - 'D-02: Every required hardware class is observed or explicitly unavailable from a named typed source with bounded execution.'
    - 'Windows lifecycle is classified from edition/build/channel evidence and unknown fails closed.'
    - 'D-03: No raw serial or protected hardware identifier leaves the native boundary.'
  artifacts:
    - path: apps/desktop/src-tauri/src/hardware_inventory.rs
      provides: Read-only source traits and Windows inventory orchestration
      min_lines: 360
    - path: apps/desktop/src-tauri/src/windows_lifecycle.rs
      provides: Versioned Windows lifecycle classifier
      min_lines: 160
  key_links:
    - from: apps/desktop/src-tauri/src/hardware_inventory.rs
      to: apps/desktop/src-tauri/src/evidence_store.rs
      via: validated redacted inventory snapshot append
---

<objective>Collect complete privacy-safe Windows inventory and lifecycle evidence through bounded documented APIs.</objective>
<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@apps/desktop/src-tauri/src/device_identity.rs
@apps/desktop/src-tauri/tests/device_identity.rs
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-RESEARCH.md
@.planning/phases/05-hardware-intelligence-and-measured-evidence/05-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define source, redaction, timeout, and lifecycle fixtures</name>
  <files>apps/desktop/src-tauri/tests/hardware_inventory.rs, apps/desktop/src-tauri/src/windows_lifecycle.rs</files>
  <behavior>
    - Windows 11, Windows 10 LTSC/ESU, unsupported consumer Windows 10, and unknown fixtures classify exactly.
    - Missing, contradictory, timed-out, or permission-denied classes remain visible as unavailable.
    - Renaming display/model strings cannot change lifecycle or capability evidence.
    - Serialized/logged/exportable projections contain no raw serial, MAC, or protected instance ID.
  </behavior>
  <action>Write synthetic collector traits/fixtures and lifecycle policy tables first. Cover every DIAG-01 class and D-01 through D-03. Record source, collected-at, quality, support, deadline outcome, and redacted/derived reference. Keep licensing `device_identity.rs` separate.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test hardware_inventory</automated></verify>
  <done>All category, lifecycle, timeout, contradiction, and privacy cases fail/pass deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement documented Windows source adapters</name>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/hardware_inventory.rs</files>
  <behavior>
    - The orchestrator collects CPU, GPU, memory, storage, network, display, audio, USB, OS, drivers, security, and installed-game evidence read-only.
    - Per-source timeout/cancellation cannot block startup or shutdown.
  </behavior>
  <action>Extend only the existing Microsoft `windows` crate feature set. Implement source-specific adapters using SetupAPI/Configuration Manager, Core Audio, IP Helper, documented system/security APIs, and bounded WMI only where necessary per research. Persist the validated projection through `EvidenceStore`; never use PowerShell, generic registry RPC, broad elevation, or name recipes.</action>
  <verify><automated>cargo test -p liiiraa-desktop --test hardware_inventory</automated></verify>
  <done>On non-Windows CI the source traits/fault corpus pass; on Windows a smoke record contains every category or a reasoned unavailable state.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                     |
| --------------------------------------- | --------------------------------------------------------------- |
| Windows device APIs -> native collector | Driver/provider data is untrusted and may be malformed or slow. |

## STRIDE Threat Register

| Threat ID       | Category               | Component       | Disposition | Mitigation Plan                                                  |
| --------------- | ---------------------- | --------------- | ----------- | ---------------------------------------------------------------- |
| T-05-05         | Information disclosure | identifiers     | mitigate    | native redaction/derivation before persistence or serialization  |
| T-05-06         | Denial of service      | source adapters | mitigate    | per-source deadlines, cancellation, size bounds, concurrency cap |
| T-05-SC         | Tampering              | dependencies    | mitigate    | reuse existing verified Microsoft crate only                     |
| </threat_model> |

<verification>Run inventory tests and the platform-gated smoke without elevation.</verification>
<success_criteria>Inventory is complete-or-explicitly-unavailable, privacy-safe, bounded, and lifecycle-aware.</success_criteria>
<output>Create `.planning/phases/05-hardware-intelligence-and-measured-evidence/05-03-SUMMARY.md` when done.</output>
