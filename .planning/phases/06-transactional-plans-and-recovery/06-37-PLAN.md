---
phase: 06-transactional-plans-and-recovery
plan: '37'
type: tdd
wave: 14
depends_on: ['06-30', '06-36']
files_modified:
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/src/plan_executor.rs
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/tests/broker_protocol.rs
  - apps/desktop/src-tauri/tests/recovery_executor.rs
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'In the dedicated physical build, apply and restore Tauri commands use a real Windows named-pipe transport to the installed broker instead of returning BrokerUnavailable.'
    - 'The transport performs the authenticated framed handshake and request exchange against the fixed local endpoint with bounded sizes/timeouts and no fixture or network fallback.'
    - 'A disconnected, malformed, timed-out, or uncertain broker exchange remains observation-required and is reconciled from the durable journal without blindly redispatching.'
  artifacts:
    - path: apps/desktop/src-tauri/src/plan_executor.rs
      provides: WindowsNamedPipeBrokerTransport and native broker-backed execution authority
      exports: [WindowsNamedPipeBrokerTransport, NativePhysicalExecutionAuthority]
    - path: apps/desktop/src-tauri/src/main.rs
      provides: Physical-build construction and Tauri apply/restore routing to the authenticated broker
    - path: apps/desktop/src-tauri/tests/broker_protocol.rs
      provides: Handshake/framing/identity/timeout/no-fallback transport proof
    - path: apps/desktop/src-tauri/tests/recovery_executor.rs
      provides: Tauri apply/restore through transport plus observation-first uncertainty proof
  key_links:
    - from: apps/desktop/src-tauri/src/main.rs
      to: apps/desktop/src-tauri/src/plan_executor.rs
      via: phase6-physical feature constructs WindowsNamedPipeBrokerTransport and injects it into native execution authority
      pattern: 'WindowsNamedPipeBrokerTransport.*NativePhysicalExecutionAuthority'
    - from: apps/desktop/src-tauri/src/plan_executor.rs
      to: '\\.\pipe\LiiiraaBoost\optimizer-v1'
      via: fixed local-only length-prefixed authenticated exchange
      pattern: 'LiiiraaBoost.*optimizer-v1'
    - from: apps/desktop/src-tauri/src/main.rs
      to: apply_plan/restore_plan/restore_plan_operation/restore_recovery_checkpoint
      via: physical commands call PlanExecutor::execute with authenticated broker client
      pattern: 'execute_physical_plan'
---
<objective>Close the installed desktop-to-service gap with a real authenticated Windows named-pipe transport and physical-only Tauri wiring.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-30-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-36-SUMMARY.md
@apps/desktop/src-tauri/src/main.rs
@apps/desktop/src-tauri/src/plan_executor.rs
@apps/desktop/src-tauri/tests/broker_protocol.rs
@apps/desktop/src-tauri/tests/recovery_executor.rs
<interfaces>
`BrokerTransport` exposes `authenticate(&mut self) -> BrokerSessionMaterial` and `exchange(&mut self, frame: &[u8]) -> Vec<u8>`. `AuthenticatedBrokerClient<T>` already builds bounded MAC-bound generated envelopes. `PlanExecutor::execute` accepts a mutable `PrivilegedBrokerPort`; `reject_unavailable_mutation` currently stops every physical command. Add Cargo feature `phase6-physical`. Only that feature may construct the real transport; ordinary development/fixture builds retain explicit BrokerUnavailable and may not silently select fixtures after a physical failure.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the installed desktop named-pipe execution path</name>
  <read_first>apps/desktop/src-tauri/src/main.rs; apps/desktop/src-tauri/src/plan_executor.rs; apps/desktop/src-tauri/Cargo.toml; apps/desktop/src-tauri/tests/broker_protocol.rs; apps/desktop/src-tauri/tests/recovery_executor.rs; 06-CONTEXT.md D-04, D-05, D-19, D-21 through D-24, D-31; 06-30 authenticated handshake/frame contract</read_first>
  <files>apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/src/plan_executor.rs, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/tests/broker_protocol.rs, apps/desktop/src-tauri/tests/recovery_executor.rs</files>
  <behavior>
    - `WindowsNamedPipeBrokerTransport` opens only the fixed local endpoint, verifies local pipe/server identity, performs the exact generated handshake, and exchanges fixed length-prefixed frames within 64 KiB and finite connect/read/write deadlines.
    - Physical-build startup constructs one reconnectable authenticated broker authority and injects it beside the recovery store; `apply_plan`, all three restore commands, startup reconciliation, and tray resume call the real executor path.
    - Nonphysical builds remain explicitly unavailable for mutation; once `phase6-physical` is compiled, pipe absence/auth failure/timeout/malformed response never falls back to fixtures, browser adapters, callbacks, TCP, shell, or a second transport.
    - Response loss or disconnect after durable reservation returns unknown/observation-required; reconnect first observes/reconciles journal and Windows state and never repeats the mutation automatically.
  </behavior>
  <action>RED: extend broker-protocol tests with a scripted byte pipe and Windows-gated local-pipe cases for handshake bytes, server identity, partial/oversized frames, finite deadlines, disconnect, reconnect, malformed response, wrong endpoint, and total absence of fallback; extend recovery-executor tests to call each Tauri intent through a fake `BrokerTransport`, prove one dispatch on apply/restore, and prove response-loss/startup reconciliation without redispatch. Commit `test(06-37): add failing desktop pipe transport path`. GREEN: implement `WindowsNamedPipeBrokerTransport` with the already pinned Windows pipe/file APIs, fixed endpoint, explicit local/server verification, exact frame reads/writes, and bounded timeouts. Introduce a `NativePhysicalExecutionAuthority` that owns `PlanExecutor<RecoveryStore>` plus `AuthenticatedBrokerClient<WindowsNamedPipeBrokerTransport>` under serialization. Under Cargo feature `phase6-physical`, construct that authority at Tauri startup and replace `reject_unavailable_mutation` for apply/restore/recovery with native recomputation, durable prepare, `PlanExecutor::execute`, ordered progress, and observation-first reconciliation. Keep the unavailable path only for nonphysical builds; never use `catch { fixture }`, `executePhysicalMutation`, TCP, arbitrary endpoints, or renderer-supplied authority per D-31. Commit `feat(06-37): connect desktop to optimizer named pipe`. REFACTOR after physical/nonphysical compile matrices and focused tests pass.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test broker_protocol --test recovery_executor &amp;&amp; rtk cargo check -p liiiraa-desktop --features phase6-physical --target x86_64-pc-windows-msvc &amp;&amp; rtk cargo check -p liiiraa-desktop --target x86_64-pc-windows-msvc</automated></verify>
  <acceptance_criteria>- Physical Tauri apply and every restore command reach the scripted/real pipe transport exactly once.
- Fixed endpoint, handshake, frame bounds, deadlines, and server identity fail closed.
- Physical errors have zero fixture/callback/network/shell fallback.
- Response loss and restart reconcile by observation without a second mutation dispatch.
- Both physical and ordinary build profiles compile with explicit, distinct mutation availability.</acceptance_criteria>
  <done>The installed physical desktop can execute and recover through the authenticated service boundary instead of unconditionally returning BrokerUnavailable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Tauri native host -> local named pipe | Unprivileged native intent crosses into authenticated privileged transport. |
| broker response -> recovery journal verdict | Lost or forged responses could fabricate success or trigger duplicate mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-37A | Spoofing/Elevation | pipe server/handshake | mitigate | Fixed endpoint, local/server identity checks, MAC session, generated validation, no fallback. |
| T-06-37B | Tampering/Denial | framing/timeouts | mitigate | Exact length prefix, 64 KiB cap, finite deadlines, malformed/partial rejection. |
| T-06-37C | Replay/Repudiation | uncertain exchange | mitigate | Durable reservation, monotonic counter, observation-first reconnect, no automatic redispatch. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-37A | CRITICAL | V2 Authentication; V4 Access Control | block | transport identity/handshake/no-fallback tests | Any spoofable endpoint or fixture fallback blocks runner and MSI. |
| T-06-37B | HIGH | V5 Validation; V13 API | block | frame/deadline/malformed corpus | Any unbounded or alternate transport blocks physical build. |
| T-06-37C | CRITICAL | V3 Session; V7 Logging | block | response-loss/restart recovery tests | Any blind second dispatch blocks runner and promotion. |
</threat_model>
<verification>Broker/recovery suites and both Windows feature compile matrices pass; the packaged runner in 06-33 drives this exact path.</verification>
<success_criteria>A physical Tauri command has a complete executable route through the authenticated named pipe to the allowlisted interactive-user effect and back to durable observation.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-37-SUMMARY.md` with TDD commits, compile matrices, and no-fallback evidence.</output>
