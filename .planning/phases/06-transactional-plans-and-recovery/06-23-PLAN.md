---
phase: 06-transactional-plans-and-recovery
plan: '23'
type: tdd
wave: 3
depends_on: ['06-01', '06-09', '06-12']
files_modified:
  - apps/desktop/src-tauri/src/recovery_store/migrations.rs
  - apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs
  - apps/desktop/src-tauri/src/plan_auth.rs
  - apps/desktop/src-tauri/tests/advanced_preference.rs
autonomous: true
requirements: [PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - 'Advanced enablement is an append-audited, device-local preference that survives restart and never cloud-syncs.'
    - 'Enable and revoke require different fresh action-scoped strong-auth proofs; hardware or security-posture change invalidates the preference before mutation.'
    - 'Revocation/invalidation preserves recovery and history while blocking new Advanced work until a fresh re-enable proof succeeds.'
    - 'D-13: Advanced is a persistent, revocable per-PC local preference protected by fresh strong authentication, and relevant hardware or security-posture change invalidates it until revalidation.'
  artifacts:
    - path: apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs
      provides: Append-only device-local Advanced preference authority and derived projection
    - path: apps/desktop/src-tauri/tests/advanced_preference.rs
      provides: Enable/revoke/invalidate/restart/tamper/strong-auth lifecycle evidence
  key_links:
    - from: apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs
      to: apps/desktop/src-tauri/src/recovery_store/migrations.rs
      via: append-only event table and projection rebuild
      pattern: 'advanced_preference_events'
    - from: apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs
      to: apps/desktop/src-tauri/src/plan_auth.rs
      via: action-discriminated consumed proof before enable or revoke append
      pattern: 'ConsumedAdvancedPreferenceProof'
---
<objective>Implement the complete D-13 device-local Advanced preference authority through TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md @apps/desktop/src-tauri/src/recovery_store/mod.rs @apps/desktop/src-tauri/src/plan_auth.rs @packages/contracts-source/src/transactional-plans.tsp</context>
<tasks>
<task type="auto">
  <name>Task 1 RED: Specify the durable Advanced preference lifecycle</name>
  <read_first>apps/desktop/src-tauri/src/recovery_store/migrations.rs; apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs and tests/advanced_preference.rs (confirm absent); apps/desktop/src-tauri/src/plan_auth.rs; packages/contracts-source/src/transactional-plans.tsp; 06-CONTEXT.md D-09–D-13, D-16, D-29, D-32, D-33</read_first>
  <files>apps/desktop/src-tauri/tests/advanced_preference.rs</files>
  <action>Write failing tests for initial disabled state; enable with a fresh `enable-advanced-preference` proof; exact device plus hardware/security-posture fingerprint binding; persistence and identical projection after database/process restart; revoke with a distinct fresh `revoke-advanced-preference` proof; cross-action/stale/replayed/wrong-device proof rejection; automatic append-audited invalidation when hardware or security posture changes; fresh revalidation/re-enable after invalidation; immutable event history; projection rebuild; SQLite FULL/IOERR/tamper failures; no cloud/account-sync write; and recovery availability in disabled/revoked/invalidated/offline states. Assert no preference mutation occurs when proof consumption or durable append fails.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test advanced_preference</automated></verify>
  <acceptance_criteria>- RED failures are lifecycle/storage assertions, not missing infrastructure.
- Enable and revoke cannot share or replay a proof.
- Restart, hardware change, security-posture change, and recovery-without-preference cases are explicit.</acceptance_criteria>
  <done>RED D-13 lifecycle contract is committed.</done>
</task>
<task type="auto">
  <name>Task 2 GREEN: Persist append-audited enable, revoke, and invalidation</name>
  <read_first>apps/desktop/src-tauri/src/recovery_store/migrations.rs; apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs; apps/desktop/src-tauri/src/plan_auth.rs; apps/desktop/src-tauri/tests/advanced_preference.rs; apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs</read_first>
  <files>apps/desktop/src-tauri/src/recovery_store/migrations.rs, apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs, apps/desktop/src-tauri/src/plan_auth.rs</files>
  <action>Add an append-only `advanced_preference_events` migration inside the existing dedicated recovery database, keyed to the local installation/device authority with monotonic sequence, event kind `enabled|revoked|invalidated`, hardware evidence fingerprint, security-posture fingerprint, opaque consumed-proof receipt reference for enable/revoke, reason code, timestamp, previous/head integrity link, and no credentials/reusable proof/raw hardware identifiers. Build the current projection only by reducing validated events. Enable/revoke must consume the exact action-scoped proof before append and become effective only after FULL-durable commit; invalidation is a native automatic event when trusted current hardware or security-posture fingerprint differs. On open/restart, verify/rebuild the projection and fail new Advanced mutation closed on integrity/storage/identity uncertainty while preserving read-only history and all recovery commands. Do not write the preference to account sync, localStorage, Tauri Store, or cloud state.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test advanced_preference</automated></verify>
  <acceptance_criteria>- Enabled state survives restart only for the exact unchanged device/posture binding.
- Enable, revoke, and invalidate are immutable attributable events; no update/delete path exists.
- Changed hardware/posture appends invalidation before any new Advanced apply admission.
- Recovery never depends on preference/auth/subscription state.</acceptance_criteria>
  <done>The native store is the sole durable D-13 preference authority.</done>
</task>
<task type="auto">
  <name>Task 3 REFACTOR: Exhaust restart, proof, posture, and corruption transitions</name>
  <read_first>apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs; apps/desktop/src-tauri/src/plan_auth.rs; apps/desktop/src-tauri/tests/advanced_preference.rs</read_first>
  <files>apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs, apps/desktop/src-tauri/tests/advanced_preference.rs</files>
  <action>Add table/property cases over all state/event pairs, repeated reopen, proof cross-use, posture oscillation, failed append, chain tamper, and concurrent enable/revoke serialization. Consolidate projection helpers without a permissive wildcard. Commit refactor only if green.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop advanced_preference</automated></verify>
  <acceptance_criteria>- Every invalid transition returns a stable closed blocker.
- Concurrent or replayed operations produce one durable event and no split projection.
- Restart projection is byte/field equivalent to pre-close authority.</acceptance_criteria>
  <done>The D-13 lifecycle is exhaustive, durable, and fail-closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| strong-auth receipt/current posture -> local preference mutation | Replayed proof or stale device posture could enable high-risk authority. |
| SQLite history -> restart projection | Tamper or partial writes could fabricate enabled state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-23A | Spoofing/Elevation | preference enable/revoke | mitigate | Separate one-use action-scoped proofs bound to exact device/posture and opaque receipt custody. |
| T-06-23B | Tampering/Repudiation | preference persistence | mitigate | FULL-durable append-only events under the existing keyed audit anchor, verify/rebuild on reopen. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-23A | HIGH | V2 Authentication — fresh strong proof; V3 Session Management — one-use action binding; V4 Access Control — device/posture scope | blocking | Enable/revoke cross-action, stale, replay, wrong-device/posture tests | Any proof reuse or scope bypass blocks 06-24, 06-25, Advanced UI, and physical promotion. |
| T-06-23B | HIGH | V7 Logging — immutable attributable lifecycle; V6 Cryptography — existing keyed audit anchor | blocking | Restart/rebuild/tamper/FULL/IOERR tests | Any fabricated/lost enabled state blocks command wiring and Advanced admission. |
</threat_model>
<verification>The focused TDD suite proves append-only enable/revoke/invalidate, restart persistence, posture revalidation, strong-auth separation, and recovery independence.</verification>
<success_criteria>D-13 is a real device-local durable authority, not an in-memory policy Boolean.</success_criteria>
<output>Create `06-23-SUMMARY.md` with RED/GREEN/REFACTOR evidence.</output>
