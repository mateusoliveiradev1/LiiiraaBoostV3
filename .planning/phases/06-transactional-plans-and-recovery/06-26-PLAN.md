---
phase: 06-transactional-plans-and-recovery
plan: '26'
type: execute
wave: 15
depends_on: ['06-34']
files_modified:
  - tooling/phase6-evidence/evidence-manifest.json
  - .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'The exact operation version completes the full real clean-VM apply/reboot/restore cycle and adversarial IPC/fault/accessibility matrix before owner-PC work begins.'
    - 'Automated harness evidence is persisted before human review; failure or unavailable VM blocks the version and any correction restarts at simulation.'
  artifacts:
    - path: tooling/phase6-evidence/evidence-manifest.json
      provides: Exact clean-VM machine/build/version/GUID/hash and reviewer evidence
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
      provides: Clean-VM run transcript, findings, human verdict, and escalation
  key_links:
    - from: tooling/phase6-evidence/evidence-manifest.json
      to: phase6:verify clean-vm
      via: exact-version complete-cycle admission
      pattern: 'clean-vm'
---
<objective>Run, persist, review, and admit the clean Windows VM promotion stage.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md @tooling/phase6-evidence/evidence-manifest.json @C:/Users/Liiiraa/VM-Lab/Evidence/20260812-213313-audit.json</context>
<tasks>
<task type="auto">
  <name>Task 1: Execute and persist the clean-VM evidence run</name>
  <read_first>tooling/phase6-evidence/evidence-manifest.json; 06-VALIDATION.md Manual-Only Verifications; persisted Hyper-V audit; apps/desktop/tests/packaged/transactional-plans.ts</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>After deterministic final checks pass, use the packaged harness to re-audit VM `LiiiraaBoost-W11-25H2-Clean` and checkpoint `Clean-Windows-Ready` in an elevated session, install the exact build, create an installed checkpoint, and execute prepare/apply/observe/verify/restart/boot-reconcile/restore/verify for the exact operation version. Run legitimate client, same-user spoof, replay, wrong-session, remote-client, crash, disk-full, drift-before-apply, drift-before-restore, conflict, and System Restore available/unavailable/frequency cases. Run Narrator/keyboard PT-BR/English at 150%/200%. Persist exact VM/checkpoint/build/operation version/artifact hashes, prior/applied/restored GUIDs, broker dispatch counts, redacted diagnostics, test outputs, accessibility observations, failures, and `humanReview: pending` in the manifest/UAT. Any unavailable environment or failed assertion must persist BLOCKED evidence, fail the command, block later stages, and require a corrected new version starting at simulation.</action>
  <verify><automated>rtk pnpm phase6:verify -- --mode final --stage clean-vm --require-run-evidence</automated></verify>
  <acceptance_criteria>- The harness actually runs before the checkpoint and persists exact physical provenance.
- Invalid IPC cases produce zero mutation; apply/reboot/restore returns to the exact prior GUID.
- Failure/unavailability is recorded as BLOCKED, never PASS/pending completion.</acceptance_criteria>
  <done>A complete clean-VM evidence package exists for human inspection.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review the already-produced clean-VM evidence</name>
  <action>This checkpoint performs no evidence mutation or harness run. Pause before execution and collect only the reviewer verdict for Task 3 to persist and mechanically validate against Task 1 evidence.</action>
  <what-built>The packaged harness produced a redacted exact-version clean-VM evidence package, manifest cell, and UAT transcript including GUID lifecycle, reboot/recovery, adversarial IPC, fault, System Restore, and accessibility results.</what-built>
  <how-to-verify>1. Inspect the exact VM/checkpoint/build/version/hash identities and attached command outputs. 2. Confirm the interactive user's scheme changed to the exact owned GUID and returned to the exact prior GUID after reboot/recovery. 3. Confirm every invalid IPC case caused zero dispatch/mutation and all required fault/System Restore/accessibility cells passed. 4. Confirm the package is redacted and no simulated/fixture evidence is labeled physical.</how-to-verify>
  <resume-signal>Reply `APPROVED clean-vm &lt;operation-version&gt;` only if the produced evidence passes every check, or describe the exact failure. Failure/unavailability blocks this and all later stages.</resume-signal>
</task>
<task type="auto">
  <name>Task 3: Persist the clean-VM reviewer verdict and admit the stage</name>
  <read_first>tooling/phase6-evidence/evidence-manifest.json; .planning/phases/06-transactional-plans-and-recovery/06-UAT.md; the exact Task 2 reviewer response</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>Append the exact reviewer response, reviewer identity, UTC timestamp, and APPROVED/REJECTED verdict to the clean-VM evidence without changing run artifacts. Require approved operation version/build/hashes to match simulation and Task 1 exactly. Run the evaluator; it must reject missing/rejected/mismatched human review. On rejection preserve BLOCKED evidence and fail. A correction must mint a new operation version and restart at simulation.</action>
  <verify><automated>rtk pnpm phase6:verify -- --mode final --stage clean-vm</automated></verify>
  <acceptance_criteria>- Only exact matching technical evidence plus explicit human approval admits clean-vm.
- Rejected/missing/mismatched review blocks owner-PC.
- No run or reviewer record is overwritten.</acceptance_criteria>
  <done>The exact version is clean-VM admitted or explicitly blocked.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| packaged build -> clean Hyper-V Windows | Privileged effects, identity, reboot, and recovery can differ from simulation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-26A | Elevation/Tampering | VM broker/Power APIs | mitigate | Exact checkpoint/build/version, identity/replay matrix, GUID verification, automated persistence before human review. |
| T-06-26B | Repudiation | stage evidence | mitigate | Immutable run hashes plus post-checkpoint exact reviewer verdict and no skip/override. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-26A | CRITICAL | V4 Access Control — physical effects require proven identity; V5 Input Validation — exact state identity | blocking-human | Harness run plus GUID/IPC/fault evidence and human confirmation | Any unavailable/failed identity/effect/reboot/restore/adversarial case blocks 06-27 and Phase 6. |
| T-06-26B | CRITICAL | V7 Logging — immutable attributable evidence | blocking-human | Exact run hashes and persisted reviewer verdict | Missing/mismatched/rejected evidence blocks stage; correction restarts at simulation. |
</threat_model>
<verification>The clean-VM evaluator passes only after the harness run is persisted and the exact evidence is independently approved.</verification>
<success_criteria>The same simulation-admitted version has proven a complete reversible cycle on the clean VM.</success_criteria>
<output>Create `06-26-SUMMARY.md` only after Task 3 passes; otherwise update UAT and return BLOCKED.</output>
