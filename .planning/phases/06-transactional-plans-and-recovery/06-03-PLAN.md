---
phase: 06-transactional-plans-and-recovery
plan: '03'
type: execute
wave: 2
depends_on: ['06-01', '06-02']
files_modified:
  - crates/plan-engine/src/lib.rs
  - crates/plan-engine/src/domain.rs
  - crates/plan-engine/src/revision.rs
  - crates/plan-engine/src/risk.rs
  - crates/plan-engine/src/dependency.rs
  - crates/plan-engine/src/reconcile.rs
  - crates/plan-engine/src/promotion.rs
  - crates/plan-engine/src/executor.rs
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Independent TDD plans share one compile-checked set of generated-contract-backed domain interfaces.'
  artifacts:
    - path: crates/plan-engine/src/domain.rs
      provides: Closed value types, ports, commands, decisions, and errors
    - path: crates/plan-engine/src/lib.rs
      provides: Public module surface for downstream adapters
  key_links:
    - from: crates/plan-engine/src/domain.rs
      to: liiiraa-contracts-rust
      via: generated transport mapping boundary
      pattern: 'liiiraa_contracts_rust'
---
<objective>Freeze compile-time interfaces for revisions, risk, rollback, reconciliation, promotion, and execution.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md @crates/contracts-rust/src/lib.rs @crates/plan-engine/Cargo.toml</context>
<tasks>
<task type="auto">
  <name>Task 1: Define immutable plan and admission interfaces</name>
  <read_first>crates/plan-engine/src/domain.rs, revision.rs, risk.rs, dependency.rs (confirm absent); crates/contracts-rust/src/lib.rs; 06-RESEARCH.md Patterns 1 and 4; 06-CONTEXT.md D-09–D-17</read_first>
  <files>crates/plan-engine/src/domain.rs, crates/plan-engine/src/revision.rs, crates/plan-engine/src/risk.rs, crates/plan-engine/src/dependency.rs</files>
  <action>Define closed Rust types and public signatures for immutable plan inputs/revisions, registered operation versions, Phase 5 evidence admission, risk ceiling, proportional approval requirements, dependency DAG, restore target, and fail-closed errors. Interfaces accept renderer intent but never renderer compatibility/risk/auth/success claims. Leave behavior to Plans 05–07; provide only compile-valid types and traits, with no permissive default.</action>
  <verify><automated>rtk cargo check -p liiiraa-plan-engine</automated></verify>
  <acceptance_criteria>- Public types are immutable and generated-transport-backed.
- No API permits lowering operation risk, mutating an approved revision, or executing Extreme.
- Missing/degraded/contradictory evidence has an explicit non-admitted representation.</acceptance_criteria>
  <done>Revision, risk, and dependency TDD plans can run without redefining contracts.</done>
</task>
<task type="auto">
  <name>Task 2: Define recovery and execution interfaces</name>
  <read_first>crates/plan-engine/src/reconcile.rs, promotion.rs, executor.rs, lib.rs (confirm absent); crates/plan-engine/src/domain.rs; 06-RESEARCH.md Patterns 2, 3 and Four-Stage Promotion Matrix; 06-CONTEXT.md D-01–D-08 and D-18–D-30</read_first>
  <files>crates/plan-engine/src/reconcile.rs, crates/plan-engine/src/promotion.rs, crates/plan-engine/src/executor.rs, crates/plan-engine/src/lib.rs</files>
  <action>Define public signatures and closed types for observation-first reconciliation, dependency-scoped rollback decisions, four-stage exact-version promotion, signed revocation effects, redacted diagnostic projection, durable journal port, broker observe/mutate port, strong-auth verifier port, executor event reducer, cancellation boundary, restart checkpoint, and mutation gate. Export all modules. Recovery APIs must remain callable independent of subscription/auth state per D-04.</action>
  <verify><automated>rtk cargo check -p liiiraa-plan-engine && rtk pnpm test:architecture</automated></verify>
  <acceptance_criteria>- Every external effect is reachable only through a narrow port requiring a prepared transaction identity and expected prior state.
- Recovery ports have no entitlement input.
- Promotion cannot skip stages or override a failure; revocation exposes block-and-alert plus local recovery only.</acceptance_criteria>
  <done>All behavior plans implement against one explicit interface set.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| renderer/generated DTO -> pure domain | Untrusted claims must become intent only. |
| pure executor -> privileged port | Effects require prepared identity and expected prior state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03A | Tampering/Elevation | public interfaces | mitigate | Closed types omit authoritative renderer booleans and generic mutation APIs. |
| T-06-03B | Denial | recovery API | mitigate | Recovery signatures have no subscription or online-auth dependency. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-03A | HIGH | V4 Access Control — server/native authority is deny-by-default; V5 Input Validation — closed typed inputs | blocking | `rtk cargo check -p liiiraa-plan-engine` plus architecture gate | Any renderer authority or generic mutation surface blocks 06-05 through 06-08 and 06-14. |
| T-06-03B | HIGH | V4 Access Control — recovery authorization must remain independently available | blocking | Compile tests and recovery-without-entitlement cases | Any subscription/auth dependency on recovery blocks executor wiring and physical promotion. |
</threat_model>
<verification>`rtk cargo check -p liiiraa-plan-engine` and architecture tests pass.</verification>
<success_criteria>Every downstream plan has compile-checked contracts and no authority ambiguity.</success_criteria>
<output>Create `06-03-SUMMARY.md` when done.</output>
