---
phase: 06-transactional-plans-and-recovery
plan: '11'
type: tdd
wave: 2
depends_on: ['06-01']
files_modified: [packages/desktop-client/src/plans.ts, packages/desktop-client/src/plans.test.ts, packages/desktop-client/src/index.ts]
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'React receives immutable runtime-validated plan/recovery projections and sends intent through a closed command registry.'
    - 'A missed sequence, reload, or tray reopen marks progress stale and performs one deduplicated authoritative snapshot read; it never retries mutation.'
    - 'Production rejects fixture provenance and never falls back to deterministic success.'
  artifacts:
    - path: packages/desktop-client/src/plans.ts
      provides: PlanAuthority port with native/deterministic adapters
    - path: packages/desktop-client/src/plans.test.ts
      provides: Contract, provenance, sequence, reconnect, and cancellation conformance
  key_links:
    - from: packages/desktop-client/src/plans.ts
      to: generated transactional validator
      via: validate unknown before immutable projection
      pattern: 'transactional.*Validator'
---
<objective>Implement the renderer-safe plan authority through adapter-conformance TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@packages/desktop-client/src/evidence.ts @packages/desktop-client/src/evidence.test.ts @packages/contracts-source/src/transactional-plans.tsp @.planning/phases/06-transactional-plans-and-recovery/06-PATTERNS.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify PlanAuthority conformance</name><read_first>packages/desktop-client/src/plans.ts and plans.test.ts (confirm absent); packages/desktop-client/src/evidence.ts; packages/desktop-client/src/index.ts; 06-CONTEXT.md D-04, D-24, D-25, D-31</read_first><files>packages/desktop-client/src/plans.test.ts</files><action>Write failing Vitest cases for closed commands `compose`, `revise`, `approve`, `apply`, `restoreOperation`, `restorePlan`, `restoreCheckpoint`, `readExecution`, `subscribeExecution`, `previewDiagnostic`, and `exportDiagnostic`; generated validation, deep freeze, native/deterministic identity, production fixture refusal, contiguous sequences, one deduplicated refetch on gap, listener disposal, recovery without entitlement, and cancel-requested versus unknown-after-dispatch.</action><verify><automated>rtk pnpm --filter @liiiraa/desktop-client exec vitest --run src/plans.test.ts</automated></verify><acceptance_criteria>- RED failures identify missing PlanAuthority behavior.
- Tests reject generic command names, fixture fallback, renderer success claims, and mutation retry on reconnect.</acceptance_criteria><done>RED client contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Implement closed validated authority adapters</name><read_first>packages/desktop-client/src/plans.ts; packages/desktop-client/src/plans.test.ts; packages/desktop-client/src/evidence.ts; packages/desktop-client/src/index.ts</read_first><files>packages/desktop-client/src/plans.ts, packages/desktop-client/src/index.ts</files><action>Implement `PLAN_COMMANDS`, `PlanAuthority`, native invoke/channel adapter, deterministic adapter restricted to explicit test composition, immutable snapshot, closed client errors, cancellation semantics, contiguous event reduction, stale marker, and one refetch gate. Renderer inputs contain intent/references only; no compatible/risk/authenticated/verified booleans. Export only public types/factories from index.</action><verify><automated>rtk pnpm --filter @liiiraa/desktop-client exec vitest --run src/plans.test.ts</automated></verify><acceptance_criteria>- Native and deterministic adapters pass the same conformance cases while preserving origin.
- Production recursively rejects fixture/scenario markers.
- Gap/reopen invokes exactly one `readExecution` and zero mutation commands.</acceptance_criteria><done>UI can consume authoritative plan/recovery state without owning transaction truth.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Harden client lifecycle</name><read_first>packages/desktop-client/src/plans.ts; packages/desktop-client/src/plans.test.ts</read_first><files>packages/desktop-client/src/plans.ts, packages/desktop-client/src/plans.test.ts</files><action>Deduplicate validation/subscription helpers, add malformed nested provenance and reconnect race cases, and preserve bounded error detail. Commit refactor if changed.</action><verify><automated>rtk pnpm --filter @liiiraa/desktop-client test -- --run</automated></verify><acceptance_criteria>- Entire desktop-client suite passes.
- No catch branch returns a fixture or fabricated success.</acceptance_criteria><done>PlanAuthority remains deterministic, bounded, and leak-free.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Tauri/fixture payload -> React | Invalid or simulated output could masquerade as native success. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-11A | Tampering/Repudiation | PlanAuthority | mitigate | Generated validation, immutable snapshots, monotonic sequence, authoritative refetch. |
| T-06-11B | Elevation | command registry | mitigate | Closed intent commands; no generic primitive or renderer authority fields. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-11A | HIGH | V5 Input Validation — validate all native payloads; V7 Logging — ordered attributable state | blocking | Desktop-client contract/sequence/reconnect tests | Invalid snapshots, fabricated success, or sequence ambiguity blocks 06-17, 06-19, and 06-20. |
| T-06-11B | HIGH | V4 Access Control — renderer may express intent only through an allowlist | blocking | Generic command and authority-field rejection tests | Any generic or renderer-authoritative command blocks UI/native wiring and physical promotion. |
</threat_model>
<verification>Desktop-client full suite and TDD gate log pass.</verification>
<success_criteria>Renderer state is an honest projection of native authority.</success_criteria>
<output>Create `06-11-SUMMARY.md`.</output>
