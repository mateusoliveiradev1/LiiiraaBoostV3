---
phase: 06-transactional-plans-and-recovery
plan: '39'
type: tdd
wave: 10
depends_on: ['06-21']
files_modified:
  - packages/contracts-source/src/transactional-plans.tsp
  - contracts/generated/desktop/v1/transactional-recovery.schema.json
  - packages/contracts-ts/src/generated/models.ts
  - packages/contracts-ts/src/generated/standalone-validators.d.ts
  - packages/contracts-ts/src/generated/standalone-validators.js
  - packages/contracts-ts/src/transactional-plans.test.ts
  - crates/contracts-rust/src/generated.rs
  - crates/contracts-rust/tests/transactional_plans_corpus.rs
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Installation custody, portable artifact custody, friends-roster custody, stage run configuration, and reboot continuation are closed TypeSpec documents generated into equivalent JSON Schema, TypeScript validators/types, and Rust transports.'
    - 'Installed custody contains desktop, service, and runner roles only; tauri-driver and msedgedriver are exclusively portable roles authenticated by the artifact manifest together with the MSI, installation manifest/signature, and all three canonical stage configs.'
    - 'The friends roster is one purpose-bound, pseudonymous, create-once document whose detached CMS signature is external and whose canonical paths are fixed by the friends config.'
    - 'The lifecycle contract permits only installed-ready -> checkpoint-ready -> running -> reboot-pending -> resumed-observation -> restored-complete, with create-once records bound to the same manifest/config/runner/operation/run tuple.'
  artifacts:
    - path: packages/contracts-source/src/transactional-plans.tsp
      provides: Canonical InstallationManifestDocument, ArtifactManifestDocument, FriendsRosterDocument, PhysicalRunConfigDocument, and PhysicalContinuationDocument
      contains: ArtifactManifestDocument
    - path: contracts/generated/desktop/v1/transactional-recovery.schema.json
      provides: Runtime schema for every physical custody and lifecycle document
    - path: packages/contracts-ts/src/generated/models.ts
      provides: Generated TypeScript physical custody/config/continuation transports
    - path: crates/contracts-rust/src/generated.rs
      provides: Generated Rust transports consumed by service and physical runner
    - path: crates/contracts-rust/tests/transactional_plans_corpus.rs
      provides: Cross-runtime valid/invalid physical document corpus
  key_links:
    - from: packages/contracts-source/src/transactional-plans.tsp
      to: contracts/generated/desktop/v1/transactional-recovery.schema.json
      via: existing TypeSpec generation pipeline
      pattern: 'ArtifactManifestDocument|FriendsRosterDocument|PhysicalRunConfigDocument|PhysicalContinuationDocument'
    - from: contracts/generated/desktop/v1/transactional-recovery.schema.json
      to: packages/contracts-ts/src/generated/models.ts and crates/contracts-rust/src/generated.rs
      via: existing schema-to-TypeScript/Rust generation, runtime validators, and parity corpus
      pattern: 'artifact-manifest|physical-run-config|physical-continuation'
---
<objective>Establish one generated authority for signed physical artifacts, the pseudonymous friends roster, immutable run configurations, and the restart-safe lifecycle.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md
@.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md
@packages/contracts-source/src/main.tsp
@packages/contracts-source/src/transactional-plans.tsp
@contracts/generated/desktop/v1/transactional-recovery.schema.json
<interfaces>
`packages/contracts-source/src/main.tsp` already imports `transactional-plans.tsp`; keep that single language-neutral source. Canonical portable paths are relative to one create-once artifact root: `artifact-manifest.json`, `artifact-manifest.json.p7s`, `liiiraa-boost.msi`, `installation-manifest.json`, `installation-manifest.json.p7s`, `configs/clean-windows-vm.run-config.json`, `configs/owner-pc.run-config.json`, `configs/friends-pc.run-config.json`, `phase6-physical-runner.exe`, `tauri-driver.exe`, and `msedgedriver.exe`. Installed roles are exactly desktop, service, and runner; the two drivers never appear in `InstallationManifestDocument` or Program Files. The friends config contains only the fixed roster-relative paths `friends/friends-roster.json` and `friends/friends-roster.json.p7s`; it contains no roster hash, participant selection, output override, or caller-provided path. The pair is created later exactly once at those paths and is authenticated independently by detached CMS over canonical `FriendsRosterDocument` bytes. Artifact and roster signatures chain to the same compiled SPKI pin; trust never comes from a document/environment/CLI field.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR physical, roster, and continuation contracts</name>
  <read_first>packages/contracts-source/src/main.tsp; packages/contracts-source/src/transactional-plans.tsp; contracts/generated/desktop/v1/transactional-recovery.schema.json; crates/contracts-rust/tests/transactional_plans_corpus.rs; 06-CONTEXT.md D-01, D-02, D-06, D-07, D-18, D-19, D-23, D-29, D-31, D-32, D-35</read_first>
  <files>packages/contracts-source/src/transactional-plans.tsp, contracts/generated/desktop/v1/transactional-recovery.schema.json, packages/contracts-ts/src/generated/models.ts, packages/contracts-ts/src/generated/standalone-validators.d.ts, packages/contracts-ts/src/generated/standalone-validators.js, packages/contracts-ts/src/transactional-plans.test.ts, crates/contracts-rust/src/generated.rs, crates/contracts-rust/tests/transactional_plans_corpus.rs</files>
  <behavior>
    - `InstallationManifestDocument` closes exactly the installed desktop/service/runner roles, canonical Program Files relative paths, exact sizes/SHA-256/Authenticode identities, ProductCode/package version, source commit/input-tree/build/operation identity, creation time, and signer SPKI identity. A tauri-driver or msedgedriver installed role is schema-invalid.
    - `ArtifactManifestDocument` requires exactly the MSI, installation manifest plus detached signature, the three stage run configs, runner, tauri-driver, and msedgedriver roles; every entry has one unique canonical relative path, size, SHA-256, version/signature policy, and no executable arguments.
    - `FriendsRosterDocument` binds schema kind/version, artifact-manifest hash, friends-config hash, operation/build/source identity, purpose, roster ID, creation time, and an ordered non-empty collection of unique `{ participantId, machineSlot }` entries. Participant IDs are purpose-bound local hashes, machine slots are unique pseudonymous labels, objects are sealed, and raw names, emails, SIDs, serials, credentials, signer identity, trust pins, paths, commands, or mutable status fields are forbidden. Its detached CMS signature remains external.
    - `PhysicalRunConfigDocument` is stage-discriminated for canonical `clean-windows-vm`, `owner-pc`, or `friends-pc`; it binds artifact-manifest path/hash, operation/build/source identities, fixed scenario set, fixed create-once run/installed-ready/checkpoint-ready/continuation/raw-envelope relative paths, `participantIdentityMode: purpose-bound-local-hash`, and the exact named Tauri command allowlist. The friends variant additionally fixes only `friends/friends-roster.json` and `friends/friends-roster.json.p7s`; neither path is caller-selectable and the config carries no roster hash or participant selector. Each config is authenticated only as an exact path/hash role inside the detached-CMS-signed artifact manifest emitted by the 06-31 build authority; it contains no arbitrary command, executable, host, script, output root, credential, signer, or trust pin.
    - `PhysicalContinuationDocument` has a closed state union `installed-ready | checkpoint-ready | running | reboot-pending | resumed-observation | restored-complete`, binds the exact artifact-manifest/config/runner hashes, stage, operation/build/run/transaction IDs, previous-record hash, and observed journal head; transitions skip no state, and resume never authorizes mutation before observation.
    - Unknown fields, absolute/traversing paths, duplicate/missing roles, installed driver roles, noncanonical roster paths/stage names, duplicate roster participants/slots, self-asserted trust, altered config identity, lifecycle skip, overwritten sequence, or mismatched continuation hash are invalid in JSON Schema and generated Rust.
  </behavior>
  <action>RED: extend the TypeScript and Rust cross-runtime corpus with valid installation/artifact manifests, valid rosters of one and multiple participants, all three stage configs, and every lifecycle state. Add adversarial examples for a driver in installed roles, either driver missing from portable roles, missing/duplicate roles, path traversal, wrong canonical roster names, duplicate/swapped participant or machine-slot entries, raw identity fields, roster/config/artifact mismatch, config stage swapping, arbitrary command/argument/output paths, signer/SPKI confusion, continuation overwrite/hash mismatch, skipped installed/checkpoint/running/reboot states, and resume-before-observation; commit `test(06-39): add failing physical custody contract corpus`. GREEN: define `InstallationManifestDocument`, `ArtifactManifestDocument`, `FriendsRosterDocument`, stage-discriminated `PhysicalRunConfigDocument`, and hash-chained `PhysicalContinuationDocument` in the existing transactional TypeSpec library per D-01/D-02/D-07/D-18/D-19/D-31/D-35. Keep installed and portable role enums distinct, the two roster paths literal in the friends config, file roles/lifecycle states closed, collection bounds explicit, all objects sealed, and both CMS signatures external to the bytes they sign. Regenerate the canonical JSON Schema, TypeScript models/runtime validators, and Rust transport without handwritten DTOs, then commit `feat(06-39): generate physical custody and continuation contracts`. REFACTOR only while regeneration drift and the complete TypeScript/Rust corpus remain green.</action>
  <verify><automated>rtk pnpm test:contracts &amp;&amp; rtk cargo test -p liiiraa-contracts-rust --test transactional_plans_corpus</automated></verify>
  <acceptance_criteria>- All five physical document families derive from TypeSpec and have generated Schema/TypeScript/Rust parity.
- Installed roles contain desktop/service/runner only; portable artifact roles include MSI, both installation-manifest bytes, all three config bytes, runner, tauri-driver, and msedgedriver.
- Friends roster entries are pseudonymous, unique, artifact/config-bound, sealed, and independently CMS-signable at the two fixed config paths.
- Stage names are exactly `clean-windows-vm`, `owner-pc`, and `friends-pc`.
- Config and continuation shapes cannot carry arbitrary command paths/arguments or skip observation-first recovery.
- RED precedes GREEN and the corpus rejects every trust/path/lifecycle mutation.</acceptance_criteria>
  <done>Downstream builder, verifier, writer, bridge, and runner share deterministic physical custody and lifecycle contracts.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| TypeSpec source -> generated consumers | Drift or handwritten duplicates could give each privileged consumer a different authority. |
| signed artifact/config/roster -> physical lifecycle | Mutable paths, participant substitution, or continuation state could become arbitrary execution, consent spoofing, or mutation replay. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-39A | Tampering/Spoofing | artifact, roster, and config identity | mitigate | Distinct installed/portable roles, closed paths/hashes, external detached CMS identities, generated runtime validation, adversarial corpus. |
| T-06-39B | Elevation | run config | mitigate | Stage-discriminated fixed commands/scenarios/paths; no arbitrary executable, argument, script, host, or trust input. |
| T-06-39C | Replay/Repudiation | continuation | mitigate | Create-once hash chain, exact tuple binding, closed transition order, observation-first resume. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-39A | CRITICAL | V5 Validation; V14 Configuration | block | Cross-runtime path/role/signature corpus | Any divergent or self-asserted trust blocks 06-35/06-31. |
| T-06-39B | CRITICAL | V4 Access Control | block | Config arbitrary-authority negative corpus | Any generic execution field blocks runner/bridge work. |
| T-06-39C | CRITICAL | V3 Session; V7 Logging | block | Lifecycle skip/replay/hash mutation corpus | Any resume without exact observation-bound continuation blocks physical stages. |
</threat_model>
<verification>Contract generation, drift, and Rust cross-runtime corpus pass with all canonical physical documents.</verification>
<success_criteria>Every physical consumer has one generated, closed, hash-bindable authority for installed bytes, portable bytes, roster membership, and lifecycle state before code or evidence can cross a privileged boundary.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-39-SUMMARY.md` with generated artifacts, corpus cases, and RED/GREEN commits.</output>
