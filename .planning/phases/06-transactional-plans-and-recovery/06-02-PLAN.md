---
phase: 06-transactional-plans-and-recovery
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, architecture/module-boundaries.json, crates/plan-engine/Cargo.toml, crates/plan-engine/src/lib.rs]
autonomous: true
requirements: [PLAN-01, PLAN-04, PLAN-06, PLAN-08]
must_haves:
  truths:
    - 'The pure plan engine has explicit domain ownership and cannot depend on renderer, Tauri, Windows, or fixture modules.'
  artifacts:
    - path: crates/plan-engine/Cargo.toml
      provides: Pure Rust engine crate manifest
    - path: crates/plan-engine/src/lib.rs
      provides: Compile-valid public crate target for workspace registration
    - path: architecture/module-boundaries.json
      provides: Enforced plan-engine ownership and dependency direction
  key_links:
    - from: Cargo.toml
      to: crates/plan-engine/Cargo.toml
      via: workspace membership
      pattern: 'crates/plan-engine'
---
<objective>Establish the pure plan engine as an enforceable workspace domain before behavior is added.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@architecture/module-boundaries.json @Cargo.toml @crates/contracts-rust/Cargo.toml @.planning/phases/06-transactional-plans-and-recovery/06-PATTERNS.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Register plan-engine ownership and workspace identity</name>
  <read_first>Cargo.toml; architecture/module-boundaries.json; crates/plan-engine/Cargo.toml (confirm absent); crates/contracts-rust/Cargo.toml; crates/contracts-rust/src/lib.rs; 06-PATTERNS.md Workspace and boundary configuration</read_first>
  <files>Cargo.toml, architecture/module-boundaries.json, crates/plan-engine/Cargo.toml, crates/plan-engine/src/lib.rs</files>
  <action>Add `crates/plan-engine` as a production domain workspace member owned by optimizer, with a compile-valid `src/lib.rs` public root containing crate documentation and `#![forbid(unsafe_code)]`; Plan 06-03 extends this target with the approved modules. Follow the exact mapped analogs from 06-PATTERNS.md: preserve the root `Cargo.toml` member/dependency ordering and workspace inheritance, copy the package metadata/dependency inheritance shape from `crates/contracts-rust/Cargo.toml`, preserve the `#![forbid(unsafe_code)]` plus explicit public-module/export style from `crates/contracts-rust/src/lib.rs`, and add the ownership record using the existing field order and closed layer/runtime/status vocabulary in `architecture/module-boundaries.json`. Permit generated contracts and pure hashing/serialization/property-test dependencies only; forbid Tauri, Windows, UI, service, network, and fixture imports. Use the repository's exact workspace dependency pins and D-31/D-34 boundary rules.</action>
  <verify><automated>rtk pnpm test:architecture && rtk cargo metadata --no-deps</automated></verify>
  <acceptance_criteria>- Architecture gate identifies `plan-engine` as a production domain module.
- A fixture, Tauri, Windows, service, or feature-shell dependency mutation is rejected.
- Cargo metadata resolves the new member without an unpinned dependency.</acceptance_criteria>
  <done>The repository can enforce pure plan-domain ownership before implementation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pure domain -> adapters | Domain decisions must not inherit renderer or privileged authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-02A | Elevation | module graph | mitigate | Canonical boundary record rejects Tauri/Windows/service/UI dependencies in the pure engine. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-02A | HIGH | V4 Access Control — enforce trusted-layer boundaries and least privilege | blocking | `rtk pnpm test:architecture && rtk cargo metadata --no-deps` | Boundary or metadata failure blocks 06-03 and every engine/executor consumer; no mutation code is admitted. |
</threat_model>
<verification>`rtk pnpm test:architecture` and `rtk cargo metadata --no-deps` pass.</verification>
<success_criteria>The pure engine has explicit, machine-enforced ownership and dependencies.</success_criteria>
<output>Create `06-02-SUMMARY.md` when done.</output>
