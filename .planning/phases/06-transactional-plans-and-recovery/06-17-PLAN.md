---
phase: 06-transactional-plans-and-recovery
plan: '17'
type: execute
wave: 3
depends_on: ['06-01', '06-11']
files_modified:
  - packages/design-system/src/shell.tsx
  - packages/design-system/src/index.ts
  - packages/design-system/src/styles.css
  - packages/design-system/src/transactional-components.test.tsx
autonomous: true
requirements: [PLAN-03, PLAN-05, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Revision, timeline, restore targets, exact three-state conflicts, and verified receipts use bespoke accessible product components.'
    - 'Risk/state is always conveyed by localized text, icon, and pattern under forced colors and reduced motion.'
  artifacts:
    - path: packages/design-system/src/shell.tsx
      provides: PlanRevisionSummary, ExecutionTimeline, RecoveryTargetList, StateTripletDiff, VerifiedReceiptDetails
    - path: packages/design-system/src/transactional-components.test.tsx
      provides: Semantics, focus, reflow, locale, and state rendering assertions
  key_links:
    - from: packages/design-system/src/shell.tsx
      to: existing Lb primitives
      via: authored composition
      pattern: 'Lb(Panel|Disclosure|Button|Dialog)'
---
<objective>Extend the existing design system with the five transactional UI primitives required by the approved UI contract.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md @packages/design-system/src/shell.tsx @packages/design-system/src/styles.css</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add semantic transactional components</name><read_first>packages/design-system/src/shell.tsx; packages/design-system/src/index.ts; packages/design-system/src/transactional-components.test.tsx (confirm absent); 06-UI-SPEC.md Component, Risk, Interaction and Accessibility contracts</read_first><files>packages/design-system/src/shell.tsx, packages/design-system/src/index.ts, packages/design-system/src/transactional-components.test.tsx</files><behavior>- Timeline is an ordered list with exactly one aria-current step.
- Recovery operation/plan/checkpoint actions are separate named controls.
- StateTripletDiff labels Prior, Requested/Applied, and Observed after reflow.
- Receipt details are immutable and disclosed after the human summary.
- Extreme explanation renders no execution control.</behavior><action>Test first, then add `PlanRevisionSummary`, `ExecutionTimeline`, `RecoveryTargetList`, `StateTripletDiff`, and `VerifiedReceiptDetails` by composing existing Lb/React Aria primitives. Preserve focus entry/return, aria-describedby blockers, localized human labels before raw IDs, and exact immutable values. Export through the public barrel; do not introduce shadcn, a registry block, a parallel token system, or a dashboard card fork.</action><verify><automated>rtk pnpm --filter @liiiraa/design-system exec vitest --run src/transactional-components.test.tsx</automated></verify><acceptance_criteria>- All five public components export and pass semantic/focus tests.
- Extreme has zero apply/confirm controls in the DOM.
- Technical IDs wrap and are preceded by readable labels.</acceptance_criteria><done>The design system owns all genuinely new transaction/recovery semantics.</done></task>
<task type="auto"><name>Task 2: Implement approved layout, status, motion, and scaling tokens</name><read_first>packages/design-system/src/styles.css; packages/design-system/src/shell.tsx; packages/design-system/src/transactional-components.test.tsx; 06-UI-SPEC.md Layout, Spacing, Typography, Color, Motion, Responsive contracts</read_first><files>packages/design-system/src/styles.css, packages/design-system/src/transactional-components.test.tsx</files><action>Add component classes using only existing Cobalt tokens and the exact 4/8/16/24/32/48/64 spacing scale, 44px targets, 52/64px rows, Manrope typography, JetBrains Mono for exact data, one primary accent, and text+icon+solid/dashed/double/dotted state signals. Use 100/160/200/220ms motion roles; reduced motion removes translation/glow/delay. Reflow at compact/narrow widths and 200% without horizontal page scroll.</action><verify><automated>rtk pnpm --filter @liiiraa/design-system test -- --run</automated></verify><acceptance_criteria>- Design-system suite passes dark/light/forced-colors/reduced-motion assertions.
- Source contains no arbitrary new spacing, fixed-height consequential dialog, nested page scroll, third-party visual class, or decorative telemetry animation.</acceptance_criteria><done>Transactional components satisfy the approved visual and accessibility constraints.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authoritative projection -> user decision | Ambiguous or inaccessible UI could induce unsafe confirmation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-17A | Spoofing/Repudiation | status/receipt UI | mitigate | Human text + icon + pattern, exact values, immutable receipt disclosure, no optimistic progress. |
| T-06-17B | Denial | keyboard/scaling | mitigate | WCAG semantics, focus ownership, 44px targets, reflow/zoom/forced-color tests. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-17A | HIGH | V7 Logging — security outcome remains attributable and immutable in presentation; V4 Access Control — no misleading executable state | blocking | Transactional component semantic/status/receipt tests | Any optimistic or spoofable status/receipt blocks 06-19 and consequential UI release. |
| T-06-17B | MEDIUM | V4 Access Control — security controls remain operable to authorized users | blocking for UI promotion | Keyboard/focus/reflow/zoom/forced-color tests | Accessibility failure blocks 06-19, 06-20, and physical promotion sign-off. |
</threat_model>
<verification>Design-system tests and package typecheck pass.</verification>
<success_criteria>Consequential plan/recovery information remains clear and operable in every required presentation.</success_criteria>
<output>Create `06-17-SUMMARY.md`.</output>
