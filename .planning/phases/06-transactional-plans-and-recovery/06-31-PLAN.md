---
phase: 06-transactional-plans-and-recovery
plan: '31'
type: tdd
wave: 12
depends_on: ['06-30']
files_modified:
  - apps/desktop/src-tauri/tauri.phase6-physical.conf.json
  - apps/desktop/src-tauri/installer/optimizer-service.wxs
  - tooling/phase6-physical/build-artifact.mjs
  - tooling/phase6-physical/build-artifact.test.mjs
autonomous: true
requirements: [PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'One exact source identity produces a Windows MSI containing the non-elevated desktop and the exact optimizer-service binary, with immutable hashes and no tracked binary requirement.'
    - 'Install starts only the named service; update stops/replaces/restarts it without downgrade or forced reboot; uninstall stops/removes it while preserving recovery journals and evidence.'
    - 'Artifact identity cannot be reused after any tracked build-input drift, hash mismatch, signature mismatch, or file replacement.'
  artifacts:
    - path: apps/desktop/src-tauri/tauri.phase6-physical.conf.json
      provides: Dedicated per-machine MSI lab bundle configuration with updater disabled
    - path: apps/desktop/src-tauri/installer/optimizer-service.wxs
      provides: Exact Windows Installer service component and lifecycle controls
    - path: tooling/phase6-physical/build-artifact.mjs
      provides: Reproducible build, staging, hashing, signing verification, and immutable artifact manifest
  key_links:
    - from: tooling/phase6-physical/build-artifact.mjs
      to: apps/desktop/src-tauri/tauri.phase6-physical.conf.json
      via: exact Tauri MSI build config
      pattern: 'tauri.phase6-physical.conf.json'
    - from: apps/desktop/src-tauri/installer/optimizer-service.wxs
      to: apps/optimizer-service
      via: one ServiceInstall/ServiceControl component for LiiiraaBoostOptimizer
      pattern: 'ServiceInstall.*LiiiraaBoostOptimizer'
---
<objective>Produce the immutable installable build that the physical runner can attribute and install safely.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-30-SUMMARY.md
@apps/desktop/src-tauri/tauri.conf.json
@apps/desktop/src-tauri/tauri.staging.conf.json
@tooling/desktop-evidence/package-signed-desktop.mjs
@apps/optimizer-service/Cargo.toml
<interfaces>
The normal public/development NSIS configuration stays unchanged and `currentUser`. This plan adds a separate physical-lab MSI configuration because Windows Installer/WiX supports Tauri fragments and `ServiceInstall`/`ServiceControl`. The desktop executable remains `asInvoker`; only MSI installation is elevated. The output root is `target/phase6-physical/{sourceCommit}/{buildId}/`, and an existing identity is never overwritten.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR immutable physical MSI assembly</name>
  <read_first>apps/desktop/src-tauri/tauri.conf.json; apps/desktop/src-tauri/tauri.staging.conf.json; apps/desktop/src-tauri/Cargo.toml; apps/optimizer-service/Cargo.toml; tooling/desktop-evidence/package-signed-desktop.mjs; tooling/desktop-evidence/package-signed-desktop.test.mjs; official Tauri 2 Windows installer WiX fragment documentation; Microsoft ServiceInstall and ServiceControl documentation; 06-CONTEXT.md D-04, D-19, D-24, D-31, D-32</read_first>
  <files>apps/desktop/src-tauri/tauri.phase6-physical.conf.json, apps/desktop/src-tauri/installer/optimizer-service.wxs, tooling/phase6-physical/build-artifact.mjs, tooling/phase6-physical/build-artifact.test.mjs</files>
  <behavior>
    - Builder rejects uncommitted drift in declared build inputs while tolerating unrelated planning-state dirt; binds HEAD commit, canonical input-tree hash, build ID, target triple, desktop/service/config/fragment hashes, size, Authenticode signer/status, MSI product/version identity, and operation version.
    - The dedicated MSI includes exactly the desktop and `liiiraa-optimizer-service.exe`; installs `LiiiraaBoostOptimizer` as an OWN_PROCESS LocalSystem service with restricted service SID and no network dependency through one WiX component, starts it after install, stops it for update/uninstall, removes it only on uninstall, forbids downgrade, and requests no automatic reboot.
    - Desktop runtime stays non-elevated; updater artifacts/endpoints remain disabled; this lab artifact is labeled self-signed-development/internal/non-distributable and makes no public-trust or production claim.
    - Output is written atomically under the commit/build directory; an existing manifest/artifact identity must be byte-identical or the builder fails instead of overwriting.
  </behavior>
  <action>RED: add Node built-in tests over temporary fake build outputs and mutated configs for dirty declared inputs, hash/signature/file replacement, wrong service name/account/path, elevated desktop manifest, downgrade/updater enablement, journal deletion, duplicate artifact identity, and unsafe install/uninstall/update controls; commit as `test(06-31): add failing physical artifact contract`. GREEN: add a separate Tauri MSI config and WiX fragment that packages the exact release service as `OWN_PROCESS` under `LocalSystem` with a restricted service SID and no network dependency, uses Windows Installer `ServiceInstall`/`ServiceControl` tables rather than scripts/custom command strings, preserves ProgramData recovery/journal/evidence files, and controls only `LiiiraaBoostOptimizer`. Have the builder emit a signed/hash-bound installation manifest containing the expected desktop image SHA-256 for 06-30 custody; include no install secret because the service creates that under Windows-protected custody on first start. Implement the builder to compile service and desktop for `x86_64-pc-windows-msvc`, assemble/sign with the existing non-exportable local development identity, re-verify signatures and hashes after staging, and atomically emit `artifact-manifest.json`. Do not change the normal NSIS bundle, enable updater artifacts, add a PFX/secret, track `.exe/.msi`, force reboot, or delete recovery custody. Commit GREEN as `feat(06-31): build immutable physical MSI`. REFACTOR only after fake and live dry-run gates pass.</action>
  <verify><automated>rtk node --test tooling/phase6-physical/build-artifact.test.mjs &amp;&amp; rtk node tooling/phase6-physical/build-artifact.mjs --dry-run --operation-version managed-power-scheme-v2</automated></verify>
  <acceptance_criteria>- Dry-run resolves exact source/input/build identities and the two required binaries without side effects.
- Windows execution emits a non-overwritable MSI plus artifact manifest with verified hashes/signatures.
- Install/update/uninstall control only the named service and preserve recovery data.
- Normal NSIS/current-user/public configuration remains unchanged.</acceptance_criteria>
  <done>An attributable physical-lab MSI can be built for the exact implementation commit and safely consumed by the guest runner.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| source tree -> build outputs | Uncommitted or substituted bytes could be attributed to the wrong commit. |
| MSI -> Service Control Manager | Installer elevation can create broad or persistent authority. |
| update/uninstall -> recovery custody | Lifecycle operations could erase evidence needed to restore Windows state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-31A | Tampering/Repudiation | artifact identity | mitigate | Declared input-tree hash, per-file SHA-256/signature verification, atomic non-overwrite directory. |
| T-06-31B | Elevation | WiX service component | mitigate | One exact service executable/name, Windows Installer tables only, non-elevated desktop, no custom shell authority. |
| T-06-31C | Denial/Repudiation | update/uninstall | mitigate | Stop/replace/start ordering, no downgrade/forced reboot, preserve journal/evidence directories. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-31A | CRITICAL | V14 Configuration; V7 Logging | block | mutation-tested manifest and post-stage hash/signature checks | Any identity drift blocks 06-33/06-34 and physical execution. |
| T-06-31B | CRITICAL | V4 Access Control | block | WiX policy tests and VM install service inspection | Any extra service/custom shell/elevated UI blocks artifact admission. |
| T-06-31C | HIGH | V7 Logging | block | update/uninstall tests with preserved journal hashes | Any evidence deletion or forced reboot blocks artifact admission. |
</threat_model>
<verification>Node mutation suite and dry-run pass everywhere; live artifact build and MSI lifecycle smoke run on Windows before 06-26.</verification>
<success_criteria>The physical run consumes real installable bytes tied to exact source, not a TypeScript callback or missing artifact path.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md` including the artifact manifest path/hash when the live Windows build runs.</output>
