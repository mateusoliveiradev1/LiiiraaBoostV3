---
phase: 06-transactional-plans-and-recovery
plan: '31'
type: tdd
wave: 16
depends_on: ['06-33', '06-35', '06-37']
files_modified:
  - apps/desktop/src-tauri/tauri.phase6-physical.conf.json
  - apps/desktop/src-tauri/installer/optimizer-service.wxs
  - tooling/phase6-physical/build-artifact.mjs
  - tooling/phase6-physical/build-artifact.test.mjs
  - target/phase6-physical/{sourceCommit}/{buildId}/liiiraa-boost.msi
  - target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json
  - target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json.p7s
  - target/phase6-physical/{sourceCommit}/{buildId}/installation-manifest.json
  - target/phase6-physical/{sourceCommit}/{buildId}/installation-manifest.json.p7s
  - target/phase6-physical/{sourceCommit}/{buildId}/configs/clean-windows-vm.run-config.json
  - target/phase6-physical/{sourceCommit}/{buildId}/configs/owner-pc.run-config.json
  - target/phase6-physical/{sourceCommit}/{buildId}/configs/friends-pc.run-config.json
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'One exact source identity produces a real non-overwritable Windows MSI containing desktop/service/runner only plus a detached-CMS-signed artifact manifest authenticating that MSI, installation manifests, three generated stage configs, runner, and the exclusively portable tauri-driver/msedgedriver pair.'
    - 'The MSI installs the signed manifest and protected ACL before starting the service; update atomically replaces matching bytes without downgrade, and rollback restores a mutually matching prior set before service restart.'
    - 'A real Windows build-and-lifecycle command creates the commit/build directory, validates hashes/signatures/ProductCode/version/MSI contents, and proves install/update/uninstall while preserving recovery custody.'
  artifacts:
    - path: apps/desktop/src-tauri/tauri.phase6-physical.conf.json
      provides: Dedicated phase6-physical MSI configuration with updater disabled and physical feature enabled
    - path: apps/desktop/src-tauri/installer/optimizer-service.wxs
      provides: Transactional file/ACL/service install-update-rollback-uninstall ordering
    - path: tooling/phase6-physical/build-artifact.mjs
      provides: Real compile/stage/sign/CMS/MSI/lifecycle/immutable-manifest authority
    - path: target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json
      provides: Create-once canonical artifact authority signed by adjacent artifact-manifest.json.p7s
    - path: target/phase6-physical/{sourceCommit}/{buildId}/configs/clean-windows-vm.run-config.json
      provides: Hash-bound closed clean-VM runner configuration
    - path: target/phase6-physical/{sourceCommit}/{buildId}/configs/owner-pc.run-config.json
      provides: Hash-bound closed owner runner configuration
    - path: target/phase6-physical/{sourceCommit}/{buildId}/configs/friends-pc.run-config.json
      provides: Hash-bound closed friends runner configuration with purpose-bound local pseudonym mode and only the two literal roster/signature paths
  key_links:
    - from: tooling/phase6-physical/build-artifact.mjs
      to: apps/desktop/src-tauri/tauri.phase6-physical.conf.json
      via: release build with Cargo feature phase6-physical
      pattern: 'phase6-physical'
    - from: tooling/phase6-physical/build-artifact.mjs
      to: apps/desktop/src-tauri/src/bin/phase6-physical-runner.rs
      via: release runner is installed and portable while tauri-driver/msedgedriver are signed/hash-bound portable-only artifact roles
      pattern: 'phase6-physical-runner.*tauri-driver.*msedgedriver'
    - from: tooling/phase6-physical/build-artifact.mjs
      to: target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json.p7s
      via: builder alone emits three generated configs, hashes every required role, writes artifact manifest last, and applies detached CMS with the 06-35 compiled SPKI signer
      pattern: 'clean-windows-vm.run-config.json.*owner-pc.run-config.json.*friends-pc.run-config.json.*artifact-manifest.json.p7s'
    - from: apps/desktop/src-tauri/installer/optimizer-service.wxs
      to: '%ProgramFiles%\Liiiraa Boost\installation-manifest.json'
      via: InstallFiles/ACL completes before ServiceControl start; rollback restores old coherent component
      pattern: 'installation-manifest.json.*ServiceControl'
---
<objective>Build, sign, inspect, install, update, uninstall, and retain the real immutable physical artifact consumed by simulation and hardware promotion.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-35-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-39-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-37-SUMMARY.md
@apps/desktop/src-tauri/tauri.conf.json
@apps/desktop/src-tauri/tauri.staging.conf.json
@tooling/desktop-evidence/package-signed-desktop.mjs
<interfaces>
The normal NSIS/current-user build stays unchanged. The physical profile compiles `liiiraa-desktop` with Cargo feature `phase6-physical`, `liiiraa-optimizer-service.exe`, and `phase6-physical-runner.exe`. The MSI and `InstallationManifestDocument` contain exactly those installed runtime roles; tauri-driver and msedgedriver are never installed into Program Files and exist only as portable `ArtifactManifestDocument` roles beside the runner. The builder must locate their exact releases, verify versions/signatures/hashes, and fail if unavailable; it may not assume Node or ambient drivers exist in the guest. Output root is `target/phase6-physical/{sourceCommit}/{buildId}/`; all builder-emitted bytes are create-once. The builder is the sole creation authority for the three canonical 06-39 run configs and `artifact-manifest.json`/`.p7s`; no later plan may generate or edit them. The friends config contains only literal `friends/friends-roster.json` and `friends/friends-roster.json.p7s` references. Plan 06-28 alone may create that separately CMS-authenticated pair once at those reserved paths without changing any builder-emitted byte. The builder reserves a version absent from UAT/evidence/artifact/git history with minimum `managed-power-scheme-v3`; only 06-38 can admit it.
</interfaces>
</context>
<rule4_decision approved="2026-08-14" option="1">
The user approved preserving `ServiceSid=restricted`, the protected installed/storage ACLs, and the fixed local-only pipe while removing all WTS/session-token acquisition from service startup. `WindowsPipeHost::prepare` validates installed custody/CMS/hash/version/ACL, protected storage/admission, and pipe creation without selecting an interactive user. Only after a real named-pipe connection and peer authentication may the service derive PID, image hash, TokenUser SID, TokenSessionId, and logon SID through `ImpersonateNamedPipeClient`, duplicate one connection-owned token, verify `RevertToSelf`, and mint a lifetime-bound `InteractiveUserEffectLease`. LocalSystem, mismatch, impersonation failure, disconnect, timeout, panic, or token reuse must produce zero mutation calls and a bounded error; recovery observation remains available and there is no WTS fallback. This Rule 4 decision narrows startup authority without relaxing T-06-31B or changing the closed operation vocabulary.
</rule4_decision>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR immutable MSI, signed custody, and artifact builder</name>
  <read_first>apps/desktop/src-tauri/tauri.conf.json; apps/desktop/src-tauri/tauri.staging.conf.json; apps/desktop/src-tauri/Cargo.toml; apps/optimizer-service/Cargo.toml; 06-33-SUMMARY.md runner path/hash; 06-35-SUMMARY.md verifier/pin identity; 06-39 generated manifest/config/continuation contracts; tooling/desktop-evidence/package-signed-desktop.mjs; official Tauri WiX fragment and Windows Installer ServiceInstall/ServiceControl/MsiLockPermissionsEx/MsiServiceConfig/rollback documentation; 06-CONTEXT.md D-04, D-06, D-19, D-24, D-31, D-32</read_first>
  <files>apps/desktop/src-tauri/tauri.phase6-physical.conf.json, apps/desktop/src-tauri/installer/optimizer-service.wxs, tooling/phase6-physical/build-artifact.mjs, tooling/phase6-physical/build-artifact.test.mjs</files>
  <behavior>
    - Builder rejects drift in declared inputs and reused operation versions; binds HEAD commit, canonical input-tree hash, build ID, target, ProductCode/package version, operation version, and every source/output hash.
    - Dedicated MSI and `InstallationManifestDocument` contain exact desktop/service/runner roles only; portable artifact root also contains exact tauri-driver/msedgedriver and the three generated canonical stage configs. A driver in installed roles or Program Files is rejected. Desktop is asInvoker, service is OWN_PROCESS LocalSystem with restricted service SID/no network dependency, updater is disabled, and no arbitrary custom action exists.
    - Builder emits each 06-39 `PhysicalRunConfigDocument` with fixed stage/scenario/command/output/continuation policy and never accepts a caller-supplied config. The friends config's roster-related fields are exactly `friends/friends-roster.json` and `friends/friends-roster.json.p7s`, with no roster hash, participant selector, or alternate path. It then hashes MSI, both installation-manifest bytes, all three configs, runner, and the portable drivers into one `ArtifactManifestDocument`.
    - After every referenced byte is final, builder creates canonical `artifact-manifest.json`, signs it as adjacent detached CMS with the existing non-exportable development certificate whose SPKI matches the compiled 06-35 pin, Authenticode-signs all executable/MSI bytes, and re-verifies CMS/SPKI/live hashes before publishing the root.
    - Windows Installer writes the coherent files and SYSTEM/service-SID protected ACL before service start; update stops then transactionally replaces then starts; failed update rolls back the whole matching set before restart; downgrade is rejected; uninstall stops/removes binaries and manifest while preserving ProgramData journal/recovery/evidence and never forces reboot.
    - Existing output identity cannot be overwritten; only byte-identical verification succeeds.
  </behavior>
  <action>RED: add Node tests over temporary fake outputs and WiX/config mutation fixtures for dirty declared inputs, reused/reserved version, missing MSI/installation-manifest/config/runner/portable-driver role, either driver inserted into `InstallationManifestDocument` or Program Files, nonliteral friends-roster paths/fields, noncanonical config stage/command/continuation field, artifact CMS/content/SPKI swap, hash/signature replacement, wrong service account/SID/name/path, service start before manifest/ACL, elevated desktop, downgrade/updater enablement, custom shell action, incoherent rollback, journal deletion, forced reboot, and duplicate artifact identity; commit `test(06-31): add failing real physical artifact contract`. GREEN: add the separate Tauri physical MSI config and WiX fragment using Windows Installer tables only. Implement compile/stage/sign logic, canonical installation manifest limited to desktop/service/runner, protected directory/file ACL, restricted named service, coherent update/rollback, and recovery-preserving uninstall. Keep tauri-driver/msedgedriver portable beside the runner and out of the MSI/installed manifest. Generate exactly the three 06-39 stage configs under `configs/`; the friends config emits only the two literal roster/signature paths. Reserve but do not admit one never-used operation version at least v3. Only after all base bytes are final, emit the closed artifact manifest with MSI, installation manifest/signature, configs, runner, tauri-driver, and msedgedriver live identities, then emit its detached CMS signature and verify it through 06-35. Publish builder-owned bytes atomically and reserve the two friends paths for the later create-once signed pair; never accept external configs, change normal NSIS, track binaries, embed a private key/install secret, use script/custom-command authority, or overwrite an identity. Commit `feat(06-31): assemble signed physical MSI artifact`. REFACTOR only while mutation tests and dry-run resolution stay green.</action>
  <verify><automated>rtk node --test tooling/phase6-physical/build-artifact.test.mjs &amp;&amp; rtk node tooling/phase6-physical/build-artifact.mjs --mode dry-run --reserve-operation-version --minimum-version managed-power-scheme-v3</automated></verify>
  <acceptance_criteria>- Mutation tests cover source/version/file/signer/WiX/update/rollback/uninstall authority.
- Dry run resolves all five exact runtime roles and one unused operation version without writing artifact bytes.
- Physical desktop feature and normal desktop profile remain distinct; no fixture or updater fallback exists.
- Signed installed manifest contains desktop/service/runner only, precedes service start, and rollback restores that coherent installed set; both drivers remain portable-only.
- Friends config contains only the two generated-contract literal roster paths and cannot select roster membership or output location.
- Builder alone creates all three configs and the detached-CMS-signed artifact manifest containing every required live hash.</acceptance_criteria>
  <done>The real builder and MSI contract are implementation-complete and ready for the mandatory live build.</done>
</task>
<task type="auto">
  <name>Task 2: Produce and lifecycle-verify the real immutable physical artifact</name>
  <read_first>tooling/phase6-physical/build-artifact.mjs; Task 1 mutation-test output; 06-33-SUMMARY.md runner identity; 06-35-SUMMARY.md SPKI trust identity; current git HEAD/status</read_first>
  <files>target/phase6-physical/{sourceCommit}/{buildId}/liiiraa-boost.msi, target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json, target/phase6-physical/{sourceCommit}/{buildId}/artifact-manifest.json.p7s, target/phase6-physical/{sourceCommit}/{buildId}/installation-manifest.json, target/phase6-physical/{sourceCommit}/{buildId}/installation-manifest.json.p7s, target/phase6-physical/{sourceCommit}/{buildId}/configs/clean-windows-vm.run-config.json, target/phase6-physical/{sourceCommit}/{buildId}/configs/owner-pc.run-config.json, target/phase6-physical/{sourceCommit}/{buildId}/configs/friends-pc.run-config.json</files>
  <action>Run the builder in real `build-and-smoke` mode on Windows. It must compile release physical desktop/service/runner, stage exact portable tauri-driver/msedgedriver, reserve one never-used operation version, generate/sign the driver-free installation manifest, assemble/sign the MSI, generate the three canonical configs, then create/sign the artifact manifest last and publish the non-overwritable builder-owned bytes. Re-read every byte for SHA-256/size/AuthentiCode/CMS/SPKI verification through 06-35, including each config hash and both portable drivers. The same command must inspect MSI tables/content/ProductCode/package version and execute a contained install -> service/manifest/live-hash inspection -> repair/update -> rollback-failure drill -> uninstall lifecycle, proving the drivers were not installed, service ordering, no downgrade/forced reboot/custom action, and preservation of ProgramData recovery/journal/evidence hashes. The restricted service must start without WTS/session-token acquisition; only a real authenticated named-pipe connection may create the client-bound token/effect lease, with mismatch/disconnect/timeout/reconnect cases remaining zero-call or observation-first as applicable. Exit nonzero and retain a BLOCKED build report if signing, elevation, portable-driver matching, lifecycle, or any byte check is unavailable; do not report that bytes could be built. Record the one actual directory plus artifact-manifest/signature, MSI, installation-manifest/signature, config, runner, and portable-driver paths/hashes in SUMMARY; these exact values and the reserved fixed roster paths are the only inputs 06-27/06-28 may invoke.</action>
  <verify><automated>rtk node tooling/phase6-physical/build-artifact.mjs --mode build-and-smoke --reserve-operation-version --minimum-version managed-power-scheme-v3 --require-msi --require-lifecycle</automated></verify>
  <acceptance_criteria>- A real `.msi`, `artifact-manifest.json` plus `.p7s`, installation manifest plus `.p7s`, and all three configs exist under one create-once commit/build directory.
- Signed artifact manifest enumerates and live-hash/signature/version-binds MSI, installation manifest/signature, three configs, runner, tauri-driver, msedgedriver, ProductCode, source, build, and reserved operation version.
- MSI content/order/ACL and live install/update/rollback/uninstall assertions pass, including absence of both drivers from installed roles and Program Files.
- Recovery/journal/evidence custody hashes survive update rollback and uninstall.
- Restricted startup succeeds without WTS or an interactive session; a real pipe peer alone owns the effect token/lease, and mismatch, disconnect, timeout, or response loss cannot cause a second mutation.
- No completion is possible from dry-run or tests alone.</acceptance_criteria>
  <done>The exact installable bytes and manifest required by 06-38/06-34 exist and have passed real Windows lifecycle verification.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| source/toolchain -> signed artifact | Drift or substituted runtime bytes could receive false attribution. |
| MSI -> Program Files/SCM | Elevated installation can create persistent privilege or incoherent custody. |
| update/rollback/uninstall -> recovery data | Lifecycle operations can strand mismatched bytes or erase restoration authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-31A | Tampering/Repudiation | artifact identity | mitigate | Exact inputs, create-once output, actual hashes, Authenticode/CMS/SPKI post-verification. |
| T-06-31B | Elevation | MSI service/runtime contents | mitigate | Distinct installed/portable roles, no installed drivers, non-elevated desktop, restricted named service, Installer tables only, protected ACL, WTS-free startup, and one token/lease derived only from the authenticated connected pipe peer. |
| T-06-31C | Denial/Replay | update/rollback/uninstall | mitigate | Monotonic version, coherent transactional component, rollback/lifecycle smoke, preserved recovery custody. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-31A | CRITICAL | V14 Configuration; V7 Logging | block | mutation tests plus real final byte/signature/hash checks | Missing/substituted/reused bytes block 06-38. |
| T-06-31B | CRITICAL | V4 Access Control | block | MSI table/content/ACL/service inspection | Extra authority, wrong ordering, or elevated UI blocks 06-38. |
| T-06-31C | HIGH | V3 Session; V7 Logging | block | live install/update/rollback/uninstall report | Downgrade, incoherent rollback, forced reboot, or recovery deletion blocks 06-38. |
</threat_model>
<verification>Mutation suite and dry run pass, then the mandatory real build-and-smoke command produces and authenticates the artifact manifest/signature and every referenced immutable byte.</verification>
<success_criteria>A detached-CMS-authenticated, safely lifecycle-tested MSI/config/runner/driver set exists for one reserved fresh operation version.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md` only after Task 2 passes, recording exact artifact-manifest/signature, MSI, installation-manifest/signature, three config, runner, driver paths/hashes, ProductCode/version/reserved operation version, and lifecycle report.</output>
