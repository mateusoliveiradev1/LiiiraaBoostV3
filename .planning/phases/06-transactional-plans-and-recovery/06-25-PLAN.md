---
phase: 06-transactional-plans-and-recovery
plan: '25'
type: execute
wave: 7
depends_on: ['06-17', '06-19', '06-22', '06-24']
files_modified:
  - packages/feature-shell/src/features/improve.tsx
  - packages/feature-shell/src/features/transactional-plans.test.tsx
autonomous: true
requirements: [PLAN-04, PLAN-05]
must_haves:
  truths:
    - 'A user can understand, strongly authenticate, enable, revoke, and revalidate the device-local Advanced preference without confusing it with the global risk ceiling.'
    - 'Restart persistence and posture invalidation are visible, accessible, and native-authoritative; recovery remains available in every state.'
  artifacts:
    - path: packages/feature-shell/src/features/improve.tsx
      provides: Device-local Advanced preference lifecycle UI within the existing risk review workspace
    - path: packages/feature-shell/src/features/transactional-plans.test.tsx
      provides: Bilingual keyboard/screen-reader/restart/invalidation/strong-auth UI evidence
  key_links:
    - from: packages/feature-shell/src/features/improve.tsx
      to: packages/desktop-client/src/plans.ts
      via: authoritative preference projection and exact enable/revoke intents
      pattern: 'enableAdvancedPreference|revokeAdvancedPreference'
---
<objective>Complete D-13 with an accessible, honest Advanced enable/revoke/revalidation experience.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md @.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md @packages/feature-shell/src/features/improve.tsx @packages/desktop-client/src/plans.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the strongly authenticated Advanced preference lifecycle</name>
  <read_first>packages/feature-shell/src/features/improve.tsx; packages/feature-shell/src/features/transactional-plans.test.tsx; packages/desktop-client/src/plans.ts; 06-CONTEXT.md D-09–D-13, D-16; 06-UI-SPEC.md Risk and Interaction contracts; 06-22-UI-CONTRACT-APPROVAL.md</read_first>
  <files>packages/feature-shell/src/features/improve.tsx, packages/feature-shell/src/features/transactional-plans.test.tsx</files>
  <behavior>- Disabled state explains device-local persistence, strong-auth requirement, recovery guarantees, and the difference from the global ceiling.
- Enable and revoke each start their own fresh strong-auth action and wait for the authoritative native projection.
- Enabled state survives renderer/app restart only when native projection remains bound to current hardware/security posture.
- Invalidated state names the hardware/security-posture reason, blocks Advanced apply, and offers fresh revalidation; recovery stays enabled.</behavior>
  <action>Test first, then extend the existing Improve risk workspace without adding an unrelated settings surface. Render the authoritative `disabled|enabled|revoked|invalidated|unavailable` projection, device-local/persistent scope, last validation, adjacent blocking reason, and exact next action. Enable and revoke must open the existing strong-auth flow with distinct actions and remain pending until native truth returns; never optimistically toggle. On invalidation, focus the state heading, explain the changed hardware/security posture, disable Advanced apply via `aria-describedby`, and offer fresh enable/revalidation. Keep the global ceiling separate: it cannot enable Advanced and lowering it does not silently revoke the persisted preference. Recovery links/actions remain available offline, signed out, revoked, or invalidated.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell exec vitest --run src/features/transactional-plans.test.tsx -t "Advanced preference"</automated></verify>
  <acceptance_criteria>- Enable/revoke use distinct one-shot intent paths and no toggle claims success early.
- Restart renders the persisted native projection; changed posture renders invalidated and blocks apply.
- PT-BR/English copy explains scope, reason, revalidation, and recovery without generic error text.
- No cloud sync, generic settings, or renderer authority control exists.</acceptance_criteria>
  <done>The user can operate the complete D-13 lifecycle without misleading authority.</done>
</task>
<task type="auto">
  <name>Task 2: Prove keyboard, screen-reader, scaling, and recovery continuity</name>
  <read_first>packages/feature-shell/src/features/improve.tsx; packages/feature-shell/src/features/transactional-plans.test.tsx; 06-UI-SPEC.md Accessibility, Responsive, Motion, and Visual Verification contracts</read_first>
  <files>packages/feature-shell/src/features/transactional-plans.test.tsx</files>
  <action>Add state-matrix tests for keyboard-only enable/revoke/revalidate, focus entry/return from strong auth, one bounded assertive invalidation announcement, adjacent described blockers, text+icon+pattern status, PT-BR/English long copy, forced colors, reduced motion, 150% app scale, and 200% zoom/reflow. Prove recovery remains reachable in every preference state and Extremo remains non-executable. Do not use visual snapshot success as authority; assert semantics and intent calls.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run</automated></verify>
  <acceptance_criteria>- Full feature-shell suite passes all preference states and presentations.
- Focus returns to the exact trigger after auth and moves to invalidation heading on posture change.
- Status never relies on color and no horizontal scroll/clipped action appears at required scale.
- Recovery commands remain present and enabled throughout.</acceptance_criteria>
  <done>D-13 is accessible and comprehensible in every required presentation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| native preference projection -> user decision | Ambiguous scope or optimistic toggles could induce unauthorized Advanced apply. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-25A | Spoofing/Elevation | Advanced UI | mitigate | Authoritative projection, distinct strong-auth actions, no optimistic toggle, adjacent blockers. |
| T-06-25B | Denial | accessible revoke/recovery | mitigate | Keyboard/focus/reflow/bilingual tests and unconditional recovery affordances. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-25A | HIGH | V2 Authentication — fresh action-specific proof; V4 Access Control — no renderer authority | blocking | Enable/revoke/pending/restart/invalidation component tests | Any optimistic or cross-action path blocks 06-20 and physical promotion. |
| T-06-25B | MEDIUM | V4 Access Control — security/recovery controls remain operable | blocking for UI promotion | Keyboard/screen-reader/forced-color/zoom/recovery tests | Any inaccessible revoke, revalidation, or recovery path blocks E2E and physical sign-off. |
</threat_model>
<verification>Focused and full feature-shell suites prove the complete accessible D-13 experience after the independent UI-contract gate.</verification>
<success_criteria>Advanced preference authority is understandable, reversible, persistent, revalidated, and accessible per PC.</success_criteria>
<output>Create `06-25-SUMMARY.md`.</output>
