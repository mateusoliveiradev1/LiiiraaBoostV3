---
phase: 06-transactional-plans-and-recovery
plan: '38'
type: tdd
wave: 16
depends_on: ['06-31']
files_modified:
  - apps/desktop/tests/packaged/transactional-plans.ts
  - apps/desktop/tests/packaged/transactional-plans.test.ts
  - tooling/phase6-evidence/src/simulation-writer.ts
  - tooling/phase6-evidence/tests/simulation-writer.test.ts
  - package.json
  - tooling/phase6-evidence/evidence-manifest.json
  - .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'A never-before-used operation version at or above managed-power-scheme-v3 completes the canonical deterministic prepare/apply/verify/restart/reconcile/restore/verify and fault matrix.'
    - 'Simulation evidence is immutable and bound to the exact MSI artifact manifest, source commit, build ID, operation version, journal/receipt hashes, and complete decision/requirement coverage.'
    - 'phase6:verify planned admits exactly the new version before any clean-VM command can execute; prior blocked v1/v2 evidence remains unchanged.'
  artifacts:
    - path: tooling/phase6-evidence/src/simulation-writer.ts
      provides: Atomic canonical deterministic evidence writer for an artifact-reserved operation version
      exports: [writeCanonicalSimulationEvidence]
    - path: tooling/phase6-evidence/evidence-manifest.json
      provides: Exact new-version deterministic predecessor admitted for physical promotion
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
      provides: Append-only simulation admission transcript preserving all earlier blockers
  key_links:
    - from: tooling/phase6-evidence/src/simulation-writer.ts
      to: target/phase6-physical/{commit}/{buildId}/artifact-manifest.json
      via: actual byte hash and reserved operationVersion/build identity
      pattern: 'artifactManifestSha256.*operationVersion'
    - from: tooling/phase6-evidence/evidence-manifest.json
      to: phase6:verify --mode planned
      via: exact complete deterministic run evidence
      pattern: 'deterministic-simulation'
---
<objective>Admit a fresh exact operation version through the full deterministic gate and bind it to the real physical artifact before clean-VM execution.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-UAT.md
@.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md
@apps/desktop/tests/packaged/transactional-plans.ts
@tooling/phase6-evidence/src/evaluate.ts
@tooling/phase6-evidence/evidence-manifest.json
<interfaces>
06-31 creates one immutable artifact directory and reserves an operation version that does not occur in the existing UAT, evidence manifest, run files, artifact directories, or git history, with minimum `managed-power-scheme-v3`. Reservation is not admission. This plan executes deterministic semantics for that exact artifact/version and then atomically appends the simulation run. `TransactionalPackagedHarness` is permitted only as deterministic simulation input and remains structurally rejected by the 06-32 physical writer.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN the complete artifact-bound simulation emitter</name>
  <read_first>apps/desktop/tests/packaged/transactional-plans.ts; apps/desktop/tests/packaged/transactional-plans.test.ts; tooling/phase6-evidence/src/evaluate.ts; tooling/phase6-evidence/tests/evaluate.test.ts; tooling/phase6-evidence/evidence-manifest.schema.json; 06-UAT.md v1/v2 immutable blockers; 06-CONTEXT.md D-01 through D-08, D-17 through D-30</read_first>
  <files>apps/desktop/tests/packaged/transactional-plans.ts, apps/desktop/tests/packaged/transactional-plans.test.ts, tooling/phase6-evidence/src/simulation-writer.ts, tooling/phase6-evidence/tests/simulation-writer.test.ts, package.json</files>
  <behavior>
    - Writer accepts only an existing hash-valid 06-31 artifact manifest whose reserved operation version is absent from every prior authoritative source and is at least managed-power-scheme-v3.
    - Canonical run includes recovery preparation, exact prior/requested/observed/restored GUIDs, durable prepared/dispatch/observation/verified and restore events, reboot/reconciliation, apply/restore receipts, revocation, all required faults, accessibility automation, PLAN-01..08, and D-01..35 coverage.
    - Callback harness bytes are labeled deterministic only; writer cannot set physical provenance, human review, Narrator comprehension, owner/friends consent, or physical PASS.
    - Failure, partial lifecycle, duplicate version/evidence ID, artifact drift, or manifest mutation writes no admission and preserves every prior run/UAT byte.
  </behavior>
  <action>RED: extend deterministic harness tests for complete journal/receipt/reboot/fault/revocation/accessibility output and add simulation-writer mutation tests for reuse of either recorded blocked operation version, incomplete cycle, changed artifact bytes, duplicate ID, missing coverage, physical relabeling, and atomic failure; commit `test(06-38): add failing fresh-version simulation admission`. GREEN: complete the canonical deterministic emitter and implement a file-based atomic writer that recomputes the 06-31 artifact manifest hash, proves the reserved version has never appeared, emits immutable deterministic run bytes, and appends only the exact simulation cell. Add a `phase6:simulate` script with closed `--artifact-manifest` input and no physical flag. Do not use the legacy callback as physical evidence, modify prior blocker records, or accept a caller-declared PASS. Commit `feat(06-38): emit artifact-bound simulation admission`.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop exec vitest --run tests/packaged/transactional-plans.test.ts &amp;&amp; rtk pnpm --filter @liiiraa/phase6-evidence exec vitest --run tests/simulation-writer.test.ts tests/evaluate.test.ts</automated></verify>
  <acceptance_criteria>- The emitter covers the full deterministic lifecycle and required fault/policy matrix.
- Reused versions, partial cycles, artifact drift, and physical relabeling fail atomically.
- Prior v1/v2 UAT and evidence bytes are never overwritten.
- Deterministic source remains inadmissible to the physical writer.</acceptance_criteria>
  <done>A tested command can emit canonical simulation evidence for the exact reserved artifact/version only.</done>
</task>
<task type="auto">
  <name>Task 2: Mint and admit the reserved never-used operation version</name>
  <read_first>06-31-SUMMARY.md exact artifact-manifest path/hash; target/phase6-physical/{commit}/{buildId}/artifact-manifest.json; tooling/phase6-evidence/evidence-manifest.json; 06-UAT.md; simulation writer from Task 1</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>Run `phase6:simulate` against the exact artifact manifest recorded by 06-31. The command must prove the reserved operation version is new and at least `managed-power-scheme-v3`, execute the complete deterministic cycle, create immutable version-bound run evidence, atomically replace only the current manifest authority while retaining references to superseded blocked attempts, and append the exact command/output/hash transcript to UAT without editing either recorded blocked-version section. Then run the planned evaluator. If any case fails, append BLOCKED for the reserved version, fail the plan, and reserve a different never-used version only through a new immutable artifact build; never override or relabel.</action>
  <verify><automated>rtk pnpm phase6:simulate -- --artifact-manifest-from-summary .planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md --minimum-version managed-power-scheme-v3 &amp;&amp; rtk pnpm phase6:verify -- --mode planned</automated></verify>
  <acceptance_criteria>- The admitted operation version did not exist before 06-31 reservation and is at least v3.
- The evidence manifest, artifact manifest, runner, MSI identity, and UAT all name the same version/build/hash tuple.
- Planned verification reports `ok: true` and highest admitted stage `deterministic-simulation` for exactly that version.
- Earlier blocked evidence remains byte-preserved and later physical cells remain pending.</acceptance_criteria>
  <done>A fresh artifact-bound operation version is deterministically admitted and is the only legal predecessor for 06-34/06-26.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| immutable artifact -> deterministic emitter | A reused or substituted identity could bypass D-06 restart semantics. |
| deterministic evidence -> physical predecessor gate | Fabricated or partial simulation could authorize privileged mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-38A | Replay/Tampering | operation version | mitigate | Search all authoritative history, minimum v3, create-once artifact/run identity, actual hashes. |
| T-06-38B | Repudiation | complete simulation evidence | mitigate | Canonical journal/receipt/fault coverage, immutable run hash, append-only UAT transcript. |
| T-06-38C | Elevation | physical predecessor admission | mitigate | Planned evaluator accepts deterministic source only and physical writer rejects it. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-38A | CRITICAL | V3 Session; V5 Validation | block | reused-version/artifact-mutation tests | Any reused or mismatched identity blocks 06-34. |
| T-06-38B | CRITICAL | V7 Logging | block | complete lifecycle/coverage/evidence hashes | Missing lifecycle/fault/receipt evidence blocks planned admission. |
| T-06-38C | CRITICAL | V4 Access Control | block | physical relabel rejection and planned evaluator | Any simulated-as-physical path blocks clean-VM execution. |
</threat_model>
<verification>Focused deterministic/evaluator suites pass, the exact live simulation command succeeds, and planned verification admits only the new deterministic stage.</verification>
<success_criteria>D-06 is satisfied by a new complete simulation-admitted operation identity propagated unchanged into every physical stage.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-38-SUMMARY.md` with the exact version, artifact tuple, evidence hashes, commands, and TDD commits.</output>
