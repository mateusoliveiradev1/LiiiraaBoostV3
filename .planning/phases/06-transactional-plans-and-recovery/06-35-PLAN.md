---
phase: 06-transactional-plans-and-recovery
plan: '35'
type: tdd
wave: 11
depends_on: ['06-39']
files_modified:
  - apps/optimizer-service/Cargo.toml
  - apps/optimizer-service/src/installation_manifest.rs
  - apps/optimizer-service/src/artifact_manifest.rs
  - apps/optimizer-service/src/bin/phase6-artifact-verifier.rs
  - apps/optimizer-service/tests/installation_manifest.rs
  - apps/optimizer-service/tests/artifact_manifest.rs
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'One compiled-SPKI trust policy verifies both canonical installed custody and the portable artifact manifest before a service listens, an MSI/runner is launched, or physical evidence is ingested.'
    - 'Detached CMS verification covers canonical manifest bytes; every referenced file is then re-read for path, SHA-256, size, Authenticode/version policy, and ACL checks rather than trusting hashes declared by a mutable document.'
    - 'The optimizer-service Windows target explicitly enables the Cryptography and WinTrust bindings required by CryptQueryObject, CertGetCertificateContextProperty, and WinVerifyTrust.'
  artifacts:
    - path: apps/optimizer-service/Cargo.toml
      provides: Explicit `Win32_Security_Cryptography` and `Win32_Security_WinTrust` windows features
    - path: apps/optimizer-service/src/installation_manifest.rs
      provides: Canonical Program Files installation-manifest CMS/SPKI/hash/version/ACL verifier
      exports: [verify_installed_manifest, VerifiedInstallationManifest]
    - path: apps/optimizer-service/src/artifact_manifest.rs
      provides: Portable artifact-manifest CMS/SPKI/hash/role/config verifier
      exports: [verify_artifact_manifest, VerifiedArtifactManifest]
    - path: apps/optimizer-service/src/bin/phase6-artifact-verifier.rs
      provides: Fixed-argument host verifier consumed before evidence ingestion and Hyper-V staging
  key_links:
    - from: apps/optimizer-service/src/artifact_manifest.rs
      to: contracts/generated/desktop/v1/transactional-recovery.schema.json
      via: generated ArtifactManifestDocument and PhysicalRunConfigDocument validation before semantic verification
      pattern: 'ArtifactManifestDocument.*PhysicalRunConfigDocument'
    - from: apps/optimizer-service/src/artifact_manifest.rs
      to: artifact-manifest.json.p7s
      via: detached CMS verification anchored only in compiled TRUSTED_INSTALLER_SPKI_SHA256
      pattern: 'CryptQueryObject.*CertGetCertificateContextProperty.*TRUSTED_INSTALLER_SPKI_SHA256'
    - from: apps/optimizer-service/src/installation_manifest.rs
      to: '%ProgramFiles%\Liiiraa Boost\installation-manifest.json.p7s'
      via: detached CMS, live hash/AuthentiCode, canonical path, ACL, and monotonic-version verification before pipe creation
      pattern: 'WinVerifyTrust.*verify_installed_manifest'
---
<objective>Implement the common fail-closed signature and live-byte custody gate for installed and portable Phase 6 artifacts.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-39-SUMMARY.md
@apps/optimizer-service/Cargo.toml
@apps/optimizer-service/src/ipc.rs
@contracts/generated/desktop/v1/transactional-recovery.schema.json
<interfaces>
06-39 generates `InstallationManifestDocument`, `ArtifactManifestDocument`, and `PhysicalRunConfigDocument`. Installed custody resolves only `%ProgramFiles%\Liiiraa Boost\installation-manifest.json` plus `.p7s`. Portable custody resolves only a caller-supplied canonical `artifact-manifest.json` plus the adjacent `.p7s`, then confines every role path below that manifest's create-once root. Both signatures are detached CMS over canonical JSON and must chain to the same compiled `TRUSTED_INSTALLER_SPKI_SHA256`; a signer/SPKI/hash supplied by the document, environment, CLI, or renderer is never trust authority. The host verifier CLI accepts exactly `--artifact-manifest <absolute-canonical-path>` and emits a bounded JSON verdict; it accepts no trust pin, executable, command, stage, or expected hash argument.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR installed and portable signed-manifest verification</name>
  <read_first>apps/optimizer-service/Cargo.toml; generated physical contracts from 06-39; apps/optimizer-service/src/ipc.rs; Microsoft CryptQueryObject, CryptMsg, CertGetCertificateContextProperty, WinVerifyTrust, file-security, and reparse-point references from 06-RESEARCH.md; 06-CONTEXT.md D-06, D-19, D-31, D-32</read_first>
  <files>apps/optimizer-service/Cargo.toml, apps/optimizer-service/src/installation_manifest.rs, apps/optimizer-service/src/artifact_manifest.rs, apps/optimizer-service/src/bin/phase6-artifact-verifier.rs, apps/optimizer-service/tests/installation_manifest.rs, apps/optimizer-service/tests/artifact_manifest.rs</files>
  <behavior>
    - Both verifiers validate generated schema first, canonicalize the exact signed JSON bytes, verify detached CMS content, derive signer SPKI through Windows certificate APIs, compare it to the compiled pin, and reject trust supplied by mutable inputs.
    - Portable verification requires the exact closed role set from 06-39, adjacent `.p7s`, canonical root containment/no reparse escape, unique paths, actual size/SHA-256, Authenticode/version policy for executable/MSI/driver roles, and actual hashes for all three stage configs and both installation-manifest bytes.
    - Installed verification resolves only the canonical Program Files pair, re-reads desktop/service/runner/driver bytes, validates Authenticode, ProductCode/package monotonicity, owner/DACL/service-SID custody, and protected last-admitted identity before service listen.
    - Missing/swapped/old/schema-invalid/signature-invalid/wrong-SPKI/hash-mismatched/unsigned/downgraded/replayed/weak-ACL/reparse artifacts fail closed. The fixed host CLI returns nonzero and bounded diagnostics without executing or installing any referenced byte.
    - Windows compilation proves the exact `Win32_Security_Cryptography` and `Win32_Security_WinTrust` bindings are enabled; tests call the typed CryptQueryObject, CertGetCertificateContextProperty, and WinVerifyTrust seams rather than leaving unreachable prose.
  </behavior>
  <action>RED: add installed and portable mutation corpora for absent signatures, CMS/content swap, wrong signer/SPKI, manifest self-asserted pin, role/path/config substitution, traversal/reparse escape, size/hash/AuthentiCode/version mismatch, downgrade/replay, weak DACL, extra/missing role, and host-CLI argument widening; include a Windows compile witness that imports the required Cryptography and WinTrust APIs. Commit `test(06-35): add failing signed custody verifier corpus`. GREEN: add `Win32_Security_Cryptography` and `Win32_Security_WinTrust` to the existing `windows = 0.62.2` feature list in `apps/optimizer-service/Cargo.toml`. Implement shared CMS/SPKI/canonical-path/live-byte helpers, then the separate installed and portable semantic verifiers over 06-39 generated documents. Add the fixed `phase6-artifact-verifier` binary solely so 06-32 and 06-34 can prove signature/pin/live bytes before ingestion/staging; it may inspect only and cannot install, launch, mutate, accept a stage, or accept caller trust. Keep raw certificates, tokens, credentials, and private keys out of logs/output. Commit `feat(06-35): verify installed and portable signed custody`. REFACTOR only while both mutation suites and the Windows target build remain green.</action>
  <verify><automated>rtk cargo test -p liiiraa-optimizer-service --test installation_manifest --test artifact_manifest &amp;&amp; rtk cargo check -p liiiraa-optimizer-service --all-targets --target x86_64-pc-windows-msvc</automated></verify>
  <acceptance_criteria>- Cargo explicitly enables `Win32_Security_Cryptography` and `Win32_Security_WinTrust`, and Windows all-target compilation reaches those APIs.
- Detached CMS content and signer SPKI are verified before any declared hash is trusted.
- Portable verification authenticates MSI, both installation-manifest bytes, all stage configs, runner, and exact drivers from live bytes.
- Installed verification enforces live hashes/AuthentiCode/version/ACL before pipe creation.
- The helper CLI has one inspection-only argument and every mutation exits nonzero before install/run/ingest.</acceptance_criteria>
  <done>The service, evidence host, and Hyper-V bridge have one executable compiled-pin trust decision for all Phase 6 custody bytes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest/signature -> compiled verifier | Mutable disk bytes attempt to assert signer, executable, config, and version identity. |
| portable artifact -> installer/runner/writer | A substituted manifest or file could gain execution or promotion authority. |
| Program Files -> LocalSystem service | Writable or downgraded installed bytes could gain persistent privilege. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-35A | Tampering/Spoofing | detached CMS and live files | mitigate | CMS content verification, compiled SPKI pin, generated schema, live hashes/AuthentiCode/versions. |
| T-06-35B | Elevation | canonical roots and ACLs | mitigate | Root containment, reparse rejection, SYSTEM/service-SID protected DACL, no ordinary-user write. |
| T-06-35C | Replay | package/config custody | mitigate | Source/build/operation identity, protected monotonic installed version, create-once portable root. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-35A | CRITICAL | V5 Validation; V14 Configuration | block | CMS/SPKI/hash/AuthentiCode mutation suites | Any self-asserted trust or byte mismatch blocks 06-30/06-32. |
| T-06-35B | CRITICAL | V4 Access Control | block | ACL/reparse/path tests | Writable/escaping custody blocks service and bridge admission. |
| T-06-35C | HIGH | V3 Session; V7 Logging | block | downgrade/replay/config-swap tests | Old or mismatched custody blocks all physical execution. |
</threat_model>
<verification>Both signed-manifest suites and Windows all-target compilation pass with the exact Cryptography/WinTrust feature set.</verification>
<success_criteria>Every Phase 6 installation, physical run, Hyper-V copy, and evidence ingestion can fail closed on one authenticated artifact identity.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-35-SUMMARY.md` with compiled pin provenance, helper identity, Cargo feature proof, and TDD commits.</output>
