---
phase: 06-transactional-plans-and-recovery
plan: '35'
type: tdd
wave: 10
depends_on: ['06-21']
files_modified:
  - packages/contracts-source/src/transactional-plans.tsp
  - contracts/generated/desktop/v1/transactional-recovery.schema.json
  - crates/contracts-rust/src/generated.rs
  - crates/contracts-rust/tests/transactional_plans_corpus.rs
  - apps/optimizer-service/src/installation_manifest.rs
  - apps/optimizer-service/tests/installation_manifest.rs
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'The installer, desktop artifact builder, and privileged service share one generated closed installation-manifest contract with one canonical installed location.'
    - 'The service trusts only a detached CMS signature whose signer SPKI hash is compiled into the service and whose manifest hashes match the live desktop, service, runner, and driver bytes.'
    - 'Absent, replaced, downgraded, schema-invalid, signature-invalid, hash-mismatched, or ACL-weakened installation custody fails before the service listens or dispatches.'
  artifacts:
    - path: packages/contracts-source/src/transactional-plans.tsp
      provides: Canonical InstallationManifestDocument and file-entry contract
      contains: InstallationManifestDocument
    - path: apps/optimizer-service/src/installation_manifest.rs
      provides: Canonical-path schema/signature/hash/version/ACL verifier
      exports: [verify_installed_manifest, VerifiedInstallationManifest]
    - path: apps/optimizer-service/tests/installation_manifest.rs
      provides: Mutation corpus for trust, downgrade, substitution, and custody failures
  key_links:
    - from: apps/optimizer-service/src/installation_manifest.rs
      to: contracts/generated/desktop/v1/transactional-recovery.schema.json
      via: generated runtime validation before semantic verification
      pattern: 'InstallationManifestDocument'
    - from: apps/optimizer-service/src/installation_manifest.rs
      to: '%ProgramFiles%\Liiiraa Boost\installation-manifest.json.p7s'
      via: detached CMS verification against compiled SPKI SHA-256 trust pins
      pattern: 'TRUSTED_INSTALLER_SPKI_SHA256'
---
<objective>Define and enforce the signed installation custody that binds the installed desktop, service, physical runner, and exact operation version.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md
@.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md
@packages/contracts-source/src/transactional-plans.tsp
@apps/optimizer-service/src/ipc.rs
<interfaces>
The canonical installed pair is `%ProgramFiles%\Liiiraa Boost\installation-manifest.json` and `%ProgramFiles%\Liiiraa Boost\installation-manifest.json.p7s`. The document binds schema version, source commit, input-tree hash, build ID, operation version, MSI ProductCode/package version, creation time, signer SPKI SHA-256, and exact relative path/SHA-256/size/Authenticode identity for desktop EXE, optimizer-service EXE, self-contained physical-runner EXE, `tauri-driver.exe`, and `msedgedriver.exe`. The detached CMS signature covers the canonical JSON bytes. Trust pins are compiled into the service binary and cannot come from the manifest or mutable environment.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN the canonical installation-manifest contract</name>
  <read_first>packages/contracts-source/src/transactional-plans.tsp; packages/contracts-source/src/main.tsp; contracts/generated/desktop/v1/transactional-recovery.schema.json; crates/contracts-rust/tests/transactional_plans_corpus.rs; 06-CONTEXT.md D-06, D-19, D-31, D-32</read_first>
  <files>packages/contracts-source/src/transactional-plans.tsp, contracts/generated/desktop/v1/transactional-recovery.schema.json, crates/contracts-rust/src/generated.rs, crates/contracts-rust/tests/transactional_plans_corpus.rs</files>
  <behavior>
    - Valid documents require every canonical identity/file field, lower-case SHA-256, bounded relative paths, monotonic package version, and the exact closed file roles.
    - Unknown fields, absolute/traversing paths, duplicate roles, missing runner/driver/service/desktop entries, signer supplied outside the signed document, and operation/build identity ambiguity are contract-invalid.
    - TypeSpec regeneration produces equivalent JSON Schema and Rust transport types with no handwritten DTO duplicate.
  </behavior>
  <action>RED: extend the cross-runtime corpus with valid and adversarial installation documents, including path traversal, missing/duplicate role, downgraded version, malformed hash, signer confusion, unknown field, and operation-version mismatch; commit the failing contract tests as `test(06-35): add failing installation manifest corpus`. GREEN: add `InstallationManifestDocument` to the existing TypeSpec transactional library, regenerate JSON Schema and Rust transports, and keep the role vocabulary closed to the five physical artifact roles. Do not create a handwritten Rust/JavaScript manifest shape, accept executable arguments, embed an install secret, or permit arbitrary files per D-31. Commit as `feat(06-35): generate installation manifest contract`.</action>
  <verify><automated>rtk pnpm test:contracts &amp;&amp; rtk cargo test -p liiiraa-contracts-rust --test transactional_plans_corpus</automated></verify>
  <acceptance_criteria>- Generated schema and Rust types derive from one TypeSpec source.
- The exact five artifact roles and identity bindings are required.
- Unsafe paths, duplicate/missing roles, unknown fields, and malformed identities are rejected.</acceptance_criteria>
  <done>The installer and service share one generated, closed installation-manifest document.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: RED/GREEN the fail-closed installed-manifest verifier</name>
  <read_first>apps/optimizer-service/src/ipc.rs; apps/optimizer-service/Cargo.toml; generated InstallationManifestDocument from Task 1; Microsoft CryptQueryObject/CertGetCertificateContextProperty/WinVerifyTrust and file-security references in 06-RESEARCH.md</read_first>
  <files>apps/optimizer-service/src/installation_manifest.rs, apps/optimizer-service/tests/installation_manifest.rs</files>
  <behavior>
    - Verifier resolves only the canonical Program Files pair, validates generated schema, verifies detached CMS bytes, pins signer SPKI SHA-256 compiled into the service, and recomputes every live file hash/size/Authenticode identity.
    - Directory and files must be owned/protected for SYSTEM plus the restricted service SID with no ordinary-user write ACE; reparse points and paths escaping the install root fail.
    - Package version/ProductCode cannot move backward relative to protected last-admitted custody; missing, swapped, old, tampered, unsigned, wrong-signer, weak-ACL, or rollback-restored mismatched bytes fail before pipe creation.
  </behavior>
  <action>RED: create mutation tests using temporary canonical-layout fixtures and Windows-gated CMS/ACL cases for absence, byte swap, manifest/signature mismatch, wrong SPKI, downgrade, replayed old manifest, reparse/path escape, weak DACL, and live hash/signature mismatch; commit `test(06-35): add failing installed custody verifier`. GREEN: implement `verify_installed_manifest` with runtime generated validation, Windows CMS/certificate APIs, compiled SPKI pins, actual byte hashing, canonical path/reparse checks, ACL inspection, and service-protected monotonic package custody. Return a typed `VerifiedInstallationManifest` with no raw signature/token logging. Never trust signer material, expected hashes, ProductCode, package version, or path from environment/CLI/renderer input. Commit `feat(06-35): verify signed installation custody`.</action>
  <verify><automated>rtk cargo test -p liiiraa-optimizer-service --test installation_manifest &amp;&amp; rtk cargo check -p liiiraa-optimizer-service --target x86_64-pc-windows-msvc</automated></verify>
  <acceptance_criteria>- Signature verification is anchored in compiled trust material, not self-asserted manifest data.
- Every installed artifact and ACL is recomputed from live bytes/security descriptors.
- Downgrade, substitution, replay, absence, and tampering stop service admission.</acceptance_criteria>
  <done>The privileged service has a deterministic fail-closed installation trust decision consumed by 06-30.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| MSI/builder -> Program Files custody | Elevated installation writes bytes later trusted by a LocalSystem service. |
| installed manifest/signature -> service startup | Mutable disk bytes attempt to assert executable and version identity. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-35A | Tampering/Spoofing | signed manifest | mitigate | Canonical JSON, detached CMS, compiled SPKI pin, actual file/AuthentiCode recomputation. |
| T-06-35B | Elevation | install ACL/path | mitigate | Canonical root, reparse rejection, SYSTEM/service-SID protected DACL, no user write. |
| T-06-35C | Replay | update/rollback custody | mitigate | Monotonic ProductCode/package version plus protected last-admitted identity. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-35A | CRITICAL | V5 Validation; V14 Configuration | block | Contract corpus and CMS/hash mutation suite | Any self-signed trust, missing role, or byte mismatch blocks 06-30. |
| T-06-35B | CRITICAL | V4 Access Control | block | ACL/reparse/path tests | Writable or escaping custody blocks service startup. |
| T-06-35C | HIGH | V3 Session; V7 Logging | block | downgrade/replay/rollback tests | Older or mismatched custody blocks update admission and physical build. |
</threat_model>
<verification>Contract generation/drift, Rust corpus, installed-manifest mutation suite, and Windows target check all pass.</verification>
<success_criteria>The service can authenticate installed desktop bytes through a canonical signed manifest that the MSI can install/update/rollback atomically.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-35-SUMMARY.md` with generated artifacts, trust-pin provenance, TDD commits, and exact test output.</output>
