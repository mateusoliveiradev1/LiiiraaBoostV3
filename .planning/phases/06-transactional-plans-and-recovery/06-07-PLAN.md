---
phase: 06-transactional-plans-and-recovery
plan: '07'
type: tdd
wave: 3
depends_on: ['06-03']
files_modified: [crates/plan-engine/src/dependency.rs, crates/plan-engine/tests/dependency_rollback.rs]
autonomous: true
requirements: [PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Partial failure restores only verified applied nodes in the affected dependency closure, in reverse topological order.'
    - 'Independent verified groups remain active; cycle or failed restore blocks later mutation.'
  artifacts:
    - path: crates/plan-engine/src/dependency.rs
      provides: DAG validation and scoped rollback planner
    - path: crates/plan-engine/tests/dependency_rollback.rs
      provides: Closure and ordering properties
  key_links:
    - from: crates/plan-engine/src/dependency.rs
      to: executor restore transaction
      via: ordered affected closure
      pattern: 'reverse.*topolog|rollback'
---
<objective>Implement dependency-scoped rollback through property-driven TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@crates/plan-engine/src/domain.rs @.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify affected closure and restore order</name><read_first>crates/plan-engine/src/dependency.rs; crates/plan-engine/tests/dependency_rollback.rs (confirm absent); crates/plan-engine/src/domain.rs; 06-CONTEXT.md D-15, D-17, D-20–D-23</read_first><files>crates/plan-engine/tests/dependency_rollback.rs</files><action>Write failing property tests proving cycle rejection, deterministic topological apply order, no restore outside the failed closure, dependent-before-dependency reverse order, preservation of independent verified nodes, safe-boundary cancellation, and global mutation block after restore failure.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test dependency_rollback</automated></verify><acceptance_criteria>- RED is an assertion failure.
- Random DAG permutations prove identical closure and order.
- Flat “undo all” behavior is explicitly rejected.</acceptance_criteria><done>RED dependency safety contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Compute deterministic rollback plans</name><read_first>crates/plan-engine/src/dependency.rs; crates/plan-engine/tests/dependency_rollback.rs; crates/plan-engine/src/domain.rs</read_first><files>crates/plan-engine/src/dependency.rs</files><action>Implement validated DAG construction, stable topological ordering, affected dependency closure, verified/applied filtering, reverse restore order, independent preservation, and restore-failure mutation gate. Return explicit causes and node sets for user explanation per D-17.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test dependency_rollback</automated></verify><acceptance_criteria>- All generated DAG properties pass.
- Output includes failed operation, affected closure, preserved operations, restore order, and blocking verdict.
- No mutation retry decision is emitted.</acceptance_criteria><done>Partial failures have deterministic, minimal-scope recovery.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Stress dependency invariants</name><read_first>crates/plan-engine/src/dependency.rs; crates/plan-engine/tests/dependency_rollback.rs</read_first><files>crates/plan-engine/src/dependency.rs, crates/plan-engine/tests/dependency_rollback.rs</files><action>Minimize graph traversal duplication and add dense/sparse graph, disconnected group, cancellation, and restore-failure properties. Commit refactor only if behavior remains green.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine dependency</automated></verify><acceptance_criteria>- Focused suite passes with deterministic seeds.
- No restored node is outside the computed failed closure.</acceptance_criteria><done>Rollback scope is exhaustively proven.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| plan dependency input -> recovery actions | A malformed graph could widen rollback or leave dependents active. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-07A | Tampering/Denial | dependency planner | mitigate | Cycle rejection, canonical DAG, reverse affected-closure properties, fail-closed restore failure. |
</threat_model>
<verification>Property suite and TDD gate commits pass.</verification>
<success_criteria>Partial failure never silently broadens or under-scopes recovery.</success_criteria>
<output>Create `06-07-SUMMARY.md`.</output>
