---
phase: 06-transactional-plans-and-recovery
plan: '32'
type: tdd
wave: 10
depends_on: ['06-21']
files_modified:
  - tooling/phase6-evidence/src/physical-writer.ts
  - tooling/phase6-evidence/tests/physical-writer.test.ts
  - tooling/phase6-evidence/src/evaluate.ts
  - tooling/phase6-evidence/tests/evaluate.test.ts
  - tooling/phase6-evidence/evidence-manifest.schema.json
  - package.json
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Only bounded output from the signed self-contained Rust guest runner can create evidenceKind physical; TypeScript runners, deterministic harnesses, callbacks, fixtures, browser runs, and relabeled files are rejected.'
    - 'Physical evidence binds exact stage predecessor, VM/checkpoint or real participant, source/build/artifact identity, operation version, GUID lifecycle, dispatch counts, journal/receipt hashes, fault results, and accessibility records.'
    - 'Incomplete, mismatched, oversized, secret-bearing, or unreviewed evidence stays FAIL/BLOCKED and cannot advance promotion.'
  artifacts:
    - path: tooling/phase6-evidence/src/physical-writer.ts
      provides: Atomic fail-closed physical evidence ingestion and redaction
      exports: [writePhysicalRunEvidence, validateGuestRunEnvelope]
    - path: tooling/phase6-evidence/evidence-manifest.schema.json
      provides: Conditional physical provenance requirements
    - path: package.json
      provides: Closed phase6:ingest-physical host command for runner envelopes
  key_links:
    - from: tooling/phase6-evidence/src/physical-writer.ts
      to: tooling/phase6-evidence/src/evaluate.ts
      via: canonical run hash and exact stage/build/runner-executable bindings
      pattern: 'physicalProvenance|phase6-physical-runner-rust-1'
---
<objective>Make fabrication-resistant physical evidence a separate authority from deterministic packaged fixtures.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-21-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-UAT.md
@tooling/phase6-evidence/src/evaluate.ts
@tooling/phase6-evidence/evidence-manifest.schema.json
@apps/desktop/tests/packaged/transactional-plans.ts
<interfaces>
`TransactionalPackagedHarness.executePhysicalMutation(callback)` remains deterministic test composition and must never be a source accepted by this writer. `Phase6RunEvidence.evidenceKind` already distinguishes deterministic/physical; add conditional physical provenance rather than accepting that label alone. Existing final evaluator ordering and strictly-later human review bindings remain authoritative.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR physical provenance ingestion</name>
  <read_first>tooling/phase6-evidence/src/evaluate.ts; tooling/phase6-evidence/tests/evaluate.test.ts; tooling/phase6-evidence/evidence-manifest.schema.json; tooling/phase6-evidence/evidence-manifest.json; apps/desktop/tests/packaged/transactional-plans.ts; apps/desktop/tests/packaged/transactional-plans.test.ts; 06-CONTEXT.md D-01, D-02, D-06, D-07, D-29, D-30, D-35</read_first>
  <files>tooling/phase6-evidence/src/physical-writer.ts, tooling/phase6-evidence/tests/physical-writer.test.ts, tooling/phase6-evidence/src/evaluate.ts, tooling/phase6-evidence/tests/evaluate.test.ts, tooling/phase6-evidence/evidence-manifest.schema.json, package.json</files>
  <behavior>
    - `evidenceKind: physical` requires source `phase6-physical-runner-rust-1`, exact signed runner path/hash/signature plus artifact-manifest path/hash, source commit/input hash/build ID, stage, predecessor evidence tuple, environment identity, operation/version, prior/requested/observed/restored GUIDs, per-case dispatch counts, journal/receipt hashes, full cycle, faults, and accessibility status.
    - Clean VM additionally requires exact VM/checkpoint/installed-checkpoint, OS build, Generation 2, Secure Boot, TPM, integration-service audit, installer/product/service identity, and run timestamps after artifact creation.
    - Any callback/simulation/fixture/browser/TypeScript/test-runner provenance, caller-supplied `physical` label without raw signed-runner envelope, missing file, runner/artifact hash mismatch, timestamp inversion, duplicated evidence ID, raw SID/credential/hardware serial, size over 64 KiB, incomplete cycle, nonzero rejected-case dispatch, or absent accessibility record fails before manifest mutation.
    - Writer canonicalizes/redacts, writes a new immutable run file atomically, then appends its ID/hash to the manifest; it never edits an existing run/review and leaves a precise BLOCKED diagnostic on failure.
  </behavior>
  <action>RED: add adversarial tests that attempt to relabel the existing callback harness, TypeScript runner files, and deterministic fixtures, mutate every physical binding including the runner EXE/signature/hash, inject secrets/oversize/duplicate IDs, omit GUID/dispatch/accessibility records, and reorder stage/timestamps; prove all fail before manifest mutation and commit `test(06-32): add failing physical evidence provenance gate`. GREEN: implement a file-based guest-run envelope reader with closed source literal `phase6-physical-runner-rust-1`, schema/semantic checks, actual SHA-256/AuthentiCode recomputation for the runner and SHA-256 recomputation for every referenced artifact, canonical redaction, atomic create-new persistence, and evaluator/schema conditional rules. Expose it through the closed root command `phase6:ingest-physical`, accepting only artifact-manifest path, runner-envelope path, and closed stage enum before resolving all other identity from signed bytes. Do not accept an in-memory callback, TypeScript runner, Boolean `physical`, browser witness, synthetic participant, generic output path, or caller-provided hash without reading bytes. Retain human review as pending until the subsequent human checkpoint; deterministic evidence remains valid only for simulation. Commit `feat(06-32): enforce physical evidence provenance`. REFACTOR after both focused suites pass.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/phase6-evidence exec vitest --run tests/physical-writer.test.ts tests/evaluate.test.ts</automated></verify>
  <acceptance_criteria>- Existing deterministic callback fixtures cannot produce or be relabeled as physical evidence.
- Every requested VM/build/operation/GUID/dispatch/accessibility binding is required and mutation-tested.
- Failure is atomic and leaves existing run/review bytes unchanged.
- RED/GREEN commits exist and evaluator planned/final behavior remains compatible.</acceptance_criteria>
  <done>Physical PASS can be written only from the real runner's bounded attributable output.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| guest output -> host evidence writer | Guest files and labels are attacker-controlled until verified. |
| physical run -> promotion evaluator | Fabricated or mismatched evidence could authorize later hardware. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-32A | Spoofing/Tampering | evidenceKind physical | mitigate | Closed runner source, actual file hashing, environment/build/version/GUID/dispatch conditional schema and mutation tests. |
| T-06-32B | Repudiation | immutable run record | mitigate | Canonical create-new bytes, unique ID, atomic append, strictly later independent review. |
| T-06-32C | Information Disclosure | diagnostic payload | mitigate | 64 KiB limit, allowlisted keys, raw secret/SID/serial scan, hashed participant identity. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-32A | CRITICAL | V5 Validation; V4 Access Control | block | relabel/mutation suite | Any callback/fixture admission or missing binding blocks 06-33/06-34 and physical stages. |
| T-06-32B | CRITICAL | V7 Logging | block | duplicate/timestamp/atomicity tests | Any overwrite or review-before-run path blocks promotion. |
| T-06-32C | HIGH | V8 Data Protection | block | secret/size/redaction tests | Any raw sensitive field blocks evidence persistence. |
</threat_model>
<verification>Physical writer and evaluator adversarial suites pass; `rtk pnpm phase6:verify -- --mode planned` stays green while final still blocks absent physical runs.</verification>
<success_criteria>Simulation evidence is cryptographically and structurally incapable of impersonating a physical Windows run.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-32-SUMMARY.md` with adversarial cases and TDD commits.</output>
