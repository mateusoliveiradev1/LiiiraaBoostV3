---
phase: 06-transactional-plans-and-recovery
plan: '32'
type: tdd
wave: 12
depends_on: ['06-21', '06-35']
files_modified:
  - tooling/phase6-evidence/src/physical-writer.ts
  - tooling/phase6-evidence/tests/physical-writer.test.ts
  - tooling/phase6-evidence/src/evaluate.ts
  - tooling/phase6-evidence/tests/evaluate.test.ts
  - tooling/phase6-evidence/evidence-manifest.schema.json
  - package.json
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Every evaluator/schema/fixture path uses the exact ordered closed requirement set PLAN-01 through PLAN-08; omission of PLAN-02, PLAN-03, or PLAN-04 fails independently.'
    - 'The CLI has explicit planned-mode gates for one pending run and one admitted stage, rejects missing values/noncanonical names/unknown `--stage`, and uses final mode only for the complete four-stage verdict.'
    - 'No physical envelope is parsed or persisted until the detached artifact-manifest CMS, compiled SPKI pin, exact role set, live hashes, config hash, runner, MSI, installation manifests, and drivers pass the 06-35 verifier.'
    - 'Friends promotion has one generated-valid, same-SPKI CMS-signed, immutable pseudonymous roster plus append-only per-machine runs, local consents, and later reviews; final admission requires exact one-to-one cardinality for every roster member.'
    - 'Legacy blocked v1/v2 evidence remains immutable and cannot be migrated into PASS by relabeling or overwrite.'
  artifacts:
    - path: tooling/phase6-evidence/src/evaluate.ts
      provides: Explicit targeted planned gates, complete final gate, and roster/cardinality evaluation
      exports: [evaluatePhase6Evidence, PHASE6_PROMOTION_STAGES]
    - path: tooling/phase6-evidence/src/physical-writer.ts
      provides: Signed-artifact-first, create-only physical run/roster/consent/review persistence
      exports: [writePhysicalRunEvidence, freezeFriendsRoster, appendPhysicalReview]
    - path: tooling/phase6-evidence/evidence-manifest.schema.json
      provides: Versioned append-only collections plus exact PLAN-01..08 coverage contract
    - path: package.json
      provides: Closed phase6 ingestion, roster-freeze, review-recording, and verification commands
  key_links:
    - from: tooling/phase6-evidence/src/physical-writer.ts
      to: apps/optimizer-service/src/bin/phase6-artifact-verifier.rs
      via: fixed 06-35 inspection command must PASS before envelope/config/run bytes are accepted
      pattern: 'phase6-artifact-verifier.*--artifact-manifest'
    - from: tooling/phase6-evidence/src/physical-writer.ts
      to: tooling/phase6-evidence/src/evaluate.ts
      via: create-only signed roster/runs/consents/reviews bind artifact, config, participant, stage, and predecessor hashes
      pattern: 'friendsRosterSha256|participantId|runEvidenceSha256'
    - from: tooling/phase6-evidence/src/evaluate.ts
      to: phase6:verify
      via: mutually exclusive `--mode planned --require-run-evidence <stage>`, `--mode planned --require-admitted-stage <stage>`, and `--mode final`
      pattern: 'requireRunEvidence|requireAdmittedStage'
---
<objective>Make Phase 6 requirement coverage exact, physical evidence ingestion cryptographically attributable, and the signed friends roster plus per-machine evidence append-only through exact stage gates.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-21-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-35-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-UAT.md
@tooling/phase6-evidence/src/evaluate.ts
@tooling/phase6-evidence/evidence-manifest.schema.json
@tooling/phase6-evidence/evidence-manifest.json
@apps/desktop/tests/packaged/transactional-plans.ts
<interfaces>
Canonical stages are exactly `deterministic-simulation`, `clean-windows-vm`, `owner-pc`, and `friends-pc`. `--require-run-evidence <stage>` means the exact stage has valid persisted run evidence with review still pending and all predecessors admitted. `--require-admitted-stage <stage>` means the exact stage and every predecessor have valid run plus required consent/review. Both are valid only with `--mode planned`; they are mutually exclusive. `--mode final` has no targeted stage option and requires all four complete. The v2 evidence shape uses create-only `runs`, `reviews`, and `consents` collections; clean VM and owner have cardinality one, deterministic has one run/no review, and friends cardinality equals its frozen roster. `PHASE6_REQUIREMENTS`, schema enum/bounds, fixtures, simulation output, and final evaluation use exactly the ordered set `PLAN-01` through `PLAN-08`. Existing v1/v2 blocked attempt bytes/UAT sections are historical input only and are never rewritten. `phase6:freeze-friends-roster` accepts exactly `--artifact-manifest <absolute-canonical-path> --roster <candidate-path>`; after 06-35 authenticates the artifact/config, it derives the two fixed output paths from the friends config and accepts no output, signer, trust-pin, participant, or stage override.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN the explicit CLI gates and multi-machine evaluator contract</name>
  <read_first>tooling/phase6-evidence/src/evaluate.ts; tooling/phase6-evidence/tests/evaluate.test.ts; tooling/phase6-evidence/evidence-manifest.schema.json; tooling/phase6-evidence/evidence-manifest.json; package.json; 06-CONTEXT.md D-01, D-02, D-06, D-07, D-29, D-35</read_first>
  <files>tooling/phase6-evidence/src/evaluate.ts, tooling/phase6-evidence/tests/evaluate.test.ts, tooling/phase6-evidence/evidence-manifest.schema.json, package.json</files>
  <behavior>
    - CLI accepts only `--mode planned --require-run-evidence <canonical-stage>`, `--mode planned --require-admitted-stage <canonical-stage>`, or bare `--mode final`; absent values, `clean-vm`, `friends-pcs`, `--stage`, unknown flags, both gates, or a targeted final invocation exit nonzero before evaluation.
    - `PHASE6_REQUIREMENTS` is exactly `['PLAN-01','PLAN-02','PLAN-03','PLAN-04','PLAN-05','PLAN-06','PLAN-07','PLAN-08']` in that order. Schema `requirementsCoverage.items.enum`, `minItems: 8`, `maxItems: 8`, `uniqueItems: true`, all evaluator fixtures, and exact-order semantic evaluation agree; dedicated negative tests remove PLAN-02, PLAN-03, and PLAN-04 one at a time and each produces `REQUIREMENT_COVERAGE_MISSING`.
    - A pending clean/owner/friends run gate evaluates only that stage plus required admitted predecessors; absent later stages do not fail it. An admitted-stage gate requires exact approved evidence through that stage and ignores later pending stages. Final requires all four stages after friends cardinality passes.
    - Schema v2 represents stage evidence as append-only collections. Deterministic has exactly one deterministic run and no review/consent; clean and owner each have exactly one physical run and later matching review; friends has a frozen roster plus exactly one physical run, local pre-export consent, and strictly-later review per roster member.
    - Friends roster entries contain only unique purpose-bound pseudonymous participant IDs and required machine slots. Roster hash is bound into every run/consent/review; missing, duplicate, extra, swapped, rejected, review-before-run, consent-after-export, or mismatched participant records block friends and final.
    - Legacy blocked manifests remain readable only as BLOCKED history and cannot be upgraded, overwritten, or counted toward the current operation-version cardinality.
  </behavior>
  <action>RED: add table-driven CLI tests for every accepted syntax and all rejected aliases/missing values/unknown flags/mode combinations. Change the shared valid fixture to emit exactly ordered PLAN-01..08, then add three named negative tests that independently remove PLAN-02, PLAN-03, and PLAN-04 and assert `REQUIREMENT_COVERAGE_MISSING`; also reject reorder, duplicate, unknown, fewer/more than eight, and schema/evaluator disagreement. Add evaluator/schema tests for targeted pending/admitted gates with absent later stages, complete final mode, frozen two-or-more-member roster, per-member one-to-one runs/consents/reviews, and every cardinality/order/hash/participant mutation. Commit `test(06-32): add failing staged CLI and friends roster gates`. GREEN: set exported `PHASE6_REQUIREMENTS` to exactly ordered PLAN-01..08 and update the schema enum plus `minItems`/`maxItems` to eight and every fixture accordingly. Introduce an explicit parsed CLI options union rather than silently defaulting or ignoring flags. Add `requireAdmittedStage` to evaluator context and make both targeted gates planned-only. Version the manifest schema to append-only collections while retaining old blocked attempt references as non-authoritative history. Implement exact requirement order/set, friends cardinality, predecessor, timestamp, consent-before-export, review-after-run, and signed-roster-hash checks; final mode alone computes release readiness. Add/adjust root scripts without adding a new package. Commit `feat(06-32): enforce staged gates and friends cardinality`.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/phase6-evidence exec vitest --run tests/evaluate.test.ts</automated></verify>
  <acceptance_criteria>- The exact canonical commands are tested; `--stage`, aliases, and valueless gates fail closed.
- Evaluator constant, JSON Schema enum/bounds, fixtures, and semantic checks all require exactly ordered PLAN-01..08; individual PLAN-02/03/04 omissions each fail explicitly.
- Targeted planned gates do not require later stages, while final requires the full chain.
- Friends admission is impossible unless roster/run/consent/review cardinalities are equal and identities/hashes/times correspond one-to-one.
- Existing blocked evidence is retained as history and never becomes current PASS.</acceptance_criteria>
  <done>The evaluator can safely review/admit one stage at a time and finally validate every declared friends machine.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: RED/GREEN/REFACTOR signed-artifact-first append-only physical writing</name>
  <read_first>tooling/phase6-evidence/src/physical-writer.ts if created by prior attempt; tooling/phase6-evidence/tests/physical-writer.test.ts if created; 06-35-SUMMARY.md exact artifact and FriendsRosterDocument verifier/compiled pin; generated ArtifactManifestDocument/FriendsRosterDocument/PhysicalRunConfigDocument from 06-39; Task 1 exact PLAN-01..08 schema/evaluator contract; apps/desktop/tests/packaged/transactional-plans.ts deterministic-only source</read_first>
  <files>tooling/phase6-evidence/src/physical-writer.ts, tooling/phase6-evidence/tests/physical-writer.test.ts, package.json</files>
  <behavior>
    - `phase6:ingest-physical -- --artifact-manifest <path> --run-envelope <path> --stage <canonical-physical-stage>` first invokes the fixed 06-35 verifier with only the manifest path, requires its compiled-pin/CMS/live-byte PASS, then selects the stage config at its exact signed-manifest path/hash; no envelope field can substitute these authorities.
    - `phase6:freeze-friends-roster -- --artifact-manifest <path> --roster <candidate-path>` first obtains 06-35 `VerifiedArtifactManifest` and the verified friends config, validates candidate bytes as generated `FriendsRosterDocument`, derives only `friends/friends-roster.json` and adjacent `.p7s`, exclusively creates canonical roster bytes, signs them with the same non-exportable certificate whose SPKI is compiled by 06-35, and re-verifies CMS/SPKI/config/artifact bindings before success. Later byte change, add/remove/reorder, duplicate slot, second freeze, caller output/signer/pin/participant/stage argument, or pre-existing one-file half-pair is rejected atomically.
    - Writer accepts only `source: phase6-physical-runner-rust-1`, the exact signed runner/config/artifact/build/operation/stage tuple, complete lifecycle/continuation chain, bounded redacted output, and actual predecessor. It creates a unique run file and appends a reference without updating/deleting any run, consent, review, roster, or legacy blocker.
    - Friends envelopes carry the verified roster SHA-256, locally matched participant/slot, and one create-only consent bound to roster/run/local preview/redacted bytes before export. `phase6:record-review -- --stage <canonical-physical-stage> --review <path>` appends only a later matching human verdict; it cannot alter roster, run, or consent bytes, and its review document must bind the exact participant/run/consent/artifact/roster hashes.
    - Callback/simulation/fixture/browser/TypeScript provenance, mutable/unsigned artifact manifests, self-declared hashes, duplicate IDs, oversize/raw secret/SID/serial data, incomplete cycle, nonzero rejected-case dispatch, and signature/config/continuation mismatch fail atomically before evidence-manifest mutation.
  </behavior>
  <action>RED: add adversarial tests that relabel the deterministic callback, bypass/forge the 06-35 verifier result, mutate artifact CMS/SPKI/manifest/config/MSI/runner/portable drivers, pass a roster output/signer/pin/stage/participant override, use wrong-SPKI/unsigned/half-created roster pairs, freeze/alter a multi-member roster, mismatch artifact/config/roster hashes, overwrite/duplicate runs/consents/reviews, reorder consent/export/review times, inject secrets/oversize, and mismatch stage/predecessor/continuation; prove every failure leaves manifest and every create-once file byte-identical. Commit `test(06-32): add failing signed physical writer gate`. GREEN: implement file-based ingestion that runs the fixed inspection-only artifact verifier before reading the envelope, resolves all identity from the verified manifest/config, calls the 06-35 same-SPKI roster verifier for friends, performs schema/semantic/redaction checks, and persists with exclusive-create plus atomic compare-and-append against the expected manifest hash. Implement the exact closed roster-freeze CLI: verify artifact/config, validate generated candidate, derive the two fixed paths, exclusively create canonical roster plus detached CMS using the existing non-exportable signer, and verify the completed pair before returning. Add review recording and keep participant association plus consent sourced from the locally previewed runner envelope. Do not accept an in-memory callback, Boolean physical label, caller output/hash/pin/signer/stage/participant, generic output path, or overwrite flag. Commit `feat(06-32): write authenticated append-only physical evidence`. REFACTOR after physical-writer and evaluator suites pass together.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/phase6-evidence exec vitest --run tests/physical-writer.test.ts tests/evaluate.test.ts</automated></verify>
  <acceptance_criteria>- CMS/SPKI/live-byte verification occurs before config/envelope parsing or persistence.
- Signed artifact manifest authenticates MSI, installation manifests, all stage configs, runner, and drivers.
- Roster plus adjacent CMS are create-only at config-fixed paths, use the artifact signer's compiled SPKI, and bind exact artifact/config identity before local participant association.
- Roster, runs, consents, and reviews are create-only with atomic failure and exact participant cardinality.
- Deterministic/legacy/unsigned/tampered evidence cannot be relabeled physical.
- RED/GREEN commits exist for both evaluator and writer behavior.</acceptance_criteria>
  <done>Only authenticated physical runner output can append evidence, including multiple independently consented/reviewed friends machines.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| portable artifact -> evidence writer | A mutable manifest could self-declare matching hashes and authorize substituted runner/config bytes. |
| participant output -> append-only evidence | Guest files and labels are attacker-controlled until verified and redacted. |
| roster/runs/consents/reviews -> final evaluator | Missing or overwritten friends machines could fabricate complete coverage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-32A | Spoofing/Tampering | artifact/run provenance | mitigate | 06-35 compiled-pin CMS/live-byte verification before ingestion; exact generated config binding. |
| T-06-32B | Repudiation/Replay | signed roster and evidence collections | mitigate | Fixed-path same-SPKI roster pair, exclusive-create files, expected-manifest-hash append, unique IDs, strictly ordered consent/run/review. |
| T-06-32C | Information Disclosure | friends diagnostics | mitigate | Local redaction/preview, purpose-bound pseudonyms, 64 KiB bound, raw secret/SID/serial rejection. |
| T-06-32D | Tampering | CLI stage gate | mitigate | Closed parser, canonical stages, planned-only targeted gates, final-only complete verdict. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-32A | CRITICAL | V5 Validation; V4 Access Control | block | signature/config/relabel mutation suite | Any unsigned/self-asserted artifact admission blocks 06-33/physical stages. |
| T-06-32B | CRITICAL | V7 Logging | block | overwrite/duplicate/timestamp/cardinality tests | Any mutable or missing per-machine record blocks friends/final. |
| T-06-32C | HIGH | V8 Data Protection | block | secret/size/redaction/consent-order tests | Any raw or unconsented export blocks persistence. |
| T-06-32D | HIGH | V4 Access Control | block | CLI accepted/rejected syntax matrix | Any ignored flag/alias/stage skip blocks promotion plans. |
</threat_model>
<verification>Evaluator and physical-writer adversarial suites pass; planned mode can target pending/admitted stages while final remains nonzero until the full roster and all four stages exist.</verification>
<success_criteria>Physical evidence is authenticated before ingestion, immutable afterward, and complete for every declared participant before final admission.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-32-SUMMARY.md` with exact CLI syntax, schema version, roster rules, verifier identity, and TDD commits.</output>
