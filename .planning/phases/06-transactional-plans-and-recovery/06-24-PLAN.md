---
phase: 06-transactional-plans-and-recovery
plan: '24'
type: execute
wave: 6
depends_on: ['06-11', '06-18', '06-23']
files_modified:
  - apps/desktop/src-tauri/src/plan_commands.rs
  - apps/desktop/src-tauri/src/plan_executor.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/capabilities/main.json
  - packages/desktop-client/src/plans.ts
  - packages/desktop-client/src/plans.test.ts
autonomous: true
requirements: [PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - 'React can read the authoritative Advanced preference and request enable/revoke only through closed named native commands.'
    - 'Native startup and every high-risk admission revalidate the persisted device/posture binding before projecting enabled state.'
    - 'The renderer receives a validated immutable projection and never supplies enabled, authenticated, or posture-valid truth.'
  artifacts:
    - path: apps/desktop/src-tauri/src/plan_commands.rs
      provides: Closed read/enable/revoke Advanced preference commands
    - path: packages/desktop-client/src/plans.ts
      provides: Runtime-validated Advanced preference projection and intent methods
    - path: packages/desktop-client/src/plans.test.ts
      provides: Native/deterministic conformance, restart, invalidation, and authority-field rejection tests
  key_links:
    - from: apps/desktop/src-tauri/src/plan_commands.rs
      to: apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs
      via: managed PlanExecutor read/enable/revoke lifecycle
      pattern: 'advanced_preference'
    - from: packages/desktop-client/src/plans.ts
      to: apps/desktop/src-tauri/src/plan_commands.rs
      via: closed PLAN_COMMANDS native invoke mapping
      pattern: 'readAdvancedPreference|enableAdvancedPreference|revokeAdvancedPreference'
---
<objective>Wire the durable D-13 preference through least-privilege native commands and the renderer-safe client authority.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-23-SUMMARY.md @apps/desktop/src-tauri/src/plan_commands.rs @apps/desktop/src-tauri/src/plan_executor.rs @packages/desktop-client/src/plans.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Register closed native preference commands and startup revalidation</name>
  <read_first>apps/desktop/src-tauri/src/plan_commands.rs; apps/desktop/src-tauri/src/plan_executor.rs; apps/desktop/src-tauri/src/main.rs; apps/desktop/src-tauri/capabilities/main.json; apps/desktop/src-tauri/src/recovery_store/advanced_preference.rs; apps/desktop/src-tauri/tests/advanced_preference.rs</read_first>
  <files>apps/desktop/src-tauri/src/plan_commands.rs, apps/desktop/src-tauri/src/plan_executor.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/capabilities/main.json</files>
  <behavior>- `read_advanced_preference` returns only the validated device-local projection.
- `enable_advanced_preference` and `revoke_advanced_preference` accept intent/proof references, recompute device/posture truth, and append through the native authority.
- Startup and pre-apply revalidation append invalidation on changed hardware/security posture before projecting or admitting Advanced.
- Recovery commands remain callable regardless of preference/auth/subscription state.</behavior>
  <action>Test first in the existing native executor suites, then add exactly three Tauri commands and the corresponding trusted-main-webview capability entries. Route them through managed `PlanExecutor` to the D-13 authority; enable/revoke consume their distinct native-verified proofs and never accept renderer booleans or raw posture claims. Revalidate current Phase 5 hardware fingerprint and native security posture on startup, read, and immediately before Advanced admission; append invalidation before returning disabled/blocked truth. Register no settings namespace, generic preference setter, cloud sync, or privileged broker command for this local policy.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test advanced_preference && rtk cargo test -p liiiraa-desktop --test recovery_executor</automated></verify>
  <acceptance_criteria>- Capability registry contains only the three exact preference commands and existing named plan commands.
- Forged enabled/authenticated/posture fields are rejected before store access.
- Restart/read/pre-apply changed posture produces one durable invalidation and zero Advanced dispatch.
- Recovery behavior is unchanged.</acceptance_criteria>
  <done>The native command registry exposes the complete D-13 lifecycle without generic authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Project the closed preference lifecycle through PlanAuthority</name>
  <read_first>packages/desktop-client/src/plans.ts; packages/desktop-client/src/plans.test.ts; packages/contracts-source/src/transactional-plans.tsp; apps/desktop/src-tauri/src/plan_commands.rs</read_first>
  <files>packages/desktop-client/src/plans.ts, packages/desktop-client/src/plans.test.ts</files>
  <behavior>- Snapshot includes immutable `disabled|enabled|revoked|invalidated|unavailable` Advanced preference truth with reason and binding freshness.
- Enable/revoke invoke only their exact commands and never optimistically change the snapshot.
- Reopen reads persisted native truth; a posture invalidation replaces enabled only after validated authoritative response.
- Production rejects fixture provenance and renderer authority fields.</behavior>
  <action>Test first, then extend `PLAN_COMMANDS`, `PlanAuthority`, native adapter, explicit deterministic-test adapter, snapshot validator, and exports with read/enable/revoke Advanced preference intent. Preserve pending strong-auth state without optimistic success; consume the next authoritative native snapshot after command completion. A reconnect/reopen reads once and never repeats enable/revoke. Reject generic settings mutation, cloud-sync shape, `enabled: true` input claims, reusable proofs, and fixture-as-native provenance.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop-client exec vitest --run src/plans.test.ts</automated></verify>
  <acceptance_criteria>- Native and deterministic adapters pass the same closed lifecycle conformance.
- Enable/revoke command count stays one across reconnect and sequence gaps.
- Persisted restart and posture invalidation projections remain attributable and immutable.
- No generic preference setter or production fixture fallback exists.</acceptance_criteria>
  <done>Renderer code can request but cannot fabricate D-13 authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| renderer -> native preference authority | Renderer intent could forge enablement, auth, or posture. |
| native projection -> renderer | Stale/fixture state could present Advanced as enabled. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-24A | Elevation/Spoofing | preference commands | mitigate | Exact capabilities, generated validation, native recomputation, distinct proof consumption, no generic setter. |
| T-06-24B | Tampering/Repudiation | PlanAuthority projection | mitigate | Runtime validation, immutable authoritative snapshot, no optimistic mutation or fixture fallback. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-24A | HIGH | V4 Access Control — deny-by-default named commands; V5 Input Validation — recompute trusted state | blocking | Native capability/forged-field/startup/pre-apply tests | Any generic command or renderer-owned truth blocks 06-25, 06-20, and physical promotion. |
| T-06-24B | HIGH | V5 Input Validation — validate unknown native projections; V7 Logging — preserve attributable state | blocking | Desktop-client conformance/reconnect/restart tests | Any optimistic, stale, or fixture-native enablement blocks UI and release evidence. |
</threat_model>
<verification>Native executor and desktop-client suites prove closed commands, exact proof routing, restart persistence, posture invalidation, and no optimistic/replayed mutation.</verification>
<success_criteria>D-13 authority reaches React only as validated native truth and returns to native only as bounded intent.</success_criteria>
<output>Create `06-24-SUMMARY.md`.</output>
