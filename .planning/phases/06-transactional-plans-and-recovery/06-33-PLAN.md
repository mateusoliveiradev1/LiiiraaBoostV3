---
phase: 06-transactional-plans-and-recovery
plan: '33'
type: tdd
wave: 13
depends_on: ['06-30', '06-31', '06-32']
files_modified:
  - apps/desktop/tests/packaged/physical-runner.ts
  - apps/desktop/tests/packaged/physical-runner.test.ts
  - apps/desktop/tests/packaged/physical-runner-cli.ts
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Inside the guest, a closed runner installs and starts the exact MSI, drives the installed desktop/native authority, and completes prepare/apply/observe/verify/reboot/reconcile/restore/verify without a mutation callback.'
    - 'Reboot continuation is durable and observation-first; crash, response loss, disk-full, drift, conflict, replay, spoof, wrong-session, and remote-client drills record exact dispatch counts and never blind-retry.'
    - 'The runner emits only the bounded raw envelope accepted by the physical writer and cannot self-declare PASS or human accessibility approval.'
  artifacts:
    - path: apps/desktop/tests/packaged/physical-runner.ts
      provides: Closed install/drive/reboot/reconcile/restore state machine
      exports: [PhysicalGuestRunner, PhysicalRunnerState]
    - path: apps/desktop/tests/packaged/physical-runner-cli.ts
      provides: Exact-argument guest CLI with no arbitrary command option
  key_links:
    - from: apps/desktop/tests/packaged/physical-runner.ts
      to: installed Liiiraa Boost desktop and optimizer service
      via: exact packaged driver and named typed Tauri commands
      pattern: 'compose_plan|apply_plan|restore_plan'
    - from: apps/desktop/tests/packaged/physical-runner.ts
      to: tooling/phase6-evidence/src/physical-writer.ts
      via: bounded guest-run envelope file
      pattern: 'phase6-guest-runner-v1'
---
<objective>Implement the real packaged guest runner that produces the physical cycle instead of simulating a callback.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-30-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-32-SUMMARY.md
@apps/desktop/tests/packaged/transactional-plans.ts
@apps/desktop/tests/packaged/driver.ts
@apps/desktop/tests/packaged/journeys.ts
@tooling/phase6-evidence/src/physical-writer.ts
<interfaces>
The runner accepts only `artifactManifest`, `stage`, `operationVersion`, `runDirectory`, and a closed `scenario` enum. It resolves executable/MSI/service identities from the verified artifact manifest. It must not import or call `TransactionalPackagedHarness.executePhysicalMutation`, accept an effect callback, or expose arbitrary process/PowerShell arguments. The output source literal is `phase6-guest-runner-v1`.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the guest physical lifecycle runner</name>
  <read_first>apps/desktop/tests/packaged/transactional-plans.ts; apps/desktop/tests/packaged/transactional-plans.test.ts; apps/desktop/tests/packaged/driver.ts; apps/desktop/tests/packaged/journeys.ts; tooling/phase6-evidence/src/physical-writer.ts; artifact-manifest contract from 06-31; Tauri command list; 06-CONTEXT.md D-01 through D-06, D-17 through D-24, D-29 through D-35</read_first>
  <files>apps/desktop/tests/packaged/physical-runner.ts, apps/desktop/tests/packaged/physical-runner.test.ts, apps/desktop/tests/packaged/physical-runner-cli.ts</files>
  <behavior>
    - Runner verifies artifact manifest and live MSI/desktop/service hashes before `msiexec /i ... /quiet /norestart`, confirms exact product/service/image identity, starts the installed desktop through the packaged driver, and uses only named transactional Tauri commands.
    - Happy path records durable recovery preparation, exact prior GUID, requested owned GUID, one broker dispatch, observed/verified apply, explicit reboot-required stop, post-boot journal reconciliation before new mutation, exact restore transaction, restored prior GUID, owned-target cleanup, journal and receipt hashes.
    - Closed drills cover legitimate client plus replay/same-user spoof/wrong-session/remote zero-dispatch; crash/response loss/disk-full/reboot observation without redispatch; drift before apply/restore and third-state conflict without overwrite/delete; System Restore observed ready/unavailable/frequency states.
    - Runner writes continuation state atomically before requesting a user-controlled reboot, never forces restart, resumes only the same build/run/stage/operation identity, and rejects changed bytes or state rollback.
    - Automated keyboard/axe checks are recorded separately; Narrator comprehension and final reviewer verdict remain pending human checkpoint fields and cannot be set by the runner.
  </behavior>
  <action>RED: implement process/driver fakes and failure-injection tests for install identity, full state order, reboot continuation, every required adversarial/fault/drift case, callback/deterministic import prohibition, bounded output, and refusal to manufacture accessibility/reviewer PASS; commit `test(06-33): add failing guest physical runner`. GREEN: implement the closed state machine and CLI using exact child-process argument arrays (`shell: false`), fixed `msiexec`, service-query, Tauri driver, and transactional command names. The live path must exercise the installed desktop and authenticated optimizer service; no injected mutation callback, fixture adapter, generic process option, raw script, remote host, or production fallback is allowed. Feed the completed raw envelope to the 06-32 writer only after restoration verification; incomplete/failed runs remain BLOCKED with immutable partial output. Commit `feat(06-33): implement packaged guest physical runner`. REFACTOR only after all tests pass.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop exec vitest --run tests/packaged/physical-runner.test.ts tests/packaged/transactional-plans.test.ts</automated></verify>
  <acceptance_criteria>- Live mode has no callback/fixture/deterministic execution path and uses the installed exact build.
- Full apply/reboot/reconcile/restore ordering and every required drill are deterministic in tests.
- Restart is user-controlled and resume is exact-identity/observation-first.
- Runner cannot set human review or Narrator comprehension to PASS.
- RED/GREEN commits exist in order.</acceptance_criteria>
  <done>A clean Windows guest can execute the complete real operation lifecycle and emit attributable raw evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| artifact manifest -> MSI/process launch | A substituted path or argument could execute arbitrary code. |
| runner -> installed desktop/service | Lab automation crosses the real privileged mutation boundary. |
| reboot state -> resumed runner | Stale or replaced continuation could repeat a mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-33A | Tampering/Elevation | process launch | mitigate | Hash-resolved fixed binaries, `shell:false`, closed args/scenarios, no callback or arbitrary command. |
| T-06-33B | Repudiation | lifecycle evidence | mitigate | Ordered state machine, actual command outputs, exact GUID/dispatch/journal/receipt bindings. |
| T-06-33C | Replay/Denial | reboot continuation | mitigate | Atomic identity-bound continuation, observation-first resume, no forced reboot or blind dispatch. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-33A | CRITICAL | V4 Access Control; V5 Validation | block | arbitrary-path/callback/substitution tests | Any generic execution or callback path blocks Hyper-V integration. |
| T-06-33B | CRITICAL | V7 Logging | block | complete-cycle and drill assertions | Missing GUID/dispatch/journal/receipt binding blocks evidence writer. |
| T-06-33C | CRITICAL | V3 Session; V4 Access Control | block | restart/replay tests | Any redispatch after restart uncertainty blocks physical execution. |
</threat_model>
<verification>Focused runner and legacy packaged suites pass; a Windows dry-run validates the real artifact without installing or mutating.</verification>
<success_criteria>The exact installable build, not a callback fixture, performs and records the complete reversible lifecycle in the guest.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md` with TDD commits and state-machine coverage.</output>
