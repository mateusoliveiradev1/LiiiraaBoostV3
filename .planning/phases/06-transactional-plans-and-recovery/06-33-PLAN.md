---
phase: 06-transactional-plans-and-recovery
plan: '33'
type: tdd
wave: 14
depends_on: ['06-32', '06-35', '06-37']
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/physical_runner.rs
  - apps/desktop/src-tauri/src/bin/phase6-physical-runner.rs
  - apps/desktop/src-tauri/tests/physical_runner.rs
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'The guest runner is a self-contained signed Rust EXE that needs no Node, TypeScript loader, package manager, or source checkout inside a clean VM or participant PC.'
    - 'It hash-verifies the artifact manifest, MSI, installed desktop/service, its own runner bytes, tauri-driver, and msedgedriver before driving only named Tauri transactional commands through the installed physical desktop.'
    - 'It completes prepare/apply/observe/verify/reboot/reconcile/restore/verify and emits only a bounded raw envelope accepted by the physical writer; it cannot declare human or physical promotion approval.'
  artifacts:
    - path: apps/desktop/src-tauri/src/bin/phase6-physical-runner.rs
      provides: Self-contained exact-config physical runner executable entrypoint
    - path: apps/desktop/src-tauri/src/physical_runner.rs
      provides: Closed install/WebDriver/reboot/reconcile/restore state machine
      exports: [PhysicalGuestRunner, PhysicalRunnerState, PhysicalRunConfig]
    - path: apps/desktop/src-tauri/tests/physical_runner.rs
      provides: Process/WebDriver/reboot/fault/no-legacy/no-generic-authority proof
  key_links:
    - from: apps/desktop/src-tauri/src/physical_runner.rs
      to: installed liiiraa-desktop.exe
      via: exact artifact-bound tauri-driver/msedgedriver WebDriver session invoking named Tauri commands
      pattern: 'compose_plan.*apply_plan.*restore_plan'
    - from: apps/desktop/src-tauri/src/physical_runner.rs
      to: apps/desktop/src-tauri/src/plan_executor.rs
      via: installed Tauri commands traverse WindowsNamedPipeBrokerTransport
      pattern: 'phase6-physical.*apply_plan'
    - from: apps/desktop/src-tauri/src/physical_runner.rs
      to: tooling/phase6-evidence/src/physical-writer.ts
      via: bounded phase6-physical-runner-rust-1 envelope consumed after host collection
      pattern: 'phase6-physical-runner-rust-1'
---
<objective>Build the genuinely executable artifact-bound runner that drives the installed Tauri desktop through the real broker path on machines with no development toolchain.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-32-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-35-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-37-SUMMARY.md
@apps/desktop/src-tauri/src/main.rs
@apps/desktop/src-tauri/src/plan_executor.rs
@apps/desktop/tests/packaged/driver.ts
<interfaces>
The executable accepts exactly `--run-config <absolute-json-path>` and no other user-selectable command, executable, host, or script arguments. `PhysicalRunConfig` is a closed generated/validated document containing artifact-manifest path/hash, stage enum, operation version, run directory, exact scenario enum, and continuation identity. The artifact manifest supplies exact MSI/desktop/service/runner/tauri-driver/msedgedriver paths, sizes, hashes, versions, and signatures. The runner source is `phase6-physical-runner-rust-1`; `apps/desktop/tests/packaged/transactional-plans.ts` is deterministic-only and must not be imported, executed, or accepted as physical provenance.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the self-contained packaged physical runner</name>
  <read_first>apps/desktop/src-tauri/Cargo.toml; apps/desktop/src-tauri/src/main.rs; apps/desktop/src-tauri/src/plan_executor.rs; apps/desktop/tests/packaged/driver.ts for command names only; tooling/phase6-evidence/src/physical-writer.ts source contract from 06-32; generated installation-manifest contract from 06-35; 06-CONTEXT.md D-01 through D-06, D-17 through D-24, D-29 through D-35</read_first>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/physical_runner.rs, apps/desktop/src-tauri/src/bin/phase6-physical-runner.rs, apps/desktop/src-tauri/tests/physical_runner.rs</files>
  <behavior>
    - Runner verifies signed artifact manifest and actual MSI/runner/driver bytes before install, uses fixed `msiexec.exe` argument arrays with `shell` absent, then verifies ProductCode, service/image identity, installed signed manifest, and all installed hashes.
    - Runner launches only manifest-bound `tauri-driver.exe` with manifest-bound `msedgedriver.exe`, creates a loopback WebDriver session through fixed WinHTTP requests, and invokes only compose/approve/apply/read/subscribe/restore/recovery transactional command names on the installed physical desktop.
    - Happy path records recovery preparation, exact prior GUID, requested owned GUID, one broker dispatch, observed/verified apply, user-controlled reboot boundary, post-boot journal reconciliation before any new mutation, restore transaction, restored prior GUID, owned-target cleanup, journal and receipt hashes.
    - Closed drills cover legitimate client plus replay/same-user spoof/wrong-session/remote zero-dispatch; crash/response loss/disk-full/reboot observation without redispatch; drift before apply/restore and third-state conflict without overwrite/delete; System Restore ready/unavailable/frequency states.
    - Continuation is written atomically before requesting reboot and resumes only after config/artifact/build/version/run-state hashes match; the runner never forces reboot, manufactures Narrator/reviewer approval, or invokes the deterministic callback harness.
  </behavior>
  <action>RED: add fake process/WinHTTP/WebDriver/native-command ports and tests for signed manifest/hash/version checks, fixed launch arrays, full state order, reboot continuation, every adversarial/fault/drift case, absent Node/toolchain assumption, deterministic TypeScript import/provenance prohibition, bounded output, and refusal to set human fields; commit `test(06-33): add failing self-contained physical runner`. GREEN: add a `[[bin]]` self-contained Rust runner and closed library state machine using pinned Windows file/process/service/WinHTTP APIs. It may launch only the manifest-resolved MSI, runner, tauri-driver, msedgedriver, and installed desktop with fixed argument templates; WebDriver binds loopback only and invokes the named Tauri API. Use no Node, TypeScript loader, npm/pnpm, arbitrary executable, shell, script, callback, remote host, or fixture adapter. Emit create-once partial BLOCKED output after every boundary and a completed raw `phase6-physical-runner-rust-1` envelope only after exact restoration verification; host-side 06-32 remains the sole physical evidence writer. Commit `feat(06-33): build self-contained physical runner`. REFACTOR only after focused tests and Windows runner build pass.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test physical_runner &amp;&amp; rtk cargo build -p liiiraa-desktop --bin phase6-physical-runner --release --target x86_64-pc-windows-msvc &amp;&amp; rtk cargo test -p liiiraa-desktop --test broker_protocol --test recovery_executor</automated></verify>
  <acceptance_criteria>- Release runner is one Windows EXE and executes without Node, TypeScript, pnpm, source files, or a test loader.
- Every executable and driver comes from the verified artifact manifest and uses fixed arguments/loopback endpoints.
- Installed Tauri apply/restore traverses the named-pipe broker; no direct service or PowrProf shortcut exists.
- Full lifecycle, drills, continuation, and bounded raw output are test-covered.
- Deterministic callback/fixture provenance and human auto-approval are structurally rejected.</acceptance_criteria>
  <done>A clean guest or participant PC can run the exact physical lifecycle using only signed/hash-bound artifact bytes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| artifact manifest -> process/WebDriver launch | Substituted paths or arguments could become arbitrary execution. |
| runner -> installed Tauri desktop -> broker | Automation crosses the real unprivileged-to-privileged chain. |
| reboot state -> resumed runner | Stale or replaced continuation could repeat a mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-33A | Tampering/Elevation | executable/driver launch | mitigate | Signed manifest, actual hashes, fixed roles/arguments, no generic process or script authority. |
| T-06-33B | Spoofing/Elevation | WebDriver/Tauri path | mitigate | Loopback-only exact drivers, installed desktop identity, named Tauri commands, real broker transport. |
| T-06-33C | Replay/Repudiation | continuation/evidence | mitigate | Atomic identity-bound state, observation-first resume, create-once raw envelopes, no forced reboot. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-33A | CRITICAL | V4 Access Control; V5 Validation | block | path/hash/argument/no-toolchain mutation tests | Any arbitrary launch or missing runtime byte blocks MSI build. |
| T-06-33B | CRITICAL | V2 Authentication; V13 API | block | WebDriver/Tauri/broker chain tests | Any direct service shortcut or non-loopback control blocks MSI build. |
| T-06-33C | CRITICAL | V3 Session; V7 Logging | block | reboot/replay/raw-envelope tests | Any redispatch or overwritable evidence blocks physical artifact creation. |
</threat_model>
<verification>Focused runner, broker, and recovery suites pass and the release Windows runner EXE is produced before 06-31 packages it.</verification>
<success_criteria>The artifact contains an attributable executor that actually runs in the guest and reaches the installed Tauri-to-broker chain without a development runtime.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md` with runner EXE path/hash/version, TDD commits, and state-machine coverage.</output>
