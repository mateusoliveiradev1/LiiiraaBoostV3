---
phase: 06-transactional-plans-and-recovery
plan: '13'
type: tdd
wave: 2
depends_on: ['06-01', '06-02', '06-04']
files_modified:
  - Cargo.toml
  - architecture/module-boundaries.json
  - apps/optimizer-service/Cargo.toml
  - apps/optimizer-service/src/main.rs
  - apps/optimizer-service/src/ipc.rs
  - apps/optimizer-service/tests/ipc_protocol.rs
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - 'Only the verified local interactive client can submit bounded allowlisted broker messages; replay, remote, wrong-session, spoofed-process, malformed, and generic requests fail before mutation.'
  artifacts:
    - path: apps/optimizer-service/src/ipc.rs
      provides: Authenticated replay-resistant named-pipe boundary
    - path: apps/optimizer-service/tests/ipc_protocol.rs
      provides: Identity, DACL, canonical MAC, nonce/counter, dedup, and generic-authority adversarial tests
  key_links:
    - from: apps/optimizer-service/src/ipc.rs
      to: generated broker request validator
      via: validate before dispatch
      pattern: 'validate.*Broker'
---
<objective>Create the isolated privileged broker and prove its local IPC boundary through TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@architecture/module-boundaries.json @apps/desktop/src-tauri/src/main.rs @packages/contracts-source/src/transactional-plans.tsp @.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify authenticated replay-resistant broker IPC</name><read_first>apps/optimizer-service/Cargo.toml, src/main.rs, src/ipc.rs, tests/ipc_protocol.rs (confirm absent); Cargo.toml; architecture/module-boundaries.json; 06-RESEARCH.md Pattern 6 and Package Legitimacy Audit; 06-CONTEXT.md D-08, D-22, D-23, D-31</read_first><files>Cargo.toml, architecture/module-boundaries.json, apps/optimizer-service/Cargo.toml, apps/optimizer-service/src/main.rs, apps/optimizer-service/tests/ipc_protocol.rs</files><action>After Plan 04 approval, add exact `windows-service=0.8.1`, the optimizer-service workspace member, production-adapter boundary record, service entrypoint, and failing adversarial protocol tests. Cover local-only pipe, explicit DACL, service/logon SID, impersonated token/session/process identity, authenticated handshake, canonical bytes/MAC, server nonce, monotonic counter, one-time transaction/step ID, durable dedup outcome, bounded length/time, wrong-session/same-user spoof/replay/remote/malformed cases, and rejection of command line/script/file/registry/service/generic/Extreme variants.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol</automated></verify><acceptance_criteria>- Lockfile resolves exact approved crate identity.
- RED test binary fails on protocol assertions, not workspace/config errors.
- Architecture gate rejects service dependency on UI/feature-shell.</acceptance_criteria><done>RED IPC security contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Authenticate and deduplicate local broker requests</name><read_first>apps/optimizer-service/src/ipc.rs; tests/ipc_protocol.rs; packages/contracts-source/src/transactional-plans.tsp; Microsoft named-pipe references recorded in 06-RESEARCH.md</read_first><files>apps/optimizer-service/src/ipc.rs, apps/optimizer-service/src/main.rs</files><action>Implement the Windows service lifecycle and local named-pipe server with explicit DACL, remote-client rejection, impersonation/token/session/process verification, per-install OS-protected authentication material, canonical request authentication, nonce/counter, durable one-time IDs and replay result handling, generated validation, bounded messages/timeouts, and a compiled operation registry hook. Fail closed on every identity/impersonation/validation/storage failure; expose no arbitrary operation.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol && rtk pnpm test:architecture</automated></verify><acceptance_criteria>- Legitimate local client passes; every spoof/replay/remote/generic case fails before operation dispatch.
- Duplicate exact request returns the durable prior outcome without a second mutation dispatch.
- No reusable IPC secret is logged or sent to React.</acceptance_criteria><done>The privileged boundary is narrow, authenticated, and replay-resistant.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Bound service shutdown and protocol errors</name><read_first>apps/optimizer-service/src/main.rs; apps/optimizer-service/src/ipc.rs; tests/ipc_protocol.rs</read_first><files>apps/optimizer-service/src/main.rs, apps/optimizer-service/src/ipc.rs, apps/optimizer-service/tests/ipc_protocol.rs</files><action>Add preshutdown admission stop, bounded current-call completion, next-boot reconciliation marker, length/timeout fuzz cases, and stable redacted error codes. Do not extend shutdown indefinitely. Commit refactor if changed.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service ipc</automated></verify><acceptance_criteria>- Preshutdown admits no new request and leaves exact in-flight identity recoverable.
- All errors are closed/redacted and no generic dispatch branch exists.</acceptance_criteria><done>Broker lifecycle preserves durable recovery semantics.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| unprivileged local process -> privileged service | Same-machine attackers can spoof or replay mutation requests. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-13A | Spoofing/Elevation | named pipe | mitigate | Explicit DACL, local-only, token/session/process verification, authenticated handshake. |
| T-06-13B | Tampering/Replay | broker request | mitigate | Canonical MAC, nonce/counter, one-time IDs, durable dedup, generated validation. |
| T-06-13C | Elevation | operation registry | mitigate | Closed compiled allowlist; no scripts, generic primitives, remote action, or Extreme variant. |
</threat_model>
<verification>IPC adversarial suite and architecture gate pass after the supply-chain checkpoint.</verification>
<success_criteria>No unverified or replayed client can reach privileged mutation.</success_criteria>
<output>Create `06-13-SUMMARY.md` with TDD and package approval evidence.</output>
