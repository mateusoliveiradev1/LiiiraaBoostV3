---
phase: 06-transactional-plans-and-recovery
plan: '13'
type: tdd
wave: 2
depends_on: ['06-01', '06-02', '06-04']
files_modified:
  - Cargo.toml
  - Cargo.lock
  - architecture/module-boundaries.json
  - apps/optimizer-service/Cargo.toml
  - apps/optimizer-service/src/main.rs
  - apps/optimizer-service/src/ipc.rs
  - apps/optimizer-service/src/dedup_store.rs
  - apps/optimizer-service/tests/ipc_protocol.rs
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - 'Only the verified local interactive client can submit bounded allowlisted broker messages; replay, remote, wrong-session, spoofed-process, malformed, and generic requests fail before mutation.'
    - 'Broker deduplication outcomes survive service restart, crash, and preshutdown, so an admitted transaction/step identity can never dispatch twice.'
  artifacts:
    - path: apps/optimizer-service/src/ipc.rs
      provides: Authenticated replay-resistant named-pipe boundary
    - path: apps/optimizer-service/tests/ipc_protocol.rs
      provides: Identity, DACL, canonical MAC, nonce/counter, dedup, and generic-authority adversarial tests
    - path: apps/optimizer-service/src/dedup_store.rs
      provides: Service-owned durable request/outcome authority with migrations and bounded retention
  key_links:
    - from: apps/optimizer-service/src/ipc.rs
      to: generated broker request validator
      via: validate before dispatch
      pattern: 'validate.*Broker'
    - from: apps/optimizer-service/src/ipc.rs
      to: apps/optimizer-service/src/dedup_store.rs
      via: reserve transaction/step before dispatch and return recorded terminal outcome on replay
      pattern: 'DedupStore.*(reserve|record|lookup)'
---
<objective>Create the isolated privileged broker and prove its local IPC boundary through TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@architecture/module-boundaries.json @apps/desktop/src-tauri/src/main.rs @packages/contracts-source/src/transactional-plans.tsp @.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md @.planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md @tooling/supply-chain/phase6-windows-service-approval.mjs</context>
<tasks>
<task type="auto"><name>Task 1 RED: Preflight the approval and specify restart-durable authenticated IPC</name><read_first>.planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md; tooling/supply-chain/phase6-windows-service-approval.mjs; apps/optimizer-service/Cargo.toml, src/main.rs, src/ipc.rs, src/dedup_store.rs, tests/ipc_protocol.rs (confirm absent); Cargo.toml; Cargo.lock; architecture/module-boundaries.json; 06-RESEARCH.md Pattern 6 and Package Legitimacy Audit; 06-CONTEXT.md D-08, D-22, D-23, D-31</read_first><files>Cargo.toml, Cargo.lock, architecture/module-boundaries.json, apps/optimizer-service/Cargo.toml, apps/optimizer-service/src/main.rs, apps/optimizer-service/tests/ipc_protocol.rs</files><action>Before changing Cargo.toml or Cargo.lock, run the Plan 04 `validate-approved` preflight and independently compare its approved exact name `windows-service`, version `0.8.1`, registry URL, immutable source URL/tag/commit, checksum, and license with the record; reject missing, REJECTED, incomplete, stale, or mismatched evidence. Only after it exits zero, add exact `windows-service=0.8.1`, the optimizer-service workspace member, production-adapter boundary record, service entrypoint, and failing adversarial protocol tests. Assert Cargo.lock version/source/checksum equal the approved record. Cover local-only pipe, explicit DACL, service/logon SID, impersonated token/session/process identity, authenticated handshake, canonical bytes/MAC, server nonce, monotonic counter, one-time transaction/step ID, service-owned SQLite reserve/outcome authority, reopen after service restart, crash after reservation and after dispatch, preshutdown, bounded retention, wrong-session/same-user spoof/replay/remote/malformed cases, and rejection of command line/script/file/registry/service/generic/Extreme variants.</action><verify><automated>rtk node tooling/supply-chain/phase6-windows-service-approval.mjs validate-approved && rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol</automated></verify><acceptance_criteria>- Cargo.lock resolves the exact version, source, and checksum from the approved record.
- RED test binary fails on protocol assertions, not workspace/config errors.
- Restart/crash/preshutdown replay tests fail because durable dedup behavior is not implemented yet.
- Architecture gate rejects service dependency on UI/feature-shell.</acceptance_criteria><done>RED IPC security contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Authenticate requests and persist dedup outcomes under service custody</name><read_first>apps/optimizer-service/src/ipc.rs; apps/optimizer-service/src/dedup_store.rs; tests/ipc_protocol.rs; packages/contracts-source/src/transactional-plans.tsp; Microsoft named-pipe references recorded in 06-RESEARCH.md</read_first><files>apps/optimizer-service/src/ipc.rs, apps/optimizer-service/src/dedup_store.rs, apps/optimizer-service/src/main.rs</files><action>Implement the Windows service lifecycle and local named-pipe server with explicit DACL, remote-client rejection, impersonation/token/session/process verification, per-install OS-protected authentication material, canonical request authentication, nonce/counter, generated validation, bounded messages/timeouts, and a compiled operation registry hook. Add a broker-owned SQLite database under the service-only ProgramData authority with an explicit DACL limited to SYSTEM, Administrators, and the broker service SID; the renderer and ordinary client receive no filesystem access. Migrate and reopen it with `foreign_keys=ON`, WAL, `synchronous=FULL`, integrity check, and schema keyed by `(transaction_id, step_id)` containing request-MAC hash, operation/version identity, closed `reserved|terminal|unknown-after-crash` state, canonical generated result/error document, created/completed timestamps, and retention eligibility. Atomically reserve the identity before dispatch; a duplicate with another request hash is rejected, a duplicate terminal identity returns the recorded outcome, and reserved/unknown replay returns an observation-required result without dispatch. Append terminal outcome durably before replying. On startup/restart/preshutdown reopen and reconcile reservations without redispatch. Prune only terminal records older than the documented maximum journal/recovery/replay horizon, never pending/unknown or identities still referenced by recovery, and record pruning totals without request data. Fail closed on identity, validation, database, migration, integrity, or custody failure; expose no arbitrary operation.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol && rtk pnpm test:architecture</automated></verify><acceptance_criteria>- Legitimate local client passes; every spoof/replay/remote/generic case fails before operation dispatch.
- Duplicate exact request returns the durable prior outcome without a second mutation dispatch.
- Closing and reopening the service/database preserves the same result and dispatch count; crash-after-reserve/dispatch returns observation-required and never dispatches again.
- Database custody, migration, integrity, or FULL-durable write failure blocks broker dispatch.
- No reusable IPC secret is logged or sent to React.</acceptance_criteria><done>The privileged boundary is narrow, authenticated, and replay-resistant.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove shutdown, restart, retention, and protocol errors</name><read_first>apps/optimizer-service/src/main.rs; apps/optimizer-service/src/ipc.rs; apps/optimizer-service/src/dedup_store.rs; tests/ipc_protocol.rs</read_first><files>apps/optimizer-service/src/main.rs, apps/optimizer-service/src/ipc.rs, apps/optimizer-service/src/dedup_store.rs, apps/optimizer-service/tests/ipc_protocol.rs</files><action>Add preshutdown admission stop, bounded current-call completion, next-boot observation marker, repeated service reopen, power-loss boundaries around reserve/dispatch/result commit, retention-boundary/pruning, length/timeout fuzz cases, and stable redacted error codes. For every restart/crash/preshutdown replay of the same transaction/step identity, assert the dispatch spy remains at one and the recorded terminal or observation-required outcome is returned. Do not extend shutdown indefinitely or delete unresolved dedup identities. Commit refactor if changed.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service ipc</automated></verify><acceptance_criteria>- Preshutdown admits no new request and leaves exact in-flight identity recoverable across reopen.
- Same-ID replay after clean restart, crash, and preshutdown produces no second dispatch.
- Retention deletes only eligible terminal outcomes and preserves every unresolved/recovery-referenced identity.
- All errors are closed/redacted and no generic dispatch branch exists.</acceptance_criteria><done>Broker lifecycle preserves durable recovery semantics.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| unprivileged local process -> privileged service | Same-machine attackers can spoof or replay mutation requests. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-13A | Spoofing/Elevation | named pipe | mitigate | Explicit DACL, local-only, token/session/process verification, authenticated handshake. |
| T-06-13B | Tampering/Replay | broker request | mitigate | Canonical MAC, nonce/counter, one-time IDs, service-custodied FULL-durable SQLite outcomes, reopen/reconcile, and generated validation. |
| T-06-13C | Elevation | operation registry | mitigate | Closed compiled allowlist; no scripts, generic primitives, remote action, or Extreme variant. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-13A | CRITICAL | V4 Access Control — authenticate client identity and enforce least privilege; V13 API — local endpoint authentication and authorization | blocking | IPC legitimate/spoofed-process/wrong-session/remote/DACL adversarial suite | Any identity, impersonation, DACL, or custody failure blocks 06-15, 06-16, 06-18, all privileged mutation, and 06-26–06-28. |
| T-06-13B | CRITICAL | V6 Cryptography — authenticated canonical messages; V3 Session Management — nonce/counter/replay control; V7 Logging — durable attributable outcomes | blocking | MAC, nonce, counter, service restart/crash/preshutdown dedup, database custody/integrity, retention, replay, malformed-length/time tests | Any replay, second dispatch, volatile-only dedup, custody failure, or authentication bypass blocks broker promotion and every physical stage. |
| T-06-13C | CRITICAL | V4 Access Control — deny by default and expose only operation-specific authorization | blocking | Generic/script/file/registry/service/remote/Extreme rejection corpus | Any generic authority path blocks all downstream mutation wiring and physical promotion. |
</threat_model>
<verification>The approval validator, Cargo.lock identity comparison, IPC adversarial suite, durable restart/crash/preshutdown dedup suite, and architecture gate pass.</verification>
<success_criteria>No unverified or replayed client can reach privileged mutation.</success_criteria>
<output>Create `06-13-SUMMARY.md` with TDD and package approval evidence.</output>
