---
phase: 06-transactional-plans-and-recovery
plan: '08'
type: tdd
wave: 3
depends_on: ['06-03']
files_modified: [crates/plan-engine/src/reconcile.rs, crates/plan-engine/tests/reconcile.rs]
autonomous: true
requirements: [PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'After crash, timeout, reboot, or response loss, observed prior means not applied, requested means applied-needs-receipt, and any third state means conflict.'
    - 'Mutation is never blindly retried and drift/conflict never silently overwrites Windows.'
  artifacts:
    - path: crates/plan-engine/src/reconcile.rs
      provides: Observation-first apply/restore reconciliation
    - path: crates/plan-engine/tests/reconcile.rs
      provides: Three-state, drift, timeout, and restore conflict proofs
  key_links:
    - from: crates/plan-engine/src/reconcile.rs
      to: exact prior/requested/observed states
      via: exhaustive decision
      pattern: 'AppliedNeedsReceipt|ConflictRequiresUserChoice'
---
<objective>Implement observation-first reconciliation through exhaustive TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@crates/plan-engine/src/domain.rs @.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md @.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify apply and restore reconciliation</name><read_first>crates/plan-engine/src/reconcile.rs; crates/plan-engine/tests/reconcile.rs (confirm absent); crates/plan-engine/src/domain.rs; 06-CONTEXT.md D-05, D-19, D-21, D-23, D-28, D-30</read_first><files>crates/plan-engine/tests/reconcile.rs</files><action>Write failing exhaustive cases for prior/requested/third-state observations across apply and restore, dispatch success/failure/timeout/response loss, abandoned mutex, unknown prior, drift before dispatch, already applied/restored, and conflict choices `keep-current` versus `restore-prior`. Assert no outcome reports success without verified observation and receipt eligibility.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test reconcile</automated></verify><acceptance_criteria>- RED compiles and fails for missing decisions.
- Each crash/timeout boundary asserts no retry command.
- Conflict tests expose all three exact values and require explicit choice.</acceptance_criteria><done>RED reconciliation oracle is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Reconcile only from observed Windows truth</name><read_first>crates/plan-engine/src/reconcile.rs; crates/plan-engine/tests/reconcile.rs; packages/contracts-source/src/transactional-plans.tsp</read_first><files>crates/plan-engine/src/reconcile.rs</files><action>Implement total apply/restore decision functions. Treat dispatch metadata as diagnostic only; compare canonical exact states, return not-applied/no-retry, applied-needs-receipt, verified-restored, drift, unknown, or conflict with safe next actions. A keep-current/restore-prior resolution creates a new transaction intent and never rewrites history.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test reconcile</automated></verify><acceptance_criteria>- Every decision is derived from observation plus durable intent.
- Timeout/unknown never becomes success or automatic mutation retry.
- Third state always pauses with an explicit conflict payload.</acceptance_criteria><done>Crash and drift outcomes are deterministic and safe.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove reconciliation totality</name><read_first>crates/plan-engine/src/reconcile.rs; crates/plan-engine/tests/reconcile.rs</read_first><files>crates/plan-engine/src/reconcile.rs, crates/plan-engine/tests/reconcile.rs</files><action>Consolidate comparison helpers, add state permutation/property coverage, and preserve closed decision outputs. Commit refactor if changed.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine reconcile</automated></verify><acceptance_criteria>- All state permutations terminate in a named decision.
- Source has no default-success or retry-on-timeout branch.</acceptance_criteria><done>Observation-first recovery is exhaustive.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| broker result -> transaction verdict | Return codes and lost responses cannot prove final OS state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-08A | Repudiation/Tampering | reconcile reducer | mitigate | Exact observation drives a total three-way decision; no dispatch-success receipt or blind retry. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-08A | HIGH | V7 Logging — security outcome is attributable to observed evidence; V5 Input Validation — exhaustive state handling | blocking | `rtk cargo test -p liiiraa-plan-engine --test reconcile` exhaustive matrix | Any optimistic success, ambiguous default, or blind retry blocks 06-14, 06-15, integration, and physical promotion. |
</threat_model>
<verification>Focused reconcile tests and TDD commit sequence pass.</verification>
<success_criteria>Every interruption yields one deterministic safe decision backed by observed state.</success_criteria>
<output>Create `06-08-SUMMARY.md`.</output>
