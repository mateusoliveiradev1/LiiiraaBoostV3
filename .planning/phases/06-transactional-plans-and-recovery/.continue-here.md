---
context: phase
phase: 06-transactional-plans-and-recovery
task: 1
total_tasks: 3
status: paused
last_updated: 2026-08-17T02:20:05.614Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual physical failures. Acknowledge them before resuming.

- [ ] CONSTRAINT: Terminal-version immutability — v68 is physically BLOCKED and must never be retried; continue only with v69.
- [ ] CONSTRAINT: Exact pipeline ordering — artifact, fixed verifier, and simulation are complete, but bridge rebind, full gates, v68 checkpoint archival, and Audit must pass before any v69 RunCleanVm.
- [ ] CONSTRAINT: Installed-checkpoint preservation — rename the fixed v68 checkpoint only; preserve GUID `84635905-a816-4b4e-9cdd-acb05b3c1dbf`, evidence, checkpoint count, and VM `Off`. Never delete or restore it during archival.
- [ ] CONSTRAINT: Durable human authorization — disclose the APPLY phrase only after the durable v69 `APPLY-PROMPT-READY` file is observed; never self-approve Task 2.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| EOF wait after parent exit | v68 hung at `webdriver-launch` because `read_to_end` waited on a pipe inherited by a native descendant | blocking | Keep the committed `PeekNamedPipe` available-byte snapshot and its descendant-inheritance regression test |
| Reusing a terminal version | A blocked physical version cannot acquire new authority later | blocking | Advance only with the existing v69 artifact/evidence tuple |
| Starting Hyper-V before authority closure | VM work before bridge/gates/Audit would break the plan ledger | blocking | Complete RED/GREEN bridge rebind, full gates, rename-only checkpoint archival, then read-only Audit |
| Rewriting evidence history | UAT and physical evidence are append-only | blocking | Preserve historical prefixes and create only monotonic successor records |

<current_state>
Phase 06, plan 06-26, Task 1 is paused at a safe pre-Hyper-V boundary. Causal pipe repair, immutable v69 artifact publication, fixed verifier, and deterministic v69 admission are committed. No v69 bridge rebind, checkpoint archival, Audit, or RunCleanVm has started.
</current_state>

<completed_work>

- RED `273ff42c`: reproduced the descendant-inherited WebDriver pipe hang and ordinary early-exit capture.
- GREEN `96882498`: used `PeekNamedPipe`, strict 16 KiB capture, clean-close classification, raw-free diagnostics, and owned-handle cleanup. Gates passed: desktop 255/255, Hyper-V 33/33, architecture 51/51, evidence 103/103, parser PASS, planned evaluator ok.
- Docs `c69cac97`: recorded immutable v68 blocker, prompt/accepted hashes, transcript, cleanup, checkpoint GUID, and causal repair.
- Artifact docs `b25b3d8a`: published v69 build `physical-5a94c1dc1ae583b7-managed-power-scheme-v69`, source `c69cac97c3373c5f1b127fc2fc0e2c0e2e58544a`, manifest SHA-256 `600d63bf5c593d80200f1957c5f8da8c4712aaf23235ff26b61a761c92bb6596`; lifecycle PASS and fixed verifier `verified`.
- Simulation `8dd07133`: admitted v69 deterministic run SHA-256 `8948c5e4380f4038d5fada4b6cc1bbe458372c3d1c5506ea46398de1a157d085`, evidence manifest SHA-256 `31ef01239e6ffc58a6c2e919764d8ba175191be80c6d692d20eb218782c6a398`, and preserved v68 predecessor SHA-256 `f4af847c37850010c0f3c4b024d2533a4e69efee2b837e4f579b478e2a3c0b9b`.
</completed_work>

<remaining_work>

1. TDD-rebind the Hyper-V bridge and its mutation corpus from exact v68 authority to exact v69 authority; include the v68 predecessor in the closed chain.
2. Run the full desktop/Hyper-V/architecture/evidence/parser/planned gates and commit RED/GREEN atomically.
3. PRE-UAC, then rename-only archive `LiiiraaBoost-Installed` to `LiiiraaBoost-Installed-BLOCKED-v68`, preserving GUID `84635905-a816-4b4e-9cdd-acb05b3c1dbf`, checkpoint count, evidence, and VM `Off`.
4. Run one read-only v69 Audit. Only after PASS may one v69 RunCleanVm start.
5. During RunCleanVm, wait for durable prompt-ready before reporting the exact APPLY phrase. On failure, cleanup exact VM to `Off` and do not retry v69. On PASS, stop at mandatory Task 2 human review.
6. After explicit approval, execute Task 3, plans 06-27 and 06-28, then phase verification/completion.
</remaining_work>

<decisions_made>

- v68 is terminal and preserved; v69 is the only legal successor.
- WebDriver output capture never waits for EOF after parent exit; it snapshots only immediately available bytes.
- The user-requested pause occurs after deterministic admission and before any v69 Hyper-V mutation.
</decisions_made>

<blockers>

- Fixed installed-checkpoint name is occupied by preserved v68 GUID `84635905-a816-4b4e-9cdd-acb05b3c1dbf`; perform the established rename-only archival before v69 Audit/run.
- Physical v69 evidence does not yet exist. No physical PASS, human review, owner/friends consent, or release readiness is claimed.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` and `C:\Users\Liiiraa\.codex\RTK.md` — mandatory repository and shell rules.
2. `.planning/phases/06-transactional-plans-and-recovery/06-26-PLAN.md` — Task 1 ordering and Task 2 blocking-human checkpoint.
3. This handoff and `.planning/HANDOFF.json` — exact pause state.
4. `.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md` and `06-35-SUMMARY.md` — v69 artifact/verifier authority.
5. `.planning/phases/06-transactional-plans-and-recovery/06-38-SUMMARY.md` and `06-UAT.md` — v69 deterministic authority and append-only ledger.
6. `.planning/phases/06-transactional-plans-and-recovery/06-34-SUMMARY.md` — v68 terminal blocker and checkpoint state.
7. `tooling/hyperv-lab/Invoke-Phase6Physical.ps1` and `tooling/hyperv-lab/phase6-physical.test.mjs` — next RED/GREEN ownership.
8. `apps/desktop/src-tauri/src/physical_runner.rs` — committed pipe fix and regression tests.

## Critical Anti-Patterns (do NOT repeat these)

- Do not call `read_to_end` on exited WebDriver stdout/stderr; a descendant may hold the pipe open.
- Do not retry v68 or overwrite any v68 evidence/checkpoint bytes.
- Do not remint an artifact unless a new causal product issue genuinely requires a monotonic successor.
- Do not start v69 Audit/RunCleanVm before bridge rebind and all gates pass.
- Do not reveal or accept APPLY before durable prompt-ready, and do not self-approve the human checkpoint.

## Infrastructure State

- No Phase 6 build, verifier, simulation, or RunCleanVm process is active.
- The last durable cleanup proves VM `LiiiraaBoost-W11-25H2-Clean` is `Off`; v69 has not touched the VM.
- Preserved fixed checkpoint: `LiiiraaBoost-Installed`, GUID `84635905-a816-4b4e-9cdd-acb05b3c1dbf`, from terminal v68.
- v69 artifact root: `target/phase6-physical/c69cac97c3373c5f1b127fc2fc0e2c0e2e58544a/physical-5a94c1dc1ae583b7-managed-power-scheme-v69`.
- Active deterministic authority: operation `managed-power-scheme-v69`, manifest `600d63bf...6596`, run `8948c5e4...d085`, evidence `31ef0123...a398`.

<context>
The product issue is fixed and verified in local/full gates. The v69 artifact and deterministic evidence are closed, but the Hyper-V bridge intentionally still names v68 because the user paused before that next TDD stage. Resume at bridge RED, not at build or simulation, and keep every external operation visible, bounded, and append-only.
</context>

<next_action>
Start with: acknowledge the four blocking constraints, then write the failing v69 bridge-authority test. Do not invoke Hyper-V until bridge GREEN and all gates pass.
</next_action>
