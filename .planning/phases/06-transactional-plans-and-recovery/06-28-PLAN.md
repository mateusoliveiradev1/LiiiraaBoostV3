---
phase: 06-transactional-plans-and-recovery
plan: '28'
type: execute
wave: 21
depends_on: ['06-27']
files_modified:
  - tooling/phase6-evidence/evidence-manifest.json
  - .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'The exact owner-admitted version completes the full cycle on every consented friends PC with local redacted preview before any export/send.'
    - 'An immutable pseudonymous roster is frozen before the first run; only one create-only run, local pre-export consent, and strictly-later review per roster member can admit friends and final.'
    - 'A failed/unavailable machine blocks the version; any correction restarts at deterministic simulation and coverage gaps remain visible.'
  artifacts:
    - path: tooling/phase6-evidence/evidence-manifest.json
      provides: Frozen friends roster plus append-only per-machine run, consent, review, and final all-four-stage verdict
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UAT.md
      provides: Per-machine run/review transcript, blockers, privacy proof, and final walkthrough
  key_links:
    - from: .planning/phases/06-transactional-plans-and-recovery/06-28-PLAN.md
      to: target/phase6-physical/{commit}/{buildId}/phase6-physical-runner.exe
      via: exact repeated local invocation with signed-manifest `configs/friends-pc.run-config.json` on every roster machine
      pattern: 'phase6-physical-runner.exe.*configs/friends-pc.run-config.json'
    - from: friends-pc.raw.json
      to: tooling/phase6-evidence/src/physical-writer.ts
      via: each locally previewed envelope is ingested with closed friends-pc stage after return
      pattern: 'phase6:ingest-physical.*friends-pc'
    - from: tooling/phase6-evidence/evidence-manifest.json
      to: phase6:verify --mode final
      via: simulation, clean-VM, owner-PC, and friends-PC exact-version chain
      pattern: 'deterministic-simulation.*clean-windows-vm.*owner-pc.*friends-pc'
    - from: tooling/phase6-evidence/evidence-manifest.json
      to: tooling/phase6-evidence/src/evaluate.ts
      via: immutable roster hash and exact one-to-one run/consent/review cardinality
      pattern: 'friendsRosterSha256.*runs.*consents.*reviews'
---
<objective>Run, persist, review, and finally verify the consented friends-PC stage for one unchanged operation version.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-27-SUMMARY.md @.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md @.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md @tooling/phase6-evidence/evidence-manifest.json</context>
<tasks>
<task type="auto">
  <name>Task 1: Execute and persist every consented friends-PC evidence run</name>
  <read_first>tooling/phase6-evidence/evidence-manifest.json; 06-UAT.md; owner-PC admitted evidence; 06-31-SUMMARY.md exact artifact root; 06-33-SUMMARY.md runner/config contract; 06-CONTEXT.md D-01, D-02, D-06, D-07, D-35; tooling/phase6-evidence/src/physical-writer.ts</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>Require the exact owner-admitted operation version/build/artifact/config/runner/driver hashes. Before copying bytes, create a roster candidate containing one unique purpose-bound pseudonymous participant ID and required machine slot for every declared participant, then run `rtk pnpm phase6:freeze-friends-roster -- --roster <roster-path>` exactly once; after the first run the roster hash, membership, cardinality, and order are immutable, and an unavailable declared member blocks rather than shrinking the roster. On each roster PC, copy the unchanged artifact directory and invoke exactly `<artifact-root>\phase6-physical-runner.exe --run-config <artifact-root>\configs\friends-pc.run-config.json`. The runner authenticates CMS/SPKI/live bytes, installs/verifies and emits installed-ready; the participant/operator relaunches the identical command so it creates/verifies the local recovery checkpoint and runs until reboot-pending; after the participant chooses to reboot, relaunch that identical command again. Runner verifies the exact continuation and observes before restore/mutation. Generate the config-defined raw envelope locally, run local redaction, render the complete preview, and create the participant consent bound to roster/run/preview hash before any export/send. Return only the consented envelope and ingest with `rtk pnpm phase6:ingest-physical -- --artifact-manifest <artifact-root>\artifact-manifest.json --run-envelope <returned-machine-envelope> --stage friends-pc`; writer re-verifies custody and appends one run plus consent for that roster member without overwrite. Persist per-machine lifecycle/GUID/effective-user/dispatch/journal/receipt/redaction/preview/no-upload/accessibility evidence and coverage gaps with review still pending. Operator control is limited to reboot timing and the recorded identical command; no alternate executable/config/path/stage/arguments exist. Any unavailable roster member, signature/config/continuation mismatch, failed assertion, privacy failure, duplicate/extra machine, or cardinality mismatch records BLOCKED and requires a new version from simulation.</action>
  <verify><automated>rtk pnpm phase6:verify -- --mode planned --require-run-evidence friends-pc</automated></verify>
  <acceptance_criteria>- Every frozen-roster machine has signed-runner provenance and a complete exact-version reversible cycle through installed Tauri and broker.
- Runner/config/envelope paths and arguments are closed/hash-bound; no legacy harness, callback, fixture, or participant-side Node path exists.
- Redaction, complete preview, explicit consent, and no automatic upload are evidenced before checkpoint review.
- Run/consent/review collections are create-only and exact one-to-one with the immutable roster.
- Failure/unavailability blocks rather than shrinking the declared required matrix.</acceptance_criteria>
  <done>A complete friends-PC evidence package exists for participant/human inspection.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review the already-produced friends-PC evidence</name>
  <read_first>tooling/phase6-evidence/evidence-manifest.json; .planning/phases/06-transactional-plans-and-recovery/06-UAT.md; every exact Task 1 participant run file/preview; admitted owner predecessor</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>This checkpoint performs no evidence mutation or runner invocation. Pause before execution and collect only the participant/reviewer verdicts for Task 3 to persist and mechanically validate against Task 1 evidence.</action>
  <what-built>The signed self-contained runner and physical-writer produced per-machine redacted evidence for the exact owner-admitted version, including GUID lifecycle, effective-user proof, reboot/recovery, faults/posture, local diagnostic preview, consent, no-upload proof, accessibility observations, and coverage gaps.</what-built>
  <how-to-verify>1. For each participant, compare exact owner predecessor/version/build/hash identity. 2. Confirm the machine applied the owned GUID and restored the exact prior GUID after reboot/recovery. 3. Inspect the complete locally previewed redacted package and no-automatic-upload evidence. 4. Each participant confirms consent and package contents. 5. Confirm all required machines/cells passed and that remaining hardware gaps are named without a 100% support claim.</how-to-verify>
  <verify><automated>rtk pnpm phase6:verify -- --mode planned --require-run-evidence friends-pc</automated><human-check>Every participant/reviewer completes the five exact evidence/privacy checks above.</human-check></verify>
  <resume-signal>Reply with one `APPROVED friends-pc &lt;operation-version&gt; &lt;pseudonymous-participant-id&gt;` verdict per frozen roster member only after that machine package passes, or describe the exact failure. Missing/rejected members block Phase 6.</resume-signal>
  <acceptance_criteria>- Automated gate reports every frozen-roster machine review-ready for one exact owner-admitted tuple.
- Humans confirm per-machine restoration, local preview/redaction, consent/no-upload, accessibility, and explicit gaps.</acceptance_criteria>
  <done>All required matching approvals exist for Task 3, or final Phase 6 admission remains blocked with exact failures.</done>
</task>
<task type="auto">
  <name>Task 3: Persist friends approvals and run the final all-four-stage verifier</name>
  <read_first>tooling/phase6-evidence/evidence-manifest.json; .planning/phases/06-transactional-plans-and-recovery/06-UAT.md; the exact Task 2 reviewer/participant response; simulation, clean-VM, and owner admitted evidence</read_first>
  <files>tooling/phase6-evidence/evidence-manifest.json, .planning/phases/06-transactional-plans-and-recovery/06-UAT.md</files>
  <action>For each frozen-roster member, create the closed review document from the exact checkpoint response, pseudonymous reviewer identity and UTC timestamp, then run `rtk pnpm phase6:record-review -- --stage friends-pc --review <member-review-path>`. The create-only recorder must bind roster hash, participant ID, operation/build/artifact/config/run/consent hashes, require the review strictly after that member's run, and leave roster/run/consent bytes untouched. Run the planned admitted-stage evaluator first; it must require exactly one PASS run, pre-export consent, and APPROVED review per frozen roster member with no missing/duplicate/extra records. Only after that succeeds, run bare final mode requiring deterministic-simulation, clean-windows-vm, owner-pc, and friends-pc for the same version. Reject any missing/rejected/mismatched/out-of-order stage, roster member, hash, consent, redaction, preview, or review. Preserve BLOCKED evidence and do not create completion summary on failure. Any correction mints a new version and restarts at simulation.</action>
  <verify><automated>rtk pnpm phase6:verify -- --mode planned --require-admitted-stage friends-pc &amp;&amp; rtk pnpm phase6:verify -- --mode final</automated></verify>
  <acceptance_criteria>- Friends admission requires exactly one matching run, consent, and later approval for every frozen-roster member and no extras.
- Final PASS requires all four stages for one exact operation version and complete cycle at each.
- Coverage gaps remain enumerated and no 100% support claim exists.
- Any failure leaves Phase 6 incomplete with immutable escalation evidence.</acceptance_criteria>
  <done>The exact version passes all four stages or remains explicitly blocked; there is no admitted-or-blocked ambiguity.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| packaged build -> friends PCs | Diverse physical hardware/posture may fail despite prior stages. |
| local diagnostics -> preview/export/send | Personal/system data may leak or be sent without consent. |
| staged evidence -> final phase verdict | A skipped/mismatched/rejected stage could fabricate completion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-28A | Elevation/Tampering | friends physical runs | mitigate | Exact predecessor/version/build, per-machine full cycle and persisted-before-review evidence. |
| T-06-28B | Information Disclosure | diagnostics | mitigate | Local redaction, complete preview, explicit participant consent, no automatic upload. |
| T-06-28C | Spoofing/Repudiation | final promotion | mitigate | Exact hashes, sequential predecessor chain, persisted human verdicts, all-four-stage evaluator, no override. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-28A | CRITICAL | V4 Access Control; V5 Input Validation | blocking-human | Per-machine GUID/reboot/recovery/fault evidence and confirmation | Any unavailable/failed machine blocks Phase 6; correction restarts at simulation. |
| T-06-28B | HIGH | V8 Data Protection | blocking-human | Redaction scan, complete preview, consent, no-upload evidence | Any privacy failure blocks friends admission and Phase 6. |
| T-06-28C | CRITICAL | V7 Logging; V4 Access Control | blocking-human | Exact-version all-four-stage hashes and persisted reviewer verdicts | Missing/mismatched/out-of-order/rejected evidence blocks final PASS with no override. |
</threat_model>
<verification>Friends and final evaluators pass only after every physical run is persisted, human-reviewed, consented, and bound to one exact version across all four stages.</verification>
<success_criteria>The first real operation version has passed simulation, clean VM, owner PC, and consented friends PCs in order with exact recovery and honest coverage limits.</success_criteria>
<output>Create `06-28-SUMMARY.md` only after both Task 3 commands pass; otherwise update UAT, return structured BLOCKED evidence, and do not create a completion summary.</output>
