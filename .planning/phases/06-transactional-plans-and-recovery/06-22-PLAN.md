---
phase: 06-transactional-plans-and-recovery
plan: '22'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
  - .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
  - .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Transactional UI implementation cannot begin while the UI contract is pending review.'
    - 'Only an independently produced checker report with exactly the canonical six unique dimensions at verdict PASS, verified report bytes, disposed findings, and separate human acknowledgement can change the UI contract to approved.'
    - 'UI-SPEC status, reviewed timestamp, six sign-off checkboxes, and approval line cannot disagree with the validated structured approval record.'
  artifacts:
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json
      provides: Closed machine-readable approval contract for checker evidence, SHA-256 report binding, six exact PASS verdicts, findings disposition, UTC timestamps, and human acknowledgement
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
      provides: Persisted approval record bound to the checker-produced report and checkpoint acknowledgement
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
      provides: Fail-closed report-digest, schema, uniqueness, evidence, acknowledgement, promotion, and UI-SPEC agreement gate
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs
      provides: Positive and adversarial approval-gate coverage
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      provides: UI authority whose status and sign-off metadata match the validated approval evidence
  key_links:
    - from: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
      to: checkerEvidence.report.path
      via: SHA-256 recomputation over exact checker report bytes and semantic comparison of checker identity, timestamp, dimensions, and findings
      pattern: '"algorithm"\\s*:\\s*"sha256"'
    - from: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
      to: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      via: validator-owned coherent promotion followed by read-only agreement validation
      pattern: 'status: approved'
---
<objective>Establish honest independent UI-contract authority before any Phase 6 component or feature implementation.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md @C:/Users/Liiiraa/.codex/get-shit-done/workflows/ui-phase.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Obtain the independent six-dimension UI checker report and acknowledgement</name>
  <action>This checkpoint performs no artifact mutation. Pause before execution and collect an independently produced, machine-readable checker report outside Plan 06-22's `files_modified`. The report must identify the checker with nonempty `name`, `provider`, and independent `runId`; use a canonical UTC `completedAtUtc`; contain exactly one result for each canonical dimension ID `copywriting`, `visuals`, `color`, `typography`, `spacing`, and `registry-safety`; use an exact verdict enum rather than prose; and enumerate findings with their dispositions. Also collect a distinct human acknowledgement identity and UTC timestamp. Task 2 may transcribe and bind this evidence but must not author, rewrite, or replace the checker report.</action>
  <what-built>`06-UI-SPEC.md` is intentionally `pending-review`; no plan may treat it as approved yet.</what-built>
  <how-to-verify>1. Run the independent Phase 6 UI checker against the current `06-UI-SPEC.md` and save its machine-readable report at an independently referenceable repository-relative path. 2. Confirm the report contains checker identity/run ID, canonical UTC completion timestamp, exactly the six canonical dimension IDs, exact verdicts, and findings/dispositions. 3. If every verdict is exactly `PASS` and every finding is explicitly `none-raised` or `resolved`, compute SHA-256 from the saved report's exact bytes and review that report. 4. Supply the report path, digest, and a separate human acknowledgement identity/timestamp together with the exact acknowledgement `I reviewed the independently produced checker report bound by this SHA-256 and approve this exact UI-SPEC.` Any `FAIL`, `BLOCK`, `FLAG`, unknown verdict, open finding, duplicate/missing dimension, absent evidence, or refusal to acknowledge keeps `06-UI-SPEC.md` pending and returns it for correction.</how-to-verify>
  <resume-signal>Provide the checker-produced report path, its SHA-256 digest, and the human acknowledgement identity/UTC timestamp plus the exact acknowledgement statement, or describe the blocking checker result. Plain `approved` is not accepted.</resume-signal>
</task>
<task type="auto">
  <name>Task 2: Implement, test, and execute the structured UI approval gate</name>
  <read_first>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md; the checker-produced machine-readable report path and checkpoint acknowledgement returned from Task 1; .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, and .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs (confirm absent before creation)</read_first>
  <files>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs</files>
  <action>Create a closed JSON Schema and dependency-free Node validator for an approval record rooted at `schemaVersion`, `checkerEvidence`, and `humanAcknowledgement`, with `additionalProperties: false` at every object level. `checkerEvidence` must contain nonempty `checkerIdentity.name`, `checkerIdentity.provider`, and independent `checkerIdentity.runId`; canonical UTC `completedAtUtc` in exact `YYYY-MM-DDTHH:mm:ss.sssZ` form whose parse-and-reemit value is identical; a repository-relative `report.path`; `report.digest` exactly `{ algorithm: "sha256", value: &lt;64 lowercase hex characters&gt; }`; exactly six unique `dimensions` objects with IDs `copywriting`, `visuals`, `color`, `typography`, `spacing`, and `registry-safety` and verdict exactly the enum `PASS`; and `findingsDisposition` exactly `{ status: "none-raised" | "all-resolved", detail: &lt;nonempty string&gt; }`. Derive that disposition from the checker report: an empty findings array requires `none-raised`, while a nonempty array requires every finding to have an ID and disposition exactly `resolved`, yielding `all-resolved`; any open/unknown finding fails. `humanAcknowledgement` must contain nonempty `acknowledgedBy` distinct from the checker identity, canonical UTC `acknowledgedAtUtc`, `reportSha256` equal to the report digest, and statement exactly `I reviewed the independently produced checker report bound by this SHA-256 and approve this exact UI-SPEC.` The independently produced report must remain outside this plan's files and expose matching checker identity/run ID, completion timestamp, dimension verdicts, and findings data. The validator must resolve the report path inside the repository, reject the approval/UI-SPEC/schema/validator files as self-referential evidence, recompute SHA-256 over the exact report bytes, fail closed on unreadable or mismatched evidence, and compare the structured record fields to the parsed checker report rather than trusting a Plan 06-22 transcription.

Write Node built-in test-runner cases before promotion. Include one valid fixture and negative cases for exact `FAIL`, `BLOCK`, `FLAG`, an unknown verdict, prose containing PASS such as `FAIL — not PASS`, duplicate dimensions, each missing dimension, unknown/extra dimensions, absent checker identity/run ID, absent report path/digest, invalid/non-UTC timestamps, missing/open findings disposition, missing/mismatched human acknowledgement, report-hash mismatch, report/record semantic mismatch, approved status with incomplete checkboxes, checked boxes with pending status, wrong/duplicate sign-off labels, and an approval-line or `reviewed_at` disagreement. Every invalid case must exit nonzero.

Only after the test suite passes, copy the Task 1 evidence into `06-22-UI-CONTRACT-APPROVAL.json` without inventing identity, verdicts, findings, digest, or acknowledgement. Use one validator-owned promotion operation: build temporary approval/UI-SPEC candidates, validate their complete agreement with the checker report, then replace the destinations with rollback-on-write-failure semantics. The promoted UI-SPEC must have frontmatter `status: approved`, `reviewed_at` exactly equal to the acknowledgement UTC timestamp, exactly the six existing canonical checked sign-off lines and no unchecked/duplicate canonical line, and approval text rendered exactly as `**Approval:** approved by {acknowledgedBy} at {acknowledgedAtUtc}; report={report.path}; sha256={report.digest.value}` from the structured fields. The normal validation command is read-only and must revalidate the persisted record, live checker report digest/semantics, and UI-SPEC agreement. On any failure, leave or restore `pending-review`, do not create an approval record that claims success, fail the task, and keep 06-17/06-19/06-20 blocked.</action>
  <verify><automated>rtk cmd /d /c "rtk node --test .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs &amp;&amp; rtk node .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs --check .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md"</automated></verify>
  <acceptance_criteria>- Schema, record, validator, and tests encode exactly six unique canonical dimensions with verdict exactly PASS; contradictory prose and every non-PASS/unknown verdict fail.
- The validator proves the independently produced report exists, its SHA-256 matches, its structured checker identity/results/findings match the approval record, and the human acknowledgement is distinct and complete.
- UI-SPEC status, reviewed_at, six exact sign-off checkboxes, and approval line agree with the validated record after the single promotion operation.
- Digest/read/parse/schema/semantic/UI disagreement fails closed and dependent UI plans remain blocked.</acceptance_criteria>
  <done>The UI contract has independently evidenced, schema-valid six-dimension authority or remains explicitly blocked.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| independent checker report -> Plan 06-22 approval record | A same-task transcription must not substitute for independently produced evidence. |
| validated approval record -> consequential UI implementation | Unreviewed, contradictory, or internally inconsistent sign-off must not authorize risk/recovery UI. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-22A | Spoofing/Repudiation | UI checker sign-off | mitigate | Closed schema, independently produced structured report, recomputed SHA-256, semantic comparison, separate human acknowledgement, adversarial tests, and exact UI-SPEC agreement gate. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-22A | HIGH | V4 Access Control — consequential controls remain deny-by-default; V7 Logging — review authority is attributable | blocking-human | `06-22-validate-ui-contract-approval.test.mjs` negative suite plus read-only validator proof of checker report SHA-256/semantics, human acknowledgement, and exact UI-SPEC agreement | Any unreadable/hash-mismatched report, missing/non-PASS/fabricated/duplicated dimension, incomplete acknowledgement, or UI-SPEC disagreement blocks 06-17, 06-19, 06-20, and physical promotion. |
</threat_model>
<verification>The Node negative suite proves fail-closed handling for non-PASS/prose/duplicate/missing/evidence/hash/acknowledgement/UI disagreement cases; the read-only validator then proves the persisted structured record, independent report bytes and semantics, and approved UI-SPEC are one coherent authority.</verification>
<success_criteria>No Phase 6 UI plan consumes a pending, fabricated, contradictory, digest-mismatched, or internally inconsistent design authority.</success_criteria>
<output>Create `06-22-SUMMARY.md` only after the independent report is verified, all six checker dimensions pass, the human acknowledgement is persisted, and the read-only validator accepts the promoted UI-SPEC.</output>
