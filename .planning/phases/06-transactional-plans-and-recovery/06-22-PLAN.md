---
phase: 06-transactional-plans-and-recovery
plan: '22'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Transactional UI implementation cannot begin while the UI contract is pending review.'
    - 'Only an actual independent checker result with all six dimensions PASS can change the UI contract to approved.'
  artifacts:
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md
      provides: Independent checker identity, report reference/hash, six verdicts, timestamp, and exact human acknowledgement
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      provides: UI authority whose status matches the persisted checker outcome
  key_links:
    - from: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md
      to: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      via: six-PASS-only status transition
      pattern: 'Copywriting.*PASS.*Visuals.*PASS.*Color.*PASS.*Typography.*PASS.*Spacing.*PASS.*Registry Safety.*PASS'
---
<objective>Establish honest independent UI-contract authority before any Phase 6 component or feature implementation.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md @C:/Users/Liiiraa/.codex/get-shit-done/workflows/ui-phase.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Obtain the independent six-dimension UI checker verdict</name>
  <action>This checkpoint performs no artifact mutation. Pause before execution and collect only the actual independent checker result for Task 2 to persist, validate, and—only on six PASS verdicts—apply to UI-SPEC.</action>
  <what-built>`06-UI-SPEC.md` is intentionally `pending-review`; no plan may treat it as approved yet.</what-built>
  <how-to-verify>1. Run the independent Phase 6 UI checker against the current `06-UI-SPEC.md`. 2. Require separate verdicts for Copywriting, Visuals, Color, Typography, Spacing, and Registry Safety. 3. Capture checker identity, report/reference, immutable report hash when available, UTC timestamp, and every finding. 4. Approve only if all six verdicts are PASS; any FLAG/BLOCK/missing dimension keeps the contract pending and returns it for correction.</how-to-verify>
  <resume-signal>Provide the independent checker result with all six named verdicts and its report reference, or describe the failing dimension. Do not reply `approved` without checker evidence.</resume-signal>
</task>
<task type="auto">
  <name>Task 2: Persist and validate the UI checker authority</name>
  <read_first>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md; the exact independent checker result returned from Task 1</read_first>
  <files>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md</files>
  <action>Persist the exact checker identity, report/reference/hash, UTC timestamp, all six named verdicts, findings, and human acknowledgement in the approval record. Mechanically reject any missing dimension or any verdict other than PASS. Only when all six are PASS, change UI-SPEC frontmatter `status` to `approved`, check the six sign-off boxes, set `Approval` to the checker-backed timestamp/reference, and leave all contract content otherwise unchanged. On any non-PASS result, preserve `pending-review`, persist the failure, fail this task, and keep 06-17/06-19/06-20 blocked. Never synthesize a checker identity or sign-off.</action>
  <verify><automated>rtk node -e "const fs=require('fs');const p='.planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.md';const u='.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md';const a=fs.readFileSync(p,'utf8'),s=fs.readFileSync(u,'utf8');for(const d of ['Copywriting','Visuals','Color','Typography','Spacing','Registry Safety'])if(!new RegExp(d+'[^\\n]*PASS','i').test(a))throw Error('missing PASS: '+d);if(!/^status: approved$/m.test(s)||((s.match(/- \[x\] Dimension/g)||[]).length!==6))throw Error('UI-SPEC not checker-approved');"</automated></verify>
  <acceptance_criteria>- Approval record identifies a real independent checker result and all six verdicts.
- UI-SPEC becomes approved only when all six verdicts are PASS.
- Any missing/failed dimension leaves dependent UI plans blocked.</acceptance_criteria>
  <done>The UI contract has honest six-dimension authority or remains explicitly blocked.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pending design contract -> consequential UI implementation | Unreviewed interaction/copy can misrepresent risk, authority, or recovery. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-22A | Spoofing/Repudiation | UI checker sign-off | mitigate | Independent checker identity/report, six explicit verdicts, persisted human acknowledgement, and mechanical six-PASS gate. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-22A | HIGH | V4 Access Control — consequential controls remain deny-by-default; V7 Logging — review authority is attributable | blocking-human | Persisted checker report identity/hash and all-six-PASS validation | Any missing/non-PASS/fabricated dimension blocks 06-17, 06-19, 06-20, and physical promotion. |
</threat_model>
<verification>The approval-record validator proves all six independent PASS results and matching approved UI-SPEC state.</verification>
<success_criteria>No Phase 6 UI plan consumes a pending or fabricated design authority.</success_criteria>
<output>Create `06-22-SUMMARY.md` only after all six checker dimensions pass and Task 2 validates the persisted authority.</output>
