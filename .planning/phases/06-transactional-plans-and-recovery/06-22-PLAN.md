---
phase: 06-transactional-plans-and-recovery
plan: '22'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json
  - .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
  - .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
  - .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Transactional UI implementation cannot begin while the UI contract is pending review.'
    - 'Only an independently produced checker report with exactly the canonical six unique dimensions at verdict PASS, verified report bytes, disposed findings, and separate human acknowledgement strictly after checker completion can change the UI contract to approved.'
    - 'The checker report and approval record identify the exact canonical review-input path and SHA-256; every check re-derives that input from the live substantive UI-SPEC so stale evidence cannot authorize a changed contract.'
    - 'UI-SPEC status, reviewed timestamp, six sign-off checkboxes, and approval line cannot disagree with the validated structured approval record.'
  artifacts:
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md
      provides: Byte-stable substantive UI-SPEC review payload excluding only mutable approval/status metadata
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json
      provides: Closed machine-readable approval contract for exact subject path/digest, checker report binding, six exact PASS verdicts, findings disposition, strict UTC chronology, and human acknowledgement
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
      provides: Persisted approval record bound to the checker-produced report and checkpoint acknowledgement
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
      provides: Fail-closed report-digest, schema, uniqueness, evidence, acknowledgement, promotion, and UI-SPEC agreement gate
    - path: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs
      provides: Positive and adversarial approval-gate coverage
    - path: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      provides: UI authority whose status and sign-off metadata match the validated approval evidence
  key_links:
    - from: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      to: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md
      via: deterministic canonicalization that strips only status/reviewed_at and the terminal Checker Sign-Off approval section, then byte-compares on every check
      pattern: 'ui-spec-review-payload-v1'
    - from: checkerEvidence.report.subject
      to: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md
      via: exact repository-relative subject path and recomputed SHA-256 agreement shared by checker report and approval record
      pattern: 'subject.*path.*digest'
    - from: .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json
      to: checkerEvidence.report.path
      via: SHA-256 recomputation over exact checker report bytes and semantic comparison of checker identity, timestamp, dimensions, and findings
      pattern: '"algorithm"\\s*:\\s*"sha256"'
    - from: .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs
      to: .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md
      via: validator-owned coherent promotion followed by read-only agreement validation
      pattern: 'status: approved'
---
<objective>Establish honest independent UI-contract authority before any Phase 6 component or feature implementation.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md @C:/Users/Liiiraa/.codex/get-shit-done/workflows/ui-phase.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Freeze the canonical substantive UI review input</name>
  <read_first>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md; .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, and .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs (confirm absent before creation)</read_first>
  <files>.planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs</files>
  <action>Implement the dependency-free `ui-spec-review-payload-v1` canonicalizer and a `--prepare-review-input` mode before independent review. Decode the UI-SPEC as UTF-8, normalize CRLF to LF, require one closed YAML frontmatter block, remove only the `status` and `reviewed_at` frontmatter lines, require exactly one terminal `## Checker Sign-Off` section, exclude that heading and all of its mutable checkbox/approval content, preserve every other substantive byte and blank line in order, and emit exactly one final LF. Reject malformed/duplicate frontmatter keys, missing or duplicate sign-off sections, content after the sign-off section, invalid UTF-8, or a subject path outside the repository. Atomically write those exact canonical bytes to `06-22-UI-CONTRACT-REVIEW-INPUT.md`; do not add a generated banner that would alter the reviewed payload. Add built-in Node tests proving deterministic CRLF/LF normalization, metadata-only status/reviewed/sign-off changes leave the payload digest unchanged, and any substantive heading/body/table/copy mutation changes it. The review-input artifact is immutable after the checker runs: subsequent promotion and `--check` only recompute the live canonical payload in memory and byte-compare it to this saved subject; they never silently refresh it.</action>
  <verify><automated>rtk cmd /d /c "rtk node --test .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs &amp;&amp; rtk node .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs --prepare-review-input .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md"</automated></verify>
  <acceptance_criteria>- The saved review input is byte-identical to the in-memory canonical payload derived from the current substantive UI-SPEC.
- Approval/status metadata is excluded without excluding any product, interaction, accessibility, visual, or safety contract content.
- Re-running preparation before checker execution is deterministic; after checker execution, normal validation cannot rewrite the subject.</acceptance_criteria>
  <done>The independent checker has one non-self-referential, byte-stable subject whose future drift is mechanically detectable.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Obtain the independent six-dimension UI checker report and acknowledgement</name>
  <action>This checkpoint performs no artifact mutation. Pause after Task 1 and collect an independently produced, machine-readable checker report outside Plan 06-22's `files_modified`. The checker must review the exact saved `06-22-UI-CONTRACT-REVIEW-INPUT.md` subject. Its report must contain `subject.path` equal to that exact repository-relative path and `subject.digest` equal to `{ algorithm: "sha256", value: &lt;SHA-256 of exact subject bytes&gt; }`; identify the checker with nonempty `name`, `provider`, and independent `runId`; use canonical UTC `completedAtUtc`; contain exactly one result for each canonical dimension ID `copywriting`, `visuals`, `color`, `typography`, `spacing`, and `registry-safety`; use an exact verdict enum rather than prose; and enumerate findings with their dispositions. Also collect a distinct human acknowledgement identity and canonical UTC timestamp strictly later than checker completion. Task 3 may transcribe and bind this evidence but must not author, rewrite, replace, or retarget the checker report.</action>
  <what-built>`06-UI-SPEC.md` is intentionally `pending-review`, and `06-22-UI-CONTRACT-REVIEW-INPUT.md` is the frozen substantive subject; no plan may treat either as approved evidence yet.</what-built>
  <how-to-verify>1. Run the independent Phase 6 UI checker against the exact `06-22-UI-CONTRACT-REVIEW-INPUT.md` bytes and save its machine-readable report at an independently referenceable repository-relative path. 2. Confirm the report contains the exact subject path and SHA-256, checker identity/run ID, canonical UTC completion timestamp, exactly the six canonical dimension IDs, exact verdicts, and findings/dispositions. 3. If every verdict is exactly `PASS` and every finding is explicitly `none-raised` or `resolved`, compute SHA-256 from the saved report's exact bytes and review that report plus its bound subject. 4. After checker completion, supply the report path/digest, subject path/digest, and a separate human acknowledgement identity/timestamp together with the exact acknowledgement `I reviewed the independently produced checker report bound by this SHA-256 and approve this exact UI-SPEC.` Any stale/mismatched subject, `FAIL`, `BLOCK`, `FLAG`, unknown verdict, open finding, duplicate/missing dimension, absent evidence, acknowledgement at/before completion, or refusal to acknowledge keeps `06-UI-SPEC.md` pending and returns it for correction.</how-to-verify>
  <resume-signal>Provide the checker-produced report path/SHA-256, its exact subject path/SHA-256, and the human acknowledgement identity plus a canonical UTC timestamp strictly later than checker completion and the exact acknowledgement statement; or describe the blocking checker result. Plain `approved` is not accepted.</resume-signal>
</task>
<task type="auto">
  <name>Task 3: Implement, test, and execute the structured UI approval gate</name>
  <read_first>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md; .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md; the checker-produced machine-readable report path and checkpoint acknowledgement returned from Task 2; .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, and .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs</read_first>
  <files>.planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.schema.json, .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs, .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs</files>
  <action>Create a closed JSON Schema and dependency-free Node validator for an approval record rooted at `schemaVersion`, `subject`, `checkerEvidence`, and `humanAcknowledgement`, with `additionalProperties: false` at every object level. `subject` must be exactly `{ canonicalization: "ui-spec-review-payload-v1", path: ".planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md", digest: { algorithm: "sha256", value: &lt;64 lowercase hex characters&gt; } }`. The checker report must carry an identical `subject` object. On promotion and every `--check`, rederive the canonical payload from the live `06-UI-SPEC.md` using Task 1's algorithm, require byte equality with the saved review-input artifact, recompute the saved subject SHA-256, and require exact path/digest agreement in both report and approval record; never rewrite the saved subject after review. `checkerEvidence` must contain nonempty `checkerIdentity.name`, `checkerIdentity.provider`, and independent `checkerIdentity.runId`; canonical UTC `completedAtUtc` in exact `YYYY-MM-DDTHH:mm:ss.sssZ` form whose parse-and-reemit value is identical; a repository-relative `report.path`; `report.digest` exactly `{ algorithm: "sha256", value: &lt;64 lowercase hex characters&gt; }`; exactly six unique `dimensions` objects with IDs `copywriting`, `visuals`, `color`, `typography`, `spacing`, and `registry-safety` and verdict exactly the enum `PASS`; and `findingsDisposition` exactly `{ status: "none-raised" | "all-resolved", detail: &lt;nonempty string&gt; }`. Derive that disposition from the checker report: an empty findings array requires `none-raised`, while a nonempty array requires every finding to have an ID and disposition exactly `resolved`, yielding `all-resolved`; any open/unknown finding fails. `humanAcknowledgement` must contain nonempty `acknowledgedBy` distinct from the checker identity, canonical UTC `acknowledgedAtUtc`, `reportSha256` equal to the report digest, `subjectSha256` equal to the subject digest, and statement exactly `I reviewed the independently produced checker report bound by this SHA-256 and approve this exact UI-SPEC.` Parse both canonical timestamps to UTC instants and require `acknowledgedAtUtc &gt; completedAtUtc`; equality or acknowledgement before completion fails. The independently produced report must remain outside this plan's files and expose matching subject path/digest, checker identity/run ID, completion timestamp, dimension verdicts, and findings data. The validator must resolve both subject and report paths inside the repository, reject the approval/UI-SPEC/schema/validator files as report evidence, recompute SHA-256 over exact subject and report bytes, fail closed on unreadable or mismatched evidence, and compare structured record fields to the parsed checker report rather than trusting a Plan 06-22 transcription.

Write Node built-in test-runner cases before promotion. Include one valid fixture whose acknowledgement is strictly later than checker completion. Add negative cases for exact `FAIL`, `BLOCK`, `FLAG`, an unknown verdict, prose containing PASS such as `FAIL — not PASS`, duplicate dimensions, each missing dimension, unknown/extra dimensions, absent checker identity/run ID, absent report path/digest, invalid/non-UTC checker or acknowledgement timestamps, acknowledgement before checker completion, acknowledgement exactly equal to checker completion, missing/open findings disposition, missing/mismatched human acknowledgement, report-hash mismatch, report/record semantic mismatch, substantive UI-SPEC body mutation after review, stale checker-report reuse against a newly derived review input, subject path mismatch, subject digest mismatch in report or approval, saved-review-input byte mutation, and metadata-only approval agreement over changed substantive content. Retain cases for approved status with incomplete checkboxes, checked boxes with pending status, wrong/duplicate sign-off labels, and an approval-line or `reviewed_at` disagreement. Prove that status/reviewed/sign-off-only mutation does not alter the canonical subject, while any other UI-SPEC mutation does. Every invalid case must exit nonzero.

Only after the test suite passes, copy the Task 2 evidence into `06-22-UI-CONTRACT-APPROVAL.json` without inventing subject, identity, verdicts, findings, digest, chronology, or acknowledgement. Use one validator-owned promotion operation: build temporary approval/UI-SPEC candidates, rederive and byte-compare the canonical subject, validate their complete agreement with the frozen subject and checker report, then replace the destinations with rollback-on-write-failure semantics. The promoted UI-SPEC must have frontmatter `status: approved`, `reviewed_at` exactly equal to the acknowledgement UTC timestamp, exactly the six existing canonical checked sign-off lines and no unchecked/duplicate canonical line, and approval text rendered exactly as `**Approval:** approved by {acknowledgedBy} at {acknowledgedAtUtc}; subject={subject.path}; subjectSha256={subject.digest.value}; report={report.path}; reportSha256={report.digest.value}` from the structured fields. The normal validation command is read-only and must rederive the current substantive UI-SPEC subject, byte-compare the frozen review input, recompute both SHA-256 digests, enforce strict acknowledgement chronology, and revalidate the persisted record, live checker report semantics, and UI-SPEC metadata agreement. On any failure, leave or restore `pending-review`, do not create an approval record that claims success, fail the task, and keep 06-17/06-19/06-20 blocked.</action>
  <verify><automated>rtk cmd /d /c "rtk node --test .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.test.mjs &amp;&amp; rtk node .planning/phases/06-transactional-plans-and-recovery/06-22-validate-ui-contract-approval.mjs --check .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-APPROVAL.json .planning/phases/06-transactional-plans-and-recovery/06-UI-SPEC.md .planning/phases/06-transactional-plans-and-recovery/06-22-UI-CONTRACT-REVIEW-INPUT.md"</automated></verify>
  <acceptance_criteria>- Schema, record, validator, and tests encode exactly six unique canonical dimensions with verdict exactly PASS; contradictory prose and every non-PASS/unknown verdict fail.
- The validator proves the independently produced report exists, its SHA-256 matches, its structured checker identity/results/findings match the approval record, and the human acknowledgement is distinct and complete.
- Report and approval record carry the exact canonical subject path/SHA-256; live UI-SPEC rederivation, saved subject bytes, and both bindings agree on promotion and every read-only check.
- Canonical UTC parsing enforces acknowledgement strictly after checker completion; before/equal/non-UTC/invalid chronology fails.
- UI-SPEC status, reviewed_at, six exact sign-off checkboxes, and approval line agree with the validated record after the single promotion operation.
- Digest/read/parse/schema/semantic/UI disagreement fails closed and dependent UI plans remain blocked.</acceptance_criteria>
  <done>The UI contract has independently evidenced, schema-valid six-dimension authority or remains explicitly blocked.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live UI-SPEC -> frozen canonical review subject | Mutable approval metadata must be excluded without allowing substantive contract drift or stale-report reuse. |
| independent checker report -> Plan 06-22 approval record | A same-task transcription, retargeted subject, or acknowledgement at/before checker completion must not substitute for independently produced evidence. |
| validated approval record -> consequential UI implementation | Unreviewed, contradictory, or internally inconsistent sign-off must not authorize risk/recovery UI. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-22A | Spoofing/Repudiation/Tampering | UI checker sign-off | mitigate | Frozen canonical substantive subject, exact subject/report path and SHA-256 binding, live UI-SPEC rederivation on every check, closed schema, independent structured report, strict post-checker acknowledgement chronology, adversarial tests, and exact UI-SPEC agreement gate. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-22A | HIGH | V4 Access Control — consequential controls remain deny-by-default; V5 Input Validation — canonical subject and time ordering are validated; V7 Logging — review authority is attributable | blocking-human | `06-22-validate-ui-contract-approval.test.mjs` body-mutation/stale-report/path/digest/chronology negative suite plus read-only validator proof of canonical subject, checker report SHA-256/semantics, strictly later human acknowledgement, and exact UI-SPEC agreement | Any substantive drift, stale/retargeted subject, unreadable/hash-mismatched report, missing/non-PASS/fabricated/duplicated dimension, acknowledgement at/before completion, incomplete acknowledgement, or UI-SPEC disagreement blocks 06-17, 06-19, 06-20, and physical promotion. |
</threat_model>
<verification>The Node suite proves canonical-payload stability plus fail-closed body mutation, stale-report reuse, subject path/digest mismatch, before/equal/invalid chronology, non-PASS/prose/duplicate/missing evidence, acknowledgement, and UI disagreement cases; the read-only validator then proves the live substantive UI-SPEC, frozen subject bytes, persisted record, independent report bytes/semantics, and approved metadata are one coherent authority.</verification>
<success_criteria>No Phase 6 UI plan consumes a pending, stale-subject, retargeted, fabricated, contradictory, chronology-invalid, digest-mismatched, or internally inconsistent design authority.</success_criteria>
<output>Create `06-22-SUMMARY.md` only after the canonical subject is frozen and verified, the independent report binds its exact path/SHA-256, all six checker dimensions pass, the strictly later human acknowledgement is persisted, and the read-only validator accepts the promoted UI-SPEC.</output>
