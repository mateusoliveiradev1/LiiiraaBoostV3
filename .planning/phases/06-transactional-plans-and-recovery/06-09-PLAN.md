---
phase: 06-transactional-plans-and-recovery
plan: '09'
type: tdd
wave: 2
depends_on: ['06-01', '06-02']
files_modified:
  - apps/desktop/src-tauri/src/recovery_store/mod.rs
  - apps/desktop/src-tauri/src/recovery_store/migrations.rs
  - apps/desktop/src-tauri/tests/recovery_store.rs
autonomous: true
requirements: [PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'Exact prior and requested state is durably committed before every external effect.'
    - 'Journal, checkpoints, approvals, transactions, receipts, and promotions are append-only, hash-verified, and recoverable after restart or storage failure.'
  artifacts:
    - path: apps/desktop/src-tauri/src/recovery_store/migrations.rs
      provides: STRICT append-oriented SQLite schema
    - path: apps/desktop/src-tauri/src/recovery_store/mod.rs
      provides: FULL-durable validated repository
    - path: apps/desktop/src-tauri/tests/recovery_store.rs
      provides: Migration, tamper, BUSY, FULL, IOERR, reopen, and projection-rebuild evidence
  key_links:
    - from: apps/desktop/src-tauri/src/recovery_store/mod.rs
      to: broker dispatch
      via: prepared commit prerequisite
      pattern: 'append_prepared'
---
<objective>Implement the append-only recovery authority through fault-driven TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@apps/desktop/src-tauri/src/evidence_store.rs @apps/desktop/src-tauri/src/evidence_store/migrations.rs @packages/contracts-source/src/transactional-plans.tsp @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify durable append-only recovery storage</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs and migrations.rs (confirm absent); apps/desktop/src-tauri/tests/recovery_store.rs (confirm absent); apps/desktop/src-tauri/src/evidence_store.rs; 06-CONTEXT.md D-18–D-20, D-26, D-29, D-30, D-32</read_first><files>apps/desktop/src-tauri/tests/recovery_store.rs</files><action>Write failing migration/repository tests for STRICT tables `plan_revisions`, `plan_operations`, `approval_events`, `recovery_checkpoints`, `transactions`, `journal_events`, `receipts`, `operation_promotions`, and rebuildable `executor_projection`; enforce WAL, synchronous=FULL, foreign keys, bounded busy timeout, increasing sequence, canonical previous/event hashes, update/delete rejection, prepare-before-dispatch witness, reopen/rebuild, upgrade, tamper, BUSY, FULL, IOERR, and disk-full-after-effect recovery.</action><verify><automated>rtk cargo test -p liiiraa-desktop --test recovery_store</automated></verify><acceptance_criteria>- RED failures are storage assertions, not missing test infrastructure.
- Tests assert no OS-dispatch spy call when prepare commit fails.
- Tamper leaves recovery readable but blocks new mutation.</acceptance_criteria><done>RED durability contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Persist FULL-durable exact intent and evidence</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs; apps/desktop/src-tauri/src/recovery_store/migrations.rs; apps/desktop/src-tauri/tests/recovery_store.rs; apps/desktop/src-tauri/src/evidence_store.rs</read_first><files>apps/desktop/src-tauri/src/recovery_store/mod.rs, apps/desktop/src-tauri/src/recovery_store/migrations.rs</files><action>Implement a dedicated recovery database connection with verified `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=FULL`, migrations, write probe, quick_check, generated-contract validation, canonical JSON, SHA-256 previous-hash chain, append-only repository methods/triggers, explicit Busy/Full/Io/Chain/Contract/Transition errors, and rebuildable projections. Never hold a SQLite transaction across IPC/Win32. Secrets, typed phrases, raw hardware identifiers, and reusable proof material never enter rows.</action><verify><automated>rtk cargo test -p liiiraa-desktop --test recovery_store</automated></verify><acceptance_criteria>- All durability/fault/migration tests pass.
- PRAGMA reads confirm FULL/WAL/foreign_keys on every opened connection.
- Prepared event is committed before the dispatch spy can run.</acceptance_criteria><done>SQLite is the exact durable authority for recovery and receipts.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove restart and corruption behavior</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs; migrations.rs; tests/recovery_store.rs</read_first><files>apps/desktop/src-tauri/src/recovery_store/mod.rs, apps/desktop/src-tauri/tests/recovery_store.rs</files><action>Extract canonical append/read helpers, add power-loss boundary model cases, projection rebuild equivalence, migration from every schema version, and deterministic integrity diagnostics. Commit refactor only with green suite.</action><verify><automated>rtk cargo test -p liiiraa-desktop recovery_store</automated></verify><acceptance_criteria>- Reopen returns byte/hash-identical history.
- Corrupt chain blocks mutation without deleting recovery evidence.
- Update/delete attempts against authoritative history fail.</acceptance_criteria><done>Recovery persistence survives interruption and exposes exact failure evidence.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite commit -> OS effect | Losing prepared intent would make an applied effect unauditable. |
| database file -> startup | Local file can be corrupt or tampered. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-09A | Tampering/Repudiation | journal | mitigate | FULL-durable append, canonical hash chain, insert-only triggers, verify-on-open. |
| T-06-09B | Denial | disk/lock failures | mitigate | Closed BUSY/FULL/IOERR states block effects and preserve guided recovery evidence. |
</threat_model>
<verification>Recovery store suite passes all required failpoints without Docker.</verification>
<success_criteria>Every external effect has recoverable, immutable prior intent and outcome evidence.</success_criteria>
<output>Create `06-09-SUMMARY.md`.</output>
