---
phase: 06-transactional-plans-and-recovery
plan: '09'
type: tdd
wave: 2
depends_on: ['06-01', '06-02']
files_modified:
  - apps/desktop/src-tauri/src/recovery_store/mod.rs
  - apps/desktop/src-tauri/src/recovery_store/migrations.rs
  - apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs
  - apps/desktop/src-tauri/tests/recovery_store.rs
autonomous: true
requirements: [PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'Exact prior and requested state is durably committed before every external effect.'
    - 'Journal, checkpoints, approvals, transactions, receipts, and promotions are append-only, HMAC-verified against a Windows-protected external head anchor, and recoverable after restart or storage failure.'
  artifacts:
    - path: apps/desktop/src-tauri/src/recovery_store/migrations.rs
      provides: STRICT append-oriented SQLite schema
    - path: apps/desktop/src-tauri/src/recovery_store/mod.rs
      provides: FULL-durable validated repository
    - path: apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs
      provides: HMAC key epochs and Windows Credential Manager external-head custody
    - path: apps/desktop/src-tauri/tests/recovery_store.rs
      provides: Migration, tamper, BUSY, FULL, IOERR, reopen, and projection-rebuild evidence
  key_links:
    - from: apps/desktop/src-tauri/src/recovery_store/mod.rs
      to: broker dispatch
      via: prepared commit prerequisite
      pattern: 'append_prepared'
    - from: apps/desktop/src-tauri/src/recovery_store/mod.rs
      to: apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs
      via: verify complete HMAC chain against protected head before mutation
      pattern: 'verify.*anchor|AnchorMismatch'
---
<objective>Implement the append-only recovery authority through fault-driven TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@apps/desktop/src-tauri/src/evidence_store.rs @apps/desktop/src-tauri/src/evidence_store/migrations.rs @packages/contracts-source/src/transactional-plans.tsp @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify durable append-only recovery storage</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs and migrations.rs (confirm absent); apps/desktop/src-tauri/tests/recovery_store.rs (confirm absent); apps/desktop/src-tauri/src/evidence_store.rs; apps/desktop/src-tauri/src/credential_store.rs; 06-CONTEXT.md D-18–D-20, D-26, D-29, D-30, D-32</read_first><files>apps/desktop/src-tauri/tests/recovery_store.rs</files><action>Write failing migration/repository tests for STRICT tables `plan_revisions`, `plan_operations`, `approval_events`, `recovery_checkpoints`, `transactions`, `journal_events`, `receipts`, `operation_promotions`, and rebuildable `executor_projection`; enforce WAL, synchronous=FULL, foreign keys, bounded busy timeout, increasing sequence, canonical previous/event hashes, update/delete rejection, prepare-before-dispatch witness, reopen/rebuild, upgrade, tamper, BUSY, FULL, IOERR, and disk-full-after-effect recovery. Add adversarial tests that rewrite the entire SQLite history and recompute every unkeyed SHA-256 link: verification must still fail because the attacker cannot reproduce the HMAC-SHA-256 chain or Windows Credential Manager head anchor. Cover missing/unavailable key or anchor, anchor rollback/ahead/lag, deliberate key rotation, interrupted rotation, and old-epoch verification. In key/anchor-unavailable state, exact history and guided recovery/export remain readable, but every new apply/restore mutation is denied.</action><verify><automated>rtk cargo test -p liiiraa-desktop --test recovery_store</automated></verify><acceptance_criteria>- RED failures are storage assertions, not missing test infrastructure.
- Tests assert no OS-dispatch spy call when prepare commit fails.
- Tamper leaves recovery readable but blocks new mutation.</acceptance_criteria><done>RED durability contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Persist FULL-durable exact intent and evidence</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs; apps/desktop/src-tauri/src/recovery_store/migrations.rs; apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs (confirm absent); apps/desktop/src-tauri/tests/recovery_store.rs; apps/desktop/src-tauri/src/evidence_store.rs; apps/desktop/src-tauri/src/credential_store.rs</read_first><files>apps/desktop/src-tauri/src/recovery_store/mod.rs, apps/desktop/src-tauri/src/recovery_store/migrations.rs, apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs</files><action>Implement a dedicated recovery database connection with verified `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=FULL`, migrations, write probe, quick_check, generated-contract validation, canonical JSON, and append-only repository methods/triggers. Define an injectable integrity-anchor port plus a Windows Credential Manager/keyring adapter in `integrity_anchor.rs`. Generate a random 256-bit journal key per install/epoch and store it only under a Phase 6-specific service/account name. Compute domain-separated HMAC-SHA-256 over database identity, epoch, sequence, event kind, canonical event bytes, and previous MAC; store only key ID/epoch and MAC in SQLite. Store the authoritative `{database_id, epoch, sequence, head_mac}` anchor outside SQLite in Windows Credential Manager. For each append, commit the HMAC event first, then compare-and-swap the external anchor from that event's exact previous head to its new head before any later mutation/effect is admitted. On reopen, permit only one recoverable lag case: the protected anchor equals the single newest event's `previous_mac` and the complete keyed chain validates; advance that anchor before mutation. Anchor-ahead, multi-event lag, database-ID/epoch mismatch, rollback, or failed compare-and-swap is corruption and blocks mutation. Rotation appends an old-key-authenticated rotation event, installs a new key/epoch, advances the external anchor from the expected old head, retains old keys only for read verification, and rejects interrupted mismatches. If key/anchor custody is unavailable or inconsistent, expose a closed `IntegrityAnchorUnavailable`/`AnchorMismatch` state: permit read-only history, diagnostic export, and guided manual recovery, but fail every new mutation closed and never silently create a replacement anchor over existing history. Preserve unkeyed SHA-256 only as a content identifier, never as tamper authority. Never hold a SQLite transaction across IPC/Win32. Secrets, typed phrases, raw hardware identifiers, and reusable proof material never enter rows.</action><verify><automated>rtk cargo test -p liiiraa-desktop --test recovery_store</automated></verify><acceptance_criteria>- All durability/fault/migration tests pass.
- PRAGMA reads confirm FULL/WAL/foreign_keys on every opened connection.
- Prepared event is committed before the dispatch spy can run.
- A whole-history SQLite rewrite with recomputed SHA-256 links fails HMAC/external-anchor verification.
- Missing key/anchor custody preserves readable history/export but denies every new mutation.</acceptance_criteria><done>SQLite plus the Windows-protected HMAC head anchor are the exact durable authority for recovery and receipts.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove restart and corruption behavior</name><read_first>apps/desktop/src-tauri/src/recovery_store/mod.rs; migrations.rs; integrity_anchor.rs; tests/recovery_store.rs</read_first><files>apps/desktop/src-tauri/src/recovery_store/mod.rs, apps/desktop/src-tauri/src/recovery_store/integrity_anchor.rs, apps/desktop/src-tauri/tests/recovery_store.rs</files><action>Extract canonical append/read helpers, add power-loss boundary model cases, projection rebuild equivalence, migration from every schema version, whole-history recomputation attacks, external-anchor rollback/ahead/lag, key rotation/interruption, missing-key read-only recovery, and deterministic integrity diagnostics. Commit refactor only with green suite.</action><verify><automated>rtk cargo test -p liiiraa-desktop recovery_store</automated></verify><acceptance_criteria>- Reopen returns byte/hash-identical history.
- Corrupt chain blocks mutation without deleting recovery evidence.
- Update/delete attempts against authoritative history fail.</acceptance_criteria><done>Recovery persistence survives interruption and exposes exact failure evidence.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite commit -> OS effect | Losing prepared intent would make an applied effect unauditable. |
| database file -> startup | Local file can be corrupt or tampered. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-09A | Tampering/Repudiation | journal | mitigate | FULL-durable append, domain-separated HMAC chain, Windows Credential Manager key + external head anchor, insert-only triggers, rotation protocol, and verify-on-open; unkeyed SHA-256 is content identity only. |
| T-06-09B | Denial | disk/lock failures | mitigate | Closed BUSY/FULL/IOERR states block effects and preserve guided recovery evidence. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-09A | HIGH | V6 Cryptography — keyed integrity with protected key custody; V7 Logging — tamper-evident audit records | blocking | Recovery-store whole-history rewrite/recomputed-SHA, anchor rollback/lag, key-loss, and rotation tests | Any HMAC/key/anchor verification failure keeps history readable but blocks all new mutation and blocks 06-14, 06-18, 06-21, and 06-26. |
| T-06-09B | HIGH | V7 Logging — preserve security events through storage failure; V4 Access Control — deny effects when authority is unavailable | blocking | `BUSY`/`FULL`/`IOERR`/disk-full failpoint suite | Any prepare durability failure blocks dispatch; unresolved storage failure blocks downstream mutation and promotion. |
</threat_model>
<verification>Recovery store suite passes all required failpoints without Docker.</verification>
<success_criteria>Every external effect has recoverable prior intent and outcome evidence whose whole-history rewrite cannot pass without the Windows-protected key/anchor; anchor loss preserves read-only recovery evidence and blocks all new mutation.</success_criteria>
<output>Create `06-09-SUMMARY.md`.</output>
