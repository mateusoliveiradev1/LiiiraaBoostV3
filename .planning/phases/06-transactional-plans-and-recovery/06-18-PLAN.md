---
phase: 06-transactional-plans-and-recovery
plan: '18'
type: execute
wave: 5
depends_on: ['06-09', '06-11', '06-12', '06-13', '06-14', '06-15', '06-16']
files_modified:
  - apps/desktop/src-tauri/src/plan_commands.rs
  - apps/desktop/src-tauri/src/plan_executor.rs
  - apps/desktop/src-tauri/tests/recovery_executor.rs
  - apps/desktop/src-tauri/tests/broker_protocol.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/capabilities/main.json
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'The unprivileged native host recomputes admission, serializes mutation, owns recovery/progress, and invokes only the authenticated broker.'
    - 'Startup/reopen reconciles unresolved journal state before new mutation; closing hides to tray without stopping the executor.'
  artifacts:
    - path: apps/desktop/src-tauri/src/plan_executor.rs
      provides: Recovery store + pure engine + auth + broker orchestration
    - path: apps/desktop/src-tauri/src/plan_commands.rs
      provides: Narrow validated Tauri intent commands and ordered channel
    - path: apps/desktop/src-tauri/tests/recovery_executor.rs
      provides: Crash/reboot/partial rollback/receipt/reconnect integration evidence
  key_links:
    - from: apps/desktop/src-tauri/src/plan_executor.rs
      to: apps/desktop/src-tauri/src/recovery_store/mod.rs
      via: prepared commit and authoritative snapshot
      pattern: 'RecoveryStore'
    - from: apps/desktop/src-tauri/src/main.rs
      to: startup reconciliation
      via: managed executor initialization before commands
      pattern: 'PlanExecutor'
---
<objective>Wire the pure transaction authority, durable store, proof verifier, and broker into the unprivileged Tauri host.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@apps/desktop/src-tauri/src/main.rs @apps/desktop/src-tauri/src/evidence_commands.rs @.planning/phases/06-transactional-plans-and-recovery/06-PATTERNS.md @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Integrate native plan executor and broker protocol</name><read_first>apps/desktop/src-tauri/src/plan_executor.rs and plan_commands.rs (confirm absent); tests/recovery_executor.rs and broker_protocol.rs (confirm absent); crates/plan-engine/src/executor.rs; apps/desktop/src-tauri/src/recovery_store/mod.rs; apps/desktop/src-tauri/src/plan_auth.rs; apps/optimizer-service/src/ipc.rs</read_first><files>apps/desktop/src-tauri/src/plan_executor.rs, apps/desktop/src-tauri/src/plan_commands.rs, apps/desktop/src-tauri/tests/recovery_executor.rs, apps/desktop/src-tauri/tests/broker_protocol.rs</files><behavior>- Compose/revise/approve/apply/restore commands revalidate generated input and recompute trusted state.
- One mutation is in flight; progress sequences are monotonic; a snapshot is always authoritative.
- Crash/reboot/timeout/drift/partial rollback/receipt failure produce exact recovery states without blind retry.
- Diagnostic export previews redacted local content before an app-owned file receipt is created.</behavior><action>Test first, then adapt `RecoveryStore`, plan-engine reducer, native proof verifier, and authenticated broker client behind `PlanExecutor`. Implement the closed PLAN_COMMANDS set, `tauri::ipc::Channel` events, authoritative `read_execution`, exact individual/plan/checkpoint restore transactions, restart checkpoint, redacted local diagnostic preview/export, mutation serialization, and recovery-first admission. Validate renderer and broker documents before mapping. Do not let Tauri perform privileged effects or return fixture success.</action><verify><automated>rtk cargo test -p liiiraa-desktop --test recovery_executor && rtk cargo test -p liiiraa-desktop --test broker_protocol</automated></verify><acceptance_criteria>- Integration tests cover the full Validation failpoint matrix and three restore target types.
- Every mutation call is preceded by durable prepared evidence and followed by observation.
- Renderer disconnect/reconnect performs zero duplicate mutations.
- Export writes only after redacted preview/consent and returns a bounded receipt.</acceptance_criteria><done>The unprivileged native authority executes and recovers through the narrow broker.</done></task>
<task type="auto"><name>Task 2: Register startup, tray continuity, and least-privilege capabilities</name><read_first>apps/desktop/src-tauri/src/main.rs; apps/desktop/src-tauri/Cargo.toml; apps/desktop/src-tauri/capabilities/main.json; apps/desktop/src-tauri/src/plan_commands.rs; apps/desktop/src-tauri/src/plan_executor.rs; packages/contracts-source/src/shell.tsp</read_first><files>apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/capabilities/main.json</files><action>Add exact workspace dependencies and modules; open/verify recovery storage and reconcile incomplete transactions/checkpoints before admitting new mutation; manage the native executor independently of the window; register only named plan commands; scope them to the trusted main webview; retain existing close-to-tray behavior during apply/recovery; on Windows shutdown stop new work and rely on next-boot reconciliation. Recovery commands remain registered offline, signed out, and without Premium.</action><verify><automated>rtk cargo test -p liiiraa-desktop && rtk pnpm verify:foundation</automated></verify><acceptance_criteria>- Startup with prepared/unknown/conflict state opens recovery and rejects new apply.
- Closing the window does not drop executor/store state.
- Capability file lists exact named plan commands and no shell/execute/generic native permission.
- Existing desktop and foundation suites pass.</acceptance_criteria><done>Lifecycle and capability wiring preserve recovery and least privilege.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| renderer -> Tauri -> broker | Untrusted intent crosses two validation/privilege boundaries. |
| journal -> startup/tray lifecycle | Missed UI events must not change authoritative execution truth. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-18A | Elevation | Tauri commands/capability | mitigate | Trusted-webview capability plus named validated intent commands; broker remains only effect authority. |
| T-06-18B | Repudiation/Denial | lifecycle/reconnect | mitigate | Native managed executor, durable snapshot, sequence gap refetch, startup recovery priority. |
</threat_model>
<verification>Desktop Rust suite, foundation gate, fault matrix, and capability assertions pass.</verification>
<success_criteria>The real execution authority survives UI/lifecycle interruption without widening privilege.</success_criteria>
<output>Create `06-18-SUMMARY.md`.</output>
