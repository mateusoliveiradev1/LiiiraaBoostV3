---
phase: 06-transactional-plans-and-recovery
plan: '10'
type: tdd
wave: 3
depends_on: ['06-03']
files_modified: [crates/plan-engine/src/promotion.rs, crates/plan-engine/tests/promotion.rs]
autonomous: true
requirements: [PLAN-01, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Each exact operation version progresses simulation -> clean VM -> owner PC -> friends only after a complete recovery cycle passes.'
    - 'Failure blocks later stages; correction creates a new version at simulation; signed revocation blocks apply but never remote rollback or local recovery.'
    - 'Friends evidence is a local redacted previewable package and never auto-uploads.'
    - 'D-01: Every exact operation version must progress in order through deterministic simulation, clean Windows VM, owner PC, and friends PCs, with no direct promotion to a later stage.'
    - 'D-02: Every promotion stage must prove prepare recovery, apply, verify, restart when required, restore, and verify again as one complete cycle.'
    - 'D-06: A failure permanently blocks that operation version at all later stages; a correction mints a new version and restarts at simulation with no manual override.'
    - 'D-07: Friends results remain in a local, redacted, verifiable diagnostic package that is previewed before explicit export or send and is never raw or automatically transmitted.'
    - 'D-08: A valid signed revocation blocks new applies and alerts affected users while local recovery remains available; it cannot trigger remote rollback or arbitrary remote execution.'
    - 'D-35: Promotion retains explicit physical hardware coverage gaps and never converts owner/friends evidence into a universal or 100% support claim.'
  artifacts:
    - path: crates/plan-engine/src/promotion.rs
      provides: Exact-version promotion, revocation, and diagnostic projection policy
    - path: crates/plan-engine/tests/promotion.rs
      provides: Stage, failure, revocation, privacy, and no-override properties
  key_links:
    - from: crates/plan-engine/src/promotion.rs
      to: operation version
      via: immutable stage evidence key
      pattern: 'operation_version'
---
<objective>Implement exact-version validation promotion and safe revocation through TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@crates/plan-engine/src/domain.rs @.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify sequential promotion and revocation</name><read_first>crates/plan-engine/src/promotion.rs; crates/plan-engine/tests/promotion.rs (confirm absent); crates/plan-engine/src/domain.rs; 06-CONTEXT.md D-01, D-02, D-06–D-08, D-35</read_first><files>crates/plan-engine/tests/promotion.rs</files><action>Write failing table/property tests for exact-version sequential stages, complete prepare/apply/verify/restart-if-required/restore/verify evidence at each stage, failure blocking later stages, correction restart at simulation, no manual override, honest incomplete hardware coverage, signed revocation validation/effects, recovery continuity, and local redacted diagnostic preview/export consent with no raw or automatic send path.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test promotion</automated></verify><acceptance_criteria>- Stage skip, cross-version evidence reuse, override, unsigned revocation, remote rollback, raw export, and auto-upload cases fail.
- RED failures are policy assertions.</acceptance_criteria><done>RED promotion/revocation contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Enforce exact-version evidence progression</name><read_first>crates/plan-engine/src/promotion.rs; crates/plan-engine/tests/promotion.rs; packages/contracts-source/src/transactional-plans.tsp</read_first><files>crates/plan-engine/src/promotion.rs</files><action>Implement monotonic exact-version stage admission, complete-cycle evidence checks, permanent block for failed version stages, new-version reset, signed revocation verifier port and effects limited to block-new-apply + alert + local recovery, and redacted diagnostic bundle projection requiring preview and explicit export/send consent. Keep untested cells explicit; never derive 100% support.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test promotion</automated></verify><acceptance_criteria>- All stage and revocation cases pass.
- Recovery remains admitted for revoked/offline/non-Premium state.
- Diagnostic projection contains no raw identifiers/secrets and has no automatic transport action.</acceptance_criteria><done>Alpha evidence progression is enforceable and privacy-preserving.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove no bypass or universal claim</name><read_first>crates/plan-engine/src/promotion.rs; crates/plan-engine/tests/promotion.rs</read_first><files>crates/plan-engine/src/promotion.rs, crates/plan-engine/tests/promotion.rs</files><action>Add arbitrary stage-history and redaction properties, consolidate stable blockers, and keep version/evidence identity explicit. Commit refactor if changed.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine promotion</automated></verify><acceptance_criteria>- No generated history advances after failure or across missing stage.
- Coverage gaps remain enumerated in every incomplete promotion projection.</acceptance_criteria><done>Promotion cannot be manually or semantically bypassed.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| test/export/revocation evidence -> promotion/apply | Synthetic, edited, raw, or unsigned evidence could widen distribution authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-10A | Spoofing/Tampering | promotion evidence | mitigate | Exact version/stage/build/evidence identity and no override transition. |
| T-06-10B | Elevation | revocation | mitigate | Signature validation; effects exclude remote execution/rollback and preserve local recovery. |
| T-06-10C | Information Disclosure | friend diagnostics | mitigate | Local redaction, preview, explicit consent, no automatic upload. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-10A | HIGH | V5 Input Validation — validate exact evidence identity; V4 Access Control — no stage override | blocking | Promotion stage/version property suite | Any stage skip, cross-version reuse, or manual override blocks 06-21 and 06-26–06-28. |
| T-06-10B | HIGH | V6 Cryptography — verify signed security directives; V4 Access Control — constrain revocation effects | blocking | Signed/unsigned revocation and no-remote-effect tests | Invalid signature or widened remote authority blocks promotion and apply. |
| T-06-10C | HIGH | V8 Data Protection — explicit consent, minimization, and no automatic disclosure | blocking | Redaction/raw-field/auto-upload rejection properties | Any privacy failure blocks friends-stage promotion and Phase 6 completion. |
</threat_model>
<verification>Promotion property suite and TDD gates pass.</verification>
<success_criteria>Only proven exact operation versions advance, and unsafe versions stop without remote authority.</success_criteria>
<output>Create `06-10-SUMMARY.md`.</output>
