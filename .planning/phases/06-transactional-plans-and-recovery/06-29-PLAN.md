---
phase: 06-transactional-plans-and-recovery
plan: '29'
type: tdd
wave: 10
depends_on: ['06-21']
files_modified:
  - apps/optimizer-service/src/ipc.rs
  - apps/optimizer-service/src/operations/mod.rs
  - apps/optimizer-service/src/dispatcher.rs
  - apps/optimizer-service/tests/physical_dispatcher.rs
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'An authenticated generated broker request reaches exactly one allowlisted PowrProf or restore-point operation with all validated request fields preserved.'
    - 'Dispatch requires an opaque interactive-user effect lease; session and SID strings alone can never authorize a user-scoped Windows effect.'
    - 'No generic command, script, PowerShell, registry, file, service, remote-host, or Extreme authority can be constructed or dispatched.'
    - 'A broker return remains diagnostic; exact post-effect observation is still required before durable success.'
  artifacts:
    - path: apps/optimizer-service/src/dispatcher.rs
      provides: Closed physical dispatcher over generated broker requests and verified client context
      exports: [PhysicalOperationDispatcher, DispatchAudit]
    - path: apps/optimizer-service/tests/physical_dispatcher.rs
      provides: RED/GREEN adversarial and exact-field dispatcher proof
  key_links:
    - from: apps/optimizer-service/src/ipc.rs
      to: apps/optimizer-service/src/dispatcher.rs
      via: generated PrivilegedBrokerRequest plus authenticated client context
      pattern: 'PrivilegedBrokerRequest.*PhysicalOperationDispatcher'
    - from: apps/optimizer-service/src/dispatcher.rs
      to: apps/optimizer-service/src/operations/power_scheme.rs
      via: exact GUID/state-hash mapping plus an unforgeable InteractiveUserEffectLease
      pattern: 'InteractiveUserEffectLease.*PowerSchemePort'
---
<objective>Connect the existing allowlisted Windows operations to a request-preserving authenticated dispatcher.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md
@.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md
@.planning/phases/06-transactional-plans-and-recovery/06-15-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-13-SUMMARY.md
@apps/optimizer-service/src/ipc.rs
@apps/optimizer-service/src/operations/mod.rs
@apps/optimizer-service/src/operations/power_scheme.rs
@apps/optimizer-service/src/restore_point.rs
<interfaces>
`Broker::submit(ticket, envelope, dispatcher, fault)` currently validates a `PrivilegedBrokerRequest` but collapses it to a payload-free `AllowedOperation`; replace that lossy seam without widening the request vocabulary. `OperationDispatcher::dispatch` must receive the generated request variant and a `DispatchContext` containing an opaque `InteractiveUserEffectLease`; public session/SID/PID/hash fields are audit metadata and are insufficient to construct that lease. `PowerSchemePort` remains the only power mutation port; `apply_managed_scheme` and `restore_managed_scheme` retain exact prior/target/drift semantics.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the closed physical dispatcher</name>
  <read_first>apps/optimizer-service/src/ipc.rs; apps/optimizer-service/src/operations/mod.rs; apps/optimizer-service/src/operations/power_scheme.rs; apps/optimizer-service/src/restore_point.rs; apps/optimizer-service/tests/ipc_protocol.rs; crates/contracts-rust/src/generated.rs generated request/response variants; 06-CONTEXT.md D-03, D-05, D-08, D-19, D-23, D-31</read_first>
  <files>apps/optimizer-service/src/ipc.rs, apps/optimizer-service/src/operations/mod.rs, apps/optimizer-service/src/dispatcher.rs, apps/optimizer-service/tests/physical_dispatcher.rs</files>
  <behavior>
    - Valid generated observe/duplicate/activate/delete/restore-point requests preserve exact request ID, transaction/step/operation version, expected/current/destination GUIDs, canonical state hashes, counter, nonce, device binding, and verified interactive session/SID through dispatch.
    - The physical dispatcher rejects a context built only from session/SID/PID/hash metadata; tests can supply a fake scoped effect lease, while the Windows lease is constructible only by the authenticated pipe host.
    - Each admitted request calls exactly one matching allowlisted port method; replay, malformed, mismatched identity, wrong operation version, generic/Extreme-shaped input, and unexpected generated variants call zero methods.
    - Duplicate/activate/delete require exact precondition and ownership evidence; third-state drift returns a closed conflict and never deletes or overwrites.
    - Dispatch failure, timeout, or response loss never becomes verified success and never authorizes a second dispatch; returned documents validate through the generated contract.
  </behavior>
  <action>RED: add `physical_dispatcher.rs` with fake PowrProf/restore-point ports and tests for every generated variant, exact field preservation, zero-call negative cases, drift/conflict, missing/metadata-only effect lease, and observation-required outcomes; run the focused test and commit the intentional behavior failure as `test(06-29): add failing physical dispatcher contract`. GREEN: change the IPC seam so `AllowedOperation` retains the generated request payload and a typed `DispatchContext` whose mandatory `InteractiveUserEffectLease` is an opaque scoped capability, not a Boolean or SID string; implement `PhysicalOperationDispatcher` as an exhaustive match that invokes only existing operation-specific APIs and emits bounded `DispatchAudit` metadata with no raw SID/token/secret. The fake lease exists only under tests; the Windows constructor is owned by 06-30 and bounded effect impersonation by 06-36. Do not add a string command registry, executable path, shell, PowerShell, WMI, registry/file/service primitive, remote host, or Extreme variant per D-08/D-31. Preserve durable reserve/unknown-before-dispatch semantics from 06-13 and observation-only success authority from D-19/D-23. Commit GREEN as `feat(06-29): connect allowlisted physical dispatcher`. REFACTOR only after the focused suite is green; keep generated transport conversion and Windows effects in separate functions and commit only if changed.</action>
  <verify><automated>rtk cargo test -p liiiraa-optimizer-service --test physical_dispatcher &amp;&amp; rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol</automated></verify>
  <acceptance_criteria>- All five generated request variants are exhaustively mapped with payloads intact.
- Session/SID strings without an opaque effect lease produce zero PowrProf/System Restore calls.
- Forbidden/generic/Extreme inputs are structurally unreachable and adversarial tests prove zero dispatch.
- Unknown, drift, conflict, timeout, and response loss never produce verified success or blind retry.
- RED precedes GREEN in git history and both focused suites pass.</acceptance_criteria>
  <done>The broker owns a closed, request-preserving physical dispatcher without generic execution authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| generated IPC request -> physical dispatcher | Untrusted local input crosses into privileged Windows effect selection. |
| dispatcher -> PowrProf/System Restore | A typed request can mutate global/user Windows state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-29A | Tampering/Elevation | request mapping | mitigate | Preserve generated payloads, exhaustive enum match, exact GUID/state preconditions, and zero-call negative tests; no string/generic authority. |
| T-06-29B | Repudiation | dispatcher outcome | mitigate | Bounded dispatch audit plus observation-required result; broker return cannot mint a receipt. |
| T-06-29C | Denial of Service | replay/timeout | mitigate | Existing durable reservation/unknown-before-dispatch gate and no automatic mutation retry. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-29A | CRITICAL | V4 Access Control; V5 Validation | block | physical_dispatcher zero-call adversarial matrix | Any constructible generic/Extreme path or lost precondition blocks 06-30 and all physical stages. |
| T-06-29B | HIGH | V7 Logging | block | generated response validation and observation-required tests | Any transport-only success blocks 06-30. |
| T-06-29C | HIGH | V4 Access Control | block | ipc_protocol restart/replay tests | Any redispatch after uncertainty blocks 06-30. |
</threat_model>
<verification>Focused dispatcher and IPC suites pass, then `rtk cargo test -p liiiraa-optimizer-service` remains green.</verification>
<success_criteria>An authenticated typed request can reach the existing physical operation exactly once, while arbitrary execution and fabricated success remain impossible.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-29-SUMMARY.md` with RED/GREEN/REFACTOR commits and exact test output.</output>
