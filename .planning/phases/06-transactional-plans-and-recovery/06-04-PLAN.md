---
phase: 06-transactional-plans-and-recovery
plan: '04'
type: execute
wave: 1
depends_on: []
files_modified:
  - tooling/supply-chain/phase6-windows-service-approval.mjs
  - .planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md
autonomous: false
requirements: [PLAN-06]
must_haves:
  truths:
    - '`windows-service` cannot enter the privileged build until a human verifies its exact registry, source, checksum, and license identity.'
    - 'The human verdict is persisted and mechanically validated by executable work after the checkpoint returns.'
  artifacts:
    - path: tooling/supply-chain/phase6-windows-service-approval.mjs
      provides: Fail-closed preparation and approval-record validator
    - path: .planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md
      provides: Human-signed exact package identity, checksum, source, license, and decision record
  key_links:
    - from: .planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md
      to: 06-13-PLAN.md
      via: blocking dependency and exact approved identity preflight
      pattern: 'windows-service.*0\.8\.1.*APPROVED'
---
<objective>Produce a mechanically verifiable human legitimacy decision before any privileged dependency or lockfile mutation.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md @tooling/supply-chain/verify-pins.mjs</context>
<tasks>
<task type="auto">
  <name>Task 1: Capture the exact pre-install registry and source candidate</name>
  <read_first>06-RESEARCH.md Package Legitimacy Audit; tooling/supply-chain/verify-pins.mjs; Cargo.lock; Cargo.toml; apps/optimizer-service/Cargo.toml (confirm absent)</read_first>
  <files>tooling/supply-chain/phase6-windows-service-approval.mjs, .planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md</files>
  <action>Create a dependency-free Node verifier with `prepare`, `validate-pending`, and `validate-approved` modes. `prepare` must fail if any manifest or Cargo.lock already contains `windows-service`, fetch/resolve only the official crates.io record for exact 0.8.1 and its linked `github.com/mullvad/windows-service-rs` source, capture registry URL, source URL and immutable tag/commit, crates.io checksum, SPDX license, dependency summary, publish identity/date, and build-script/install behavior, then write a deterministic PENDING record. `validate-pending` requires all candidate fields, exact PENDING status, and continued absence from Cargo manifests/lockfile. Never install or modify Cargo.toml/Cargo.lock in this task. `validate-approved` must require the same captured fields plus reviewer identity, UTC review timestamp, verbatim reviewer response, and exact `APPROVED` verdict; it rejects missing, rejected, incomplete, differently named, different-version, source-mismatched, or checksum-mismatched records.</action>
  <verify><automated>rtk node tooling/supply-chain/phase6-windows-service-approval.mjs prepare && rtk node tooling/supply-chain/phase6-windows-service-approval.mjs validate-pending</automated></verify>
  <acceptance_criteria>- The PENDING record contains exact registry/source/version/checksum/license evidence.
- The validator proves Cargo.toml/Cargo.lock remain untouched by this dependency.
- A similarly named crate, mutable source identity, or missing field fails closed.</acceptance_criteria>
  <done>A reviewable exact candidate exists without installing privileged code.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review windows-service 0.8.1 legitimacy</name>
  <action>This checkpoint performs no file or dependency mutation. Pause before execution, present the already-prepared evidence, and collect only the human review response for Task 3 to persist and validate.</action>
  <what-built>The automated preparation recorded the official registry identity, linked immutable source identity, checksum, license, dependency summary, and build behavior in `06-04-SUPPLY-CHAIN-APPROVAL.md`; no Cargo manifest or lockfile was changed.</what-built>
  <how-to-verify>1. Open the registry and source URLs recorded in the PENDING record. 2. Confirm exact crate name `windows-service`, version `0.8.1`, Mullvad source ownership/history, immutable tag/commit, crates.io checksum, license, dependency tree, and build/install behavior. 3. Compare every reviewed value with the PENDING record. 4. Approve only this exact identity; a similarly named crate, another version, unexplained build behavior, or any mismatch is a rejection.</how-to-verify>
  <resume-signal>Reply `APPROVED windows-service 0.8.1` with reviewer identity, or `REJECTED` with the mismatch. This gate is never auto-approvable.</resume-signal>
</task>
<task type="auto">
  <name>Task 3: Persist and mechanically validate the reviewer verdict</name>
  <read_first>.planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md; tooling/supply-chain/phase6-windows-service-approval.mjs; the exact Task 2 reviewer response</read_first>
  <files>.planning/phases/06-transactional-plans-and-recovery/06-04-SUPPLY-CHAIN-APPROVAL.md</files>
  <action>After the checkpoint returns, append the exact reviewer response, reviewer identity, UTC timestamp, and `APPROVED` or `REJECTED` verdict to the prepared record. Run `validate-approved`; it must compare the approved name/version/source/checksum/license against the prepared registry evidence and current official record. A rejection, missing response, incomplete field, stale/mismatched identity, or validator error must preserve the record and fail this task so 06-13 remains blocked. Do not touch Cargo.toml or Cargo.lock.</action>
  <verify><automated>rtk node tooling/supply-chain/phase6-windows-service-approval.mjs validate-approved</automated></verify>
  <acceptance_criteria>- The record contains the exact human verdict and all reviewed identity fields.
- Only an exact complete APPROVED record exits zero.
- Rejection or mismatch is retained as evidence and blocks downstream installation.</acceptance_criteria>
  <done>The exact dependency identity is either explicitly admitted with executable proof or explicitly rejected and blocked.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| registry/source metadata -> privileged dependency graph | Typosquatting or changed package identity can gain SYSTEM execution. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-04SC | Tampering/Elevation | windows-service supply chain | mitigate | Automated exact candidate capture, blocking non-auto-approvable human review, then post-checkpoint persisted and mechanically validated verdict before installation. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-04SC | HIGH | V14 Configuration — trusted components use verified sources and exact versions; V4 Access Control — privileged code is explicitly admitted | blocking-human, never auto-approved | Prepared registry/source/checksum/license record, exact human response, and `validate-approved` output | Missing, incomplete, rejected, stale, or identity-mismatched approval blocks 06-13 and every broker, operation, integration, and physical-promotion descendant. |
</threat_model>
<verification>The PENDING candidate is captured before dependency mutation and only the post-checkpoint exact APPROVED record passes the validator.</verification>
<success_criteria>`windows-service` installation is impossible without a complete, persisted, human-reviewed, identity-matched approval record.</success_criteria>
<output>Create `06-04-SUMMARY.md` only after `validate-approved` exits zero; on rejection retain the record and return BLOCKED without a completion summary.</output>
