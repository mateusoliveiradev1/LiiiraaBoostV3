---
phase: 06-transactional-plans-and-recovery
plan: '14'
type: tdd
wave: 4
depends_on: ['06-05', '06-06', '06-07', '06-08', '06-09', '06-10', '06-12']
files_modified: [crates/plan-engine/src/executor.rs, crates/plan-engine/tests/executor.rs]
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Mutations serialize, prior intent commits before dispatch, reads alone retry boundedly, and every effect is observed before verdict.'
    - 'Crash/reboot/restart checkpoints preempt new mutation; partial failure restores only its closure; cancel stops at a safe boundary.'
    - 'D-04: Applied changes remain active until an explicit restore transaction; closing, offline state, or Premium loss never auto-reverts them, and recovery remains subscription-independent.'
    - 'D-18: Restart-required work creates a protected checkpoint, never forces reboot, and verifies on the next boot before completion or any new mutation.'
    - 'D-19: Crash, power loss, or shutdown gives recovery priority; the executor compares real Windows state with the durable journal and never blindly repeats a mutation.'
    - 'D-20: Failed restore or unknown prior state blocks new mutation and automatic repetition, preserves evidence, and exposes guided recovery, applicable System Restore, and diagnostic export.'
    - 'D-21: Cancellation takes effect only at a safe boundary, stops later stages, and maps timeout to unknown plus recovery rather than presumed success.'
    - 'D-22: Mutations are serialized; only reads and verifications proven independent may run in parallel.'
    - 'D-23: Only reads have bounded automatic retry; mutation retry observes real state first, appends a new journal transaction, and requires renewed confirmation when uncertain.'
  artifacts:
    - path: crates/plan-engine/src/executor.rs
      provides: Pure transaction/recovery state reducer and port orchestration
    - path: crates/plan-engine/tests/executor.rs
      provides: Full failpoint state-machine evidence
  key_links:
    - from: crates/plan-engine/src/executor.rs
      to: DurableJournalPort
      via: append prepared before MutationPort call
      pattern: 'append_prepared'
---
<objective>Implement the deterministic transaction/recovery executor through fault-injection TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@crates/plan-engine/src/domain.rs @crates/plan-engine/src/dependency.rs @crates/plan-engine/src/reconcile.rs @.planning/phases/06-transactional-plans-and-recovery/06-VALIDATION.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify every durable and external failure boundary</name><read_first>crates/plan-engine/src/executor.rs; crates/plan-engine/tests/executor.rs (confirm absent); crates/plan-engine/src/domain.rs, dependency.rs, reconcile.rs; 06-VALIDATION.md Required Fault Injection; 06-CONTEXT.md D-04, D-05, D-17–D-24, D-29, D-30</read_first><files>crates/plan-engine/tests/executor.rs</files><action>Write failing deterministic adapter tests for before-prepare, BUSY/FULL/IOERR commit, after-prepare, before-dispatch, timeout before/after effect, response loss, crash before observation append, verification failure, dependency rollback/receipt failure, renderer disconnect, shutdown, reboot, stale approval, revocation, drift, unknown, and conflict. Every case asserts exact state, mutation gate, no blind retry, affected closure, preserved independent nodes/evidence, next safe action, and monotonic event sequence.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test executor</automated></verify><acceptance_criteria>- RED failures are missing reducer/orchestration assertions.
- Every Validation faultpoint has a named case.
- Tests prove at most one mutation in flight and recovery priority on startup.</acceptance_criteria><done>RED executor state-machine contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Orchestrate prepare-effect-observe-reconcile</name><read_first>crates/plan-engine/src/executor.rs; crates/plan-engine/tests/executor.rs; crates/plan-engine/src/risk.rs, dependency.rs, reconcile.rs, promotion.rs</read_first><files>crates/plan-engine/src/executor.rs</files><action>Implement the reducer/orchestrator: recompute admission; serialize mutation; observe expected prior; append+commit prepared; release store transaction; dispatch one typed effect; re-observe; reconcile; append verdict/receipt; publish ordered projection. Implement safe-boundary cancellation, bounded read retries only, restart checkpoint without forced reboot, boot-time reconcile before new mutation, dependency-scoped auto-restore, failed-restore global block, explicit retry as a new reviewed transaction, and native continuity independent of renderer/subscription.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine --test executor</automated></verify><acceptance_criteria>- Full failpoint matrix passes.
- No external call happens before prepared commit success.
- Dispatch success without observed equality cannot emit verified success/receipt.
- Recovery/apply/restore each append a new immutable transaction.</acceptance_criteria><done>The pure executor yields deterministic safe outcomes at every interruption boundary.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove state-machine safety properties</name><read_first>crates/plan-engine/src/executor.rs; crates/plan-engine/tests/executor.rs</read_first><files>crates/plan-engine/src/executor.rs, crates/plan-engine/tests/executor.rs</files><action>Add arbitrary event-sequence properties for no double dispatch, no success without observation+receipt, no mutation while recovery blocked, and exact monotonic events; remove reducer duplication without merging distinct unknown/drift/conflict states. Commit refactor if green.</action><verify><automated>rtk cargo test -p liiiraa-plan-engine executor</automated></verify><acceptance_criteria>- Property suite proves safety invariants for generated sequences.
- Timeout/cancel/crash never maps to success or automatic mutation retry.</acceptance_criteria><done>The executor is deterministic, auditable, and interruption-safe.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| journal -> broker -> observation | Non-transactional OS effects can fail between any two durable events. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-14A | Tampering/Repudiation | executor | mitigate | Prepare commit before effect, observation-derived verdict, immutable receipt, no blind retry. |
| T-06-14B | Denial | crash/reboot/renderer loss | mitigate | Native reducer, startup reconciliation priority, safe mutation gate, authoritative snapshot. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-14A | CRITICAL | V7 Logging — append complete attributable security events; V4 Access Control — effect admission follows durable policy | blocking | Full executor failpoint/property suite including no-effect-before-prepare and no-success-before-observation | Any journal-order, receipt, or blind-retry violation blocks 06-18 and all physical mutation. |
| T-06-14B | HIGH | V7 Logging — recover security state after interruption; V4 Access Control — recovery preempts new effects | blocking | Crash/reboot/renderer-loss/startup reconciliation tests | Any recovery-priority or mutation-gate failure blocks integration and promotion. |
</threat_model>
<verification>All deterministic failpoints and TDD commit gates pass.</verification>
<success_criteria>Every plan/restore transition remains recoverable and exactly explained.</success_criteria>
<output>Create `06-14-SUMMARY.md`.</output>
