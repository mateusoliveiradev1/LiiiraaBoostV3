---
phase: 06-transactional-plans-and-recovery
plan: '34'
type: tdd
wave: 17
depends_on: ['06-31', '06-33', '06-38']
files_modified:
  - tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1
  - tooling/hyperv-lab/Run-LabElevated.ps1
  - tooling/hyperv-lab/Invoke-Phase6Physical.ps1
  - tooling/hyperv-lab/phase6-physical.test.mjs
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'One elevated exact command restores only the audited clean checkpoint, stages only hash-verified MSI/runner/driver bytes, invokes the self-contained runner, and collects only bounded raw evidence.'
    - 'The path re-audits VM/checkpoint, Generation 2, Secure Boot, TPM, integration services, artifact identity, fresh simulation admission, and installed checkpoint before mutation.'
    - 'The guest needs no Node, TypeScript loader, package manager, source checkout, or separately installed test driver.'
    - 'No arbitrary guest script/command/executable, remote target, host power mutation, credential persistence, evidence relabeling, or checkpoint overwrite is available.'
  artifacts:
    - path: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      provides: Exact-target elevated Audit/RunCleanVm orchestrator
    - path: tooling/hyperv-lab/phase6-physical.test.mjs
      provides: Target, artifact, command, credential, evidence, and destructive-operation mutation tests
  key_links:
    - from: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      to: target/phase6-physical/{commit}/{buildId}/phase6-physical-runner.exe
      via: actual hash/signature verification then fixed EXE invocation with one generated run-config path
      pattern: 'phase6-physical-runner.exe.*--run-config'
    - from: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      to: tooling/phase6-evidence/src/physical-writer.ts
      via: host validates runner/artifact/version provenance before physical-writer ingestion
      pattern: 'phase6-physical-runner-rust-1.*physical-writer'
---
<objective>Expose one safe elevated Hyper-V route from the exact simulation-admitted artifact to validated clean-VM physical evidence.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-38-SUMMARY.md
@tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1
@tooling/hyperv-lab/Run-LabElevated.ps1
@tooling/hyperv-lab/README.md
@tooling/phase6-evidence/src/physical-writer.ts
<interfaces>
The exact target is VM `LiiiraaBoost-W11-25H2-Clean`, clean checkpoint `Clean-Windows-Ready`, and installed checkpoint `LiiiraaBoost-Installed`. Host elevation is required. A PSCredential exists only in process memory for PowerShell Direct and is never plaintext CLI/log/evidence data. The only guest process is staged `phase6-physical-runner.exe --run-config <generated-exact-path>`. Artifact/MSI/runner/tauri-driver/msedgedriver hashes and the operation/build tuple come from 06-31; the matching admitted deterministic predecessor comes from 06-38. There is no caller command/script/executable parameter.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the exact-target self-contained Hyper-V bridge</name>
  <read_first>tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1; tooling/hyperv-lab/Run-LabElevated.ps1; tooling/hyperv-lab/README.md; 06-31-SUMMARY.md artifact path/hash; 06-33-SUMMARY.md runner contract; 06-38-SUMMARY.md admitted simulation identity; tooling/phase6-evidence/src/physical-writer.ts; 06-UAT.md immutable blockers; 06-CONTEXT.md D-01, D-02, D-06, D-31, D-34, D-35</read_first>
  <files>tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1, tooling/hyperv-lab/Run-LabElevated.ps1, tooling/hyperv-lab/Invoke-Phase6Physical.ps1, tooling/hyperv-lab/phase6-physical.test.mjs</files>
  <behavior>
    - Audit requires elevation, exact VM/checkpoint names, one matching checkpoint, Generation 2, Secure Boot On, TPM enabled, healthy Hyper-V services/integration, exact artifact/MSI/runner/driver hashes, and 06-38 admission for the same operation/build tuple.
    - Closed actions are `Audit` and `RunCleanVm`; the run performs restore-clean, stage-exact-artifact, invoke-exact-runner, create-installed-checkpoint, collect-bounded-evidence, and status in one state machine with no generic sub-action input.
    - Staging copies only manifest-enumerated MSI, runner, tauri-driver, msedgedriver, artifact/installation manifests, signatures, and generated run config after actual hash/signature verification.
    - PowerShell Direct invokes only the runner EXE with `--run-config`; no Node/TypeScript/source/tool installation, caller command/script/PowerShell text, alternate executable, or remote host exists. Credential stays in memory and output rejects password/SID/serial/token fields.
    - Evidence collection accepts one fixed guest output path, enforces 64 KiB, copies bytes without relabeling, invokes physical-writer, and records exact VM/checkpoint/artifact/runner/operation/run hashes.
    - Any failure writes BLOCKED with completed boundaries, performs no next mutation, preserves the clean checkpoint, and never overwrites the installed checkpoint.
  </behavior>
  <action>RED: add Node tests over safe dry-run fixtures and mutate elevation, VM/checkpoint, Secure Boot/TPM/services, artifact/MSI/runner/driver hashes, simulation predecessor/version/build, paths, credentials, runner/config command, output size/secrets, checkpoint existence, partial failures, and attempted Node/TypeScript/source installation; commit `test(06-34): add failing self-contained Hyper-V bridge policy`. GREEN: implement dedicated `Audit` and `RunCleanVm` orchestration and only the exact existing Hyper-V actions required internally. Resolve paths inside `C:\Users\Liiiraa\VM-Lab` or the exact 06-31 create-once artifact root before copy/checkpoint/restore. Generate a hash-bound closed run config, stage only enumerated bytes, and invoke only `phase6-physical-runner.exe --run-config <exact-path>` through PowerShell Direct with in-memory credential. Expose no `-Command`, `-Script`, alternate executable, remote host, host mutation, Node, TypeScript, package install, source path, or physical-label parameter. Validate returned bytes through 06-32 before manifest/UAT append. Preserve clean checkpoint and refuse installed-checkpoint overwrite. Commit `feat(06-34): bridge exact artifact to clean VM`. REFACTOR after mutation/dry-run tests pass.</action>
  <verify><automated>rtk node --test tooling/hyperv-lab/phase6-physical.test.mjs &amp;&amp; rtk powershell -NoProfile -File tooling/hyperv-lab/Invoke-Phase6Physical.ps1 -Action Audit -DryRun -VmName LiiiraaBoost-W11-25H2-Clean -CheckpointName Clean-Windows-Ready -ArtifactManifestFromSummary .planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md -SimulationAdmissionFromSummary .planning/phases/06-transactional-plans-and-recovery/06-38-SUMMARY.md</automated></verify>
  <acceptance_criteria>- Mutations of target, security posture, hashes, predecessor, runner/config, credentials, or evidence fail before side effects.
- Elevated live route is fixed to audited VM/checkpoint/artifact/runner and never mutates host power state.
- Guest execution requires no development runtime/tool installation and every runtime byte is manifest-bound.
- Clean checkpoint is preserved and installed checkpoint is create-once after verified install.
- Collected bytes pass physical-writer or remain BLOCKED without relabeling.</acceptance_criteria>
  <done>The exact clean-VM run demanded by 06-26 is mechanically executable through one closed elevated command.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| elevated host -> Hyper-V VM/checkpoint | Wrong target or broad paths could damage unrelated machines/data. |
| host -> guest runner | PowerShell Direct could become arbitrary privileged remote execution. |
| guest -> host evidence | Compromised output could leak secrets or fabricate promotion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-34A | Tampering/Denial | target/path/artifact | mitigate | Exact names, resolved roots, security audit, actual hashes, create-once checkpoints. |
| T-06-34B | Elevation | guest invocation | mitigate | Fixed signed runner/config only, in-memory credential, no command/script/runtime installation. |
| T-06-34C | Information Disclosure/Spoofing | collected evidence | mitigate | Fixed path/size/redaction, artifact/runner/run hashes, physical-writer validation. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-34A | CRITICAL | V4 Access Control; V14 Configuration | block | exact-target/artifact mutation tests and elevated audit | Ambiguous target/path/hash blocks 06-26. |
| T-06-34B | CRITICAL | V4 Access Control | block | arbitrary command/runtime-install rejection tests | Any generic guest authority blocks 06-26. |
| T-06-34C | HIGH | V8 Data Protection; V5 Validation | block | bounded/redacted writer tests | Secret, oversize, mismatch, or relabeling blocks 06-26. |
</threat_model>
<verification>Policy suite and exact non-mutating audit dry run pass; live elevated audit must pass before 06-26 RunCleanVm.</verification>
<success_criteria>The clean-VM stage is reachable using only simulation-admitted, signed/hash-bound, self-contained artifact bytes.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-34-SUMMARY.md` with TDD commits, audit output, and the exact 06-26 RunCleanVm command.</output>
