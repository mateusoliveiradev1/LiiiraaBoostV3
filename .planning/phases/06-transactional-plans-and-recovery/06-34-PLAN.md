---
phase: 06-transactional-plans-and-recovery
plan: '34'
type: tdd
wave: 14
depends_on: ['06-31', '06-33']
files_modified:
  - tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1
  - tooling/hyperv-lab/Run-LabElevated.ps1
  - tooling/hyperv-lab/Invoke-Phase6Physical.ps1
  - tooling/hyperv-lab/phase6-physical.test.mjs
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'An elevated host command can restore only the exact clean checkpoint, stage only the hash-verified physical artifact/runner, invoke only the closed guest runner, and collect only its bounded evidence.'
    - 'The path re-audits exact VM/checkpoint, Generation 2, Secure Boot, TPM, integration services, artifact identity, and installed checkpoint before physical mutation.'
    - 'No arbitrary guest script/command, remote execution target, host power mutation, credential persistence, evidence relabeling, or checkpoint overwrite is available.'
  artifacts:
    - path: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      provides: Exact-target elevated Phase 6 host orchestrator
    - path: tooling/hyperv-lab/phase6-physical.test.mjs
      provides: Mutation tests for target, command, credential, evidence, and destructive-operation constraints
  key_links:
    - from: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      to: apps/desktop/tests/packaged/physical-runner-cli.ts
      via: fixed staged guest runner invocation and bounded JSON result
      pattern: 'physical-runner-cli'
    - from: tooling/hyperv-lab/Invoke-Phase6Physical.ps1
      to: tooling/phase6-evidence/src/physical-writer.ts
      via: host-side validation before persistence
      pattern: 'physical-writer'
---
<objective>Expose one safe elevated Hyper-V path from exact artifact to validated clean-VM evidence.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-31-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-33-SUMMARY.md
@tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1
@tooling/hyperv-lab/Run-LabElevated.ps1
@tooling/hyperv-lab/README.md
@.planning/phases/06-transactional-plans-and-recovery/06-UAT.md
<interfaces>
The exact target is VM `LiiiraaBoost-W11-25H2-Clean`, clean checkpoint `Clean-Windows-Ready`, and installed checkpoint `LiiiraaBoost-Installed`. Host elevation is required for Hyper-V. A PSCredential may exist only in process memory for PowerShell Direct; it is never accepted as plaintext CLI input or written to logs/evidence. The guest command is fixed to the staged 06-33 runner with a generated hash-bound config—there is no script/command parameter.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the exact-target Hyper-V physical bridge</name>
  <read_first>tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1; tooling/hyperv-lab/Run-LabElevated.ps1; tooling/hyperv-lab/README.md; apps/desktop/tests/packaged/physical-runner-cli.ts; tooling/phase6-evidence/src/physical-writer.ts; artifact manifest from 06-31; 06-UAT.md immutable blocker evidence; 06-CONTEXT.md D-01, D-02, D-06, D-31, D-34, D-35</read_first>
  <files>tooling/hyperv-lab/Invoke-LiiiraaBoostLab.ps1, tooling/hyperv-lab/Run-LabElevated.ps1, tooling/hyperv-lab/Invoke-Phase6Physical.ps1, tooling/hyperv-lab/phase6-physical.test.mjs</files>
  <behavior>
    - Preflight requires elevation and exact VM/checkpoint names, one matching checkpoint, Generation 2, Secure Boot On, TPM enabled, required Hyper-V services/integration healthy, exact artifact-manifest/file hashes, and deterministic-simulation predecessor admitted.
    - Closed actions are audit, restore-clean, stage-exact-artifact, run-exact-guest, create-installed-checkpoint, collect-bounded-evidence, and status; each resolves checked paths under declared lab/target roots and refuses wildcards, alternate VM/host, duplicate checkpoint, or broad delete/move.
    - Guest invocation uses only the staged fixed runner and generated config; no caller command/script/PowerShell text or arbitrary executable path exists. PSCredential stays in memory and output/log redaction rejects password/SID/serial/token fields.
    - Evidence collection accepts one fixed guest output path, enforces 64 KiB, copies/returns bytes without relabeling, invokes the host physical writer, and records exact VM/checkpoint/artifact/operation/run hashes.
    - Failure before/after any action writes BLOCKED evidence with completed boundaries, performs no next mutation, and never shrinks the required matrix or creates/overwrites a checkpoint.
  </behavior>
  <action>RED: add Node tests that parse/invoke safe dry-run fixtures and mutate elevation, VM/checkpoint, Secure Boot/TPM/services, artifact hashes, predecessor, paths, credentials, guest command, output size/secrets, checkpoint existence, and partial failures; commit `test(06-34): add failing Hyper-V physical bridge policy`. GREEN: add a dedicated orchestrator and extend only the existing action allowlists needed to restore/stage/run/checkpoint/collect the exact Phase 6 lab target. Verify resolved absolute paths remain inside `C:\Users\Liiiraa\VM-Lab` or `target\phase6-physical` before any copy/checkpoint/restore. PowerShell Direct may invoke only the staged fixed runner with a generated config and in-memory credential; it must not expose `-Command`, `-Script`, arbitrary executable, remote host, or host mutation options. Validate returned bytes through the 06-32 writer before any manifest/UAT append. Preserve the original clean checkpoint, refuse installed-checkpoint overwrite, and make all failures explicit. Commit `feat(06-34): add safe exact-target Hyper-V runner path`. REFACTOR only after dry-run/mutation tests pass.</action>
  <verify><automated>rtk node --test tooling/hyperv-lab/phase6-physical.test.mjs &amp;&amp; rtk powershell -NoProfile -File tooling/hyperv-lab/Invoke-Phase6Physical.ps1 -Action Audit -DryRun -VmName LiiiraaBoost-W11-25H2-Clean -CheckpointName Clean-Windows-Ready</automated></verify>
  <acceptance_criteria>- Deterministic tests reject every target/path/hash/command/credential/evidence mutation before side effects.
- Elevated live path is fixed to the audited VM/checkpoint/artifact/runner and never mutates host power state.
- Clean checkpoint is preserved; installed checkpoint is create-once after verified install.
- Collected bytes pass the physical writer or persist BLOCKED without relabeling.
- Accessibility comprehension and final evidence review remain explicit 06-26 human verification, not auto-approved here.</acceptance_criteria>
  <done>The executor has one safe elevated command path that can produce the exact clean-VM run demanded by 06-26.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| elevated host -> Hyper-V VM/checkpoint | A wrong target or broad path could damage unrelated machines/data. |
| host -> guest runner | Remote execution could become arbitrary code authority. |
| guest -> host evidence | Compromised output could leak secrets or fabricate promotion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-34A | Tampering/Denial | VM/checkpoint/path target | mitigate | Exact names, resolved-root checks, security audit, create-once checkpoints, no wildcard/broad delete. |
| T-06-34B | Elevation | guest invocation | mitigate | Fixed runner/config only, in-memory credential, no command/script/executable parameter. |
| T-06-34C | Information Disclosure/Spoofing | collected evidence | mitigate | Fixed path/size, redaction scan, artifact/run hashes, physical writer validation before append. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-34A | CRITICAL | V4 Access Control; V14 Configuration | block | exact-target mutation tests and elevated audit | Any target ambiguity or unsafe filesystem action blocks 06-26. |
| T-06-34B | CRITICAL | V4 Access Control | block | arbitrary command/script/path rejection tests | Any generic guest authority blocks 06-26. |
| T-06-34C | HIGH | V8 Data Protection; V5 Validation | block | bounded/redacted writer tests | Any raw secret, oversize, hash mismatch, or relabeling blocks 06-26. |
</threat_model>
<verification>Policy/mutation suite and non-mutating dry-run pass; the elevated audit command must PASS exact VM/checkpoint/security/services before 06-26 may mutate.</verification>
<success_criteria>The clean-VM stage is mechanically reachable without weakening target safety, privileged authority, or evidence provenance.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-34-SUMMARY.md` with TDD commits, dry-run output, and the exact elevated command for 06-26.</output>
