---
phase: 06-transactional-plans-and-recovery
plan: '36'
type: tdd
wave: 13
depends_on: ['06-30']
files_modified:
  - apps/optimizer-service/src/dispatcher.rs
  - apps/optimizer-service/src/operations/power_scheme.rs
  - apps/optimizer-service/tests/physical_user_context.rs
  - apps/optimizer-service/tests/physical_dispatcher.rs
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'Every user-scoped PowrProf observe/apply/restore effect runs while impersonating the exact authenticated interactive client token, never LocalSystem.'
    - 'RevertToSelf and token-handle closure are guaranteed after success, error, unwind, disconnect, shutdown, and timeout-drain boundaries before another request is admitted.'
    - 'Windows tests prove the effective SID/session during the effect and prove no token/handle accumulation across repeated success and failure cycles.'
  artifacts:
    - path: apps/optimizer-service/src/operations/power_scheme.rs
      provides: Scoped interactive-user impersonation around every PowrProf effect
      exports: [InteractiveUserEffectGuard]
    - path: apps/optimizer-service/tests/physical_user_context.rs
      provides: Windows effective-SID, cleanup, timeout, unwind, and handle-leak proof
  key_links:
    - from: apps/optimizer-service/src/dispatcher.rs
      to: apps/optimizer-service/src/operations/power_scheme.rs
      via: opaque InteractiveUserEffectLease enters a scoped guard at the effect boundary
      pattern: 'with_interactive_user.*WindowsPowrProf'
    - from: apps/optimizer-service/src/operations/power_scheme.rs
      to: Win32 Security impersonation APIs
      via: SetThreadToken/ImpersonateLoggedOnUser with RAII RevertToSelf
      pattern: 'ImpersonateLoggedOnUser.*RevertToSelf'
---
<objective>Execute the allowlisted power effect in the authenticated interactive user's Windows context with bounded token custody and cleanup.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-29-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-30-SUMMARY.md
@apps/optimizer-service/src/dispatcher.rs
@apps/optimizer-service/src/operations/power_scheme.rs
@apps/optimizer-service/src/windows_pipe.rs
<interfaces>
06-30 produces an owned least-rights `AuthenticatedClientToken` only after pipe impersonation proves the client PID/image/session/logon SID. 06-29 requires an opaque `InteractiveUserEffectLease` in `DispatchContext`. This plan consumes that lease at the final PowrProf boundary. SID/session strings remain audit assertions and cannot impersonate. Timeout means close admission, mark outcome unknown, and drain/join the active effect thread through guard cleanup before any next request; it never abandons a still-impersonated reusable worker.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR bounded interactive-user PowrProf impersonation</name>
  <read_first>apps/optimizer-service/src/dispatcher.rs; apps/optimizer-service/src/windows_pipe.rs; apps/optimizer-service/src/operations/power_scheme.rs; apps/optimizer-service/tests/power_scheme.rs; apps/optimizer-service/tests/physical_dispatcher.rs; 06-CONTEXT.md D-03, D-05, D-19, D-21, D-23, D-31; Microsoft token duplication, ImpersonateLoggedOnUser, SetThreadToken, OpenThreadToken, GetTokenInformation, RevertToSelf, and process-handle-count references from 06-RESEARCH.md</read_first>
  <files>apps/optimizer-service/src/dispatcher.rs, apps/optimizer-service/src/operations/power_scheme.rs, apps/optimizer-service/tests/physical_user_context.rs, apps/optimizer-service/tests/physical_dispatcher.rs</files>
  <behavior>
    - Before each PowrProf observation or mutation, the effect thread activates the opaque duplicated client token and verifies the effective TokenUser SID plus TokenSessionId equal the authenticated lease; mismatch calls zero PowrProf methods.
    - A non-cloneable RAII guard restores the service identity exactly once on normal return, every mapped Win32 error, Rust unwind caught at the worker boundary, client disconnect, service shutdown, and timeout drain.
    - Timeout never kills or reuses an impersonated thread: admission closes, result becomes observation-required/unknown, the worker is drained and joined through RevertToSelf, and only then can service shutdown or later observation proceed.
    - Repeated successful, failed, panicking, and timed-out cases leave the service effective SID as LocalSystem and keep process handle count within a fixed zero-growth tolerance.
  </behavior>
  <action>RED: add Windows-gated integration tests that create a controlled client token, record effective TokenUser/TokenSessionId inside a fake PowrProf call, inject errors/unwind/timeout/disconnect/shutdown, and compare effective service identity plus handle counts before/after repeated cycles; extend dispatcher tests so metadata-only/fabricated leases make zero calls. Commit as `test(06-36): add failing interactive user effect proof`. GREEN: implement a non-cloneable `InteractiveUserEffectGuard` over the 06-30 owned token with `ImpersonateLoggedOnUser` or `SetThreadToken`, immediate effective-token verification, Drop-based `RevertToSelf`, explicit cleanup error escalation, and `catch_unwind` at the dedicated effect worker boundary. Route every `WindowsPowrProf` method through one `with_interactive_user` closure and pass no raw handle to operation/domain code. Treat cleanup failure, timeout, or uncertain effect as mutation-blocking observation-required state per D-19/D-21/D-23; never fall back to LocalSystem execution. Commit as `feat(06-36): bind PowrProf to interactive user token`. REFACTOR only after both Windows and fake-port suites remain green.</action>
  <verify><automated>rtk cargo test -p liiiraa-optimizer-service --test physical_user_context --test physical_dispatcher --test power_scheme &amp;&amp; rtk cargo check -p liiiraa-optimizer-service --target x86_64-pc-windows-msvc</automated></verify>
  <acceptance_criteria>- Effective SID/session inside every PowrProf call matches the authenticated interactive client.
- LocalSystem fallback and metadata-only authorization are impossible.
- Success, error, unwind, timeout-drain, disconnect, and shutdown all restore service identity.
- Repeated cases demonstrate no token/handle leak and no second dispatch after uncertainty.</acceptance_criteria>
  <done>The physical dispatcher can mutate only in a proven, bounded interactive-user context and returns to service identity before continuing.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authenticated pipe token -> effect worker | A privileged service temporarily adopts user identity for user-scoped APIs. |
| effect worker -> PowrProf | Wrong token, leaked impersonation, or timeout reuse could mutate the wrong profile. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-36A | Spoofing/Elevation | client effect lease | mitigate | Opaque duplicated token, effective SID/session verification, no metadata constructor. |
| T-06-36B | Elevation/Denial | impersonation cleanup | mitigate | Non-cloneable RAII guard, catch_unwind, timeout drain/join, fail-closed cleanup. |
| T-06-36C | Information Disclosure | token handle/audit | mitigate | No raw handle/SID logging; bounded ownership and handle-leak tests. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-36A | CRITICAL | V2 Authentication; V4 Access Control | block | effective TokenUser/TokenSessionId integration tests | Wrong-user or LocalSystem effect blocks 06-37 and all physical execution. |
| T-06-36B | CRITICAL | V4 Access Control; V7 Error Handling | block | error/unwind/timeout/disconnect/shutdown cleanup matrix | Any unreverted/reused impersonation blocks desktop transport admission. |
| T-06-36C | HIGH | V8 Data Protection | block | handle-count and redacted-audit assertions | Any raw token leakage or unbounded handle growth blocks physical build. |
</threat_model>
<verification>Windows identity/leak suite, dispatcher suite, power lifecycle suite, and Windows target check pass.</verification>
<success_criteria>The exact interactive user observed by the desktop is the identity under which PowrProf observes, applies, verifies, and restores.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-36-SUMMARY.md` with effective-SID evidence, cleanup matrix, handle counts, and TDD commits.</output>
