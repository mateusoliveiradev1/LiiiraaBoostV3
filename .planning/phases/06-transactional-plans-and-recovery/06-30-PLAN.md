---
phase: 06-transactional-plans-and-recovery
plan: '30'
type: tdd
wave: 11
depends_on: ['06-29']
files_modified:
  - apps/optimizer-service/src/main.rs
  - apps/optimizer-service/src/windows_pipe.rs
  - apps/optimizer-service/tests/windows_service_host.rs
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07, PLAN-08]
must_haves:
  truths:
    - 'The installed Windows service creates the local-only named pipe, authenticates the exact native desktop process, and continuously serves framed generated requests through the physical dispatcher.'
    - 'Stop, shutdown, preshutdown, disconnect, oversized frame, malformed frame, wrong session, replay, and service restart all fail closed without duplicate mutation.'
    - 'No listener exists on TCP, remote named pipes, shell commands, or arbitrary executable/script paths.'
  artifacts:
    - path: apps/optimizer-service/src/windows_pipe.rs
      provides: Local named-pipe accept/auth/frame/dispatch loop with bounded shutdown
      exports: [WindowsPipeHost, PipeHostConfig]
    - path: apps/optimizer-service/tests/windows_service_host.rs
      provides: Deterministic host lifecycle and Windows-only elevated integration cases
  key_links:
    - from: apps/optimizer-service/src/main.rs
      to: apps/optimizer-service/src/windows_pipe.rs
      via: Windows service Running to Preshutdown lifecycle
      pattern: 'WindowsPipeHost.*run'
    - from: apps/optimizer-service/src/windows_pipe.rs
      to: apps/optimizer-service/src/dispatcher.rs
      via: authenticated length-bounded generated message
      pattern: 'PhysicalOperationDispatcher'
---
<objective>Turn the service shell into a real authenticated local broker host.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/06-transactional-plans-and-recovery/06-13-SUMMARY.md
@.planning/phases/06-transactional-plans-and-recovery/06-29-SUMMARY.md
@apps/optimizer-service/src/main.rs
@apps/optimizer-service/src/ipc.rs
@apps/optimizer-service/src/dispatcher.rs
@apps/optimizer-service/tests/ipc_protocol.rs
<interfaces>
The service name is `LiiiraaBoostOptimizer`; the only endpoint is `\\.\pipe\LiiiraaBoost\optimizer-v1`. `PipeSecurityPolicy::service_only` supplies the explicit DACL and `PIPE_REJECT_REMOTE_CLIENTS`. `Broker::authenticate_client` requires local transport, successful impersonation/token query, exact interactive session/logon SID, nonzero PID, and exact process-image hash before a session ticket exists.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN/REFACTOR the Windows service pipe host</name>
  <read_first>apps/optimizer-service/src/main.rs; apps/optimizer-service/src/ipc.rs; apps/optimizer-service/src/dispatcher.rs; apps/optimizer-service/tests/ipc_protocol.rs; apps/optimizer-service/src/dedup_store.rs; 06-CONTEXT.md D-19, D-21, D-22, D-23, D-24, D-31; Microsoft named-pipe security and impersonation references from 06-RESEARCH.md</read_first>
  <files>apps/optimizer-service/src/main.rs, apps/optimizer-service/src/windows_pipe.rs, apps/optimizer-service/tests/windows_service_host.rs</files>
  <behavior>
    - Service Running starts one local-only pipe accept loop with the exact DACL, remote clients rejected, a fixed maximum frame size, finite read/write/request timeouts, and generated-document validation before mapping.
    - Client authentication derives PID, image SHA-256, token/session/logon SID through Win32 pipe impersonation and restores the service token on every branch; identity failure sends no session secret and dispatches nothing.
    - Each accepted connection performs the authenticated handshake then processes canonical framed requests serially; disconnect or crash after reservation returns observation-required after restart and never redispatches.
    - Stop/Shutdown/Preshutdown closes admission, marks the broker stopping, drains only the bounded in-flight request, reports proper service pending/stopped states, and leaves durable dedup/recovery evidence intact.
  </behavior>
  <action>RED: create platform-neutral host-core tests plus `#[cfg(windows)]` named-pipe integration cases covering local admission, remote/spoof/wrong-session rejection, partial/oversized/malformed frames, disconnect, concurrent mutation serialization, preshutdown, and restart dedup; commit failing behavioral tests as `test(06-30): add failing service host lifecycle`. GREEN: implement `windows_pipe.rs` using only Windows named-pipe/token/process APIs already pinned, fixed length-prefixed JSON frames, constant bounds, and the 06-29 `PhysicalOperationDispatcher`; wire it into `run_service`, remove the nonfunctional dispatcher note, and retain correct service status transitions. On first service start, create the install secret under service-only Windows-protected custody; on every start, read the builder-emitted signed/hash-bound installation manifest for the expected desktop image hash and reject absent, changed, or unprotected custody. Never take either value from renderer input, command-line text, environment variables, or plaintext logs. Do not open TCP, allow remote pipe clients, accept command lines/scripts, or expose generic service/file/registry operations per D-31. Commit GREEN as `feat(06-30): host authenticated optimizer pipe`. REFACTOR only while all lifecycle and existing IPC suites remain green.</action>
  <verify><automated>rtk cargo test -p liiiraa-optimizer-service --test windows_service_host &amp;&amp; rtk cargo test -p liiiraa-optimizer-service --test ipc_protocol &amp;&amp; rtk cargo check -p liiiraa-optimizer-service --target x86_64-pc-windows-msvc</automated></verify>
  <acceptance_criteria>- The service main loop actually owns the exact named-pipe host and physical dispatcher.
- Identity/token impersonation cleanup, frame bounds, shutdown, and restart-dedup branches are tested.
- No network listener, generic command, PowerShell/script, remote pipe, or fixture fallback exists.
- RED and GREEN commits exist in order.</acceptance_criteria>
  <done>The privileged broker is an installable, continuously serving, authenticated local Windows service.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| desktop process -> named pipe | A same-machine attacker can spoof or replay privileged requests. |
| service control manager -> service lifecycle | Stop/update/crash boundaries can strand an uncertain mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-30A | Spoofing/Elevation | pipe authentication | mitigate | Reject remote clients, explicit DACL, impersonate/query token, exact session/SID/PID/image hash, authenticated handshake. |
| T-06-30B | Tampering/Denial | framing/concurrency | mitigate | Fixed frame bounds/timeouts, generated validation, serial mutation admission, bounded errors. |
| T-06-30C | Repudiation | restart/preshutdown | mitigate | Unknown-before-dispatch durable dedup and observation-first restart; never erase custody. |
## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-30A | CRITICAL | V2 Authentication; V4 Access Control | block | Windows host spoof/remote/session test matrix | Any unauthenticated session or leaked ticket blocks artifact creation. |
| T-06-30B | HIGH | V5 Validation; V13 API | block | malformed/oversized/concurrency tests | Any unbounded or generic message path blocks artifact creation. |
| T-06-30C | CRITICAL | V7 Logging | block | preshutdown/restart dedup tests | Any duplicate effect or lost unresolved identity blocks artifact creation. |
</threat_model>
<verification>All optimizer-service tests and the Windows target check pass; a Windows-only elevated smoke case can start, interrogate, stop, and restart the host without mutation.</verification>
<success_criteria>The physical dispatcher is reachable only through the authenticated local service boundary and survives lifecycle interruptions without blind redispatch.</success_criteria>
<output>Create `.planning/phases/06-transactional-plans-and-recovery/06-30-SUMMARY.md` with RED/GREEN/REFACTOR evidence.</output>
