---
phase: 06-transactional-plans-and-recovery
plan: '15'
type: tdd
wave: 4
depends_on: ['06-08', '06-13']
files_modified:
  - apps/optimizer-service/src/operations/mod.rs
  - apps/optimizer-service/src/operations/power_scheme.rs
  - apps/optimizer-service/tests/power_scheme.rs
autonomous: true
requirements: [PLAN-03, PLAN-06, PLAN-07]
must_haves:
  truths:
    - 'The first real operation duplicates the exact active scheme into a deterministic GUID named Liiiraa Verificado, activates and verifies it, then restores the exact prior GUID.'
    - 'External changes pause as drift/conflict; owned target deletion occurs only when GUID and canonical state still match.'
  artifacts:
    - path: apps/optimizer-service/src/operations/power_scheme.rs
      provides: Narrow PowrProf observe/duplicate/name/activate/verify/restore/delete-owned adapter
    - path: apps/optimizer-service/tests/power_scheme.rs
      provides: Fake PowrProf apply/restore/drift/error properties
  key_links:
    - from: apps/optimizer-service/src/operations/power_scheme.rs
      to: Windows PowrProf APIs
      via: windows crate typed calls
      pattern: 'Power(GetActive|Duplicate|WriteFriendlyName|SetActive|Delete)'
---
<objective>Implement the single admitted Windows power-scheme operation through port-driven TDD.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/Liiiraa/.codex/get-shit-done/references/tdd.md</execution_context>
<context>@apps/optimizer-service/src/ipc.rs @.planning/phases/06-transactional-plans-and-recovery/06-RESEARCH.md @.planning/phases/06-transactional-plans-and-recovery/06-CONTEXT.md</context>
<tasks>
<task type="auto"><name>Task 1 RED: Specify exact power-scheme lifecycle</name><read_first>apps/optimizer-service/src/operations/mod.rs, power_scheme.rs, tests/power_scheme.rs (confirm absent); apps/optimizer-service/src/ipc.rs; 06-RESEARCH.md Pattern 5; 06-CONTEXT.md D-03–D-05, D-26, D-28, D-30</read_first><files>apps/optimizer-service/src/operations/mod.rs, apps/optimizer-service/tests/power_scheme.rs</files><action>Define the closed operation registry and failing fake-PowrProf tests for exact active GUID/metadata observation, journaled destination GUID, duplicate current plan, exact UTF-16 name `Liiiraa Verificado`, verify created state, activate, re-observe exact GUID, restore exact prior GUID, already-applied/restored reconciliation, access denial, duplicate-exists ownership checks, drift before apply/restore, third-state conflict, and delete only unchanged owned target. Assert no `powercfg`, subprocess, registry, generic GUID write, performance setting, or gain claim.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service --test power_scheme</automated></verify><acceptance_criteria>- RED compiles and fails on adapter behavior.
- Registry contains only named observe/duplicate/activate/delete-owned operations and no Extreme/generic variant.
- Tests model the intended interactive-user context explicitly.</acceptance_criteria><done>RED power operation contract is committed.</done></task>
<task type="auto"><name>Task 2 GREEN: Apply and exactly restore Liiiraa Verificado</name><read_first>apps/optimizer-service/src/operations/power_scheme.rs; apps/optimizer-service/tests/power_scheme.rs; apps/optimizer-service/src/operations/mod.rs; official PowrProf references in 06-RESEARCH.md</read_first><files>apps/optimizer-service/src/operations/power_scheme.rs, apps/optimizer-service/src/operations/mod.rs</files><action>Implement a testable `PowerSchemePort` and Windows adapter using documented PowrProf APIs from `windows` bindings. Use GUIDs as identity; generate destination before effect; preflight access; snapshot canonical prior/target metadata; duplicate/name/verify/activate/observe; restore prior; delete only owned unchanged target. Execute user-scoped calls only under the verified broker client context. Return closed exact states; never claim performance improvement from cloning/activation.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service --test power_scheme</automated></verify><acceptance_criteria>- Fake adapter suite passes every apply/restore/drift/conflict case.
- Success requires observed active GUID equality.
- Prior scheme is never modified or deleted.</acceptance_criteria><done>The first real operation is isolated, verified, and exactly reversible.</done></task>
<task type="auto"><name>Task 3 REFACTOR: Prove idempotent observation and ownership</name><read_first>apps/optimizer-service/src/operations/power_scheme.rs; tests/power_scheme.rs</read_first><files>apps/optimizer-service/src/operations/power_scheme.rs, apps/optimizer-service/tests/power_scheme.rs</files><action>Add arbitrary GUID/state/error sequence properties for observation, ownership, already-applied/restored, and conflict; consolidate resource cleanup around Win32 ownership rules. Commit refactor if changed.</action><verify><automated>rtk cargo test -p liiiraa-optimizer-service power_scheme</automated></verify><acceptance_criteria>- Observations are repeatable without mutation.
- No generated state sequence deletes or overwrites an externally changed scheme.</acceptance_criteria><done>Power adapter ownership and recovery preconditions are property-proven.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| privileged adapter -> user-scoped Power Manager | Wrong user or stale GUID could change/delete another scheme. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-15A | Elevation/Tampering | PowrProf adapter | mitigate | Verified impersonated session, GUID identity, expected-prior precondition, observe-after-effect. |
| T-06-15B | Tampering | owned target cleanup | mitigate | Delete only exact owned GUID whose canonical state still matches; otherwise conflict. |

## ASVS L1 Security Gate
| Threat ID | Severity | ASVS L1 control | Gate disposition | Verification evidence | Dependency / blocking behavior |
|---|---|---|---|---|---|
| T-06-15A | CRITICAL | V4 Access Control — privileged effect runs only for the verified user/context; V5 Input Validation — expected-state precondition | blocking | Fake PowrProf identity/GUID/drift/access-denial suite, then Plans 06-26–06-28 real Windows proof | Wrong-user effect, stale GUID, or unobserved success blocks 06-18 and physical promotion. |
| T-06-15B | HIGH | V4 Access Control — mutation is limited to the exact owned resource | blocking | Ownership/change/delete-conflict property tests | Any ability to delete/overwrite an external or changed scheme blocks integration and promotion. |
</threat_model>
<verification>Fake PowrProf suite passes; real Windows proof is owned by the sequential physical promotion gates in Plans 06-26 through 06-28.</verification>
<success_criteria>Liiiraa Verificado applies and restores without modifying the prior plan or hiding drift.</success_criteria>
<output>Create `06-15-SUMMARY.md`.</output>
