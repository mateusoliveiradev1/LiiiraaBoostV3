---
phase: 04-identity-commerce-devices-and-administration
plan: "23"
type: execute
wave: 9
depends_on: ["04-18", "04-19", "04-20", "04-21", "04-22"]
files_modified:
  - apps/web/vercel.json
  - apps/account/vercel.json
  - apps/admin/vercel.json
  - apps/desktop/src/staging-runtime.ts
  - apps/desktop/src-tauri/tauri.staging.conf.json
  - apps/desktop/staging/internal-channel.schema.json
  - apps/desktop/staging/internal-channel.json
  - apps/desktop/staging/CHANGE-NOTES.md
  - .github/workflows/phase-4-surfaces.yml
autonomous: true
requirements: [WEB-04, WEB-05, WEB-06, WEB-07, IDEN-01, IDEN-06]
user_setup:
  - service: Vercel
    why: Separate public, account, and admin non-production origins
    env_vars:
      - name: PUBLIC_STAGING_ORIGIN
        source: Vercel public project domain
      - name: ACCOUNT_STAGING_ORIGIN
        source: Vercel account project domain
      - name: ADMIN_STAGING_ORIGIN
        source: Vercel admin project domain
    dashboard_config:
      - task: Link each app to a separate Vercel project with separate environment variables
        location: Vercel project settings
  - service: Google and Discord
    why: Sandbox social login callbacks for web and desktop
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: Google OAuth test application
      - name: GOOGLE_CLIENT_SECRET
        source: Google OAuth test application
      - name: DISCORD_CLIENT_ID
        source: Discord OAuth test application
      - name: DISCORD_CLIENT_SECRET
        source: Discord OAuth test application
    dashboard_config:
      - task: Register exact account and desktop staging callback URIs; keep apps in test/private mode
        location: Provider developer consoles
must_haves:
  decision_ids: [D-57, D-61, D-62, D-63]
  truths:
    - "Public/account/admin deploy as separate Vercel projects/origins connected to the same staging API, per D-57."
    - "Desktop staging builds use a restricted numbered Internal channel with separate manifest/change notes/exact identity/rollback; never Stable or public Beta, per D-61."
    - "Provider URLs are limited to bounded early testing; owned domain/callback/email identity is a blocking broader-beta promotion gate, per D-62."
    - "No trusted public installer or production-readiness claim is emitted, per D-63."
  artifacts:
    - path: apps/desktop/staging/internal-channel.json
      provides: Restricted internal numbered build manifest
    - path: .github/workflows/phase-4-surfaces.yml
      provides: Separate Vercel deployments and internal desktop artifact/evidence workflow
  key_links:
    - from: apps/account/vercel.json
      to: STAGING_API_ORIGIN
      via: exact generated-client API origin; never admin/public cookie sharing
---
<objective>Deploy isolated web/admin surfaces and a restricted numbered desktop staging channel against the staging API.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@tooling/web-evidence/playwright.config.ts @apps/desktop/src-tauri/tauri.conf.json @.planning/phases/03-complete-web-experience/03-30-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 04-23-01: Deploy three isolated Vercel staging origins</name>
  <read_first>D-57, D-62, D-63; existing Next origin/CSP/cookie policies; account/admin production authority adapters</read_first>
  <files>apps/web/vercel.json, apps/account/vercel.json, apps/admin/vercel.json, .github/workflows/phase-4-surfaces.yml</files>
  <action>Configure independent project roots/builds, exact staging origins/CSP/cookies/noindex, and generated API origin for public, account, and admin. CI deploys preview/staging through protected environments and runs cross-origin/auth/consent smokes. Provider preview URLs are explicitly bounded; add a promotion assertion requiring owned domains and callback/email identities before broader closed beta. No surface accepts shared cookies or production provider/database values.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence exec playwright test tests/security-artifacts.spec.ts --grep @staging-origin-smoke --workers=1</automated></verify>
  <acceptance_criteria>Three distinct origins/builds have exact headers/cookies/noindex and reach only the staging API; cross-origin/session leakage probes fail closed.</acceptance_criteria>
  <done>The web control-plane surfaces are independently deployed and isolated.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 04-23-02: Produce the restricted Internal desktop channel</name>
  <read_first>D-61 through D-63; current Tauri config/updater channel model; offline entitlement and account authority adapters</read_first>
  <files>apps/desktop/src/staging-runtime.ts, apps/desktop/src-tauri/tauri.staging.conf.json, apps/desktop/staging/internal-channel.schema.json, apps/desktop/staging/internal-channel.json, apps/desktop/staging/CHANGE-NOTES.md, .github/workflows/phase-4-surfaces.yml</files>
  <behavior>- Manifest requires channel `internal`, monotonically numbered build, commit/digest/checksum, access scope, change notes, rollback target, API/contract version.
- Stable/Beta/Experimental/public download/trusted publisher claims reject.
- Runtime admits exact staging origins/API/keys and visibly identifies Internal build.
- Rollback selects a previous restricted manifest by immutable identity.</behavior>
  <action>RED: write manifest/runtime admission tests for missing identity, mutable rollback, forbidden channel/trust claims, and wrong API/contract/key configuration; run them and record the expected missing-manifest/runtime failures. GREEN: add the separate Tauri staging overlay and generated-contract configuration; build the numbered Internal artifact and record exact identity/change notes/checksums/SBOM/provenance/access/rollback metadata until the focused tests pass. REFACTOR: deduplicate manifest admission and CI projection, rerun the focused suite, and preserve Phase 2 signing truth without any public installer/channel claim.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test -- --run internal-channel</automated></verify>
  <acceptance_criteria>The manifest/runtime rejects public release claims and one exact restricted build can be selected and rolled back by immutable identity.</acceptance_criteria>
  <done>Invited PCs have an honest restricted desktop staging artifact.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `apps/*/vercel.json`: separate project/root/header/env contracts.
- `InternalChannelManifest`, `admitStagingRuntime`, fields `buildNumber`, `buildId`, `commit`, `digest`, `checksum`, `contractVersion`, `rollbackBuildId`, `accessScope`.
- CI flags: `--channel internal`, `--build-id`, `--rollback-build-id`; never Stable/Beta.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deployment origins/internal artifact -> invited tester | Misconfiguration could leak cookies or imply public trust. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-ORIGIN | Spoofing/Disclosure | Vercel surfaces | mitigate | Separate projects/origins/CSP/cookies/exact callbacks and artifact scans. |
| T-04-CHANNEL | Spoofing | desktop manifest | mitigate | Closed Internal channel schema, restricted access, exact identity/rollback, no public trust claims. |
</threat_model>
<verification>Run focused origin and Internal-manifest checks after each task, then run the complete security/account/admin cross-origin suites and deployed smoke as the plan gate; record runtimes.</verification>
<success_criteria>Every invited-testing surface is isolated, connected, restricted, and honest about non-production authority.</success_criteria>
<output>Create `04-23-SUMMARY.md` when done.</output>
