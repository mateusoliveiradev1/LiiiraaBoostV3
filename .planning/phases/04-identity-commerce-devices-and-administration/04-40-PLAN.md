---
phase: 04-identity-commerce-devices-and-administration
plan: '40'
type: execute
wave: 26
depends_on: ['04-38', '04-39', '04-61']
files_modified:
  - apps/api/src/staging/provision-invitations.ts
  - apps/api/src/staging/provision-invitations.test.ts
  - .github/workflows/staging-deploy.yml
  - .planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md
autonomous: false
requirements: [WEB-04, WEB-07, IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-07]
gap_closure: true
must_haves:
  truths:
    - 'Exactly three requested test identities receive single-use invitations without emails, raw invitation tokens, or passwords entering Git or logs.'
    - 'Render, Account Vercel and Admin Vercel run preview=false against the same real Neon staging authority.'
    - 'Web and desktop login, restart persistence and logout/revocation pass against deployed origins before Phase 4 UAT resumes.'
    - 'The published Admin origin proves invitations, governance/approvals, jobs, search, live/reconnecting/degraded states, incidents, configuration, privacy, and emergency controls against real PostgreSQL authority with fixtures disabled.'
  artifacts:
    - path: .planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md
      provides: Exact-origin, commit, build, database, authority, consent, audit, degraded-state, and rollback observations
    - path: apps/api/src/staging/provision-invitations.test.ts
      provides: Secret-safe idempotent bootstrap invitation preflight
  key_links:
    - from: .planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md
      to: tooling/web-evidence/tests/admin-operations.spec.ts
      via: exact commit and published-origin browser-to-API-to-PostgreSQL smoke receipts with preview/fixture flags false
    - from: apps/api/src/staging/provision-invitations.ts
      to: packages/control-plane-application/src/use-cases/manage-beta-invitations.ts
      via: bootstrap delegates to the same digest-only invitation authority rather than a parallel product path
---

<objective>Provision the three safe tester invitations, deploy the real composition, and prove the usable flow.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<tasks>
<task type="auto" tdd="true">
<name>Task 04-40-01: Add secret-driven idempotent invitation provisioning</name>
<read_first>apps/api/src/staging/invitations.ts; packages/control-plane-adapters/src/postgres/real-identity.ts; existing staging migration/deploy workflow</read_first>
<files>apps/api/src/staging/provision-invitations.ts, apps/api/src/staging/provision-invitations.test.ts, .github/workflows/staging-deploy.yml</files>
<action>Create an operator command reading three emails from secret environment input, generating cryptographically random single-use invite tokens, storing only digests, and writing raw URLs only to an explicitly protected local output. Re-running must not duplicate active invitations or reveal prior tokens.</action>
<verify><automated>rtk pnpm --filter @liiiraa/api exec vitest --run src/staging/provision-invitations.test.ts</automated></verify>
<acceptance_criteria>No email/token/password is tracked by Git, emitted by CI, or stored plaintext in PostgreSQL; ordinary app routes cannot issue invitations.</acceptance_criteria>
<done>The protected bootstrap can provision exactly the requested tester identities idempotently without creating a second invitation authority.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
<name>Task 04-40-02: Supply the three tester emails</name>
<read_first>apps/api/src/staging/provision-invitations.test.ts; .github/workflows/staging-deploy.yml secret-input contract; 04-USER-SETUP.md</read_first>
<files>.planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md</files>
<action>Request the three email addresses only after the secure provisioner is implemented and tested. Store them in the authorized deployment secret/provider input, never in repository files.</action>
<verify><automated>rtk pnpm --filter @liiiraa/api exec vitest --run src/staging/provision-invitations.test.ts</automated></verify>
<acceptance_criteria>The automated secret-path preflight is green before the user supplies protected input; the UAT record stores only opaque invitation references and the input-location receipt.</acceptance_criteria>
<done>All three addresses exist only in the authorized protected input and no address or token is added to tracked files or CI output.</done>
<resume-signal>Provide the three emails or confirm they were entered in the protected secret.</resume-signal>
</task>
<task type="checkpoint:human-verify" gate="blocking">
<name>Task 04-40-03: Deploy and validate the real web/desktop flow</name>
<read_first>04-61-SUMMARY.md; 04-VALIDATION.md staging rows; tooling/web-evidence/tests/admin-operations.spec.ts; .github/workflows/staging-deploy.yml; tooling/phase4-evidence/evidence-manifest.json</read_first>
<files>.planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md</files>
<action>Run Neon migrations, provision invites through the digest-only authority, deploy the immutable API, set Account/Admin preview and fixture flags false, deploy the isolated Vercel surfaces, and package desktop against the exact HTTPS origins. Run the published-origin automated smoke first and record commit, OCI digest, Vercel deployment IDs, Neon branch/migration receipt, desktop build, and contract hashes. Then present human UAT for signup/login/reload/restart, account mutations, desktop browser login and Credential Manager custody, logout/revocation, tester/admin isolation, consent revoke, invitation issue/resend/revoke, function switch, permission impact and independent approval, job progress/receipt, universal search authorization, live/reconnecting/degraded freshness, incident/configuration/privacy/emergency flows, and fail-closed mutations. Every observation must prove browser -> API -> PostgreSQL authority; a fixture response or optimistic fake success is a critical failure.</action>
<verify><automated>rtk pnpm phase4:verify -- --stage internal-staging &amp;&amp; rtk pnpm exec playwright test tooling/web-evidence/tests/admin-operations.spec.ts --grep @published-authority</automated></verify>
<acceptance_criteria>The exact published build passes real identity, consent, audit, invitation, governance, approval, jobs, live freshness, incidents, configuration, privacy, emergency, account, and desktop flows with preview/fixture flags false; reconnect/degraded mutations fail closed.</acceptance_criteria>
<done>04-REAL-AUTH-UAT.md contains immutable deployment identities, automated receipts, human pass/fail observations, and explicit evidence that PostgreSQL—not mocks—is final authority.</done>
<resume-signal>Type approved only after the exact deployed build passes every real-auth observation, or describe the failure.</resume-signal>
</task>
</tasks>
<artifacts_this_phase_produces>

- `04-REAL-AUTH-UAT.md`: exact deployed-origin evidence for identity, Admin invitations/governance/operations, consent, live degradation, account, desktop, and PostgreSQL authority.
- Secret-safe idempotent tester invitation bootstrap and its automated preflight.
  </artifacts_this_phase_produces>
<verification>Deployed API integration test plus human web/desktop test on exact immutable builds.</verification>
<success_criteria>Three invited users can create and use persistent staging accounts; mocks are disabled on deployed account/admin/desktop auth paths.</success_criteria>
<output>Create 04-40-SUMMARY.md when approved.</output>
