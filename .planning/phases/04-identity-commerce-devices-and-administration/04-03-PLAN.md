---
phase: 04-identity-commerce-devices-and-administration
plan: "03"
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - packages/contracts-source/src/control-plane.tsp
  - packages/contracts-source/src/main.tsp
  - packages/contracts-ts/src/generated/models.ts
  - packages/contracts-ts/src/generated/index.ts
  - packages/contracts-ts/src/generated/standalone-validators.js
  - packages/contracts-ts/src/generated/standalone-validators.d.ts
  - crates/contracts-rust/src/generated.rs
  - tooling/contract-compat/oasdiff.lock.json
  - tooling/contract-compat/src/check-openapi.ts
autonomous: true
requirements: [WEB-04, WEB-05, WEB-06, WEB-07, IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-05, IDEN-06, IDEN-07, IDEN-08, IDEN-09]
must_haves:
  truths:
    - "Every HTTP, desktop, entitlement, consent, commerce, and audit boundary uses one generated language-neutral contract source."
    - "Phase 3 FutureAuthorityCommand, NoChangeReceipt, and fixture AdminAuditEvent remain byte-compatible for Storybook/visual regression."
    - "Production authority records cannot carry fixture provenance or simulated no-change fields."
  artifacts:
    - path: packages/contracts-source/src/control-plane.tsp
      provides: Versioned closed models for Phase 4 commands, projections, receipts, errors, events, and offline entitlement bytes
    - path: tooling/contract-compat/src/check-openapi.ts
      provides: Pinned oasdiff 1.26.0 compatibility gate
  key_links:
    - from: packages/contracts-source/src/main.tsp
      to: packages/contracts-source/src/control-plane.tsp
      via: exactly one import
    - from: packages/contracts-source/src/control-plane.tsp
      to: packages/contracts-ts/src/generated/models.ts
      via: canonical generation, never handwritten DTO duplication
---

<objective>
Define and generate the complete Phase 4 transport contract without breaking Phase 3 deterministic fixtures.

Purpose: Give every downstream plan exact versioned interfaces and boundary validators.
Output: TypeSpec authority, generated TypeScript/Rust models, standalone validators, and OpenAPI compatibility gate.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/contracts-source/src/web.tsp
@packages/contracts-source/src/main.tsp
@architecture/decisions/0002-contract-versioning-and-compatibility.md
@.planning/phases/04-identity-commerce-devices-and-administration/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-03-01: Specify closed control-plane contracts</name>
  <read_first>packages/contracts-source/src/web.tsp; architecture/decisions/0002-contract-versioning-and-compatibility.md; 04-PATTERNS.md contract assignment</read_first>
  <files>packages/contracts-source/src/control-plane.tsp, packages/contracts-source/src/main.tsp</files>
  <behavior>- Valid identity/session/subscription/invoice/device/support/consent/admin projections admit bounded values.
- Invalid unknown states, duplicate bounded collections, fixture provenance, missing expectedVersion, and overlong identifiers fail validation.
- OfflineEntitlementEnvelope exposes opaque payloadBytes, signature, keyId, audience, device binding, issued/expires timestamps, and schema version without authorizing parsed-but-unverified claims.</behavior>
  <action>RED: add contract-admission tests/fixtures that demand every listed closed model, reject unknown states and fixture provenance, and require expectedVersion plus the D-22 seven-day envelope; run `pnpm test:contracts` and record the expected missing-schema failures. GREEN: add bounded scalars, closed unions, and authoritative command/projection/receipt/error/event models, including expected versions/ETags, correlation IDs, redacted failures, provider event identity, consent scope, audit references, and opaque signed bytes; import the source once and regenerate until the focused contract tests pass. REFACTOR: consolidate repeated bounded scalars without changing emitted schemas, preserve existing web.tsp models byte-for-byte, and rerun generation plus the focused tests.</action>
  <verify><automated>rtk pnpm test:contracts -- --run control-plane-contract</automated></verify>
  <acceptance_criteria>TypeSpec compiles, every new boundary is closed and bounded, and existing Phase 3 fixture schemas remain unchanged.</acceptance_criteria>
  <done>The canonical contract source covers all Phase 4 cross-process/network data.</done>
</task>

<task type="auto">
  <name>Task 04-03-02: Generate runtimes and enforce compatibility</name>
  <read_first>tooling/contract-generation/src/generate.ts; tooling/contract-compat/src/check-compat.ts; packages/contracts-ts/src/generated/index.ts; crates/contracts-rust/src/generated.rs</read_first>
  <files>packages/contracts-ts/src/generated/models.ts, packages/contracts-ts/src/generated/index.ts, packages/contracts-ts/src/generated/standalone-validators.js, packages/contracts-ts/src/generated/standalone-validators.d.ts, crates/contracts-rust/src/generated.rs, tooling/contract-compat/oasdiff.lock.json, tooling/contract-compat/src/check-openapi.ts</files>
  <action>Regenerate TypeScript/Rust outputs from TypeSpec, expose production validators separately from fixture validators, and add a checksummed/pinned oasdiff 1.26.0 runner for the first non-empty OpenAPI operations. Fail on drift or breaking changes; never patch generated output manually.</action>
  <verify><automated>rtk pnpm test:contracts</automated></verify>
  <acceptance_criteria>Generation is deterministic, TS/Rust validators agree on the corpus, and the OpenAPI compatibility job executes rather than reporting a missing-tool pass.</acceptance_criteria>
  <done>Generated consumers and compatibility evidence are ready for downstream plans.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- TypeSpec models: `AccountProjection`, `SessionProjection`, `SubscriptionProjection`, `InvoiceProjection`, `DeviceBindingProjection`, `SupportCaseProjection`, `DiagnosticConsent`, `AdminCommand`, `AuthorityReceipt`, `AuthorityError`, `ProviderEvent`, `AuditEvent`, `OfflineEntitlementEnvelope`.
- Fields: `expectedVersion`, `aggregateVersion`, `etag`, `correlationId`, `providerEventId`, `consentId`, `payloadBytes`, `signature`, `keyId`, `audience`, `deviceBinding`, `issuedAt`, `expiresAt`.
- CLI gate: `check-openapi --oasdiff-version 1.26.0`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted JSON/HTTP/IPC -> domain | External data must pass generated schema validation before mapping. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-03-T | Tampering | generated contracts | mitigate | TypeSpec-only authority, drift check, TS/Rust parity, compatibility gate. |
| T-04-03-I | Information Disclosure | errors/projections | mitigate | Bounded redacted errors and explicit minimization fields; no secrets/raw serials/card data. |
</threat_model>

<verification>Run `rtk pnpm test:contracts` and `rtk pnpm contracts:compat`.</verification>
<success_criteria>All generated artifacts are deterministic, compatible, and production/fixture authority remains disjoint.</success_criteria>
<output>Create `.planning/phases/04-identity-commerce-devices-and-administration/04-03-SUMMARY.md` when done.</output>
