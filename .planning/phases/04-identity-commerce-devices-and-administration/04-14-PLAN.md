---
phase: 04-identity-commerce-devices-and-administration
plan: "14"
type: tdd
wave: 4
depends_on: ["04-03", "04-04", "04-06", "04-32"]
files_modified:
  - packages/control-plane-domain/src/devices/device-binding.ts
  - packages/control-plane-domain/src/devices/device-binding.test.ts
  - packages/control-plane-application/src/use-cases/bind-device.ts
  - packages/control-plane-application/src/use-cases/transfer-device.ts
  - apps/api/src/modules/devices/routes.ts
  - apps/api/src/modules/devices/device-concurrency.test.ts
autonomous: true
requirements: [WEB-05, IDEN-04, IDEN-05]
must_haves:
  decision_ids: [D-23, D-24, D-25, D-26, D-27, D-28]
  truths:
    - "First Premium PC binding requires the user-confirmed friendly/protected identity and consequences, per D-23."
    - "Exactly one active Premium PC survives concurrent binds; account sessions remain independent."
    - "Cooldown, theft revocation, explainable revalidation, and single-use 24-hour support exception follow D-25 through D-28 without stranding the current PC."
  artifacts:
    - path: packages/control-plane-domain/src/devices/device-binding.ts
      provides: One-PC/cooldown/exception domain decisions
      exports: [decideDeviceBinding]
    - path: apps/api/src/modules/devices/device-concurrency.test.ts
      provides: Real PostgreSQL race evidence
  key_links:
    - from: bind-device.ts
      to: device_bindings
      via: entitlement row lock + expectedVersion + partial unique index + audit/outbox transaction
---
<objective>Implement transactionally race-safe one-PC binding, revocation, cooldown, and exception redemption.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@packages/control-plane-domain/src/devices/device-evidence.ts @packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-14-01: Serialize device binding and transfer decisions</name>
  <read_first>D-23 through D-28; device evidence decision; generated device contracts; PostgreSQL partial index</read_first>
  <files>packages/control-plane-domain/src/devices/device-binding.ts, device-binding.test.ts, packages/control-plane-application/src/use-cases/bind-device.ts, transfer-device.ts, apps/api/src/modules/devices/routes.ts, device-concurrency.test.ts</files>
  <behavior>- First bind requires explicit confirmation and active Premium.
- 20+ concurrent binds result in exactly one active device and typed conflicts for losers.
- Ordinary pre-cooldown transfer keeps current PC active and returns eligibility date.
- Theft revokes immediately but replacement waits unless a reviewed exception exists.
- Exception is single-use, expires at 24 hours, requires recent strong auth, and only the customer confirms replacement; old PC revokes atomically.
- Minor evidence change retains binding; substantial change enters revalidation.</behavior>
  <action>RED pure tables plus real concurrent transactions. GREEN by authorizing ownership/step-up, locking entitlement and expected version, evaluating the pure decision, and atomically writing binding/version/audit/outbox. Redeem exception with a locked one-use row. Return remote projection on conflict. REFACTOR shared cooldown timestamps and reason codes.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run device-concurrency</automated></verify>
  <acceptance_criteria>Race tests never produce two active PCs; every cooldown/theft/exception/revalidation result matches D-23–D-28.</acceptance_criteria>
  <done>WEB-05, IDEN-04, and the server half of IDEN-05 are transactional.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `DeviceBindingState`, `DeviceBindingCommand`, `decideDeviceBinding`, `bindDevice`, `transferDevice`.
- HTTP operations: device projection, confirm first bind, revoke, transfer eligibility, exception redeem, revalidation submit.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Concurrent clients/support exception -> entitlement | Races or forged exceptions could bypass one-PC policy. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-DEVICE-RACE | Tampering/Elevation | bind transaction | mitigate | Row lock, expected version, partial unique index, single-use exception, concurrency suite. |
</threat_model>
<verification>Run domain and real PostgreSQL concurrency suites.</verification>
<success_criteria>One-PC authority remains singular under races and every transfer path is explainable/audited.</success_criteria>
<output>Create `04-14-SUMMARY.md` with TDD gates.</output>
