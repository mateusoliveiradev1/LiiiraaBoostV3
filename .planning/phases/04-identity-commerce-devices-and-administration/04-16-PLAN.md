---
phase: 04-identity-commerce-devices-and-administration
plan: "16"
type: tdd
wave: 6
depends_on: ["04-11", "04-12", "04-15", "04-10", "04-32"]
files_modified:
  - packages/control-plane-domain/src/admin/authorization.ts
  - packages/control-plane-domain/src/admin/authorization.test.ts
  - packages/control-plane-application/src/use-cases/assume-admin-role.ts
  - packages/control-plane-application/src/use-cases/execute-admin-command.ts
  - apps/api/src/modules/admin/routes.ts
  - apps/api/src/modules/admin/admin-authorization.test.ts
autonomous: true
requirements: [WEB-06, WEB-07, IDEN-03]
must_haves:
  decision_ids: [D-08, D-09, D-37, D-38, D-39, D-42]
  truths:
    - "Support, Operations, Security, and Audit are separate singular active roles; no general super-admin exists, per D-08 and D-09."
    - "Critical commands require recent scoped passkey/MFA, reason, impact review, confirmation, and immutable event; bulk/irreversible commands are absent, per D-37 and D-38."
    - "Handoff minimizes/removes access and break-glass exposes only redacted metadata without diagnostic content, per D-39 and D-42."
  artifacts:
    - path: packages/control-plane-domain/src/admin/authorization.ts
      provides: Server-side role/resource/action/step-up policy
      exports: [authorizeAdminCommand, authorizeAdminProjection]
    - path: apps/api/src/modules/admin/routes.ts
      provides: Isolated admin-origin projection/command routes
  key_links:
    - from: execute-admin-command.ts
      to: audit chain
      via: authorization before detail; aggregate mutation and immutable audit append in one transaction
---
<objective>Implement isolated least-privilege administration with strong step-up, handoff, break-glass limits, and immutable audit.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/admin/src/admin-preview-model.ts @packages/control-plane-domain/src/audit/audit-chain.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-16-01: Authorize every admin projection and command server-side</name>
  <read_first>D-08, D-09, D-37 through D-39, D-42; ADMIN_ROLE_ROUTE_ACCESS UX map; generated AdminCommand; audit chain</read_first>
  <files>packages/control-plane-domain/src/admin/authorization.ts, authorization.test.ts, packages/control-plane-application/src/use-cases/assume-admin-role.ts, execute-admin-command.ts, apps/api/src/modules/admin/routes.ts, admin-authorization.test.ts</files>
  <behavior>- Developer identity assumes exactly one role and optional non-production Premium test grant; every assumption is audited.
- URL role never grants authority; denied records are filtered before query/search/detail.
- Critical command rejects absent/stale/wrong-action passkey/MFA, reason, impact review, or confirmation.
- Bulk/irreversible command IDs do not exist.
- Cross-role handoff requires reason and removes no-longer-justified access.
- Break-glass returns only minimum redacted metadata under strong auth/expiry/reason/alert/audit and never diagnostic content.</behavior>
  <action>RED with a role×resource×action×step-up table and PostgreSQL transactional tests. GREEN with one assumed-role session claim, authorization before loading sensitive detail, command policy map, atomic audit/outbox, explicit handoff, and bounded break-glass metadata. Exclude every bulk/irreversible operation until two-person approval exists. REFACTOR role projection filtering into a reusable server policy.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run admin-authorization</automated></verify>
  <acceptance_criteria>All unauthorized combinations see no denied record/detail, all critical requirements are enforced, and no super-admin/bulk bypass exists.</acceptance_criteria>
  <done>WEB-06, WEB-07, IDEN-03 administration authority is server-enforced and audited.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `AdminRole`, `AdminCommandPolicy`, `authorizeAdminProjection`, `authorizeAdminCommand`, `assumeAdminRole`, `executeAdminCommand`.
- HTTP operations: role assumption/release, role-filtered queues/detail, critical command, handoff, bounded break-glass metadata.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Admin browser/session -> control plane | Privileged actions and sensitive projections cross an isolated origin. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-ADMIN-AUTHZ | Elevation/Repudiation | admin API | mitigate | Singular role, server authorization before load, action-scoped step-up, reason/review/confirm, immutable audit. |
</threat_model>
<verification>Run role/action integration matrix.</verification>
<success_criteria>Administrative authority is isolated, least-privilege, consent-aware, and auditable.</success_criteria>
<output>Create `04-16-SUMMARY.md` with TDD gates.</output>
