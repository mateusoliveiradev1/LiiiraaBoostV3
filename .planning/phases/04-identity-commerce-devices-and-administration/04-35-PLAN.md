---
phase: 04-identity-commerce-devices-and-administration
plan: "35"
type: tdd
wave: 8
depends_on: ["04-17", "04-20", "04-34"]
files_modified:
  - apps/desktop/src-tauri/src/account_sync.rs
  - apps/desktop/src/account-authority.ts
  - apps/desktop/src/features/account-experience.tsx
  - packages/feature-shell/src/features/account-settings.tsx
  - apps/desktop/tests/browser/account-authority.spec.ts
autonomous: true
requirements: [WEB-04, IDEN-01, IDEN-02]
must_haves:
  decision_ids: [D-52, D-53, D-54, D-55, D-56]
  truths:
    - "Desktop replaces fixture identity, `.local` email, simulated Premium, and local-only device authority with real account projections and explicit online/offline/stale/pending states, per D-52 and D-53."
    - "Desktop synchronizes on launch, resume, reconnection, and relevant mutations; unconfirmed changes never render as successful authority, per D-54."
    - "A stale shared-data write preserves both the remote projection and safe local draft for user review, per D-55."
    - "Next-contact revocation signs the rendered account out and blocks new Premium work while warnings, history, and restoration remain available, per D-56."
  artifacts:
    - path: apps/desktop/src-tauri/src/account_sync.rs
      provides: Version-aware account projection and lifecycle synchronization orchestrator
      exports: [sync_account]
    - path: apps/desktop/src/account-authority.ts
      provides: Generated-command adapter and authoritative desktop account state
      exports: [DesktopAccountAuthority]
    - path: apps/desktop/tests/browser/account-authority.spec.ts
      provides: Real projection, lifecycle sync, conflict, and revocation browser evidence
  key_links:
    - from: apps/desktop/src/account-authority.ts
      to: apps/desktop/src-tauri/src/account_sync.rs
      via: generated Tauri IPC commands carrying versioned account projections and bounded authority errors
    - from: apps/desktop/src/features/account-experience.tsx
      to: apps/desktop/src/account-authority.ts
      via: lifecycle-triggered online/offline/stale/pending/conflict projection, never fixture authority
---

<objective>
Activate truthful shared-account synchronization and desktop account UI after native identity custody is established.

Purpose: Deliver D-52 through D-56 without mixing browser callback and credential-manager ownership into the React projection task.
Output: Version-aware native sync, generated desktop adapter, real account surfaces, and focused browser evidence.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-identity-commerce-devices-and-administration/04-CONTEXT.md
@.planning/phases/04-identity-commerce-devices-and-administration/04-PATTERNS.md
@.planning/phases/04-identity-commerce-devices-and-administration/04-20-SUMMARY.md
@.planning/phases/04-identity-commerce-devices-and-administration/04-17-SUMMARY.md
@apps/desktop/src/features/account-experience.tsx
@packages/feature-shell/src/features/account-settings.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-35-01: Synchronize and render authoritative desktop account truth</name>
  <read_first>D-52 through D-56; generated account/session/device/subscription contracts; Plan 04-17 version-conflict receipts; Plan 04-20 native credential/session commands; Phase 3 fixture composition rules</read_first>
  <files>apps/desktop/src-tauri/src/account_sync.rs, apps/desktop/src/account-authority.ts, apps/desktop/src/features/account-experience.tsx, packages/feature-shell/src/features/account-settings.tsx, apps/desktop/tests/browser/account-authority.spec.ts</files>
  <behavior>- Launch, resume, reconnection, and successful relevant mutation fetch the latest versioned account projection through the authenticated native boundary.
- Online, offline, stale, pending, conflict, and revoked states render explicitly; pending work never appears committed.
- Fixture name, `.local` email, simulated Premium, and local-only device authority do not enter the production composition while Storybook/test fixture composition remains available.
- A version conflict presents the remote value and preserves the safe local draft until the user explicitly resubmits.
- Next-contact revocation clears account authority and blocks the next new Premium action while warnings, history, diagnostics, and restoration remain reachable.</behavior>
  <action>RED: extend `account-authority.spec.ts` with lifecycle trigger, fixture-provenance rejection, pending/stale/conflict, remote-plus-draft, and next-contact revocation cases that fail before real sync/projection wiring exists. GREEN: implement `sync_account` over the Plan 04-20 credential boundary and Plan 04-17 versioned account API, expose a generated-contract `DesktopAccountAuthority`, and replace production fixture authority in both desktop account surfaces while retaining fixture-only Storybook/test entrypoints. REFACTOR: centralize lifecycle triggers and bounded user-facing authority verdicts, rerun the same focused `@authority-smoke` browser check, and record measured runtime below 30 seconds; run the complete account-authority, fixture guard, and desktop verification suites at the plan gate.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop exec playwright test tests/browser/account-authority.spec.ts --grep @authority-smoke --workers=1</automated></verify>
  <acceptance_criteria>The focused browser journey passes below 30 seconds; real projections replace production fixture authority; lifecycle sync, conflict preservation, and revocation behavior match D-52 through D-56.</acceptance_criteria>
  <done>Desktop account truth is real, version-aware, lifecycle-synchronized, explicit under degraded states, and local-first for safety data.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `apps/desktop/src-tauri/src/account_sync.rs`: `sync_account` lifecycle/version orchestrator and bounded conflict/revocation results.
- `apps/desktop/src/account-authority.ts`: `DesktopAccountAuthority`, lifecycle trigger types, and generated IPC/HTTP projection mapping.
- `apps/desktop/src/features/account-experience.tsx`: production account composition for online/offline/stale/pending/conflict/revoked states.
- `packages/feature-shell/src/features/account-settings.tsx`: shared truthful account/security/subscription/device rendering without production fixture provenance.
- `apps/desktop/tests/browser/account-authority.spec.ts`: focused and full projection, sync, conflict, and revocation witnesses.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Native authenticated account projection -> desktop React UI | Versioned remote authority crosses into a local-first renderer that retains safe drafts and safety data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-35-S | Spoofing | production account composition | mitigate | Generated projection validation, fixture-provenance rejection, and authenticated native command boundary. |
| T-04-35-T | Tampering | shared-data mutation | mitigate | Expected version, remote-plus-draft conflict result, and explicit resubmission. |
| T-04-35-D | Denial of Service | revocation/offline handling | mitigate | Block only new paid authority while preserving local warnings, history, diagnostics, and restoration. |
</threat_model>

<verification>
Record the focused `@authority-smoke` task check below 30 seconds, then run the complete desktop account-authority spec, production-to-fixture architecture guard, and `rtk pnpm --filter @liiiraa/desktop verify` as plan-level gates with measured runtimes.
</verification>

<success_criteria>
Desktop account state is authoritative, version-aware, lifecycle-synchronized, conflict-safe, revocation-aware, and does not displace local safety data.
</success_criteria>

<output>Create `.planning/phases/04-identity-commerce-devices-and-administration/04-35-SUMMARY.md` with TDD gates when done.</output>
