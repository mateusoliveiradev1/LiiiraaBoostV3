---
phase: 04-identity-commerce-devices-and-administration
plan: "07"
type: tdd
wave: 3
depends_on: ["04-02", "04-03", "04-06", "04-31"]
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - Cargo.lock
  - packages/contracts-ts/src/fixtures/offline-entitlement/manifest.json
  - packages/contracts-ts/src/fixtures/offline-entitlement/valid.json
  - packages/contracts-ts/src/fixtures/offline-entitlement/invalid.json
  - packages/contracts-ts/src/offline-entitlement.ts
  - packages/contracts-ts/src/offline-entitlement.test.ts
  - apps/desktop/src-tauri/src/offline_entitlement.rs
  - apps/desktop/src-tauri/tests/offline_entitlement.rs
autonomous: true
requirements: [IDEN-06]
must_haves:
  decision_ids: [D-22]
  truths:
    - "Premium offline authority is exactly seven days, never the superseded 30-day statement, per D-22."
    - "Signature verification covers the exact received payload bytes before parsing and rejects tamper, wrong key/account/device/version/time/rollback, per D-31."
  artifacts:
    - path: packages/contracts-ts/src/fixtures/offline-entitlement/manifest.json
      provides: Exact-byte cross-language corpus identities and hashes
    - path: apps/desktop/src-tauri/src/offline_entitlement.rs
      provides: verify_offline_entitlement before deserialization
      exports: [verify_offline_entitlement]
  key_links:
    - from: offline_entitlement.rs
      to: fixtures/offline-entitlement
      via: same raw byte files and public keys as TypeScript verifier
---
<objective>Establish exact-byte Ed25519 parity for the seven-day offline entitlement.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@packages/contracts-source/src/control-plane.tsp @crates/contracts-rust/src/lib.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-07-01: Verify signed offline entitlement bytes across TypeScript and Rust</name>
  <read_first>Generated OfflineEntitlementEnvelope; 04-RESEARCH.md Pattern 4 and Pitfall 6; contracts Rust validation-before-deserialization pattern</read_first>
  <files>apps/desktop/src-tauri/Cargo.toml, Cargo.lock, packages/contracts-ts/src/fixtures/offline-entitlement/manifest.json, packages/contracts-ts/src/fixtures/offline-entitlement/valid.json, packages/contracts-ts/src/fixtures/offline-entitlement/invalid.json, packages/contracts-ts/src/offline-entitlement.ts, packages/contracts-ts/src/offline-entitlement.test.ts, apps/desktop/src-tauri/src/offline_entitlement.rs, apps/desktop/src-tauri/tests/offline_entitlement.rs</files>
  <behavior>- Canonical valid bytes verify in both runtimes for issuedAt through issuedAt+7 days.
- One-byte tamper, altered whitespace/reserialization, unknown/wrong/retired key, wrong account/device/audience/version, expiry, future issue, and clock rollback fail.
- Previous key is accepted only inside its declared rotation window.
- Failure yields online-verification-required without fraud accusation.</behavior>
  <action>RED: expand the Wave 0 cross-language witness into a hashed positive/negative corpus and record the expected missing-verifier failures. GREEN: add exact `ed25519-dalek@3.0.0` and `base64@0.22.1` pins to the desktop crate, update Cargo.lock, then implement Node built-in crypto fixture signing and Rust verification over decoded opaque payload bytes before JSON parsing; validate the generated schema only after signature and persist last trusted time monotonically through an injected store. REFACTOR: share verdict codes/key-ring fixtures, rerun both runtimes plus `cargo build`, and verify Cargo supply-chain evidence.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contracts-ts test -- --run offline-entitlement && rtk cargo test -p liiiraa-desktop offline_entitlement && rtk cargo build -p liiiraa-desktop</automated></verify>
  <acceptance_criteria>Both runtimes produce identical verdict codes for every corpus record and enforce exactly 604800 seconds.</acceptance_criteria>
  <done>IDEN-06 cryptographic and cross-language evidence is green.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- TS `verifyOfflineEntitlementBytes`, `OfflineEntitlementVerdict`; Rust `verify_offline_entitlement`, `OfflineEntitlementVerdict`.
- Fixture IDs for valid, tamper, reserialized, wrong-key, wrong-device, wrong-account, wrong-version, clock-rollback, and expiry cases.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stored/downloaded entitlement bytes -> desktop authority | Local bytes may be forged, replayed, or rolled back. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-OFFLINE-TOKEN | Tampering | entitlement verifier | mitigate | Exact-byte Ed25519 verification before parsing plus key/audience/device/version/time checks. |
</threat_model>
<verification>Execute both runtimes against the same fixture manifest.</verification>
<success_criteria>Seven-day offline claims cannot be forged by JSON mutation or cross-runtime ambiguity.</success_criteria>
<output>Create `04-07-SUMMARY.md` with TDD gates.</output>
