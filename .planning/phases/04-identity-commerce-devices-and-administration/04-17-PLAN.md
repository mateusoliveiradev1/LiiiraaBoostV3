---
phase: 04-identity-commerce-devices-and-administration
plan: "17"
type: tdd
wave: 6
depends_on: ["04-12", "04-13", "04-14", "04-15", "04-32"]
files_modified:
  - packages/control-plane-application/src/use-cases/project-account.ts
  - packages/control-plane-application/src/use-cases/update-account.ts
  - packages/control-plane-application/src/use-cases/project-account.test.ts
  - apps/api/src/modules/account/routes.ts
  - apps/api/src/modules/account/account-projection.test.ts
autonomous: true
requirements: [WEB-04, WEB-05, IDEN-01, IDEN-02, IDEN-04]
must_haves:
  decision_ids: [D-52, D-54, D-55]
  truths:
    - "Profile, security, sessions, billing, invoices, support, and linked-device shared truth comes from PostgreSQL while local diagnostics/history/restoration stay local, per D-52."
    - "Every write requires expectedVersion; stale writes return remote projection while preserving the client draft, per D-55."
    - "Payment/device/security mutations remain pending until authoritative projection confirms them."
  artifacts:
    - path: packages/control-plane-application/src/use-cases/project-account.ts
      provides: Complete minimized AccountProjection assembler
      exports: [projectAccount]
    - path: packages/control-plane-application/src/use-cases/update-account.ts
      provides: Version-aware account mutation boundary
      exports: [updateAccount]
  key_links:
    - from: apps/api/src/modules/account/routes.ts
      to: project-account.ts
      via: owner-authorized generated projection with ETag/aggregateVersion
---
<objective>Expose one truthful, version-aware account projection that joins all Phase 4 authorities without synchronizing local-only PC history.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/account/src/account-preview-model.ts @packages/control-plane-application/src/use-cases</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-17-01: Project and mutate shared account truth with conflicts</name>
  <read_first>D-52 through D-55; AccountHomeScenario contradiction pattern; generated AccountProjection; identity/commerce/device/support use cases</read_first>
  <files>packages/control-plane-application/src/use-cases/project-account.ts, update-account.ts, project-account.test.ts, apps/api/src/modules/account/routes.ts, account-projection.test.ts</files>
  <behavior>- Owner projection includes identity, plan, security methods, sessions, billing/invoices, support, and active device; excludes technical activity/unshared diagnostics/history/restoration.
- Contradictory pending/active billing, plan, device, or recommendation states reject.
- Current expectedVersion commits and returns new ETag; stale version returns conflict with remote projection and untouched localDraft token.
- Pending provider/device/security actions never appear successful before reconciliation.
- Projection is minimized and user-scoped.</behavior>
  <action>RED with projection contradiction tables, ownership tests, and concurrent expected-version writes. GREEN by authorizing owner before loading, reading an atomic snapshot, mapping domain states into generated projection, and transactionally updating only the named aggregate. Return typed 409 with remote projection and opaque draft correlation. REFACTOR shared projection provenance (`online`, `offline`, `stale`, `pending`, `conflict`).</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run account-projection</automated></verify>
  <acceptance_criteria>Complete account truth is consistent, minimized, versioned, and conflict-preserving under concurrent writes.</acceptance_criteria>
  <done>WEB-04 account authority is ready for web and desktop consumers.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `projectAccount`, `updateAccount`, `AccountProjectionProvenance`, HTTP GET/PATCH account projection with `ETag` and `If-Match`/`expectedVersion`.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Multiple clients -> shared account aggregate | Stale or cross-user writes could overwrite authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-ACCOUNT-VERSION | Tampering | account mutation | mitigate | Owner admission, expectedVersion/ETag, atomic snapshot, remote projection conflict. |
</threat_model>
<verification>Run account projection/concurrency suite.</verification>
<success_criteria>One complete shared projection serves both clients without last-write-wins or local-data upload.</success_criteria>
<output>Create `04-17-SUMMARY.md` with TDD gates.</output>
