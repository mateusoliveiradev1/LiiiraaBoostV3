---
phase: 04-identity-commerce-devices-and-administration
plan: "22"
type: execute
wave: 7
depends_on: ["04-13", "04-15", "04-16", "04-17", "04-27", "04-28", "04-29", "04-30"]
files_modified:
  - apps/api/Dockerfile
  - apps/api/.dockerignore
  - apps/api/src/config/env.ts
  - apps/api/src/app.ts
  - apps/api/src/server.ts
  - apps/api/src/staging/seed.ts
  - apps/api/src/staging/invitations.ts
  - apps/api/staging.render.yaml
  - .github/workflows/phase-4-staging-api.yml
autonomous: true
requirements: [WEB-04, WEB-06, IDEN-01, IDEN-09]
user_setup:
  - service: Neon
    why: Synthetic isolated development/preview/staging PostgreSQL branches
    env_vars:
      - name: STAGING_DATABASE_URL
        source: Neon staging branch connection details
    dashboard_config:
      - task: Create isolated synthetic-only dev, preview, and staging branches
        location: Neon project branching settings
  - service: Stripe
    why: Sandbox commerce and signed webhook reconciliation
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: Stripe sandbox API keys
      - name: STRIPE_WEBHOOK_SECRET
        source: Stripe sandbox webhook endpoint
    dashboard_config:
      - task: Register only the staging API webhook endpoint in test mode
        location: Stripe Developers -> Webhooks
  - service: AWS SES and S3
    why: Sandbox email, private support objects, immutable audit anchors
    env_vars:
      - name: AWS_REGION
        source: Selected sandbox region
      - name: SUPPORT_BUCKET
        source: Private staging attachment bucket
      - name: AUDIT_ANCHOR_BUCKET
        source: Object-Lock staging audit bucket
    dashboard_config:
      - task: Verify invited tester recipients while SES remains sandboxed
        location: AWS SES verified identities
      - task: Create separate lifecycle attachment and Object-Lock anchor buckets with role-based access
        location: AWS S3
  - service: Render and GHCR
    why: Deploy the immutable container-compatible staging API artifact
    env_vars:
      - name: STAGING_API_ORIGIN
        source: Render service URL
    dashboard_config:
      - task: Grant Render read-only access to the private GHCR image by digest
        location: Render registry credentials
must_haves:
  decision_ids: [D-09, D-44, D-45, D-57, D-58, D-59, D-60, D-63]
  truths:
    - "A remotely hosted Fastify staging API runs the same immutable OCI artifact intended for later ECS/Fargate, per D-57 and D-58."
    - "Local development runs Node directly against synthetic Neon and Docker Desktop remains optional, per D-59."
    - "Staging identities are invitation-only and isolated; no production clone/public registration/real money exists, per D-44, D-60, D-63."
  artifacts:
    - path: apps/api/Dockerfile
      provides: Reproducible container-compatible API/worker image
    - path: apps/api/src/config/env.ts
      provides: Fail-closed non-production environment admission
      exports: [admitApiEnvironment]
    - path: apps/api/src/staging/invitations.ts
      provides: Single-use invited tester provisioning
  key_links:
    - from: .github/workflows/phase-4-staging-api.yml
      to: apps/api/Dockerfile
      via: build once, attest/scan, publish GHCR, deploy Render by immutable digest
---
<objective>Deploy the invitation-only synthetic Fastify/PostgreSQL/provider control plane as a promotable OCI artifact.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/api/src/worker.ts @.github/workflows/ci.yml @.planning/phases/04-identity-commerce-devices-and-administration/04-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-22-01: Compose and fail-close the staging API environment</name>
  <read_first>D-44, D-57 through D-60, D-63; API modules/worker; provider port configuration; generated contracts</read_first>
  <files>apps/api/src/config/env.ts, apps/api/src/app.ts, apps/api/src/server.ts, apps/api/src/staging/seed.ts, apps/api/src/staging/invitations.ts</files>
  <behavior>- Missing/wildcard origin, non-synthetic database, live Stripe key, production AWS resource, public signup, or Stable/Beta channel refuses startup.
- Exact public/account/admin/desktop staging origins are admitted.
- Seed creates isolated synthetic developer/tester identities and no shared account.
- Invitation is single-use/expiring and creates one isolated dataset.
- API starts directly under Node/pnpm without Docker.</behavior>
  <action>RED: create environment/seed/invite composition tests that expect startup rejection for every forbidden origin/database/provider/channel state and fail because app composition is absent. GREEN: compose Fastify modules plus the entitlement, email, lifecycle, and anchor workers behind generated validation, exact CORS/origin/cookie policies, sandbox adapters, and synthetic database assertion; add deterministic seeds and single-use invitations until the focused suite passes. REFACTOR: centralize environment admission and module registration, rerun the same measured sub-30-second suite, and retain the explicit D-09 developer test grant/singular-role rules without accepting live keys or production data.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run staging-config</automated></verify>
  <acceptance_criteria>Only exact synthetic/sandbox/invited configurations start; all production/public authority sentinels fail before listening.</acceptance_criteria>
  <done>The complete API runs locally and remotely under the same fail-closed configuration contract.</done>
</task>
<task type="auto">
  <name>Task 04-22-02: Build, attest, publish, and deploy the immutable API image</name>
  <read_first>apps/api/package.json; existing CI SHA-pinned action pattern; Render digest deployment research</read_first>
  <files>apps/api/Dockerfile, apps/api/.dockerignore, apps/api/staging.render.yaml, .github/workflows/phase-4-staging-api.yml</files>
  <action>Create a non-root multi-stage Node 24.18.0/pnpm 11.17.0 image running Fastify/worker entrypoints with health/readiness checks. CI verifies pins/contracts/tests/migrations, generates SBOM/provenance, scans, publishes GHCR, and deploys Render strictly by digest with protected staging environment/OIDC or short-lived credentials. Local image build is optional; CI is authoritative.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run container-contract && rtk node tooling/ci/verify-required-artifacts.mjs --ci .github/workflows/phase-4-staging-api.yml</automated></verify>
  <acceptance_criteria>One digest identifies tested/published/deployed API, health/readiness/migration checks pass, and mutable tags cannot promote.</acceptance_criteria>
  <done>The staging control plane is remotely reachable through an immutable promotable artifact.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `admitApiEnvironment`, `seedSyntheticStaging`, `issueInvitation`, `redeemInvitation`, Fastify `buildApp`/`startServer`.
- Environment keys: `STAGING_DATABASE_URL`, exact surface origins, sandbox provider keys/buckets, `STAGING_BUILD_ID`.
- OCI labels: revision, source, created, SBOM/provenance references; Render digest manifest.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI/providers/invites -> staging control plane | Secrets, data, and deployed artifact cross external systems. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-STAGING-DATA | Disclosure | env/seed | mitigate | Synthetic-only URL/resource/live-key refusal and isolated invitation datasets. |
| T-04-IMAGE | Tampering | OCI deployment | mitigate | Exact toolchains, SBOM/provenance/scan, deploy tested digest only. |
</threat_model>
<verification>Run API staging config/container contract and CI reachability gates, then hosted health/migration smoke.</verification>
<success_criteria>The complete sandbox control plane is remote, invitation-only, synthetic, and artifact-identical.</success_criteria>
<output>Create `04-22-SUMMARY.md` when done.</output>
