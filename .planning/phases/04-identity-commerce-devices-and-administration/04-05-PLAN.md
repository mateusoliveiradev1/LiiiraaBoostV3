---
phase: 04-identity-commerce-devices-and-administration
plan: "05"
type: tdd
wave: 2
depends_on: ["04-01", "04-02", "04-03", "04-33"]
files_modified:
  - packages/control-plane-application/src/ports/identity.ts
  - packages/control-plane-adapters/src/identity/better-auth-spike.ts
  - packages/control-plane-adapters/src/identity/better-auth.spike.test.ts
  - tooling/identity-adapter-spike/package.json
  - pnpm-lock.yaml
  - .planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md
  - .planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-REPLAN-REQUIRED.md
autonomous: false
requirements: [IDEN-01, IDEN-02, IDEN-03]
must_haves:
  decision_ids: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]
  truths:
    - "The final identity framework is accepted only if verified email/password, Google, Discord, passkeys, TOTP/recovery codes, action-scoped step-up, reviewed recovery, revocation, abuse controls, and Windows PKCE all pass D-01 through D-10."
    - "A failing Better Auth invariant terminates downstream identity execution and requires replacement research/plans; framework capability is never treated as product-policy proof."
  artifacts:
    - path: packages/control-plane-application/src/ports/identity.ts
      provides: IdentityProviderPort independent of Better Auth
      exports: [IdentityProviderPort, IdentityProviderResult]
    - path: .planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md
      provides: PASS/REJECT evidence matrix and chosen adapter decision
  key_links:
    - from: better-auth-spike.ts
      to: IdentityProviderPort
      via: adapter cannot leak Better Auth session/provider objects
    - from: 04-IDENTITY-SPIKE.md
      to: 04-04-PLAN.md
      via: only an explicitly approved PASS checkpoint permits production package/schema adoption
---

<objective>Terminate the Better Auth uncertainty before production dependency adoption or identity-schema commitment, with a blocking replacement-planning branch on REJECT.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/04-identity-commerce-devices-and-administration/04-RESEARCH.md @packages/web-features/src/preview-machine.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 04-05-01: Prove or reject the Better Auth adapter</name>
  <read_first>04-RESEARCH.md Open Question 1 and Mandatory Security Spikes; generated identity contracts; packages/web-features/src/preview-machine.ts action policies</read_first>
  <files>packages/control-plane-application/src/ports/identity.ts, packages/control-plane-adapters/src/identity/better-auth-spike.ts, packages/control-plane-adapters/src/identity/better-auth.spike.test.ts, tooling/identity-adapter-spike/package.json, pnpm-lock.yaml, .planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md</files>
  <behavior>- Unverified email cannot create an authoritative session; verified email, Google, Discord, and passkey can.
- MFA step-up covers password, social, and passkey sessions and accepts only TOTP/passkey/recovery code.
- Recovery without all factors enters review and a 24-hour critical-action hold with trusted-session contest notice.
- Desktop flow uses external browser, authorization code, S256 PKCE, random state, exact issuer/redirect, one-shot callback, no client secret, and revocable sessions.
- Enumeration, replay, redirect confusion, stale step-up, and rate-abuse cases fail closed.</behavior>
  <action>RED: expand the Wave 0 spike witness into the complete matrix through `IdentityProviderPort`, run it before the probe exists, and record the expected failures for native PKCE, cross-method step-up, recovery, revocation, and abuse cases. GREEN: install the approved Better Auth identities only in `tooling/identity-adapter-spike`, build a bounded probe using documented APIs and a Windows loopback harness whose authorization code is exchanged by the backend, and rerun until every row has executable evidence. REFACTOR: remove all framework types from the port and production package graph, rerun the focused suite, then record PASS only when every row is green; otherwise record REJECT with exact failed invariants.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/control-plane-adapters test -- --run better-auth.spike</automated></verify>
  <acceptance_criteria>The evidence file gives a binary PASS/REJECT for every locked identity behavior; a PASS has no untested method-wide MFA or native-client caveat.</acceptance_criteria>
  <done>Better Auth is either approved behind the port or explicitly rejected without changing product behavior.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 04-05-02: Accept PASS or stop for replacement identity planning</name>
  <read_first>.planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md; D-01 through D-10; 04-RESEARCH.md Open Questions (RESOLVED)</read_first>
  <files>.planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md, .planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-REPLAN-REQUIRED.md</files>
  <action>Run the complete spike suite and present the binary evidence matrix. If every row is PASS, record explicit approval to unblock Plan 04-04 and downstream identity plans. If any row is REJECT, write 04-IDENTITY-REPLAN-REQUIRED.md with failed invariants and the unchanged IdentityProviderPort contract, leave this checkpoint incomplete, and require `$gsd-plan-phase 4 --research` plus replacement plans before execution resumes; do not treat rejection as phase progress.</action>
  <what-built>An isolated identity adapter verdict that precedes every production identity dependency and schema decision.</what-built>
  <how-to-verify>Confirm that each D-01–D-10 row links to an executable result, the desktop authorization code is exchanged only by the API, and no failed/untested row is labeled PASS.</how-to-verify>
  <resume-signal>Type "approve PASS" only when all rows pass; on REJECT run replacement research/planning and do not approve this checkpoint.</resume-signal>
  <verify><automated>rtk pnpm --filter @liiiraa/control-plane-adapters test -- --run better-auth.spike</automated></verify>
  <acceptance_criteria>Only an all-green PASS unblocks 04-04; any REJECT leaves the phase blocked on executable replacement plans.</acceptance_criteria>
  <done>The identity adapter is approved for production adoption, or the plan remains blocked with a concrete replacement-planning artifact.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `IdentityProviderPort`: begin/complete sign-in, verify email, enroll factor, step-up, revoke/list sessions, begin/complete recovery.
- `IdentityProviderResult`: closed success/failure algebra with redacted codes.
- `04-IDENTITY-SPIKE.md`: per-method PASS/REJECT table and adapter verdict.
- `04-IDENTITY-REPLAN-REQUIRED.md`: rejection evidence and mandatory replacement-planning input, created only on REJECT.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser/provider -> identity adapter | OAuth codes, assertions, factors, and recovery evidence are untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-OAUTH | Spoofing | native OAuth | mitigate | PKCE S256, state, issuer, exact redirect, one-shot listener, external browser. |
| T-04-RECOVERY | Elevation of Privilege | recovery | mitigate | Reviewed recovery, 24-hour hold, trusted-session contest, generic responses. |
</threat_model>
<verification>Focused spike suite must pass all rows; inspect RED then GREEN commits.</verification>
<success_criteria>Identity framework uncertainty is resolved by evidence, not assumption.</success_criteria>
<output>Create `04-05-SUMMARY.md` with RED/GREEN/REFACTOR commits.</output>
