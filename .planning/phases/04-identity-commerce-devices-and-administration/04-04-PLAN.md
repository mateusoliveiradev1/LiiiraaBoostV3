---
phase: 04-identity-commerce-devices-and-administration
plan: "04"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-05"]
files_modified:
  - apps/api/package.json
  - packages/control-plane-adapters/package.json
  - architecture/dependency-allowlist.json
  - architecture/dependency-review.md
  - pnpm-lock.yaml
  - packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql
  - packages/control-plane-adapters/src/postgres/database.ts
  - packages/control-plane-adapters/src/postgres/migrate.ts
  - packages/control-plane-adapters/src/postgres/migrations.test.ts
autonomous: true
requirements: [WEB-04, WEB-05, WEB-06, WEB-07, IDEN-04, IDEN-09]
must_haves:
  decision_ids: [D-43, D-44, D-45, D-46, D-50]
  truths:
    - "PostgreSQL is the sole authority for Phase 4 shared state and cache cannot satisfy an authority read, per D-43."
    - "Synthetic Neon and isolated PostgreSQL migrations are repeatable without requiring Docker Desktop, per D-44 and D-45."
    - "Audit rows are insert-only, corrections append, and prohibited sensitive values have no schema columns, per D-46 and D-50."
  artifacts:
    - path: packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql
      provides: Identity, session, commerce, device, support, consent, inbox/outbox, audit, deletion, and retention schema
      contains: CREATE UNIQUE INDEX
    - path: packages/control-plane-adapters/src/postgres/migrate.ts
      provides: migrateControlPlane and schemaHash migration runner
      exports: [migrateControlPlane, inspectControlPlaneSchema]
  key_links:
    - from: packages/control-plane-adapters/src/postgres/migrate.ts
      to: 0001_control_plane.sql
      via: ordered checksum-verified migration execution
    - from: device_bindings
      to: premium_entitlements
      via: foreign key plus partial unique active binding index
---

<objective>
Install the approved server dependencies and establish the authoritative PostgreSQL schema with executable migration proof.

Purpose: Make transactions, idempotency, one-PC concurrency, audit integrity, and retention enforceable in real PostgreSQL.
Output: Exact dependency graph, reviewed SQL migration, migration runner, and integration tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-identity-commerce-devices-and-administration/04-DEPENDENCY-APPROVAL.md
@apps/api/src/testing/postgres.ts
@architecture/module-boundaries.json
</context>

<tasks>

<task type="auto">
  <name>Task 04-04-01: Install only approved exact dependencies</name>
  <read_first>04-DEPENDENCY-APPROVAL.md; architecture/dependency-allowlist.json; apps/api/package.json; packages/control-plane-adapters/package.json</read_first>
  <files>apps/api/package.json, packages/control-plane-adapters/package.json, architecture/dependency-allowlist.json, architecture/dependency-review.md, pnpm-lock.yaml</files>
  <action>Require the approved 04-01 package record and a PASS verdict from the terminating 04-05 identity checkpoint. Then add the exact audited Fastify, CORS, helmet, Kysely, pg, Testcontainers, approved Better Auth/passkey/oauth-provider adapter dependencies, Stripe, S3, and SES pins to their narrowest production package. Regenerate allowlist/review evidence and the frozen lockfile with lifecycle scripts disabled. Do not accept version substitution or carry an identity package into production after a REJECT verdict.</action>
  <verify><automated>rtk pnpm supply-chain:check</automated></verify>
  <acceptance_criteria>Every installed external identity/version is approved, exact, allowlisted, lockfile-resolved, and has no lifecycle-script execution.</acceptance_criteria>
  <done>The Phase 4 dependency graph is reproducible and review-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04-02: Apply and verify the control-plane migration</name>
  <read_first>04-RESEARCH.md transactional pattern; apps/api/src/testing/postgres.ts; packages/control-plane-adapters/src/postgres/database.ts</read_first>
  <files>packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql, packages/control-plane-adapters/src/postgres/database.ts, packages/control-plane-adapters/src/postgres/migrate.ts, packages/control-plane-adapters/src/postgres/migrations.test.ts</files>
  <behavior>- Fresh PostgreSQL migrates once and re-run is idempotent.
- Concurrent active device rows for one Premium entitlement violate the partial unique index.
- Provider inbox identity, aggregate versions, audit sequence/hash, outbox claim, consent scope, deletion/retention fields, and insert-only audit privileges exist.
- Schema inspection finds no raw serial, full card, bearer token, password plaintext, or diagnostic blob columns.</behavior>
  <action>RED: create fresh/upgrade/replay, privilege, constraint, schema-minimization, and concurrent-active-device migration tests; run them against the isolated PostgreSQL harness and record the expected missing-migration failures. GREEN: implement explicit SQL with UUID/UTC/check constraints, expected-version columns, provider/event uniqueness, row-lockable entitlement heads, partial unique active-device index, append-only audit privileges, lifecycle state, and minimized object metadata; add checksum execution and Neon compatibility until the focused migration suite passes. REFACTOR: consolidate inspection helpers and migration metadata without weakening SQL constraints, then rerun the same live PostgreSQL suite and schema hash check.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api db:migrate:test</automated></verify>
  <acceptance_criteria>A fresh and upgraded live PostgreSQL database reaches the expected schema hash; rollback/reapply test and privilege/constraint probes pass.</acceptance_criteria>
  <done>The authoritative schema is migrated and integration-verified.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- SQL objects: `identities`, `sessions`, `security_factors`, `recovery_holds`, `subscriptions`, `invoices`, `provider_inbox`, `outbox_jobs`, `premium_entitlements`, `device_bindings`, `support_cases`, `case_messages`, `diagnostic_consents`, `object_metadata`, `audit_events`, `audit_chain_heads`, `deletion_requests`.
- Functions: `migrateControlPlane`, `inspectControlPlaneSchema`; CLI script `db:migrate:test`.
- Constraint: one active device per Premium entitlement partial unique index.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API credentials -> PostgreSQL | Application writes authoritative customer/security state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04-T | Tampering | migrations/audit rows | mitigate | Checksummed ordered migrations, insert-only audit role, append correction model. |
| T-04-04-E | Elevation of Privilege | device uniqueness | mitigate | Aggregate locks plus partial unique index and version columns. |
| T-04-SC | Tampering | package install | mitigate | Plan 04-01 approval, exact pins, frozen lockfile, disabled scripts. |
</threat_model>

<verification>Run migration integration tests against real PostgreSQL plus supply-chain and architecture gates.</verification>
<success_criteria>The approved stack installs reproducibly and the authoritative schema is demonstrably live.</success_criteria>
<output>Create `.planning/phases/04-identity-commerce-devices-and-administration/04-04-SUMMARY.md` when done.</output>
