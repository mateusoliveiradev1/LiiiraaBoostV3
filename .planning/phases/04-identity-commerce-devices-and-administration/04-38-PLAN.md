---
phase: 04-identity-commerce-devices-and-administration
plan: '38'
type: execute
wave: 13
depends_on: ['04-37']
files_modified:
  - apps/desktop/src-tauri/src/identity.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src/desktop-auth.ts
  - apps/desktop/src/features/account-experience.tsx
  - apps/desktop/tests/browser/desktop-auth.spec.ts
autonomous: true
requirements: [IDEN-01, IDEN-02, IDEN-03, IDEN-04]
gap_closure: true
must_haves:
  truths:
    - 'Desktop login opens the system browser, completes one-time PKCE/loopback exchange, and writes only the opaque session credential to Windows Credential Manager.'
    - 'The renderer never receives, stores, logs, or clips the credential and demo mode is never mistaken for an authenticated account.'
---

<objective>Connect the existing native PKCE and credential-custody modules to the desktop login UI and live API.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<tasks>
<task type="auto" tdd="true">
<name>Task 04-38-01: Register a native end-to-end desktop authentication command</name>
<read_first>apps/desktop/src-tauri/src/identity.rs; apps/desktop/src-tauri/src/credential_store.rs; apps/desktop/src-tauri/src/main.rs; apps/desktop/src-tauri/src/account_sync.rs</read_first>
<files>apps/desktop/src-tauri/src/identity.rs, apps/desktop/src-tauri/src/main.rs</files>
<action>Implement bounded HTTPS calls for authorization and exchange, bind the loopback listener before requesting authorization, open only the admitted system-browser URL, receive once with timeout, exchange once, write credential through CredentialStore, and return only the generated session projection/status to the renderer. Register sign-in and sign-out commands.</action>
<verify><automated>rtk cargo test -p liiiraa-desktop identity</automated></verify>
<acceptance_criteria>State/issuer/redirect/code mismatch, replay, timeout, non-HTTPS API and invalid exchange all fail closed; tests prove Debug/serialized responses redact credentials.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
<name>Task 04-38-02: Replace the desktop login preview with native authority</name>
<read_first>apps/desktop/src/features/account-experience.tsx; apps/desktop/src/account-authority.ts</read_first>
<files>apps/desktop/src/desktop-auth.ts, apps/desktop/src/features/account-experience.tsx, apps/desktop/tests/browser/desktop-auth.spec.ts</files>
<action>Invoke native sign-in from the login form, show opening/waiting/success/failure states, navigate only after a real session, synchronize the account immediately, and invoke native revocation on logout. Keep an explicitly labeled offline demo path that never unlocks authenticated account/Premium claims.</action>
<verify><automated>rtk pnpm --filter @liiiraa/desktop exec playwright test tests/browser/desktop-auth.spec.ts</automated></verify>
<acceptance_criteria>The former “prepared” copy and local-account shortcut are absent from the production login path; renderer storage contains no credential.</acceptance_criteria>
</task>
</tasks>
<verification>Run Rust identity/account tests, desktop type/lint/build and focused Playwright.</verification>
<success_criteria>A Windows user signs in through the browser and the desktop reloads a real account from Credential Manager custody.</success_criteria>
<output>Create 04-38-SUMMARY.md.</output>
