---
phase: 04-identity-commerce-devices-and-administration
plan: "24"
type: execute
wave: 10
depends_on: ["04-22", "04-23"]
files_modified:
  - tooling/phase4-evidence/package.json
  - tooling/phase4-evidence/src/evaluate.ts
  - tooling/phase4-evidence/src/evaluate.test.ts
  - tooling/phase4-evidence/evidence-manifest.json
  - tooling/phase4-evidence/real-pc-coverage.json
  - architecture/module-boundaries.json
  - package.json
autonomous: true
requirements: [WEB-04, WEB-05, WEB-06, WEB-07, IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-05, IDEN-06, IDEN-07, IDEN-08, IDEN-09]
must_haves:
  decision_ids: [D-64, D-65, D-66, D-67, D-68]
  truths:
    - "Every requirement and critical security/privacy/billing/device/data/recovery/accessibility gate has immutable evidence tied to one build."
    - "Real-PC sufficiency is a coverage matrix across D-67 dimensions, never a tester-count claim."
    - "A critical failure downgrades the exact build and later passes cannot erase it, per D-64 and D-65."
  artifacts:
    - path: tooling/phase4-evidence/src/evaluate.ts
      provides: Fail-closed promotion/evidence evaluator
      exports: [evaluatePhase4Evidence]
    - path: tooling/phase4-evidence/real-pc-coverage.json
      provides: Windows/hardware/journey coverage matrix
  key_links:
    - from: evidence-manifest.json
      to: deployed build identities
      via: exact commit/image digest/desktop build/contracts/schema hashes and test artifacts
---
<objective>Create the immutable integrated Phase 4 evidence bundle and fail-closed promotion evaluator.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@architecture/decisions/0005-cross-cutting-acceptance-policy.md @.planning/phases/04-identity-commerce-devices-and-administration/04-VALIDATION.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-24-01: Evaluate build-bound evidence and coverage</name>
  <read_first>D-64 through D-68; acceptance policy ADR; all Phase 4 plan summaries; 04-VALIDATION.md</read_first>
  <files>tooling/phase4-evidence/package.json, src/evaluate.ts, src/evaluate.test.ts, evidence-manifest.json, real-pc-coverage.json, architecture/module-boundaries.json, package.json</files>
  <behavior>- Missing/stale/wrong-build requirement or critical gate evidence rejects.
- Any prior critical failure for the build rejects even if a later isolated test passes.
- Promotion order is local->CI/preview->internal staging->invited friend alpha->frozen RC->production; Phase 4 cannot produce RC/production authority.
- Coverage matrix requires Windows 10/11, Intel/AMD CPU, NVIDIA/AMD/Intel GPU, notebook/desktop, storage/network, and clean install/upgrade/offline/failure/restoration/rollback/uninstall dimensions.
- Known security/privacy/billing/licensing/data/restoration/signing/update/accessibility/compatibility defect always rejects; only documented non-material minor defects may remain.</behavior>
  <action>RED omission, mutation, stale-hash, wrong-build, overwritten-failure, unsupported-stage, and coverage-gap tests. GREEN with an active tooling package that resolves exact immutable artifact/test/schema/contract hashes and computes the highest allowed stage without mutating history. Register the tooling root and root `phase4:verify` script. Do not infer owner/legal/security approval or production readiness.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/phase4-evidence test -- --run && rtk pnpm phase4:verify -- --stage internal-staging</automated></verify>
  <acceptance_criteria>Every omission/mismatch/critical failure/gap fails closed and the integrated build reaches at most its evidenced non-production stage.</acceptance_criteria>
  <done>An immutable build-bound evidence bundle and promotion verdict exist.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `evaluatePhase4Evidence`, `Phase4EvidenceManifest`, `PromotionStage`, `CriticalGate`, `RealPcCoverageMatrix`.
- Root CLI `pnpm phase4:verify -- --stage <local|preview|internal-staging|invited-alpha>`.
- Evidence fields: commit, OCI digest, desktop build ID, contract/schema hashes, requirement witnesses, gate history, defects, coverage cells, ownerReviewRequired.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test/deployment artifacts -> promotion decision | Mutable or incomplete evidence could falsely promote a build. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-EVIDENCE | Tampering/Repudiation | evidence evaluator | mitigate | Immutable hashes, exact build binding, append failure history, omission/mutation tests. |
</threat_model>
<verification>Run evaluator tests and the integrated internal-staging gate.</verification>
<success_criteria>Promotion status is reproducible, build-bound, and cannot erase critical failures or invent production authority.</success_criteria>
<output>Create `04-24-SUMMARY.md` when done.</output>
