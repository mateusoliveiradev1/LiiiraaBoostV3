---
phase: 04-identity-commerce-devices-and-administration
plan: "11"
type: tdd
wave: 4
depends_on: ["04-03", "04-04", "04-05", "04-32"]
files_modified:
  - packages/control-plane-domain/src/identity/authentication.ts
  - packages/control-plane-domain/src/identity/authentication.test.ts
  - packages/control-plane-application/src/use-cases/authenticate.ts
  - packages/control-plane-adapters/src/identity/better-auth-adapter.ts
  - apps/api/src/modules/identity/routes.ts
  - apps/api/src/modules/identity/identity-conformance.test.ts
autonomous: true
requirements: [IDEN-01]
must_haves:
  truths:
    - "Verified email/password, Google, Discord, and passkeys authenticate while Microsoft/public signup/unverified email fail closed, per D-01, D-02, and D-60."
    - "Multiple individually visible/revocable sessions are allowed independently of the one-PC Premium rule, per D-07."
  artifacts:
    - path: apps/api/src/modules/identity/routes.ts
      provides: Generated-contract identity/session HTTP endpoints
      exports: [registerIdentityRoutes]
    - path: packages/control-plane-application/src/use-cases/authenticate.ts
      provides: authenticate, listSessions, revokeSession transactional use cases
  key_links:
    - from: apps/api/src/modules/identity/routes.ts
      to: authenticate.ts
      via: validate generated request, map session actor, call use case, validate response
    - from: desktop authorization exchange route
      to: IdentityProviderPort
      via: API alone exchanges the authorization code and issues/rotates the desktop session; provider secrets never enter the desktop
---
<objective>Implement real web identity and multi-session authority behind the approved provider port.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md @packages/control-plane-application/src/ports/identity.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-11-01: Authenticate and manage sessions through generated contracts</name>
  <read_first>04-IDENTITY-SPIKE.md PASS evidence; generated identity/session contracts; PostgreSQL identity/session schema</read_first>
  <files>packages/control-plane-domain/src/identity/authentication.ts, authentication.test.ts, packages/control-plane-application/src/use-cases/authenticate.ts, packages/control-plane-adapters/src/identity/better-auth-adapter.ts, apps/api/src/modules/identity/routes.ts, identity-conformance.test.ts</files>
  <behavior>- Invitation + verified email/password, Google, Discord, and passkey produce bounded sessions.
- Unverified email, Microsoft, missing invitation, disabled/revoked identity, replay, wrong origin, and public registration reject generically.
- User lists and revokes individual web/desktop sessions; revoking one does not revoke device binding.
- Desktop begin/exchange endpoints bind authorization code to one PKCE/state/callback transaction; only the API performs provider exchange and session issuance.
- Session records expose no token and provider errors are redacted.</behavior>
  <action>RED against the generated HTTP contract and IdentityProviderPort, including desktop begin/exchange tests that fail if a client secret or provider token is required by the desktop. GREEN by mapping the Plan 04-05 approved adapter into domain admission and PostgreSQL session authority. Add backend-owned desktop authorization start/exchange routes that validate one-shot PKCE/state/issuer/callback evidence, perform provider code exchange only inside the API, issue/rotate the desktop session, and return only the native credential-custody result. Enforce invitation, verified email, exact origins/CSRF, rate/risk hooks, and provider-independent errors. REFACTOR controllers to remain thin and rerun the focused suite.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run identity-conformance</automated></verify>
  <acceptance_criteria>Every launch method and rejection case passes; session list/revoke is independently authoritative and no public signup exists.</acceptance_criteria>
  <done>IDEN-01 and D-01/D-02/D-07 web authority are implemented.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `AuthenticationDecision`, `authenticate`, `listSessions`, `revokeSession`, `registerIdentityRoutes`.
- HTTP operations: identity registration/verification, web callback, desktop authorization start/exchange, session list/revoke.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser/provider -> identity API | Credentials/assertions and session cookies cross origins. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-IDENTITY | Spoofing | identity routes | mitigate | Verified invitation/email, exact provider allowlist/origins, generic errors, throttling. |
| T-04-SESSION | Elevation of Privilege | sessions | mitigate | Hashed authority, secure cookies, owner-scoped list/revoke, token redaction. |
</threat_model>
<verification>Run identity conformance and generated contract checks.</verification>
<success_criteria>Real invited identity works across supported methods with independently revocable sessions.</success_criteria>
<output>Create `04-11-SUMMARY.md` with TDD gates.</output>
