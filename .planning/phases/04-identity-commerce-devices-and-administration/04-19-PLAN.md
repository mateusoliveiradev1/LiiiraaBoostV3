---
phase: 04-identity-commerce-devices-and-administration
plan: "19"
type: execute
wave: 7
depends_on: ["04-16", "04-34"]
files_modified:
  - apps/admin/src/admin-authority.ts
  - apps/admin/src/admin-runtime.ts
  - apps/admin/src/features/admin-preview.tsx
  - apps/admin/src/features/admin-authority.test.tsx
  - apps/admin/src/admin-preview-model.ts
  - tooling/web-evidence/tests/admin-authority.spec.ts
  - tooling/web-evidence/tests/admin-consent-revocation.spec.ts
autonomous: true
requirements: [WEB-06, WEB-07, IDEN-03]
must_haves:
  truths:
    - "The isolated admin origin projects only the server-admitted singular role; URL role/query never widens authorization."
    - "Critical actions visibly require recent strong step-up, reason, impact review, confirmation, and immutable receipt."
    - "Consent revoke/expiry clears the current diagnostic view immediately and the audit receipt remains."
    - "Phase 3 admin fixtures/visual regressions remain deterministic while production composition removes fixture authority."
  artifacts:
    - path: apps/admin/src/admin-authority.ts
      provides: Generated-client role/consent/audit adapter
      exports: [createAdminAuthority]
    - path: tooling/web-evidence/tests/admin-consent-revocation.spec.ts
      provides: Cross-origin live revocation evidence
  key_links:
    - from: admin-authority.ts
      to: apps/api/src/modules/admin/routes.ts
      via: session-derived role and generated command/projection contracts
---
<objective>Activate the isolated admin application with real least-privilege, consent-bound, audited authority.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/admin/src/admin-preview-model.ts @apps/admin/src/features/admin-preview.tsx @tooling/web-evidence/tests/admin.spec.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-19-01: Bind admin UI to server-authorized projections and receipts</name>
  <read_first>Generated admin/consent/audit contracts; admin-role UX map; production admin routes; shared workflow interruption states</read_first>
  <files>apps/admin/src/admin-authority.ts, apps/admin/src/admin-runtime.ts, apps/admin/src/features/admin-preview.tsx, apps/admin/src/features/admin-authority.test.tsx, apps/admin/src/admin-preview-model.ts</files>
  <behavior>- Assumed server role controls navigation/queue/detail before search/filter.
- Critical actions cannot issue until action-scoped step-up/reason/impact/confirm are complete.
- Diagnostic view streams only consented fields and clears on revoke/expiry.
- Denied/break-glass views reveal only redacted metadata.
- Fixture runtime remains deterministic; production bundle has no preview authority import.</behavior>
  <action>RED: expand the Wave 0 admin witnesses with adapter/component failures for role admission, step-up, consent abort/clear, redaction, and fixture isolation; run the focused test and record the expected missing-authority failures. GREEN: add the production generated client, keep role query as navigation only, render server projections/receipts, and connect AbortSignal/revocation notifications to clear in-memory diagnostic state until the focused suite passes. REFACTOR: centralize projection admission/cleanup, rerun the same measured sub-30-second suite, and preserve bilingual accessibility, mobile authority omission, and all Phase 3 fixture stories/screenshots.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/admin test -- --run admin-authority</automated></verify>
  <acceptance_criteria>Role, step-up, consent, redaction, and receipt UI exactly reflects server outcomes with no client-side authority widening.</acceptance_criteria>
  <done>The admin UI is real-authority capable and fixture-isolated.</done>
</task>
<task type="auto">
  <name>Task 04-19-02: Prove role isolation and live consent revocation</name>
  <read_first>tooling/web-evidence/tests/admin.spec.ts; account consent routes; admin production runtime</read_first>
  <files>tooling/web-evidence/tests/admin-authority.spec.ts, tooling/web-evidence/tests/admin-consent-revocation.spec.ts</files>
  <action>Add role×route/query/detail negative journeys, developer singular role assumption audit, critical step-up, handoff, break-glass redaction, and cross-origin consent grant/revoke/expiry. Assert `Cache-Control: no-store`, stream abort, DOM clearing, absence of durable URLs/diagnostic bytes, and immutable access/revocation event. Preserve existing visual candidates untouched.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence exec playwright test tests/admin-consent-revocation.spec.ts --grep @consent-smoke --workers=1</automated></verify>
  <acceptance_criteria>Denied roles never observe records and revocation closes an active view immediately with an immutable audit witness.</acceptance_criteria>
  <done>WEB-06/07 has browser-level isolated-admin evidence.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `AdminAuthority`, `createAdminAuthority`, production admin runtime mapping, consent-stream lifecycle hook.
- Playwright suites `admin-authority.spec.ts` and `admin-consent-revocation.spec.ts`.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Isolated admin origin -> privileged API | Operator UI may attempt role or consent bypass. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-ADMIN-UI | Elevation/Disclosure | admin client | mitigate | Server-derived role, authorize-before-projection, continuous consent abort/clear, redaction. |
</threat_model>
<verification>Run focused admin unit/consent smoke checks after each task and both complete Playwright specs, build, artifact isolation, accessibility, and fixture regression as the plan gate; record runtimes.</verification>
<success_criteria>Authorized staff operate only within singular role and active consent, with immutable receipts.</success_criteria>
<output>Create `04-19-SUMMARY.md` when done.</output>
