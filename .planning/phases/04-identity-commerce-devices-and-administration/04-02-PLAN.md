---
phase: 04-identity-commerce-devices-and-administration
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/testing/postgres.ts
  - apps/api/src/testing/determinism.ts
  - packages/control-plane-domain/package.json
  - packages/control-plane-application/package.json
  - packages/control-plane-adapters/package.json
  - architecture/module-boundaries.json
autonomous: true
requirements: [WEB-04, WEB-05, WEB-06, WEB-07, IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-05, IDEN-06, IDEN-07, IDEN-08, IDEN-09]
must_haves:
  truths:
    - "All Phase 4 behavior has a deterministic test owner before implementation begins."
    - "Real PostgreSQL semantics can be exercised through CI Testcontainers or an explicitly synthetic Neon URL, per D-43 through D-45."
    - "Domain, application, adapter, and composition dependency directions are executable and production packages cannot import fixture authority."
  artifacts:
    - path: apps/api/src/testing/postgres.ts
      provides: createPostgresHarness and withSerializableTransactions test utilities
      exports: [createPostgresHarness, requireSyntheticDatabase]
    - path: apps/api/src/testing/determinism.ts
      provides: FrozenClock and SequenceIds deterministic test sources
      exports: [FrozenClock, SequenceIds]
    - path: architecture/module-boundaries.json
      provides: Active control-plane-domain, control-plane-application, control-plane-adapters, and api-app ownership records
  key_links:
    - from: packages/control-plane-adapters
      to: packages/control-plane-application
      via: adapter layer may depend inward on ports, never the reverse
    - from: apps/api/src/testing/postgres.ts
      to: TEST_DATABASE_URL
      via: rejects non-synthetic databases unless running an isolated Testcontainers instance
---

<objective>
Establish Wave 0 package boundaries and deterministic validation infrastructure for every Phase 4 slice.

Purpose: Make concurrency, security, adapter, and cross-runtime work testable before authority is connected.
Output: Four active workspace roots, PostgreSQL/determinism harness contracts, and enforced fixture isolation.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@architecture/module-boundaries.json
@.planning/phases/04-identity-commerce-devices-and-administration/04-PATTERNS.md
@.planning/phases/04-identity-commerce-devices-and-administration/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Activate control-plane workspace boundaries</name>
  <read_first>architecture/module-boundaries.json; package.json; pnpm-workspace.yaml; 04-PATTERNS.md Fixture Boundary</read_first>
  <files>apps/api/package.json, apps/api/tsconfig.json, packages/control-plane-domain/package.json, packages/control-plane-application/package.json, packages/control-plane-adapters/package.json, architecture/module-boundaries.json</files>
  <action>Create manifest-backed roots `@liiiraa/api`, `@liiiraa/control-plane-domain`, `@liiiraa/control-plane-application`, and `@liiiraa/control-plane-adapters`. Activate canonical domain/application/adapter/composition records with public roots and inward-only edges. Do not add external Phase 4 packages or change the lockfile. Encode that account/admin/desktop production composition cannot import `@liiiraa/web-preview`, while Storybook/test roots retain fixture access.</action>
  <verify><automated>rtk pnpm test:architecture</automated></verify>
  <acceptance_criteria>Workspace discovery finds each new root once; architecture mutations prove reverse-layer and production-to-fixture imports fail.</acceptance_criteria>
  <done>The new package roots are active and dependency direction is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02-02: Create deterministic PostgreSQL and clock/ID harnesses</name>
  <read_first>04-VALIDATION.md Wave 0 Requirements; apps/web/vitest.config.ts; tooling/web-evidence/vitest.config.ts</read_first>
  <files>apps/api/vitest.config.ts, apps/api/src/testing/postgres.ts, apps/api/src/testing/determinism.ts</files>
  <behavior>- Harness rejects an unlabeled or production-like database URL.
- CI mode exposes an isolated Testcontainers strategy without requiring Docker Desktop in ordinary local mode.
- FrozenClock and SequenceIds return exact reproducible values and fail on exhaustion.</behavior>
  <action>RED: create harness meta-tests for unsafe URLs, deterministic exhaustion, and daemon-free local selection; run the focused command and record the expected failures because the harness exports do not exist. GREEN: implement configuration that separates unit and PostgreSQL suites, validates `TEST_DATABASE_URL` as synthetic, and defines the Testcontainers/Neon strategy without importing unapproved packages; rerun until green. REFACTOR: remove duplicated environment parsing, rerun the same focused suite, and record measured startup below 30 seconds.</action>
  <verify><automated>rtk pnpm exec vitest --run apps/api/src/testing</automated></verify>
  <acceptance_criteria>The harness meta-tests pass without a database daemon, unsafe database URLs fail closed, and deterministic sources are reusable by later plans.</acceptance_criteria>
  <done>Wave 0 validation/security/concurrency harness contracts exist before dependent features.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `apps/api/package.json`: `@liiiraa/api` root and `test`, `test:postgres`, `db:migrate:test` script names.
- `apps/api/src/testing/postgres.ts`: `PostgresHarness`, `createPostgresHarness`, `requireSyntheticDatabase`.
- `apps/api/src/testing/determinism.ts`: `FrozenClock`, `SequenceIds`.
- New architecture module IDs: `control-plane-domain`, `control-plane-application`, `control-plane-adapters`, `api-app`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test configuration -> PostgreSQL | A local/CI command may otherwise target non-synthetic data. |
| Fixture packages -> production composition | Deterministic preview authority must not satisfy production truth. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-02-I | Information Disclosure | postgres harness | mitigate | Reject unmarked databases and require synthetic database identity. |
| T-04-02-E | Elevation of Privilege | module graph | mitigate | Canonical inward-only layers and production-to-fixture negative mutations. |
</threat_model>

<verification>Run architecture tests and the focused harness meta-suite.</verification>
<success_criteria>Wave 0 scaffolds all missing harness classes and creates no production authority.</success_criteria>
<output>Create `.planning/phases/04-identity-commerce-devices-and-administration/04-02-SUMMARY.md` when done.</output>
