---
phase: 04-identity-commerce-devices-and-administration
plan: "18"
type: execute
wave: 7
depends_on: ["04-17", "04-34"]
files_modified:
  - apps/account/src/account-authority.ts
  - apps/account/src/account-runtime.ts
  - apps/account/src/features/account-preview.tsx
  - apps/account/src/features/account-authority.test.tsx
  - apps/account/src/account-preview-model.ts
  - tooling/web-evidence/tests/account-authority.spec.ts
autonomous: true
requirements: [WEB-04, WEB-05, IDEN-01, IDEN-02, IDEN-04]
must_haves:
  truths:
    - "A signed-in user manages every WEB-04 area through real projections while the existing routes, bilingual accessibility, and Phase 3 visual vocabulary remain intact."
    - "Checkout shows pending until reconciliation and device transfer shows cooldown/eligibility/current-PC safety exactly."
    - "Fixture authority remains available only to Storybook/visual tests; production account composition has no fixture import."
  artifacts:
    - path: apps/account/src/account-authority.ts
      provides: Generated-client AccountAuthority adapter
      exports: [createAccountAuthority]
    - path: tooling/web-evidence/tests/account-authority.spec.ts
      provides: Real account journey and fixture-isolation evidence
  key_links:
    - from: account-authority.ts
      to: apps/api/src/modules/account/routes.ts
      via: generated client with ETag/expectedVersion and typed pending/conflict results
---
<objective>Activate the existing account application against real identity, billing, device, security, and support authority.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/account/src/account-preview-model.ts @apps/account/src/features/account-preview.tsx @tooling/web-evidence/tests/account.spec.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-18-01: Add production account authority composition</name>
  <read_first>AccountProjection generated type; apps/account/src/account-preview-model.ts contradiction checks; Phase 3 account feature and account.spec.ts</read_first>
  <files>apps/account/src/account-authority.ts, apps/account/src/account-runtime.ts, apps/account/src/features/account-preview.tsx, apps/account/src/features/account-authority.test.tsx, apps/account/src/account-preview-model.ts</files>
  <behavior>- Production runtime renders authenticated profile/security/session/subscription/invoice/device/support projection.
- Mutation shows reviewing/reauth/confirm/issuing/pending/conflict/offline/stale/error/complete accurately.
- Conflict preserves remote value and safe local draft for review.
- Preview runtime still renders deterministic Phase 3 scenarios and remoteStateChanged false only there.
- Production bundle rejects @liiiraa/web-preview.</behavior>
  <action>RED: expand the Wave 0 account witness with adapter/component cases for authority, pending, conflict, offline/stale, and fixture isolation; run the focused test and record the expected missing-adapter/runtime failures. GREEN: create the generated-client adapter and server-owned runtime composition, map authoritative projection into the established view model, and generalize workflow receipt/error guards while preserving interruption states until the focused suite passes. REFACTOR: remove duplicated projection mapping, confirm production cannot import fixture authority, and rerun the same test under the measured sub-30-second budget while preserving PT-BR/English, keyboard, screen reader, reflow, reduced motion, and safe drafts.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/account test -- --run account-authority</automated></verify>
  <acceptance_criteria>Production account paths use real authority with truthful pending/conflict states and fixture tests remain deterministic.</acceptance_criteria>
  <done>The account UI is activated without redesign or fixture leakage.</done>
</task>
<task type="auto">
  <name>Task 04-18-02: Prove full account journeys and accessibility</name>
  <read_first>tooling/web-evidence/tests/account.spec.ts; existing W10-W13/W18 screenshot identities; account production runtime</read_first>
  <files>tooling/web-evidence/tests/account-authority.spec.ts</files>
  <action>Add invited sign-in, passkey/MFA/recovery/session, pending checkout/reconciliation, invoice/refund/cancel, device cooldown/replace, support/consent, stale/conflict, offline, and sign-out journeys using accessibility-first locators. Run production-authority assertions separately from unchanged deterministic fixture/visual tests; do not rebaseline Phase 3 screenshots unless authorized by their existing evidence workflow.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence exec playwright test tests/account-authority.spec.ts --grep @authority-smoke --workers=1</automated></verify>
  <acceptance_criteria>Every WEB-04/05 account journey passes in both locales and required accessibility states; Phase 3 fixture/visual suite remains byte-stable.</acceptance_criteria>
  <done>Account authority has browser-level evidence.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `AccountAuthority`, `createAccountAuthority`, `AccountRuntimeConfig`, production projection mapper.
- Playwright journey IDs for identity, security, commerce, device, support, consent, conflict, and offline account flows.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Account browser -> API | User mutations and protected projections cross account origin. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-ACCOUNT-CSRF | Spoofing/Tampering | account authority | mitigate | Server session, exact origin, CSRF, generated validation, expected versions. |
| T-04-FIXTURE | Spoofing | production bundle | mitigate | Architecture/bundle scan rejects preview imports; tests keep separate composition. |
</threat_model>
<verification>Run the focused account unit/smoke checks after each task and the complete account Playwright, build, accessibility, and fixture-guard suites as the plan gate; record both runtimes.</verification>
<success_criteria>Users can operate the complete real account without losing established UX or test fixtures.</success_criteria>
<output>Create `04-18-SUMMARY.md` when done.</output>
