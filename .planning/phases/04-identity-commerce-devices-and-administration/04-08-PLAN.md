---
phase: 04-identity-commerce-devices-and-administration
plan: "08"
type: tdd
wave: 4
depends_on: ["04-03", "04-04", "04-33"]
files_modified:
  - packages/control-plane-application/src/ports/commerce.ts
  - packages/control-plane-adapters/src/commerce/stripe-webhook.ts
  - packages/control-plane-adapters/src/commerce/stripe-webhook.permutation.test.ts
autonomous: true
requirements: [IDEN-09]
must_haves:
  truths:
    - "Checkout navigation never grants Premium; only reconciled provider truth may change entitlement, per D-16."
    - "Duplicate, delayed, replayed, and reordered events converge without duplicate charge, invoice, subscription, or entitlement, per D-17."
  artifacts:
    - path: packages/control-plane-adapters/src/commerce/stripe-webhook.ts
      provides: verifyRawWebhook and reconcileProviderState adapter
      exports: [verifyRawWebhook, reconcileProviderState]
  key_links:
    - from: stripe-webhook.ts
      to: provider_inbox
      via: raw signature validation before parsing and unique provider/event admission
---
<objective>Prove the event/reconciliation-driven commerce authority against adversarial webhook delivery.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/04-identity-commerce-devices-and-administration/04-RESEARCH.md @packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-08-01: Reconcile signed Stripe events idempotently</name>
  <read_first>04-RESEARCH.md Patterns 2 and webhook pitfalls; generated ProviderEvent contract; PostgreSQL inbox/outbox schema</read_first>
  <files>packages/control-plane-application/src/ports/commerce.ts, packages/control-plane-adapters/src/commerce/stripe-webhook.ts, packages/control-plane-adapters/src/commerce/stripe-webhook.permutation.test.ts</files>
  <behavior>- Invalid/missing/stale raw-body signature creates no inbox row.
- Every permutation of checkout, invoice paid/failed, subscription update, refund, dispute, cancel, and duplicate events converges to current retrieved provider state.
- Duplicate provider/event ID is acknowledged once and produces no duplicate internal objects/outbox jobs.
- Provider retrieval failure remains retryable and never grants Premium.
- Outbound mutation carries an idempotency key.</behavior>
  <action>RED with signed raw fixtures and exhaustive relevant event permutations against real PostgreSQL. GREEN with signature-before-parse admission, unique durable inbox insert, quick acknowledgement, provider object retrieval, and typed port mapping. Do not encode product lifecycle rules in the Stripe adapter. REFACTOR deterministic permutation generation and redacted provider errors.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/control-plane-adapters test -- --run stripe-webhook.permutation</automated></verify>
  <acceptance_criteria>All event orders/replays converge and no unverified or unreconciled event mutates entitlement.</acceptance_criteria>
  <done>IDEN-09 adapter-level evidence is green.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `CommerceProviderPort`, `ProviderReconciliation`, `verifyRawWebhook`, `reconcileProviderState`, provider idempotency-key contract.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stripe HTTP -> provider inbox | Attacker or delivery retries can spoof/replay/reorder events. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-WEBHOOK-REPLAY | Spoofing/Tampering | raw webhook | mitigate | Raw signature/timestamp, unique inbox, retrieval reconciliation, idempotent transaction. |
</threat_model>
<verification>Run permutation suite against isolated PostgreSQL.</verification>
<success_criteria>Provider delivery behavior cannot duplicate or prematurely grant authority.</success_criteria>
<output>Create `04-08-SUMMARY.md` with TDD gates.</output>
