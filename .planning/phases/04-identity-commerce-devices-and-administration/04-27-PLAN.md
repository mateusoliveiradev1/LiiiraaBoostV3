---
phase: 04-identity-commerce-devices-and-administration
plan: "27"
type: tdd
wave: 6
depends_on: ["04-03", "04-04", "04-07", "04-13", "04-14"]
files_modified:
  - packages/control-plane-application/src/ports/entitlement-signing.ts
  - packages/control-plane-application/src/use-cases/issue-offline-entitlement.ts
  - packages/control-plane-adapters/src/crypto/staging-entitlement-signer.ts
  - apps/api/src/modules/entitlements/routes.ts
  - apps/api/src/modules/entitlements/issuance.test.ts
autonomous: true
requirements: [IDEN-04, IDEN-06, IDEN-09]
must_haves:
  decision_ids: [D-22, D-23, D-29, D-30, D-31, D-43, D-50, D-56]
  truths:
    - "An authenticated active Premium device can obtain the exact signed seven-day envelope consumed by desktop PremiumAuthority."
    - "Issuance and silent renewal derive only from reconciled PostgreSQL subscription/entitlement/device state; checkout navigation or an unreconciled event cannot issue authority."
    - "Revocation and key/version rotation commit transactionally and make the next online renewal fail closed without exposing private signing material."
  artifacts:
    - path: packages/control-plane-application/src/use-cases/issue-offline-entitlement.ts
      provides: Issue, renew, revoke, and rotate seven-day entitlement use case
      exports: [issueOfflineEntitlement, revokeOfflineEntitlement]
    - path: packages/control-plane-application/src/ports/entitlement-signing.ts
      provides: Signing-key custody interface that never exposes private key bytes
      exports: [EntitlementSigningPort]
    - path: apps/api/src/modules/entitlements/routes.ts
      provides: Generated issuance, renewal, and revocation/version HTTP routes
  key_links:
    - from: issue-offline-entitlement.ts
      to: premium_entitlements/device_bindings/subscriptions
      via: one transaction locks reconciled entitlement and active-device versions before constructing exact payload bytes
    - from: staging-entitlement-signer.ts
      to: packages/contracts-ts/src/offline-entitlement.ts
      via: signer returns signature/keyId over the same opaque bytes verified by TS/Rust corpus
---

<objective>Issue, renew, rotate, and revoke the real signed seven-day offline entitlement from authoritative PostgreSQL state.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@packages/contracts-ts/src/offline-entitlement.ts @packages/control-plane-application/src/use-cases/reconcile-commerce.ts @packages/control-plane-application/src/use-cases/bind-device.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-27-01: Sign authoritative entitlement bytes and expose renewal routes</name>
  <read_first>Generated OfflineEntitlementEnvelope; D-22/D-29–D-31/D-56; Plan 04-07 exact-byte corpus; reconciled commerce and active-device transactions</read_first>
  <files>packages/control-plane-application/src/ports/entitlement-signing.ts, packages/control-plane-application/src/use-cases/issue-offline-entitlement.ts, packages/control-plane-adapters/src/crypto/staging-entitlement-signer.ts, apps/api/src/modules/entitlements/routes.ts, apps/api/src/modules/entitlements/issuance.test.ts</files>
  <behavior>- Active reconciled Premium plus the singular active device issues bytes expiring exactly 604800 seconds after issuedAt.
- Pending/expired/disputed/revoked subscription, wrong/revalidating device, stale version, and unreconciled event issue no bytes.
- Renewal is idempotent for one entitlement/device/version and silently advances issuedAt/key version after online contact.
- Revocation/version transaction prevents subsequent renewal and records audit/outbox evidence.
- The custody port signs opaque bytes and exposes keyId/public verification data but never private key material.</behavior>
  <action>RED: create issuance integration tests that consume the Plan 04-07 corpus/verifier and fail because no server use case, custody adapter, or route exists. GREEN: define the narrow custody port, implement a staging Node-crypto signer backed by an injected protected key handle, lock reconciled subscription/entitlement/device rows, serialize one canonical bounded payload, sign exact bytes, and atomically persist issuance/version/revocation/audit/outbox state; add generated issuance/renewal/revocation routes. REFACTOR: share payload encoding with the fixture authority, rerun focused PostgreSQL/crypto tests, and prove private key bytes never enter DTOs, logs, tables, or responses.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run entitlement-issuance</automated></verify>
  <acceptance_criteria>Real API bytes pass both TS and Rust verifiers for exactly seven days; every unreconciled, stale, wrong-device, revoked, or key-custody failure returns no authority.</acceptance_criteria>
  <done>The PostgreSQL entitlement state is linked to exact signed bytes consumed by PremiumAuthority.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `EntitlementSigningPort`, `issueOfflineEntitlement`, `revokeOfflineEntitlement`, and entitlement issuance/renewal/revocation routes.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PostgreSQL entitlement/device state -> signing custody -> desktop | Paid authority crosses database, key-custody, API, and client boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-27-E | Elevation/Tampering | entitlement issuance | mitigate | Reconciled-state locks, expected versions, exact-byte signing, protected key handle, idempotent issuance/revocation. |
</threat_model>
<verification>Run issuance integration tests, the Plan 04-07 cross-language corpus, and key-material scans.</verification>
<success_criteria>The desktop can renew a real, revocable, exact-byte seven-day entitlement from server authority.</success_criteria>
<output>Create `04-27-SUMMARY.md` with TDD gates.</output>
