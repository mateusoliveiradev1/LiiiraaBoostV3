---
phase: 04-identity-commerce-devices-and-administration
plan: "30"
type: tdd
wave: 5
depends_on: ["04-04", "04-10"]
files_modified:
  - packages/control-plane-application/src/use-cases/anchor-audit-chain.ts
  - apps/api/src/workers/audit-anchors.ts
  - apps/api/src/workers/audit-anchors.test.ts
  - apps/api/src/modules/audit/anchor-health.test.ts
autonomous: true
requirements: [WEB-06, WEB-07, IDEN-03]
must_haves:
  decision_ids: [D-46, D-47, D-49]
  truths:
    - "Actual audit chain heads reach separately controlled immutable storage every 15 minutes or 1,000 events."
    - "Daily latest-anchor verification and monthly complete-segment drills expose retry/failure health without allowing the API to forge health."
    - "Anchor signing/deletion/retention authority remains outside ordinary application credentials."
  artifacts:
    - path: apps/api/src/workers/audit-anchors.ts
      provides: Durable cadence, write, verify, retry, and health handler
    - path: packages/control-plane-application/src/use-cases/anchor-audit-chain.ts
      provides: Due-anchor and verification use cases
      exports: [anchorDueAuditHeads, verifyAuditAnchors]
  key_links:
    - from: audit_chain_heads
      to: AuditAnchorPort
      via: durable due job carries chain/segment/sequence/hash and receives immutable object checksum/retention receipt
---
<objective>Invoke the audit-anchor adapter on a durable schedule and continuously verify that database heads reached immutable storage.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@packages/control-plane-domain/src/audit/audit-chain.ts @packages/control-plane-adapters/src/storage/audit-anchor.ts @.planning/phases/04-identity-commerce-devices-and-administration/04-AUDIT-SECURITY-ADR.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-30-01: Schedule, write, verify, and health-project audit anchors</name>
  <read_first>Resolved audit cadence/custody/retention; AuditAnchorPort; audit chain heads/outbox schema</read_first>
  <files>packages/control-plane-application/src/use-cases/anchor-audit-chain.ts, apps/api/src/workers/audit-anchors.ts, apps/api/src/workers/audit-anchors.test.ts, apps/api/src/modules/audit/anchor-health.test.ts</files>
  <behavior>- A chain becomes due at 15 minutes or 1,000 events and duplicate claims write one immutable anchor.
- Daily verification reads/checks the latest object; monthly drill verifies a complete segment and database head continuity.
- Write/read/checksum/retention/KMS failure retries within a bound and projects unhealthy until resolved.
- Ordinary API credentials cannot sign, delete, shorten retention, or mark an unverified anchor healthy.
- Anchor receipt retains five-year purpose/expiry and exact chain/segment/sequence/hash/object version.</behavior>
  <action>RED: create threshold/time, duplicate worker, storage failure, forged-health, custody, daily, and monthly drill tests that fail because no invoker exists. GREEN: implement due-head selection, durable idempotency key, AuditAnchorPort invocation, immutable receipt persistence, retry/terminal health projection, daily verification, and monthly segment drill. REFACTOR: share receipt verification codes, rerun focused tests, and prove separate signing/storage credentials are required by composition.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/api test -- --run audit-anchor-worker</automated></verify>
  <acceptance_criteria>Real chain heads are periodically written and verified in immutable storage; failures remain visibly unhealthy and retryable.</acceptance_criteria>
  <done>D-47 has an executable scheduler/invoker rather than an unused adapter.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- `packages/control-plane-application/src/use-cases/anchor-audit-chain.ts`: `anchorDueAuditHeads` and `verifyAuditAnchors` use cases.
- `apps/api/src/workers/audit-anchors.ts`: 15-minute/1,000-event scheduler, idempotent writer, verifier, retry, and health projection.
- `apps/api/src/workers/audit-anchors.test.ts`: threshold, duplicate-claim, storage-failure, daily verification, and monthly drill witness.
- `apps/api/src/modules/audit/anchor-health.test.ts`: fail-visible health and ordinary-credential forgery-denial witness.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Database chain head -> separately controlled immutable anchor | Application/DB compromise must not forge or suppress healthy evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-30-R | Repudiation/Tampering | anchor worker | mitigate | Durable cadence, external immutable receipt, separate custody, verification drills, fail-visible health. |
</threat_model>
<verification>Run anchor worker/health tests and the full audit mutation/privilege suite.</verification>
<success_criteria>Audit tampering evidence is periodically externalized and continuously health-checked.</success_criteria>
<output>Create `04-30-SUMMARY.md` with TDD gates.</output>
