---
phase: 04-identity-commerce-devices-and-administration
plan: '63'
type: execute
wave: 26
depends_on: ['04-38', '04-39', '04-61']
files_modified:
  - apps/account/package.json
  - apps/account/src/account-auth.ts
  - apps/account/src/app/api/internal-download/route.ts
  - apps/account/src/app/account-shell.css
  - apps/account/src/features/account-authority.tsx
  - apps/account/src/features/internal-download.ts
  - apps/account/src/features/internal-download.test.ts
  - architecture/dependency-allowlist.json
  - architecture/dependency-review.md
  - .github/workflows/phase-4-surfaces.yml
  - pnpm-lock.yaml
autonomous: false
requirements: [WEB-04, IDEN-01, IDEN-02, IDEN-03, IDEN-07]
gap_closure: true
must_haves:
  truths:
    - 'Only an authenticated invited account session can download the numbered Internal Windows installer.'
    - 'The installer remains in private object storage; neither a public blob URL nor a storage credential reaches HTML, JavaScript, Git, logs, or browser redirects.'
    - 'The account surface labels the artifact as an unsigned Internal test build and keeps public stable distribution blocked.'
    - 'The exact local Internal installer is uploaded immutably and its digest is recorded without committing the binary.'
  artifacts:
    - path: apps/account/src/app/api/internal-download/route.ts
      provides: Same-origin session-gated private installer stream
    - path: apps/account/src/features/internal-download.test.ts
      provides: Fail-closed authentication, configuration, and private-blob delivery proof
    - path: .github/workflows/phase-4-surfaces.yml
      provides: Deployment gate requiring the private Internal artifact configuration
  key_links:
    - from: apps/account/src/app/api/internal-download/route.ts
      to: /v1/identity/session
      via: forwarded same-origin session cookie before any Blob read
    - from: apps/account/src/app/api/internal-download/route.ts
      to: Vercel Private Blob
      via: server-only get() stream with private access
    - from: apps/account/src/features/account-authority.tsx
      to: /api/internal-download
      via: authenticated account Downloads action with explicit Internal trust copy
---

<objective>Deliver the current Windows Internal build privately to invited Phase 4 testers without weakening the public signed-release boundary.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<tasks>
<task type="auto" tdd="true">
<name>Task 04-63-01: Add the fail-closed private download boundary</name>
<read_first>apps/account/src/account-auth.ts; apps/account/src/features/account-authority.tsx; apps/account/next.config.ts; Vercel Private Blob official delivery contract</read_first>
<files>apps/account/package.json, apps/account/src/app/api/internal-download/route.ts, apps/account/src/features/internal-download.ts, apps/account/src/features/internal-download.test.ts, architecture/dependency-allowlist.json, architecture/dependency-review.md</files>
<action>Add the exact reviewed Vercel Blob dependency. Implement a Node route that forwards the incoming same-origin cookie to /v1/identity/session, rejects missing or invalid sessions before storage access, reads one fixed server-owned pathname from private Blob storage, and streams it with attachment, no-store, nosniff, and frame-denial headers. Do not accept a pathname, URL, account ID, or token from the browser.</action>
<verify><automated>rtk pnpm --filter @liiiraa/account exec vitest --run src/features/internal-download.test.ts</automated></verify>
<acceptance_criteria>Unauthenticated, malformed, missing-artifact, and unavailable-storage requests fail closed; successful delivery never redirects to or serializes the private Blob URL.</acceptance_criteria>
<done>The account deployment owns a tested authenticated stream boundary for exactly one immutable Internal installer.</done>
</task>
<task type="auto" tdd="true">
<name>Task 04-63-02: Present honest invited-tester download UI</name>
<read_first>apps/account/src/features/account-authority.tsx; apps/account/src/app/account-shell.css; apps/web/src/features/releases.tsx</read_first>
<files>apps/account/src/features/account-authority.tsx, apps/account/src/app/account-shell.css, apps/account/src/features/account-authority.test.tsx</files>
<action>Replace the production Downloads fallback with an authenticated Internal download panel. State that the build is restricted to invited testing, not publicly signed, and may show Windows warnings; preserve the separate public release link as unavailable truth rather than presenting the Internal build as stable.</action>
<verify><automated>rtk pnpm --filter @liiiraa/account exec vitest --run src/features/account-authority.test.tsx src/features/internal-download.test.ts</automated></verify>
<acceptance_criteria>The action exists only inside an authenticated authoritative projection and all trust labels distinguish Internal testing from public stable distribution.</acceptance_criteria>
<done>An invited tester with a valid session can intentionally download the correct installer while every public trust claim remains false.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
<name>Task 04-63-03: Provision and prove the private artifact</name>
<read_first>.github/workflows/phase-4-surfaces.yml; apps/desktop/src-tauri/tauri.staging.conf.json; apps/desktop/src/internal-channel.test.ts</read_first>
<files>.github/workflows/phase-4-surfaces.yml, .planning/phases/04-identity-commerce-devices-and-administration/04-REAL-AUTH-UAT.md</files>
<action>Create or connect a private Blob store to the Account staging project, upload the exact local numbered NSIS artifact without placing it in Git, bind the fixed pathname through protected deployment configuration, deploy, and prove unauthenticated denial plus authenticated byte-for-byte download. Record only pathname identity, size, SHA-256, build ID, and deployment receipt.</action>
<verify><automated>rtk pnpm --filter @liiiraa/account verify</automated></verify>
<acceptance_criteria>The deployed unauthenticated route returns denial, the authenticated route returns the exact expected SHA-256, and direct private Blob access is denied.</acceptance_criteria>
<done>The exact Internal installer is available only through the authenticated Account route and Plan 04-40 may resume.</done>
<resume-signal>Continue after protected storage, upload, deployment, and authenticated download proof pass.</resume-signal>
</task>
</tasks>
<verification>Account unit/type/build gates plus deployed authenticated and unauthenticated download probes against the immutable artifact digest.</verification>
<success_criteria>Invited Phase 4 testers can download the exact Internal installer after login without creating a public unsigned distribution channel.</success_criteria>
<output>Create 04-63-SUMMARY.md after the deployed proof passes.</output>
