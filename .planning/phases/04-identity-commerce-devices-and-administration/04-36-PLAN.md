---
phase: 04-identity-commerce-devices-and-administration
plan: '36'
type: execute
wave: 11
depends_on: ['04-24']
files_modified:
  - packages/control-plane-adapters/src/postgres/migrations/0002_real_identity.sql
  - packages/control-plane-adapters/src/postgres/real-identity.ts
  - packages/control-plane-adapters/src/index.ts
  - apps/api/src/modules/identity/real-routes.ts
  - apps/api/src/staging/runtime.ts
  - apps/api/src/staging/main.mjs
  - apps/api/src/staging/real-auth.test.ts
autonomous: true
requirements: [IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-07]
gap_closure: true
must_haves:
  truths:
    - 'Invitation-only signup, password login, logout, and session lookup use Neon/PostgreSQL authority and survive process restarts.'
    - 'Invitation tokens and session credentials are stored only as SHA-256 digests; passwords use a memory-hard salted hash and generic failures.'
    - 'The deployed staging entrypoint starts the real Fastify composition rather than a health-only preview server.'
---

<objective>Replace the health-only staging identity preview with a persistent invitation-only authentication slice.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<tasks>
<task type="auto" tdd="true">
<name>Task 04-36-01: Persist invitations, identities, password credentials, authorization codes, and sessions</name>
<read_first>packages/control-plane-adapters/src/postgres/database.ts; packages/control-plane-adapters/src/postgres/migrations/0001_control_plane.sql; packages/control-plane-application/src/ports/identity.ts</read_first>
<files>packages/control-plane-adapters/src/postgres/migrations/0002_real_identity.sql, packages/control-plane-adapters/src/postgres/real-identity.ts, packages/control-plane-adapters/src/index.ts</files>
<action>Add an idempotent migration and PostgreSQL repository for single-use invitation digests, identity creation, memory-hard password verification, opaque session issuance/revocation, bearer/cookie lookup, and one-time PKCE authorization-code state. Never persist raw invitation/session/code values and never log credentials.</action>
<verify><automated>rtk pnpm --filter @liiiraa/control-plane-adapters exec vitest --run src/postgres/real-identity.test.ts</automated></verify>
<acceptance_criteria>Concurrent invitation redemption admits exactly one account; wrong password/expired/revoked/replayed credentials fail generically; process recreation reads the same PostgreSQL authority.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
<name>Task 04-36-02: Expose real auth and readiness routes from the staging process</name>
<read_first>apps/api/src/app.ts; apps/api/src/server.ts; apps/api/src/staging/main.mjs; apps/api/src/modules/identity/routes.ts; apps/api/src/modules/account/routes.ts</read_first>
<files>apps/api/src/modules/identity/real-routes.ts, apps/api/src/staging/runtime.ts, apps/api/src/staging/main.mjs, apps/api/src/staging/real-auth.test.ts</files>
<action>Compose Fastify with CSRF bootstrap, invitation signup, sign-in, sign-out, current-session, desktop authorization/exchange, and account routes backed by PostgreSQL. Read secrets only from environment, allow the exact account/admin origins, set secure HttpOnly SameSite=None cookies for browser sessions, accept bearer tokens for desktop, and make /ready report database connectivity and real capabilities.</action>
<verify><automated>rtk pnpm --filter @liiiraa/api exec vitest --run src/staging/real-auth.test.ts</automated></verify>
<acceptance_criteria>/v1/account returns 401 without authority rather than 404; signup/login/session/logout are exercised against a PostgreSQL-compatible test boundary; readiness cannot claim authority when the database is unavailable.</acceptance_criteria>
</task>
</tasks>
<verification>Run focused adapter/API tests, TypeScript checks, migration replay, architecture and production fixture guards.</verification>
<success_criteria>A restarted staging API authenticates invitation-created accounts from PostgreSQL without any fixture or in-memory authority.</success_criteria>
<output>Create 04-36-SUMMARY.md.</output>
