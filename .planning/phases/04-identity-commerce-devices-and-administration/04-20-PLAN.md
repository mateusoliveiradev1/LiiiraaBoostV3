---
phase: 04-identity-commerce-devices-and-administration
plan: "20"
type: tdd
wave: 7
depends_on: ["04-07", "04-11", "04-12", "04-17"]
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - Cargo.lock
  - apps/desktop/src-tauri/src/identity.rs
  - apps/desktop/src-tauri/src/credential_store.rs
  - apps/desktop/src-tauri/tests/identity.rs
autonomous: true
requirements: [IDEN-01, IDEN-02]
must_haves:
  decision_ids: [D-10, D-56]
  truths:
    - "Desktop auth opens the system browser with PKCE/secure callback and stores tokens only in Windows Credential Manager, per D-10."
    - "The desktop forwards the one-shot authorization result to the API and never performs provider exchange or owns a provider/client secret."
    - "Web revocation clears the Credential Manager entry on the desktop's next authenticated contact, per D-56."
  artifacts:
    - path: apps/desktop/src-tauri/src/identity.rs
      provides: External-browser PKCE and callback lifecycle
      exports: [begin_desktop_sign_in, complete_desktop_callback]
    - path: apps/desktop/src-tauri/src/credential_store.rs
      provides: Windows Credential Manager token custody
  key_links:
    - from: apps/desktop/src-tauri/src/identity.rs
      to: apps/api/src/modules/identity/routes.ts
      via: desktop sends the one-shot authorization code, state, verifier, and callback evidence to the API; the API alone exchanges with the provider and issues the session
---
<objective>Activate native desktop system-browser identity and Windows Credential Manager custody.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/desktop/src-tauri/src/main.rs @apps/api/src/modules/identity/routes.ts @.planning/phases/04-identity-commerce-devices-and-administration/04-IDENTITY-SPIKE.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-20-01: Authenticate through the system browser and custody credentials natively</name>
  <read_first>D-10 and D-56; identity spike callback evidence; generated session contracts; API desktop exchange route</read_first>
  <files>apps/desktop/src-tauri/Cargo.toml, Cargo.lock, apps/desktop/src-tauri/src/identity.rs, apps/desktop/src-tauri/src/credential_store.rs, apps/desktop/src-tauri/tests/identity.rs</files>
  <behavior>- WebView initiates sign-in but never receives social password/client secret/bearer token; OS browser and one-shot PKCE callback complete.
- Desktop forwards the authorization code/state/verifier/callback proof to the API exchange route; no provider exchange, provider secret, or session authority executes in Rust.
- Refresh credential writes/reads/deletes only through Windows Credential Manager; SQLite/localStorage/log/deep-link scans contain no token.
- Revocation on contact signs out and deletes the Credential Manager entry without deleting local safety data.</behavior>
  <action>RED: create Rust callback, credential-custody, revocation, and API-handoff tests, including a failing assertion that Rust never contacts a provider token endpoint or holds a client secret. GREEN: add the project-approved `keyring@4.1.5` and `windows@0.62.2` pins to Cargo.toml/Cargo.lock, implement random state/verifier and S256 using the approved Windows/native boundary, receive the one-shot loopback/claimed callback selected by the spike, forward code/state/verifier/callback proof to the API desktop exchange route, and store only the API-issued rotated credential in Windows Credential Manager. REFACTOR callback ownership, credential deletion, and redacted errors; rerun the same focused Rust identity suite and record its measured runtime below 30 seconds. Run cargo build, supply-chain validation, and packaged Windows callback smoke at the plan gate.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop identity</automated></verify>
  <acceptance_criteria>Packaged-compatible native auth tests pass, no bearer token reaches prohibited stores/channels, and next-contact revocation clears the credential.</acceptance_criteria>
  <done>Desktop identity and credential custody are native, one-shot, provider-secret-free, and revocable.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- Rust commands `begin_desktop_sign_in`, `complete_desktop_callback`, `revoke_desktop_session`; `CredentialStore`.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| System browser/callback/credential manager -> desktop UI | OAuth code and refresh credentials cross OS/native boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-DESKTOP-OAUTH | Spoofing/Disclosure | callback | mitigate | PKCE/state/issuer/exact callback/one-shot listener; no WebView token. |
| T-04-DESKTOP-TOKEN | Information Disclosure | credential store | mitigate | Credential Manager only, rotation/revocation/redaction and storage scans. |
</threat_model>
<verification>Record the focused Rust identity task check below 30 seconds, then run `rtk cargo build -p liiiraa-desktop`, Cargo supply-chain validation, and packaged Windows system-browser/callback/Credential Manager smoke as plan-level gates with measured runtimes.</verification>
<success_criteria>Desktop authentication is real, system-browser based, API-exchanged, securely stored, and revocable.</success_criteria>
<output>Create `04-20-SUMMARY.md` with TDD gates.</output>
