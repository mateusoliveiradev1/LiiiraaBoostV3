---
phase: 04-identity-commerce-devices-and-administration
plan: "20"
type: tdd
wave: 7
depends_on: ["04-07", "04-11", "04-12", "04-17"]
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - Cargo.lock
  - apps/desktop/src-tauri/src/identity.rs
  - apps/desktop/src-tauri/src/credential_store.rs
  - apps/desktop/src-tauri/src/account_sync.rs
  - apps/desktop/src-tauri/tests/identity.rs
  - apps/desktop/src/account-authority.ts
  - apps/desktop/src/features/account-experience.tsx
  - packages/feature-shell/src/features/account-settings.tsx
  - apps/desktop/tests/browser/account-authority.spec.ts
autonomous: true
requirements: [WEB-04, IDEN-01, IDEN-02]
must_haves:
  decision_ids: [D-10, D-52, D-53, D-54, D-55, D-56]
  truths:
    - "Desktop auth opens the system browser with PKCE/secure callback and stores tokens only in Windows Credential Manager, per D-10."
    - "Desktop replaces fixture name/.local email/simulated Premium/local device authority with real online/offline/stale/pending projections, per D-53."
    - "Sync runs on launch, resume, reconnection, and relevant mutations; conflict preserves remote and local draft, per D-54/D-55."
    - "Web revocation clears credentials and blocks new Premium work on next contact while safety data remains, per D-56."
  artifacts:
    - path: apps/desktop/src-tauri/src/identity.rs
      provides: External-browser PKCE and callback lifecycle
      exports: [begin_desktop_sign_in, complete_desktop_callback]
    - path: apps/desktop/src-tauri/src/credential_store.rs
      provides: Windows Credential Manager token custody
    - path: apps/desktop/src-tauri/src/account_sync.rs
      provides: Version-aware account sync orchestrator
  key_links:
    - from: apps/desktop/src/account-authority.ts
      to: Tauri identity/account_sync commands
      via: generated IPC and HTTP contracts with no WebView token handling
    - from: apps/desktop/src-tauri/src/identity.rs
      to: apps/api/src/modules/identity/routes.ts
      via: desktop sends the one-shot authorization code, state, verifier, and callback evidence to the API; the API alone exchanges with the provider and issues the session
---
<objective>Activate real desktop identity, secure credential custody, and truthful shared-account synchronization.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@apps/desktop/src/features/account-experience.tsx @packages/feature-shell/src/features/account-settings.tsx @apps/desktop/src-tauri/src/main.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 04-20-01: Authenticate and synchronize desktop account authority</name>
  <read_first>D-10 and D-52 through D-56; identity spike callback evidence; generated account/session contracts; current desktop account surfaces</read_first>
  <files>apps/desktop/src-tauri/Cargo.toml, Cargo.lock, apps/desktop/src-tauri/src/identity.rs, apps/desktop/src-tauri/src/credential_store.rs, apps/desktop/src-tauri/src/account_sync.rs, apps/desktop/src-tauri/tests/identity.rs, apps/desktop/src/account-authority.ts, apps/desktop/src/features/account-experience.tsx, packages/feature-shell/src/features/account-settings.tsx, apps/desktop/tests/browser/account-authority.spec.ts</files>
  <behavior>- WebView initiates sign-in but never receives social password/client secret/bearer token; OS browser and one-shot PKCE callback complete.
- Desktop forwards the authorization code/state/verifier/callback proof to the API exchange route; no provider exchange, provider secret, or session authority executes in Rust.
- Refresh credential writes/reads/deletes only through Windows Credential Manager; SQLite/localStorage/log/deep-link scans contain no token.
- Launch/resume/reconnect/post-mutation sync renders online/offline/stale/pending/conflict states.
- Stale write preserves safe local draft and remote projection.
- Revocation on contact signs out, clears credential, blocks new paid actions, preserves local warnings/history/restoration.</behavior>
  <action>RED: create Rust callback/credential/API-handoff tests and desktop state witnesses, including a failing assertion that Rust never contacts a provider token endpoint or holds a client secret. GREEN: add exact approved `keyring@4.1.6` and project-approved `windows@0.62.2` pins to Cargo.toml/Cargo.lock, implement random state/verifier and S256 using the approved Windows/native boundary, receive the one-shot loopback/claimed callback selected by the spike, forward code/state/verifier/callback proof to the API desktop exchange route, and store only the API-issued rotated credential in Windows Credential Manager. Implement version-aware sync and map the real projection while retaining fixture composition. REFACTOR sync triggers/redacted errors, rerun cargo supply-chain/build/tests, and prove no provider exchange or backend/session secret moved into the client tier.</action>
  <verify><automated>rtk cargo test -p liiiraa-desktop identity && rtk cargo build -p liiiraa-desktop</automated></verify>
  <acceptance_criteria>Packaged-compatible auth/sync tests pass, no bearer token reaches prohibited stores/channels, and revocation/conflict behavior is truthful.</acceptance_criteria>
  <done>Desktop shares real account truth without surrendering local-first safety data.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
- Rust commands `begin_desktop_sign_in`, `complete_desktop_callback`, `sync_account`, `revoke_desktop_session`; `CredentialStore`.
- TS `DesktopAccountAuthority` and lifecycle triggers `launch`, `resume`, `reconnect`, `post-mutation`.
</artifacts_this_phase_produces>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| System browser/callback/credential manager -> desktop UI | OAuth code and refresh credentials cross OS/native boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-DESKTOP-OAUTH | Spoofing/Disclosure | callback | mitigate | PKCE/state/issuer/exact callback/one-shot listener; no WebView token. |
| T-04-DESKTOP-TOKEN | Information Disclosure | credential store | mitigate | Credential Manager only, rotation/revocation/redaction and storage scans. |
</threat_model>
<verification>Run focused Rust identity/build checks after the task, then run the complete desktop account-authority Playwright, fixture guard, and packaged Windows smoke as the plan gate; record runtimes.</verification>
<success_criteria>Desktop authentication and shared projection are real, secure, version-aware, and local-first.</success_criteria>
<output>Create `04-20-SUMMARY.md` with TDD gates.</output>
