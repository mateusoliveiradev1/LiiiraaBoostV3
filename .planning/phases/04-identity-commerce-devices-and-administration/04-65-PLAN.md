---
phase: 04-identity-commerce-devices-and-administration
plan: '65'
type: tdd
wave: 28
depends_on: ['04-35', '04-38']
files_modified:
  - apps/desktop/src/account-authority.ts
  - apps/desktop/src/account-authority.test.ts
  - apps/desktop/src/features/account-experience.tsx
  - apps/desktop/src/features/account-experience.test.ts
autonomous: true
requirements: [IDEN-02, IDEN-03]
gap_closure: true
must_haves:
  truths:
    - 'One successful native sign-out immediately invalidates the desktop account authority before navigation, so one click reaches the login screen and stays there.'
    - 'A synchronization started before sign-out cannot republish the former authenticated projection after revocation.'
    - 'Repeated sign-out/revocation notifications are idempotent and never erase local warnings, history, diagnostics, or restoration data.'
  artifacts:
    - path: apps/desktop/src/account-authority.ts
      provides: Explicit confirmed-sign-out transition with synchronization invalidation
      exports: [DesktopAccountAuthority]
    - path: apps/desktop/src/account-authority.test.ts
      provides: One-click, concurrent-sync, and idempotent-revocation regression evidence
  key_links:
    - from: apps/desktop/src/features/account-experience.tsx
      to: apps/desktop/src/account-authority.ts
      via: native 204 confirmation invokes the authority transition before routing to login
---

<objective>Make desktop logout a single, durable authority transition instead of a navigation race.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/debug/desktop-sign-out-double-action.md @.planning/phases/04-identity-commerce-devices-and-administration/04-UAT.md @.planning/phases/04-identity-commerce-devices-and-administration/04-35-SUMMARY.md @.planning/phases/04-identity-commerce-devices-and-administration/04-38-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 04-65-01: Lock the one-click logout race with focused regressions</name>
<read_first>.planning/debug/desktop-sign-out-double-action.md; apps/desktop/src/account-authority.ts; apps/desktop/src/account-authority.test.ts; apps/desktop/src/features/account-experience.tsx; apps/desktop/src/features/account-experience.test.ts</read_first>
<files>apps/desktop/src/account-authority.test.ts, apps/desktop/src/features/account-experience.test.ts</files>
<behavior>- A successful native sign-out changes the shared authority snapshot to revoked before `/login` resolution.
- A deferred online synchronization resolved after sign-out is ignored.
- A queued resume/reconnect synchronization cannot restart until a new authenticated session is established.
- Calling the transition twice produces the same revoked snapshot and one stable user-visible outcome.</behavior>
<action>Add failing unit/component witnesses around the singleton authority rather than mocking navigation alone. Exercise an online projection, delayed transport response, server-confirmed sign-out, route resolution, repeated revocation event, and preservation of local safety navigation.</action>
<verify><automated>rtk pnpm --filter @liiiraa/desktop exec vitest --run src/account-authority.test.ts src/features/account-experience.test.ts</automated></verify>
<acceptance_criteria>The new witnesses reproduce the current first-click redirect back to the authenticated account and the stale synchronization overwrite before implementation.</acceptance_criteria>
<done>The UAT issue is represented by deterministic focused tests.</done>
</task>

<task type="auto" tdd="true">
<name>Task 04-65-02: Publish confirmed sign-out before navigating</name>
<read_first>Tests from Task 04-65-01; DesktopAccountAuthority sequencing and queue logic; desktopAuth.signOut result handling; ACCOUNT_AUTHORITY_REVOKED_EVENT consumers</read_first>
<files>apps/desktop/src/account-authority.ts, apps/desktop/src/features/account-experience.tsx</files>
<action>Add an idempotent `confirmSignedOut` authority transition that increments/invalidates the synchronization sequence, clears queued account synchronizations, publishes `{ state: 'revoked', error: 'unauthorized' }`, and leaves local safety data untouched. After the native sign-out returns `signed-out`, call this transition and await its publication before emitting the shared chrome event and navigating to `/login`. Do not interpret a transport failure as logout success.</action>
<verify><automated>rtk pnpm --filter @liiiraa/desktop verify</automated></verify>
<acceptance_criteria>One click remains on login; late/queued synchronization cannot resurrect the prior projection; retry and revocation remain fail-closed and idempotent.</acceptance_criteria>
<done>Desktop session removal, in-memory account authority, chrome identity, and routing agree after the first successful action.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Native server-confirmed revocation -> renderer authority | A successful 204 must invalidate all renderer projections without allowing a stale asynchronous response to restore them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-65-S | Spoofing | account authority snapshot | mitigate | Only the native signed-out result invokes the confirmed transition; ordinary navigation cannot manufacture authenticated or revoked authority. |
| T-04-65-T | Tampering | synchronization sequence | mitigate | Monotonic invalidation rejects all pre-revocation responses and clears queued triggers. |
| T-04-65-R | Repudiation | logout outcome | mitigate | Preserve the existing server-side session-revocation audit; renderer publishes one deterministic state rather than inventing success. |
</threat_model>

<verification>Run focused Vitest regressions first, then the complete desktop verify gate without Docker.</verification>
<success_criteria>Real desktop logout succeeds with one action and no stale authority can reauthenticate the renderer.</success_criteria>
<output>Create `04-65-SUMMARY.md` with the reproduced race and passing one-click evidence.</output>
