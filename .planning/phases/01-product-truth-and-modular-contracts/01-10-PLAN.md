---
phase: 01-product-truth-and-modular-contracts
plan: "10"
type: tdd
wave: 13
depends_on: ["01-12", "01-16", "01-17", "01-18", "01-19", "01-20"]
files_modified:
  - package.json
  - turbo.json
  - .github/workflows/ci.yml
  - tooling/ci/verify-required-artifacts.mjs
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06]
must_haves:
  truths:
    - "Quick and full root commands reach every Phase 1 gate, including contract compatibility."
    - "The final acceptance-policy mode rejects every planned, missing, or unresolved evidence reference."
    - "CI cannot pass after removal of any required gate or artifact."
  artifacts:
    - path: tooling/ci/verify-required-artifacts.mjs
      provides: "Omission and reachability meta-gate"
    - path: .github/workflows/ci.yml
      provides: "Least-privilege frozen quick/full CI"
  key_links:
    - from: package.json
      to: tooling/acceptance-policy/src/policy.ts
      via: "final-mode acceptance command"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "authoritative root verification"
---
<objective>
Integrate every Phase 1 invariant into final local and CI gates.

Purpose: Prevent drift, breaking contracts, deception, architectural violations, or planned evidence from passing release checks.
Output: Quick/full orchestration, omission meta-gate, and least-privilege CI.
</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/01-product-truth-and-modular-contracts/01-16-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-17-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-20-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-10-01: Build root gates and the omission meta-gate</name>
  <files>package.json, turbo.json, tooling/ci/verify-required-artifacts.mjs</files>
  <behavior>
    - Quick includes format, types, drift, compatibility, architecture, contracts, adapters, runtime truth, and planned-policy structure.
    - Full adds properties, artifact/E2E truth, audits, and acceptance policy in final mode.
    - Removing contracts:compat, any negative proof, any manifest, or any final evidence fails.
  </behavior>
  <action>Write meta-gate mutation tests first. Wire stable `verify:quick` and `verify` with no continue-on-error, watch, or optional gate. Invoke acceptance policy explicitly as `--mode final` in full verification. Add compatibility gate, its accepted/breaking fixtures, and ADR to required-artifact reachability. Keep quick under target by moving only property/advisory/artifact-depth work to full.</action>
  <verify><automated>rtk pnpm verify:quick &amp;&amp; rtk pnpm verify</automated></verify>
  <done>Both commands terminate green only with every requirement's final resolvable evidence and compatibility gate present.</done>
</task>
<task type="auto">
  <name>Task 01-10-02: Add least-privilege frozen CI</name>
  <files>.github/workflows/ci.yml, tooling/ci/verify-required-artifacts.mjs</files>
  <action>Create SHA-pinned Linux quick and Linux/Windows full jobs using exact Node, pnpm, Rust, and Cargo tools, read-only repository permissions, allowlist verification before frozen install, and authoritative root commands. Upload only bounded redacted failures; use no cloud credentials or write scopes. Assert CI reachability includes `contracts:compat` and final acceptance mode.</action>
  <verify><automated>rtk pnpm exec prettier --check .github/workflows/ci.yml &amp;&amp; rtk node tooling/ci/verify-required-artifacts.mjs --ci .github/workflows/ci.yml</automated></verify>
  <done>CI fails closed on missing tools, contract compatibility, generated drift, production truth, or unresolved final evidence.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01 | Tampering | Gate graph | mitigate | Mutation-tested meta-gate. |
| T-10-02 | Elevation of privilege | CI permissions | mitigate | Read-only jobs and no cloud credentials. |
</threat_model>
<verification>Run root quick/full and CI reachability from a clean frozen install.</verification>
<success_criteria>All FOUND-01..06 evidence is final, executable, compatibility-checked, and CI-reachable.</success_criteria>
<output>Create `01-10-SUMMARY.md`.</output>
