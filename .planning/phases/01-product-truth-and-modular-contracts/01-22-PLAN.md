---
phase: 01-product-truth-and-modular-contracts
plan: '22'
type: tdd
wave: 15
depends_on: ['01-12', '01-21']
files_modified:
  - architecture/module-boundaries.json
  - dependency-cruiser.config.mjs
  - tooling/architecture-tests/src/check-workspace.ts
  - tooling/architecture-tests/src/policy.test.ts
  - architecture/OWNERSHIP.md
  - architecture/README.md
autonomous: true
requirements: [FOUND-05]
gap_closure: true
must_haves:
  truths:
    - 'Every active pnpm workspace package enters the live TypeScript architecture graph even when no canonical module record owns it.'
    - 'An undeclared active package fails the live workspace adapter with UNKNOWN_OWNER before dependency evaluation.'
    - 'Live mutations prove forbidden dependency and cycle diagnostics through the same workspace adapter used by the root architecture gate.'
    - 'All current package records, statuses, and public roots match the implemented repository while legitimate future boundaries remain reserved.'
    - 'Cargo discovery and the canonical evaluateGraph diagnostic authority remain behaviorally unchanged.'
  artifacts:
    - path: tooling/architecture-tests/src/check-workspace.ts
      provides: 'Policy-independent pnpm workspace discovery and complete deterministic graph normalization'
    - path: tooling/architecture-tests/src/policy.test.ts
      provides: 'Live negative mutations for unknown ownership, forbidden direction, and cycles'
    - path: architecture/module-boundaries.json
      provides: 'Complete current ownership records with truthful active/reserved status and public roots'
    - path: dependency-cruiser.config.mjs
      provides: 'Workspace-derived includeOnly scope while retaining policy-derived forbidden rules'
    - path: architecture/OWNERSHIP.md
      provides: 'Human ownership table synchronized to the canonical policy'
    - path: architecture/README.md
      provides: 'Contributor guidance for active and reserved module semantics'
  key_links:
    - from: pnpm-workspace.yaml
      to: tooling/architecture-tests/src/check-workspace.ts
      via: 'workspace root discovery that does not read module-boundaries policy'
    - from: tooling/architecture-tests/src/check-workspace.ts
      to: dependency-cruiser.config.mjs
      via: 'escaped includeOnly pattern built from discovered package roots'
    - from: tooling/architecture-tests/src/check-workspace.ts
      to: tooling/architecture-tests/src/policy.ts
      via: 'complete discovered-root graph passed to the unchanged evaluateGraph evaluator'
    - from: architecture/module-boundaries.json
      to: architecture/OWNERSHIP.md
      via: 'same ordered module inventory, status, owner, runtime class, and public roots'
---

<objective>
Close the self-blind live TypeScript architecture discovery boundary without changing the canonical evaluator or Cargo behavior.

Purpose: Restore FOUND-05 and ROADMAP success criterion 4 by making repository existence an independent input from ownership policy, so a newly added workspace package cannot evade unknown-owner, forbidden-edge, or cycle enforcement.
Output: A complete active module constitution, policy-independent workspace discovery, live negative mutation evidence, and synchronized contributor ownership guidance.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
@C:/Users/Liiiraa/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-product-truth-and-modular-contracts/01-VERIFICATION.md
@.planning/phases/01-product-truth-and-modular-contracts/01-PATTERNS.md
@.planning/phases/01-product-truth-and-modular-contracts/01-12-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-21-SUMMARY.md
@pnpm-workspace.yaml
@architecture/module-boundaries.json
@architecture/decisions/0003-module-ownership-and-direction.md
@dependency-cruiser.config.mjs
@tooling/architecture-tests/src/check-cargo.ts
@tooling/architecture-tests/src/check-workspace.ts
@tooling/architecture-tests/src/policy.ts
@tooling/architecture-tests/src/policy.test.ts
@tooling/workspace-smoke/check-toolchain.mjs

<interfaces>
- Preserve `runLiveWorkspaceCheck(policyInput: unknown): Promise&lt;WorkspaceCheckResult&gt;` as the production entry point; optional test seams may be additive, but default behavior must still inspect `process.cwd()`.
- Preserve `createDependencyCruiserRestrictions(policyInput: unknown): DependencyCruiserRestriction[]` as the policy-derived rule generator.
- Preserve `runArchitectureAdapters(workspaceAdapter, cargoAdapter): Promise&lt;ArchitectureCheckResult&gt;` and its exact `workspace=1, cargo=1` execution-count behavior.
- Reuse `normalizeRepositoryPath`, `runtimeClassForPath`, `NormalizedDependencyGraph`, and `evaluateGraph`; do not create a second owner lookup or diagnostic taxonomy.
- Add exported `discoverPnpmWorkspaceRoots(repositoryRoot: string): string[]` and `createWorkspaceRootPattern(repositoryRoot: string): string` contracts for independently discovered, escaped, deterministically sorted package roots.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-22-01 RED: Specify complete live workspace discovery and mutation failures</name>
  <read_first>
    - .planning/phases/01-product-truth-and-modular-contracts/01-VERIFICATION.md
    - .planning/phases/01-product-truth-and-modular-contracts/01-PATTERNS.md
    - pnpm-workspace.yaml
    - architecture/module-boundaries.json
    - dependency-cruiser.config.mjs
    - tooling/architecture-tests/src/check-workspace.ts
    - tooling/architecture-tests/src/policy.ts
    - tooling/architecture-tests/src/policy.test.ts
    - tooling/workspace-smoke/check-toolchain.mjs
  </read_first>
  <files>tooling/architecture-tests/src/policy.test.ts</files>
  <behavior>
    - Discovery returns the twelve current pnpm package roots in stable repository-relative order without consulting `architecture/module-boundaries.json`.
    - Removing an active package's module record does not remove that package from discovery; the live adapter returns `UNKNOWN_OWNER` for its package sentinel before evaluating edges.
    - A temporary undeclared package placed under an allowed pnpm workspace root reaches the live adapter and fails with `UNKNOWN_OWNER`.
    - Giving the temporary package a test-only canonical owner and seeding an import against forbidden layer direction fails with the existing `LAYER_DIRECTION` diagnostic.
    - Seeding two source files with a dependency cycle fails with the existing `CYCLE` diagnostic.
    - The mutation suite always removes its exact temporary directory and leaves the clean live architecture gate passing.
  </behavior>
  <action>
Add a sequential `live workspace discovery boundary` test group before changing implementation. Assert the exact twelve-root inventory from 01-PATTERNS.md and prove that discovery is invariant when a cloned policy omits one active record. Add one filesystem mutation harness using `mkdtemp` with the prefix `tooling/liiiraa-architecture-mutation-`; resolve and validate that the created directory is an immediate child of the repository's `tooling` directory before writing or removing it. Seed only package manifests, a minimal source tree, and imports needed for the three negative cases. Exercise `runLiveWorkspaceCheck` for every case, compare complete diagnostic objects and ordering, and perform exact-path recursive cleanup in `finally`. Use the canonical policy unchanged for the unknown-owner case, then a test-local declared module for the forbidden-edge and cycle cases. Do not alter `policy.ts`, duplicate `UNKNOWN_OWNER`, or mock `evaluateGraph`; RED must fail because discovery/configuration currently excludes the seeded root and omits discovery sentinel nodes.
  </action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/architecture-tests test -- --run -t "live workspace discovery boundary"</automated>
  </verify>
  <acceptance_criteria>
    - The new focused group executes all inventory, unknown-owner, forbidden-edge, cycle, and cleanup assertions.
    - Failure is caused by the current self-scoped discovery implementation, not syntax, missing fixtures, or an unrelated existing test.
    - The RED commit changes only `tooling/architecture-tests/src/policy.test.ts`.
  </acceptance_criteria>
  <done>The missing live-discovery behavior is captured by deterministic failing tests that use the production adapter and unchanged canonical evaluator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-22-02 GREEN: Discover pnpm roots independently and complete canonical ownership</name>
  <read_first>
    - tooling/architecture-tests/src/policy.test.ts
    - tooling/architecture-tests/src/check-workspace.ts
    - tooling/architecture-tests/src/check-cargo.ts
    - tooling/architecture-tests/src/policy.ts
    - tooling/workspace-smoke/check-toolchain.mjs
    - dependency-cruiser.config.mjs
    - pnpm-workspace.yaml
    - architecture/module-boundaries.json
    - packages/contracts-source/src/main.tsp
    - tooling/acceptance-policy/src/policy.ts
    - tooling/contract-compat/src/check-compat.ts
    - tooling/contract-generation/src/generate.ts
    - tooling/contract-generation-spike/src/run-spike.ts
    - tooling/workspace-smoke/check-toolchain.mjs
  </read_first>
  <files>tooling/architecture-tests/src/check-workspace.ts, dependency-cruiser.config.mjs, architecture/module-boundaries.json</files>
  <behavior>
    - Workspace discovery is derived from `pnpm-workspace.yaml` plus package manifests and is unaffected by module policy additions or removals.
    - Every discovered package contributes a normalized `&lt;root&gt;/package.json` sentinel node even when dependency-cruiser reports no source module or edge.
    - Dependency-cruiser scans all discovered roots, while forbidden direction, public-root, production-fixture, and cycle rules remain derived from canonical policy.
    - The existing Cargo adapter, its metadata normalization, and execution count remain unchanged.
  </behavior>
  <action>
Implement `discoverPnpmWorkspaceRoots(repositoryRoot)` inside `check-workspace.ts` using the bounded manifest traversal conventions from `tooling/workspace-smoke/check-toolchain.mjs`: read the workspace patterns from `pnpm-workspace.yaml`, accept the repository's package-root patterns, exclude `node_modules`, dot paths, generated distributions, and symlink traversal, require a `package.json`, normalize separators, deduplicate, and sort. Fail closed with a stable error if the workspace file or a supported pattern is malformed instead of silently falling back to policy roots. Implement `createWorkspaceRootPattern(repositoryRoot)` by escaping those discovered roots with the existing regex helper. Update `dependency-cruiser.config.mjs` so only `options.includeOnly` comes from `createWorkspaceRootPattern(process.cwd())`; keep `forbidden` produced by `createDependencyCruiserRestrictions(canonicalPolicy)` and retain the existing `node_modules|dist` exclusion and TypeScript config.

Extend workspace graph normalization so every discovered package root contributes a deterministic manifest sentinel node before the dependency-cruiser nodes and edges are sorted, then pass that complete graph to the unchanged `evaluateGraph`. `runLiveWorkspaceCheck` must discover first, cruise the same root set, merge sentinel/source nodes without duplicates, and return the existing `WorkspaceCheckResult` shape. Preserve `createCanonicalRootPattern` only where existing tests or rule construction still need policy roots; it must no longer control live inclusion.

Update `architecture/module-boundaries.json` in its established field order. Add separate active tooling records for `acceptance-policy` (owner `architecture`, public root `tooling/acceptance-policy/src/policy.ts`), `contract-compat` (owner `contracts`, public root `tooling/contract-compat/src/check-compat.ts`), `contract-generation` (owner `contracts`, public root `tooling/contract-generation/src/generate.ts`), `contract-generation-spike` (owner `contracts`, public root `tooling/contract-generation-spike/src/run-spike.ts`), and `workspace-smoke` (owner `architecture`, public root `tooling/workspace-smoke/check-toolchain.mjs`). Each record uses its exact package root, `layer: "tooling"`, `runtimeClass: "tooling"`, and `status: "active"`; do not merge them into a catch-all tooling record or add exceptions. Change `contracts-source`, `desktop-client`, and `desktop-simulator` to `active`, and correct `contracts-source.publicRoots` to `packages/contracts-source/src/main.tsp`. Leave legitimate unimplemented boundaries reserved. Do not modify `check-cargo.ts` or `policy.ts`.
</action>
<verify>
<automated>rtk pnpm test:architecture &amp;&amp; rtk pnpm --filter @liiiraa/architecture-tests check</automated>
</verify>
<acceptance_criteria> - The focused RED group is green for exact twelve-root discovery and all three live negative mutations. - Deleting any one active module record from a test-local policy yields `UNKNOWN_OWNER` without changing the discovered root set. - All five formerly omitted tooling packages have distinct canonical active records with real public roots. - All implemented TypeScript packages are active and the TypeSpec package points at `src/main.tsp`. - `pnpm test:architecture` reports exactly one workspace and one Cargo adapter execution, and every pre-existing Cargo assertion remains green.
</acceptance_criteria>
<done>The live TypeScript graph covers all active pnpm packages independently of ownership policy and the canonical constitution truthfully owns the current repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-22-03 REFACTOR: Synchronize contributor truth and run closure gates</name>
  <read_first>
    - architecture/module-boundaries.json
    - architecture/decisions/0003-module-ownership-and-direction.md
    - architecture/OWNERSHIP.md
    - architecture/README.md
    - tooling/architecture-tests/src/check-workspace.ts
    - tooling/architecture-tests/src/policy.test.ts
    - package.json
  </read_first>
  <files>architecture/OWNERSHIP.md, architecture/README.md</files>
  <action>
Update the ordered ownership table in `architecture/OWNERSHIP.md` to mirror every canonical module record, including the five tooling modules, three corrected active statuses, and the TypeSpec `src/main.tsp` public root. State both ADR 0003 meanings exactly: active identifies an implementation root participating in the executable foundation; reserved allocates a future boundary without asserting an implementation exists. Keep JSON as the executable authority and keep the existing ownership-change checklist.

Update `architecture/README.md` so contributor workflow explains that pnpm/Cargo workspace metadata determines what exists, `module-boundaries.json` determines ownership and legal dependency direction, every existing workspace package must be active, and reserved records must not be used to hide implemented roots. Preserve the empty-shell warning, Phase 1 scope fence, existing commands, and references to the shared canonical evaluator. After documentation is synchronized, refactor only obvious duplication in the new discovery/test helpers while preserving the GREEN behavior and complete deterministic diagnostics.
</action>
<verify>
<automated>rtk pnpm docs:check &amp;&amp; rtk node tooling/ci/verify-required-artifacts.mjs --docs &amp;&amp; rtk pnpm test:architecture</automated>
</verify>
<acceptance_criteria> - Human ownership rows and status semantics match `module-boundaries.json` without stale reserved labels or nonexistent entrypoints. - Documentation identifies workspace metadata as existence authority and canonical policy as ownership/dependency authority. - The mutation suite leaves no `liiiraa-architecture-mutation-*` directory after success or failure. - Documentation, architecture, quick, full, acceptance, and Cargo gates all terminate green.
</acceptance_criteria>
<done>Executable policy, live discovery evidence, Cargo regression coverage, and contributor documentation agree on every current module.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                      |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Workspace manifests → live architecture graph   | Repository-controlled package roots are treated as existence evidence before ownership is consulted.             |
| Dependency-cruiser output → canonical evaluator | Tool output is normalized, completed with discovery sentinels, and evaluated by the existing fail-closed policy. |
| Mutation fixture → real worktree                | Tests temporarily create one bounded tooling package and must validate and remove only that exact path.          |

## STRIDE Threat Register

| Threat ID       | Category          | Component                        | Disposition | Mitigation Plan                                                                                                                              |
| --------------- | ----------------- | -------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-22-01         | Tampering         | Workspace discovery              | mitigate    | Derive roots from pnpm workspace configuration and manifests, never the ownership allowlist being audited.                                   |
| T-22-02         | Repudiation       | Empty or dependency-free package | mitigate    | Add one deterministic manifest sentinel node per discovered package so absence of source edges cannot erase ownership evidence.              |
| T-22-03         | Denial of service | Filesystem traversal             | mitigate    | Bound traversal to workspace patterns; exclude symlinks, dot paths, dependencies, and generated distributions; sort and deduplicate results. |
| T-22-04         | Tampering         | Mutation cleanup                 | mitigate    | Use a validated architecture-specific temporary child path and exact-path `finally` cleanup.                                                 |
| T-22-SC         | Tampering         | Dependency installation          | accept      | No dependency or lockfile change is permitted by this plan.                                                                                  |
| </threat_model> |

<verification>
1. Run `rtk pnpm --filter @liiiraa/architecture-tests test -- --run -t "live workspace discovery boundary"` and confirm exact inventory plus UNKNOWN_OWNER, LAYER_DIRECTION, and CYCLE cases.
2. Run `rtk pnpm test:architecture` and confirm `Architecture adapters executed: workspace=1, cargo=1.` with no diagnostics on the clean repository.
3. Run `rtk pnpm docs:check` and `rtk node tooling/ci/verify-required-artifacts.mjs --docs`.
4. Run `rtk pnpm verify:quick`, `rtk pnpm verify`, and `rtk pnpm acceptance:check -- --mode final`.
5. Confirm `rtk git status --short` shows no mutation package residue and no generated drift beyond the six planned implementation/documentation files.
</verification>

<success_criteria>

- FOUND-05 is true for every active TypeScript and Cargo workspace member, not only predeclared policy roots.
- An undeclared pnpm package is observable and rejected as `UNKNOWN_OWNER` by the live root adapter.
- Live forbidden-direction and cycle mutations fail through the same complete graph and unchanged canonical evaluator.
- Current module records and contributor docs contain all twelve pnpm packages with truthful status/public roots.
- Cargo normalization, execution, diagnostics, and final acceptance behavior remain green.
  </success_criteria>

## Source Audit

| Source       | ID       | Item                                                                                  | Plan                    | Status         | Notes                                                                            |
| ------------ | -------- | ------------------------------------------------------------------------------------- | ----------------------- | -------------- | -------------------------------------------------------------------------------- |
| GOAL         | —        | Automated gates prevent architectural drift                                           | 01-22                   | COVERED        | Independent workspace discovery closes the only failed architecture truth.       |
| GOAL         | —        | Fixture deception and quality-obligation omissions remain prevented                   | existing verified plans | OUT OF GAP     | `01-VERIFICATION.md` marks production truth and acceptance policy VERIFIED.      |
| REQ          | FOUND-05 | Reject forbidden cross-module imports and circular dependencies                       | 01-22                   | COVERED        | Complete live graph plus unknown-owner, forbidden-edge, and cycle mutations.     |
| REQ          | FOUND-01 | Versioned generated cross-language contracts                                          | existing verified plans | OUT OF GAP     | Verified; this plan does not change contract generation or validation.           |
| REQ          | FOUND-02 | Shared simulator/production conformance                                               | existing verified plans | OUT OF GAP     | Verified; adapters are not modified.                                             |
| REQ          | FOUND-03 | Truth provenance                                                                      | existing verified plans | OUT OF GAP     | Verified; provenance contracts are not modified.                                 |
| REQ          | FOUND-04 | Production fixture refusal                                                            | existing verified plans | OUT OF GAP     | Verified; production truth defenses are not modified.                            |
| REQ          | FOUND-06 | Five-dimension acceptance coverage                                                    | existing verified plans | OUT OF GAP     | Verified; final acceptance is regression-tested only.                            |
| RESEARCH     | —        | Machine-readable architecture constitution drives dependency-cruiser and Cargo policy | 01-22                   | COVERED        | Ownership remains canonical while existence discovery becomes ecosystem-owned.   |
| RESEARCH     | —        | Reserved records do not scaffold future product modules                               | 01-22                   | COVERED        | Legitimate future records remain reserved; only implemented roots become active. |
| CONTEXT      | —        | No phase CONTEXT.md decisions exist                                                   | —                       | NOT APPLICABLE | Gap closure is governed by verification and pattern evidence.                    |
| VERIFICATION | FOUND-05 | Five active packages omitted from live TypeScript discovery                           | 01-22                   | COVERED        | Exact root cause and all listed missing items are addressed.                     |

## Artifacts this phase produces

- `tooling/architecture-tests/src/check-workspace.ts`
  - New export `discoverPnpmWorkspaceRoots(repositoryRoot: string): string[]`.
  - New export `createWorkspaceRootPattern(repositoryRoot: string): string`.
  - Complete graph normalization with one manifest sentinel node per discovered package.
- `dependency-cruiser.config.mjs`
  - Workspace-derived `options.includeOnly`; canonical-policy-derived `forbidden` rules remain.
- `tooling/architecture-tests/src/policy.test.ts`
  - New sequential `live workspace discovery boundary` inventory and mutation suite.
- `architecture/module-boundaries.json`
  - New module IDs `acceptance-policy`, `contract-compat`, `contract-generation`, `contract-generation-spike`, and `workspace-smoke`.
  - Corrected active status for `contracts-source`, `desktop-client`, and `desktop-simulator`.
  - Corrected public root `packages/contracts-source/src/main.tsp`.
- `architecture/OWNERSHIP.md`
  - Complete synchronized ownership/public-entrypoint table and active/reserved semantics.
- `architecture/README.md`
  - Explicit separation of workspace existence discovery from canonical ownership/dependency policy.

<output>Create `.planning/phases/01-product-truth-and-modular-contracts/01-22-SUMMARY.md` when done.</output>
