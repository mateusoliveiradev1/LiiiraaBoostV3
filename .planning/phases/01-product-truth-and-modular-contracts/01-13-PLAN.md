---
phase: 01-product-truth-and-modular-contracts
plan: "13"
type: tdd
wave: 4
depends_on: ["01-04", "01-11"]
files_modified:
  - tooling/contract-generation-spike-rust/Cargo.toml
  - tooling/contract-generation-spike-rust/src/main.rs
  - tooling/contract-generation-spike-rust/tests/generation.rs
  - architecture/decisions/0001-contract-generation-spike.md
autonomous: true
requirements: [FOUND-01, FOUND-03]
must_haves:
  truths:
    - "Rust consumes the exact persisted TypeSpec spike schema after deterministic regeneration."
    - "Unsupported constructs fail instead of receiving generated-code patches."
  artifacts:
    - path: architecture/decisions/0001-contract-generation-spike.md
      provides: "Closed cross-language generator decision"
  key_links:
    - from: tooling/contract-generation-spike-rust/tests/generation.rs
      to: tooling/contract-generation-spike/generated/spike.schema.json
      via: "exact shared artifact generated before Cargo"
---
<objective>Prove Rust generation parity and close the generator ADR.</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/01-product-truth-and-modular-contracts/01-04-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-11-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-13-01: Generate Rust from the exact regenerated spike schema</name>
  <files>tooling/contract-generation-spike-rust/Cargo.toml, tooling/contract-generation-spike-rust/src/main.rs, tooling/contract-generation-spike-rust/tests/generation.rs</files>
  <behavior>
    - Typify consumes the exact persisted schema and emits deterministic compiling Rust.
    - Serialization preserves version and provenance discriminators.
  </behavior>
  <action>Write Rust tests first. The terminating verification must invoke the TypeSpec spike generator before Cargo so a clean checkout always materializes the exact schema. Format generated output deterministically and reject unsupported constructs without hand-editing output.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation-spike test -- --run &amp;&amp; rtk cargo test -p contract-generation-spike-rust</automated></verify>
  <done>Schema generation precedes Cargo and both languages prove the same unchanged vectors.</done>
</task>
<task type="auto">
  <name>Task 01-13-02: Close the generator decision</name>
  <files>architecture/decisions/0001-contract-generation-spike.md</files>
  <action>Record exact versions, chosen representation, shared schema path, semantic matrix, regeneration order, ownership, rejection criteria, and any semantics-preserving fallback. Leave no open generator question.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation-spike test -- --run &amp;&amp; rtk cargo test -p contract-generation-spike-rust</automated></verify>
  <done>ADR 0001 contains a final evidence-backed pass/fail decision.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-01 | Tampering | Rust generation input | mitigate | Deterministic TypeSpec generation before Cargo. |
</threat_model>
<verification>Delete the shared schema then run the exact task command; it must recreate it before Cargo.</verification>
<success_criteria>Cross-language generator parity is reproducible from a clean checkout.</success_criteria>
<output>Create `01-13-SUMMARY.md`.</output>
