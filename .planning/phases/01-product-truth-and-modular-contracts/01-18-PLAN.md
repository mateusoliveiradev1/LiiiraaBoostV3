---
phase: 01-product-truth-and-modular-contracts
plan: "18"
type: tdd
wave: 8
depends_on: ["01-07", "01-15"]
files_modified:
  - packages/contracts-ts/package.json
  - packages/contracts-ts/src/index.ts
  - packages/contracts-ts/src/validation.ts
  - packages/contracts-ts/src/validation.test.ts
  - crates/contracts-rust/src/lib.rs
  - crates/contracts-rust/src/validation.rs
  - crates/contracts-rust/tests/golden_corpus.rs
  - crates/contracts-rust/tests/provenance_properties.rs
autonomous: true
requirements: [FOUND-01, FOUND-03]
must_haves:
  truths:
    - "Public TypeScript and Rust entrypoints expose cached runtime validators."
    - "Both runtimes accept/reject every shared vector identically before deserialization."
    - "Consumer tests import validators only from public package/crate roots."
  artifacts:
    - path: packages/contracts-ts/src/index.ts
      provides: "Public generated transport and validator exports"
    - path: crates/contracts-rust/src/lib.rs
      provides: "Public Rust transport and validator exports"
  key_links:
    - from: packages/contracts-ts/src/validation.ts
      to: contracts/generated/desktop/v1/diagnostic-value.schema.json
      via: "cached Ajv schema validation"
    - from: crates/contracts-rust/src/validation.rs
      to: contracts/corpus/manifest.json
      via: "jsonschema verdict parity"
---
<objective>Implement and publicly wire cross-language boundary validators.</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/01-product-truth-and-modular-contracts/01-07-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-15-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-18-01: Add the public TypeScript validator</name>
  <files>packages/contracts-ts/package.json, packages/contracts-ts/src/index.ts, packages/contracts-ts/src/validation.ts, packages/contracts-ts/src/validation.test.ts</files>
  <behavior>
    - Ajv validates unknown input before typed mapping.
    - Errors contain bounded structural metadata and never payload values.
    - A consumer test imports validation from `@liiiraa/contracts-ts`.
  </behavior>
  <action>Compile schemas once per process, return typed success or redacted error, export validators and generated transports from the explicit public root/package exports, and test every shared vector through the public import.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contracts-ts test -- --run</automated></verify>
  <done>Public TypeScript imports validate all corpus vectors with stable redacted errors.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 01-18-02: Add the public Rust validator and parity properties</name>
  <files>crates/contracts-rust/src/lib.rs, crates/contracts-rust/src/validation.rs, crates/contracts-rust/tests/golden_corpus.rs, crates/contracts-rust/tests/provenance_properties.rs</files>
  <behavior>
    - jsonschema validation precedes generated-model deserialization.
    - The crate root exposes validator API.
    - Corpus verdicts and illegal provenance properties match TypeScript.
  </behavior>
  <action>Compile schemas once, deserialize only after validation, redact values, export from `lib.rs`, and test consumers through `liiiraa_contracts_rust` public API. Add proptest for illegal combinations and round-trip valid transports. Root `test:contracts` must cover drift, parity, and properties.</action>
  <verify><automated>rtk pnpm test:contracts</automated></verify>
  <done>Both public runtimes produce identical corpus verdicts and closed provenance properties.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-18-01 | Information disclosure | Validation errors | mitigate | Bounded structural redaction. |
| T-18-02 | Tampering | Public exports | mitigate | Consumer-only public import tests. |
</threat_model>
<verification>Run `test:contracts` and confirm illegal cross-kind metadata fails in both runtimes.</verification>
<success_criteria>Validators are canonical, public, parity-tested, and available before the desktop client plan.</success_criteria>
<output>Create `01-18-SUMMARY.md`.</output>
