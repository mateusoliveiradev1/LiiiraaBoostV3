---
phase: 01-product-truth-and-modular-contracts
plan: "11"
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - rust-toolchain.toml
  - Cargo.toml
  - tooling/workspace-smoke/check-toolchain.mjs
  - tooling/workspace-smoke/package.json
  - tooling/workspace-smoke/toolchain.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-05, FOUND-06]
must_haves:
  truths:
    - "Rust and workspace topology pins are exact and machine-checked."
    - "Pin, package-manager, and forbidden-name mutations fail precisely."
  artifacts:
    - path: rust-toolchain.toml
      provides: "Exact stable Rust toolchain"
    - path: tooling/workspace-smoke/check-toolchain.mjs
      provides: "Cross-toolchain smoke policy"
  key_links:
    - from: tooling/workspace-smoke/check-toolchain.mjs
      to: Cargo.toml
      via: "workspace topology validation"
---
<objective>Pin Cargo/Rust and prove the complete workspace toolchain contract.</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/01-product-truth-and-modular-contracts/01-02-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-11-01: Pin Rust and test workspace topology</name>
  <files>rust-toolchain.toml, Cargo.toml, tooling/workspace-smoke/check-toolchain.mjs, tooling/workspace-smoke/package.json, tooling/workspace-smoke/toolchain.test.ts</files>
  <behavior>
    - Exact Node, pnpm, TypeScript, and Rust pins pass.
    - Changed pins, missing roots, unsupported manager versions, and catch-all package names fail.
  </behavior>
  <action>Write mutation tests first. Pin Rust 1.97.1 stable with rustfmt/clippy and resolver-3 over crates and tooling Rust members. Check repository configuration and actionable local mismatch diagnostics without side effects; export terminating `verify:workspace`.</action>
  <verify><automated>rtk pnpm verify:workspace &amp;&amp; rtk cargo metadata --format-version 1 --no-deps</automated></verify>
  <done>Toolchain and topology mutation tests pass and Cargo metadata resolves deterministically.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | Tampering | Rust pin | mitigate | Exact file and mutation checks. |
</threat_model>
<verification>Run the task command from the approved frozen workspace.</verification>
<success_criteria>The complete JS/Rust workspace contract is executable before any Rust consumer plan.</success_criteria>
<output>Create `01-11-SUMMARY.md`.</output>
