---
phase: 01-product-truth-and-modular-contracts
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - turbo.json
  - .npmrc
  - tsconfig.base.json
  - eslint.config.mjs
  - prettier.config.mjs
autonomous: true
requirements: [FOUND-01, FOUND-05, FOUND-06]
must_haves:
  truths:
    - "The JavaScript workspace uses exact mutually compatible pins and strict checks."
    - "No future-phase runtime or unreviewed lifecycle script enters the lockfile."
  artifacts:
    - path: package.json
      provides: "Authoritative package manager, engine, and root command baseline"
    - path: pnpm-lock.yaml
      provides: "Frozen reviewed dependency graph"
  key_links:
    - from: package.json
      to: architecture/dependency-allowlist.json
      via: "exact approved dependency pins"
---
<objective>
Create the pinned JavaScript workspace and strict root policy.

Purpose: Establish the deterministic TypeScript foundation used by every later Phase 1 plan.
Output: pnpm/Turborepo workspace, frozen lockfile, strict compiler, lint, and formatting policy.
</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/01-product-truth-and-modular-contracts/01-RESEARCH.md
@.planning/phases/01-product-truth-and-modular-contracts/01-01-SUMMARY.md
</context>
<tasks>
<task type="auto">
  <name>Task 01-02-01: Create the pinned pnpm and Turbo workspace</name>
  <files>package.json, pnpm-lock.yaml, pnpm-workspace.yaml, turbo.json, .npmrc</files>
  <action>Create a private workspace over apps, packages, and tooling; pin pnpm 11.17.0 and Node 24.18.0; install only exact dependencies approved by Plan 01-01; disable unreviewed lifecycle scripts; expose terminating generate, check, test, build, and verify task shapes. Do not add UI, cloud, database, auth, Windows, or optimizer runtimes.</action>
  <verify><automated>rtk pnpm install --frozen-lockfile &amp;&amp; rtk pnpm exec turbo --version</automated></verify>
  <done>A clean frozen install succeeds and Turbo resolves the approved exact version without lifecycle-policy exceptions.</done>
</task>
<task type="auto">
  <name>Task 01-02-02: Enforce strict TypeScript, lint, and formatting policy</name>
  <files>tsconfig.base.json, eslint.config.mjs, prettier.config.mjs, package.json</files>
  <action>Pin TypeScript 6.0.3 and the compatible type-aware ESLint baseline. Enable strictest practical compiler settings, forbidden catch-all package names, one Prettier authority, and terminating root check commands. Keep TypeScript 7 excluded until the complete verified toolchain supports it.</action>
  <verify><automated>rtk pnpm exec tsc --version &amp;&amp; rtk pnpm exec eslint --version &amp;&amp; rtk pnpm exec prettier --check package.json</automated></verify>
  <done>TypeScript reports 6.0.3 and the root static-policy commands terminate successfully.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | Lockfile and pins | mitigate | Frozen install plus exact allowlist comparison. |
| T-02-02 | Elevation of privilege | Lifecycle scripts | mitigate | Deny scripts unless explicitly reviewed. |
</threat_model>
<verification>Run the two task commands from a clean frozen install.</verification>
<success_criteria>The JavaScript workspace is exact-pinned, strict, terminating, and contains no future-phase runtime.</success_criteria>
<output>Create `.planning/phases/01-product-truth-and-modular-contracts/01-02-SUMMARY.md`.</output>
