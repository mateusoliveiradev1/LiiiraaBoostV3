---
phase: 01-product-truth-and-modular-contracts
plan: "09"
type: tdd
wave: 11
depends_on: ["01-12", "01-18", "01-19"]
files_modified:
  - packages/desktop-production-reference/package.json
  - packages/desktop-production-reference/src/index.ts
  - packages/desktop-production-reference/src/composition.ts
  - tooling/fixture-guard/package.json
  - tooling/fixture-guard/src/static-guard.ts
  - tooling/fixture-guard/src/runtime-guard.ts
  - tooling/fixture-guard/src/fixture-guard.test.ts
  - tooling/fixture-guard/fixtures/static-runtime-leaks.json
  - tooling/fixture-guard/fixtures/production-fixture-type.ts
autonomous: true
requirements: [FOUND-03, FOUND-04, FOUND-05]
must_haves:
  truths:
    - "Fixture provenance is unrepresentable at the production TypeScript output boundary."
    - "Static and runtime guards independently refuse fixture dependencies, identity, and nested values."
    - "Production composition is exported through the package's public build entry."
  artifacts:
    - path: packages/desktop-production-reference/src/composition.ts
      provides: "Production-only typed composition"
    - path: tooling/fixture-guard/fixtures/production-fixture-type.ts
      provides: "Deliberate negative TypeScript boundary proof"
  key_links:
    - from: packages/desktop-production-reference/src/index.ts
      to: packages/desktop-production-reference/src/composition.ts
      via: "public exported production entrypoint"
    - from: packages/desktop-production-reference/package.json
      to: packages/desktop-production-reference/src/index.ts
      via: "exports and build entry"
---
<objective>
Make fixture provenance impossible at the production type, graph, and runtime boundaries.

Purpose: Establish three independent defenses before built-artifact and subprocess evidence.
Output: Production-specific result type, public composition, static/runtime guards, and negative fixtures.
</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@DESIGN.md
@.planning/phases/01-product-truth-and-modular-contracts/01-12-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-18-SUMMARY.md
@.planning/phases/01-product-truth-and-modular-contracts/01-19-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-09-01: Exclude fixture provenance from the production TypeScript boundary</name>
  <files>packages/desktop-production-reference/package.json, packages/desktop-production-reference/src/index.ts, packages/desktop-production-reference/src/composition.ts, tooling/fixture-guard/fixtures/production-fixture-type.ts</files>
  <behavior>
    - Production output types exclude fixture provenance structurally, not only by AdapterKind.
    - A deliberate fixture-valued production implementation fails the type test.
    - The clean composition is reachable only through the package export/build entry.
  </behavior>
  <action>Define production-specific inspection/value output types using an explicit non-fixture provenance union. Export the composition from `src/index.ts`; configure package exports and build entry to that index. Add a negative type-test fixture whose expected compile error disappears if fixture becomes assignable. The test must fail if no intended compiler diagnostic executes.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/fixture-guard test -- --run -t "type-boundary"</automated></verify>
  <done>Compile-time proof excludes fixture values and the distributable entry resolves to the production composition.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 01-09-02: Enforce independent static and runtime refusal</name>
  <files>tooling/fixture-guard/package.json, tooling/fixture-guard/src/static-guard.ts, tooling/fixture-guard/src/runtime-guard.ts, tooling/fixture-guard/src/fixture-guard.test.ts, tooling/fixture-guard/fixtures/static-runtime-leaks.json</files>
  <behavior>
    - Production graph imports/exports involving simulator or fixtures fail.
    - Identity mismatch or nested fixture provenance fails before presentation.
    - Both static and runtime groups execute with exact non-zero case counts.
  </behavior>
  <action>Implement the static guard from actual workspace graph plus canonical policy and a recursive runtime guard over identity, mode, schema version, and provenance-bearing values. Consolidate seeded graph/response leaks in one fixture matrix. Keep guards independent and assert each group count.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/fixture-guard test -- --run -t "static|runtime"</automated></verify>
  <done>Both named groups execute and independently fail their seeded leak while clean unavailable production truth passes.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01 | Spoofing | Fixture as PC data | mitigate | Compile-time, graph, and recursive runtime refusal. |
| T-09-02 | Tampering | Composition export | mitigate | Public package entry is the only build route. |
</threat_model>
<verification>Run the exact two task commands; each reports its expected executed-case count.</verification>
<success_criteria>Production fixture refusal is typed, statically wired, runtime enforced, and publicly reachable.</success_criteria>
<output>Create `01-09-SUMMARY.md`.</output>
