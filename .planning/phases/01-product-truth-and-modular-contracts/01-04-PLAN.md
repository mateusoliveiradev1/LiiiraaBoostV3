---
phase: 01-product-truth-and-modular-contracts
plan: "04"
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - tooling/contract-generation-spike/package.json
  - tooling/contract-generation-spike/src/run-spike.ts
  - tooling/contract-generation-spike/src/run-spike.test.ts
  - tooling/contract-generation-spike/fixtures/spike.tsp
  - tooling/contract-generation-spike/fixtures/valid.json
  - tooling/contract-generation-spike/fixtures/invalid.json
  - tooling/contract-generation-spike/generated/spike.schema.json
autonomous: true
requirements: [FOUND-01, FOUND-03]
must_haves:
  truths:
    - "TypeSpec preserves closed envelopes, five provenance discriminators, bounds, and byte stability."
    - "The exact selected schema persists at a deterministic path for the Rust spike."
  artifacts:
    - path: tooling/contract-generation-spike/generated/spike.schema.json
      provides: "Byte-stable shared spike schema"
  key_links:
    - from: tooling/contract-generation-spike/src/run-spike.ts
      to: tooling/contract-generation-spike/generated/spike.schema.json
      via: "deterministic TypeSpec emission"
---
<objective>
Prove TypeSpec-to-JSON-Schema and TypeScript semantic preservation.

Purpose: Resolve generator uncertainty before production contract authoring.
Output: Executable semantic matrix and committed byte-stable spike schema.
</objective>
<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/01-product-truth-and-modular-contracts/01-RESEARCH.md
@.planning/phases/01-product-truth-and-modular-contracts/01-02-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 01-04-01: Prove and persist the TypeSpec semantic matrix</name>
  <files>tooling/contract-generation-spike/package.json, tooling/contract-generation-spike/src/run-spike.ts, tooling/contract-generation-spike/src/run-spike.test.ts, tooling/contract-generation-spike/fixtures/spike.tsp, tooling/contract-generation-spike/generated/spike.schema.json</files>
  <behavior>
    - Closed concrete versioned envelopes and five provenance variants retain discriminators and bounds.
    - Repeated generation writes byte-identical schema at the declared generated path.
  </behavior>
  <action>Write RED semantic and byte-stability tests. Implement the exact approved TypeSpec emitters, normalization, and atomic output to `tooling/contract-generation-spike/generated/spike.schema.json`. Try the reusable envelope first; switch automatically to concrete envelopes only if the unchanged semantic matrix proves no broadening.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation-spike test -- --run -t "schema|determinism"</automated></verify>
  <done>The shared schema artifact exists on a clean checkout after the command and is byte-stable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 01-04-02: Prove accepted and rejected TypeScript vectors</name>
  <files>tooling/contract-generation-spike/fixtures/valid.json, tooling/contract-generation-spike/fixtures/invalid.json, tooling/contract-generation-spike/src/run-spike.test.ts</files>
  <behavior>
    - Valid variants pass.
    - Unknown versions, kinds, extra fields, missing metadata, and out-of-bound values fail.
  </behavior>
  <action>Author stable vector matrices and assert every row executes against the persisted schema and generated TypeScript. Never weaken a vector to make either encoding pass.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation-spike test -- --run</automated></verify>
  <done>The full matrix passes and reports its chosen representation as structured evidence.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Tampering | Schema emission | mitigate | Byte-stable committed artifact and unchanged semantic matrix. |
| T-04-02 | Spoofing | Provenance discriminator | mitigate | Literal variant tests. |
</threat_model>
<verification>Delete the generated spike directory and run the package test; it must regenerate before validation and remain byte-stable.</verification>
<success_criteria>The TypeScript spike closes with exact reusable evidence for the Rust consumer.</success_criteria>
<output>Create `01-04-SUMMARY.md`.</output>
