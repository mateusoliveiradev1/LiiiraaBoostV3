---
phase: 03-complete-web-experience
plan: "47"
type: tdd
wave: 27
depends_on: ["03-44"]
files_modified:
  - apps/web/next.config.ts
  - apps/web/src/public-shell.test.ts
  - apps/account/proxy.ts
  - apps/account/src/account-security.test.ts
  - apps/admin/proxy.ts
  - apps/admin/src/admin-security.test.ts
autonomous: true
gap_closure: true
requirements: [WEB-08]
must_haves:
  truths:
    - "React and Next.js 16.2.12 Turbopack development receives the eval capability it requires on public, account, and admin."
    - "Production CSP on public, account, and admin never contains 'unsafe-eval'."
    - "The three surfaces retain separate CSP builders, nonce/static-rendering behavior, origins, cookies, and fail-closed authority boundaries per D-07, D-54, D-68, and D-84."
  artifacts:
    - path: apps/web/src/public-shell.test.ts
      provides: "RED/GREEN public development-versus-production CSP proof"
    - path: apps/account/src/account-security.test.ts
      provides: "RED/GREEN account nonce-CSP mode proof"
    - path: apps/admin/src/admin-security.test.ts
      provides: "RED/GREEN admin nonce-CSP mode proof"
  key_links:
    - from: apps/web/next.config.ts
      to: apps/web/src/public-shell.test.ts
      via: "explicit public CSP builder exercised in development and production modes"
      pattern: "development|production"
    - from: apps/account/proxy.ts
      to: apps/account/src/account-security.test.ts
      via: "account proxy selects the mode-aware nonce policy"
      pattern: "accountHeaderContract"
    - from: apps/admin/proxy.ts
      to: apps/admin/src/admin-security.test.ts
      via: "admin proxy and denial responses share the mode-aware strict policy"
      pattern: "adminHeaderContract"
---

<objective>
Close the reported React/Turbopack development CSP incompatibility without weakening any production response.

Purpose: Make the visual-review development environment clean while retaining the separate, fail-closed production policies required by WEB-08 and D-54.
Output: Three independently tested environment-aware CSP implementations, with tests written RED before production changes.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-UAT.md
@.planning/debug/phase-03-development-csp-turbopack.md
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@apps/web/next.config.ts
@apps/account/proxy.ts
@apps/admin/proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement the public static CSP mode split</name>
  <files>apps/web/next.config.ts, apps/web/src/public-shell.test.ts</files>
  <read_first>
    - apps/web/next.config.ts
    - apps/web/src/public-shell.test.ts
    - .planning/debug/phase-03-development-csp-turbopack.md
  </read_first>
  <behavior>
    - RED 1: an explicitly constructed development public header contains `unsafe-eval` in its script directive.
    - RED 2: production and test public headers omit that capability while retaining the existing self/inline/static rendering contract.
    - RED 3: frame, object, base, form, origin, cookie, and report-only assertions remain unchanged.
  </behavior>
  <action>Write the failing mode-matrix tests first and run them to capture RED. Then extract the public CSP/header construction into a pure exported function with an explicit runtime-mode input; the Next `headers()` path may use `process.env.NODE_ENV`, but tests must not depend on mutating ambient module state. Append the React debugging capability only for the exact `development` mode. Preserve static public delivery, report-only probe semantics, no cookies, and every existing security directive per D-07, D-68, and D-84.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/web exec vitest run src/public-shell.test.ts -t "public CSP"</automated>
  </verify>
  <acceptance_criteria>
    - The public test is observed failing before the implementation change and passing afterward.
    - Development contains the required eval capability exactly once in `script-src`.
    - Production and test omit it and retain the existing public static CSP contract.
  </acceptance_criteria>
  <done>Public Turbopack development is compatible by explicit policy while the production public header remains no-eval and cookie-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and implement the account nonce-CSP mode split</name>
  <files>apps/account/proxy.ts, apps/account/src/account-security.test.ts</files>
  <read_first>
    - apps/account/proxy.ts
    - apps/account/src/account-security.test.ts
    - apps/web/next.config.ts
  </read_first>
  <behavior>
    - RED 1: account development CSP contains the React debugging capability in addition to the request nonce and strict-dynamic.
    - RED 2: account production and test CSP omit it.
    - RED 3: every mode remains noindex, private/no-store, frame-closed, third-party-free, cookie-free, and redirect-free.
  </behavior>
  <action>Write the failing account mode table before changing the proxy and prove RED. Make `accountHeaderContract` accept an explicit runtime mode and have `accountProxy` pass the actual environment. Add the allowance only for exact development; keep the cryptographically fresh request nonce, strict-dynamic, disconnected authority markers, safe-context validation, no session cookie, and existing security headers unchanged per D-04, D-49, D-68, and WEB-08.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/account exec vitest run src/account-security.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The account development assertion fails before production code changes and passes afterward.
    - Nonce generation remains per request and is present in all account script policies.
    - Production and test account responses never include the development-only capability.
  </acceptance_criteria>
  <done>Account development supports React debugging without altering production nonce, authority, cookie, or indexing protections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove and implement the admin nonce-CSP mode split</name>
  <files>apps/admin/proxy.ts, apps/admin/src/admin-security.test.ts</files>
  <read_first>
    - apps/admin/proxy.ts
    - apps/admin/src/admin-security.test.ts
    - apps/admin/src/admin-runtime.ts
  </read_first>
  <behavior>
    - RED 1: admitted admin development responses include the React debugging capability beside nonce and strict-dynamic.
    - RED 2: production/test admitted responses and authored denial responses omit it.
    - RED 3: exact-origin admission, cross-surface cookie rejection, noindex, denied framing, role scope, bounded JSON, and localized HTML denial remain intact.
  </behavior>
  <action>Start with failing development/production/denial tests. Give `adminHeaderContract` an explicit runtime mode and route the same selected contract through admitted responses and denial responses. Permit eval only for exact development on the dedicated admin origin; production and test remain strict. Do not relax origin comparison, role validation, cookie stripping, safe-context rejection, document-navigation classification, or redaction per D-54 through D-60.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/admin exec vitest run src/admin-security.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The admin development assertion is RED before implementation and GREEN afterward.
    - Production/test admin policy and authored denial policy omit the development-only capability.
    - All exact-origin, role, cookie, noindex, HTML/JSON denial, and redaction tests remain green.
  </acceptance_criteria>
  <done>Admin development works under Turbopack while production and denial paths preserve the strict isolated-origin boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- A pure, testable public CSP builder with explicit runtime mode.
- Mode-aware account and admin nonce-CSP contracts.
- Independent RED/GREEN regression proof for public, account, and admin development versus production behavior.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime mode to response CSP | An environment classification controls whether a development-only execution capability is emitted. |
| browser request to account/admin proxy | Nonces and isolated-origin policy protect dynamic preview responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-47-01 | Elevation of Privilege | CSP mode selection | mitigate | Exact `development` comparison plus production/test negative assertions on all three surfaces. |
| T-03-47-02 | Tampering | nonce CSP | mitigate | Preserve cryptographic per-request nonces and strict-dynamic while changing only the mode-dependent token. |
| T-03-47-03 | Spoofing | admin origin | mitigate | Keep exact dedicated-origin admission and cross-surface cookie rejection unchanged. |
| T-03-SC | Tampering | package supply chain | accept | No package installation or dependency change. |
</threat_model>

<verification>
Run the three focused security suites, then `rtk pnpm --filter @liiiraa/web build`, `rtk pnpm --filter @liiiraa/account build`, and `rtk pnpm --filter @liiiraa/admin build`. Each optimized build must pass with production policy still excluding the development-only capability.
</verification>

<success_criteria>
- Public, account, and admin tests each prove development inclusion independently.
- Public, account, and admin tests each prove production and test exclusion independently.
- All existing origin, nonce, cookie, indexing, framing, safe-context, and denial assertions pass.
- No new package identity or authority is introduced.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-47-SUMMARY.md` when done.</output>
