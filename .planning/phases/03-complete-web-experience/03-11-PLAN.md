---
phase: 03-complete-web-experience
plan: "11"
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-04", "03-05", "03-06", "03-07", "03-08", "03-10"]
files_modified:
  - architecture/module-boundaries.json
  - tooling/architecture-tests/src/check-workspace.test.ts
  - pnpm-lock.yaml
  - package.json
  - turbo.json
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "All seven Phase 3 roots are active, workspace-discovered, exact-pinned, and enforce the predeclared legal graph."
    - "Public, account, and admin build/check/test independently; root web verification commands terminate."
  artifacts:
    - path: pnpm-lock.yaml
      provides: "Frozen approved Phase 3 dependency resolution"
    - path: package.json
      provides: "Root web lifecycle commands"
  key_links:
    - from: pnpm-lock.yaml
      to: .planning/phases/03-complete-web-experience/03-DEPENDENCY-APPROVAL.md
      via: "Exact direct dependency parity"
      pattern: "next@16\\.2\\.12"
---

<objective>
Resolve the approved lockfile, atomically activate all web modules, and wire terminating root lifecycle commands.

Purpose: Complete Wave 0 workspace activation only after contracts, manifests, and human package approval exist.
Output: Frozen lock resolution, active module graph, live architecture proof, and root `web:*` commands.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-DEPENDENCY-APPROVAL.md
@architecture/module-boundaries.json
@tooling/architecture-tests/src/check-workspace.test.ts
@package.json
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the exact approved dependency graph</name>
  <files>pnpm-lock.yaml</files>
  <read_first>03-DEPENDENCY-APPROVAL.md; all Phase 3 package manifests; .npmrc; pnpm-workspace.yaml; current pnpm-lock.yaml</read_first>
  <action>Run repository-pinned pnpm 11.17.0 under Node 24.18.0. First resolve the reviewed manifests into `pnpm-lock.yaml` with `pnpm install --lockfile-only --no-frozen-lockfile --ignore-scripts --strict-peer-dependencies`. Confirm the diff contains only the exact approved direct identities and their transitive resolution, with `minisearch@7.2.0` owned only by `@liiiraa/web`. Then materialize that exact lock using `pnpm install --frozen-lockfile --ignore-scripts --strict-peer-dependencies`; `.npmrc` must continue to report `ignore-scripts=true`, `strict-peer-dependencies=true`, and `verify-store-integrity=true`. Refuse any version substitution, unapproved identity, lifecycle exception, peer relaxation, or package outside its owning manifest. Do not run TypeScript, Next.js, Turbo, or web lifecycle checks until the frozen installation succeeds.</action>
  <verify><automated>rtk pnpm install --lockfile-only --no-frozen-lockfile --ignore-scripts --strict-peer-dependencies && rtk pnpm install --frozen-lockfile --ignore-scripts --strict-peer-dependencies && rtk pnpm verify:workspace && rtk pnpm supply-chain:check && rtk pnpm list -r --depth 0</automated></verify>
  <acceptance_criteria>The lockfile resolves all seven roots, a frozen lifecycle-disabled install materializes every workspace link and approved external package, exact direct versions match approval/research, strict peers/store integrity remain enabled, and existing importers do not drift unexpectedly.</acceptance_criteria>
  <done>The approved web dependency graph is installed reproducibly from the frozen lock without executing lifecycle scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate the live graph and root lifecycle</name>
  <files>architecture/module-boundaries.json, tooling/architecture-tests/src/check-workspace.test.ts, package.json, turbo.json</files>
  <read_first>architecture/module-boundaries.json reserved records; Phase 3 reservation/isolation tests; package.json root scripts; turbo.json; all new manifests</read_first>
  <behavior>All seven roots are active/discovered with exact package names and legal edges; omitted root, wrong runtime class, fixture edge, duplicate owner, or missing root command fails.</behavior>
  <action>Only after Task 1 has completed the frozen lifecycle-disabled installation, change only the seven Phase 3 module records from reserved to active. Upgrade reservation tests to live activation/parity tests. Add root commands `web:check`, `web:test`, `web:build`, `web:verify:quick`, and `web:verify`; make `verify:quick`/`verify` delegate existing desktop gates plus the bounded web lifecycle without dropping foundation work. Add Turbo outputs for `.next/**` while excluding caches. Do not make one app depend on another or hide admin inside public.</action>
  <verify><automated>rtk pnpm test:architecture && rtk pnpm web:check</automated></verify>
  <acceptance_criteria>Seven roots are live once, legal graph passes, every illegal mutation still fails, and all root commands terminate without watch mode.</acceptance_criteria>
  <done>The complete Phase 3 workspace foundation is active and executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved manifests → resolved lock | Transitive resolution and lifecycle scripts can alter supply-chain trust. |
| Reserved policy → live graph | Activation must preserve owner, runtime class, and cross-surface isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-10 | Tampering | pnpm lock | mitigate | Exact approval parity, ignored scripts, strict peers, frozen resolution. |
| T-03-01 | Elevation | live module graph | mitigate | Activation mutation suite and production-to-fixture refusal. |
| T-03-SC | Tampering | npm installs | mitigate | Blocking approval dependency plus lock and lifecycle policy. |
</threat_model>

## Artifacts this phase produces

- Files: modified `pnpm-lock.yaml`, `architecture/module-boundaries.json`, `tooling/architecture-tests/src/check-workspace.test.ts`, `package.json`, `turbo.json`.
- Symbols/routes/CLI: active module records for seven IDs; test suite `Phase 3 live web activation`; root scripts `web:check`, `web:test`, `web:build`, `web:verify:quick`, `web:verify`; no route; no command-line flag.

<verification>
Run workspace, supply-chain, architecture, root web check, and full unchanged foundation quick gates.
</verification>

<success_criteria>
Every Phase 3 package is exact-pinned, discoverable, independently checkable, and protected by the live architecture policy.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-11-SUMMARY.md` when done.</output>
