---
phase: 03-complete-web-experience
plan: "31"
type: tdd
wave: 14
depends_on: ["03-19", "03-24", "03-29", "03-30"]
files_modified:
  - tooling/web-evidence/src/publication.ts
  - tooling/web-evidence/src/publication.test.ts
  - tooling/web-evidence/src/rollback-bundle.ts
  - tooling/web-evidence/src/rollback-bundle.test.ts
  - package.json
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Publication admits code, content, routes, manifests, support matrix, assets, screenshots, channels, policies, and evidence atomically."
    - "Any route/link/contract/security/privacy/accessibility/responsive/performance/SEO/localization/screenshot/evidence/visual/E2E failure blocks publication."
    - "Rollback restores one previously approved code/content/manifest/asset bundle and never blindly reverts external data."
  artifacts:
    - path: tooling/web-evidence/src/publication.ts
      provides: "Atomic publication admission gate"
      exports: ["evaluateWebPublication"]
    - path: tooling/web-evidence/src/rollback-bundle.ts
      provides: "Approved immutable bundle/rollback resolver"
      exports: ["createApprovedWebBundle", "resolveWebRollback"]
  key_links:
    - from: tooling/web-evidence/src/publication.ts
      to: tooling/web-evidence/visual-manifest.json
      via: "Exact artifact/evidence coherence"
      pattern: "contentId|buildId|capture|WEB-0"
---

<objective>
Implement the atomic publication and safe rollback policy.

Purpose: Deliver D-76, D-77, D-80, D-81, D-83, D-84, D-85, and D-86 as fail-closed executable gates.
Output: Pure publication evaluator, immutable approved bundle record, rollback resolver, mutation suite, and root commands.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tooling/acceptance-policy/src/policy.ts
@tooling/ci/verify-required-artifacts.mjs
@tooling/web-evidence/src/content-publication.test.ts
@tooling/web-evidence/visual-manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit one coherent web publication bundle</name>
  <files>tooling/web-evidence/src/publication.ts, tooling/web-evidence/src/publication.test.ts, package.json</files>
  <read_first>03-CONTEXT.md D-76, D-77, D-80, D-81, D-83, D-84, D-86; content/release/capture/visual manifests; acceptance policy evaluator; root lifecycle scripts</read_first>
  <behavior>
    - Exact matching build/content/route/release/screenshot/policy/evidence IDs admit.
    - Any missing, stale, mismatched, unlocalized, unreviewed, unsafe, failed, empty, or development-artifact input blocks with stable code.
    - Published Phase 3 bundle keeps download unavailable and all preview authority disconnected.
  </behavior>
  <action>Implement pure `evaluateWebPublication(input)` over explicit repository/build artifact sets. Require three independent build artifacts; canonical route/link parity; schema/type/build; security/privacy; WCAG/responsive/visual/performance/SEO; PT-BR/en; evidence/review dates; capture sidecars; release/support/channel/policy coherence; W01-W18; four final quality manifests. Add one in-memory mutation per D-84 gate and hash source artifacts before/after. Add root `web:publication:check -- --mode planned|final`; final requires observed files/commands and Phase 3 blocked-download truth.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence test -- --run -t "atomic publication" && rtk pnpm web:publication:check -- --mode planned</automated></verify>
  <acceptance_criteria>Every mutation fails its named dimension; exact bundle passes planned mode; final cannot pass with missing/unresolved evidence; source hashes unchanged.</acceptance_criteria>
  <done>A faulty or incoherent subset can never be published.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve rollback only to an approved immutable bundle</name>
  <files>tooling/web-evidence/src/rollback-bundle.ts, tooling/web-evidence/src/rollback-bundle.test.ts</files>
  <read_first>03-CONTEXT.md D-85; publication evaluator output; existing foundation acceptance modes; Git build/provenance patterns</read_first>
  <behavior>
    - Approved bundle records bind commit, build/content/route/release/asset/evidence hashes and three app artifacts.
    - Rollback selects one complete prior approved record, validates every hash, and returns deployment inputs only.
    - Partial, mutable, current-unapproved, or external-data/migration rollback requests fail.
  </behavior>
  <action>Implement `createApprovedWebBundle` and `resolveWebRollback` as pure deterministic functions. Bundle code/content/manifests/assets together; require exact prior final approval and immutable hashes. Return explicit three-artifact redeploy plan and state that databases/external data/migrations are outside rollback. Add tamper, missing-app, mixed-version, unapproved, external-data, and latest-faulty negatives.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence test -- --run -t "approved web rollback"</automated></verify>
  <acceptance_criteria>Only one exact previously approved bundle resolves; every partial/mixed/tampered/external-data case fails; no deployment is executed by the pure evaluator.</acceptance_criteria>
  <done>Rollback semantics restore a coherent approved web version without unsafe data reversal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evidence/artifacts → publication | Missing or mismatched subsets can produce a deceptive deployment. |
| Faulty deployment → rollback target | Rollback can mix versions or harm external state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-11 | Tampering/Repudiation | publication bundle | mitigate | Atomic exact hashes, final evidence, mutation tests. |
| T-03-08 | Spoofing | release artifact | mitigate | Blocked-download truth and development-artifact scan remain mandatory. |
| T-03-SC | Tampering | packages | mitigate | No new dependency installation. |
</threat_model>

## Artifacts this phase produces

- Files: `tooling/web-evidence/src/publication.ts`, `publication.test.ts`, `rollback-bundle.ts`, `rollback-bundle.test.ts`; modified root `package.json`.
- Types/fields: `WebPublicationInput`, `WebPublicationResult`, `PublicationFailure`, `ApprovedWebBundle{commit,buildId,contentId,routeHash,releaseHash,assetHashes,evidenceHashes,appArtifacts}`, `WebRollbackPlan`.
- Symbols/routes/CLI: `evaluateWebPublication`, `createApprovedWebBundle`, `resolveWebRollback`; root command `web:publication:check`; flag `--mode planned|final`; no product route.

<verification>
Run publication/rollback mutation suites and planned gate; final remains owned by Plan 03-32.
</verification>

<success_criteria>
Publication and rollback are atomic, evidence-bound, and cannot expose a development installer or revert external data.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-31-SUMMARY.md` when done.</output>
