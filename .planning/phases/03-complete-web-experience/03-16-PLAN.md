---
phase: 03-complete-web-experience
plan: "16"
type: execute
wave: 8
depends_on: ["03-11", "03-12", "03-14"]
files_modified:
  - apps/account/next.config.ts
  - apps/account/proxy.ts
  - apps/account/src/app/[locale]/layout.tsx
  - apps/account/src/app/[locale]/not-found.tsx
  - apps/account/src/app/[locale]/error.tsx
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Account has its own dynamic CSP, origin, noindex, cookie policy, shell, and authored failures."
    - "The shell persistently identifies deterministic preview provenance and disconnected authority."
  artifacts:
    - path: apps/account/proxy.ts
      provides: "Per-request account nonce and security headers"
    - path: apps/account/src/app/[locale]/layout.tsx
      provides: "Localized AccountShell with preview rail"
  key_links:
    - from: apps/account/src/app/[locale]/layout.tsx
      to: apps/account/src/index.ts
      via: "Fixture/disconnected composition identity"
      pattern: "ACCOUNT_WEB_COMPOSITION"
---

<objective>
Build the account-specific security policy, shell, and failure boundary.

Purpose: Implement D-07, D-24, D-25, D-26, D-47, D-49, D-61, D-66, D-67, and D-68 without real session authority.
Output: Nonce CSP, noindex/cookie isolation, responsibility navigation, persistent preview rail, and authored 404/500 states.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/account/src/index.ts
@packages/web-core/src/routes.ts
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the account origin and header contract</name>
  <files>apps/account/next.config.ts, apps/account/proxy.ts</files>
  <read_first>account scaffold; 03-RESEARCH.md Surface-Specific Security Policy; generated safe-context/route contracts; account threat patterns; Next 16 proxy guidance</read_first>
  <behavior>Each request receives a cryptographically random nonce; CSP/robots/referrer/permissions/cookie policies differ from public/admin; nonce cannot derive from scenario, route, locale, or clock.</behavior>
  <action>Implement request-nonce CSP with strict-dynamic, object/base/frame/form restrictions, `noindex,nofollow,noarchive`, no third-party origins, no real auth cookie issuance, and distinct account origin defaults. Validate only allowlisted cross-origin context through web-core. Keep any benign preview cookie host-only, `Secure`, `HttpOnly` where server-owned, `SameSite=Lax`, and non-authoritative; prefer no cookie when not required.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/account test -- --run -t "account security boundary"</automated></verify>
  <acceptance_criteria>Nonce varies across requests and is not predictable; account headers are exact and noindex; no session/auth cookie or arbitrary redirect exists.</acceptance_criteria>
  <done>Account has a distinct dynamic security boundary without identity authority.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the account shell and authored failures</name>
  <files>apps/account/src/app/[locale]/layout.tsx, apps/account/src/app/[locale]/not-found.tsx, apps/account/src/app/[locale]/error.tsx</files>
  <read_first>03-UI-SPEC.md account shell, state, responsive, accessibility and copy contracts; canonical account route projection; design-system public root; account composition identity</read_first>
  <action>Render the AccountShell with skip link, one main landmark, responsibility navigation (Overview, Profile, Security, Subscription/Invoices, Device, Downloads, Privacy, Support), locale control, explicit cross-origin notices, and persistent deterministic-preview rail. Author 404 and 500 with preserved locale, redacted opaque correlation IDs, safe recovery, preserved explicitly safe local work, and no failure-hiding redirect. At narrow widths show one responsibility at a time with full labels, never icon-only ambiguity.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/account test -- --run -t "account shell|account errors"</automated></verify>
  <acceptance_criteria>All responsibility links resolve canonical IDs; preview rail is always visible; keyboard/focus/reflow/forced-colors tests pass; failures remain explicit and redacted.</acceptance_criteria>
  <done>The account preview has a complete truthful product-register shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser request → account preview | Dynamic headers and context must not create identity/session authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | account shell | mitigate | Persistent preview boundary and disconnected composition marker. |
| T-03-04 | Information disclosure | account context/cookies | mitigate | Safe-context allowlist, no session state, host-only minimal storage. |
| T-03-06 | Clickjacking | account CSP | mitigate | Nonce CSP and frame-ancestors denial. |
| T-03-SC | Tampering | package installs | mitigate | Locked approved dependencies only. |
</threat_model>

## Artifacts this phase produces

- Files: modified `apps/account/next.config.ts`; new `apps/account/proxy.ts`, `src/app/[locale]/layout.tsx`, `not-found.tsx`, `error.tsx`.
- Symbols/routes/CLI: `accountHeaderContract`, `createRequestNonce`, `AccountLocaleLayout`, `AccountNotFound`, `AccountError`; routes `/[locale]/*`, authored 404 and 500; no CLI flag.

<verification>
Build/start account independently, assert headers twice, and run account shell keyboard/error tests plus WEB-08.
</verification>

<success_criteria>
Account is independently secure, persistently preview-labeled, noindex, and incapable of issuing a real session.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-16-SUMMARY.md` when done.</output>
