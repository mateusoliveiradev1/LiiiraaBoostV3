---
phase: 03-complete-web-experience
plan: "15"
type: execute
wave: 6
depends_on: ["03-11", "03-12"]
files_modified:
  - apps/web/next.config.ts
  - apps/web/proxy.ts
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/app/robots.ts
  - apps/web/src/app/sitemap.ts
  - apps/web/src/app/[locale]/not-found.tsx
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "The public shell is static-first, bilingual, cacheable, and owns no auth cookie or third-party runtime script."
    - "Navigation, sitemap, robots, locale alternates, and 404 derive from the canonical route manifest."
  artifacts:
    - path: apps/web/src/app/[locale]/layout.tsx
      provides: "Localized public/docs/releases shell"
    - path: apps/web/proxy.ts
      provides: "Locale/origin boundary without nonce-forced dynamic rendering"
  key_links:
    - from: apps/web/src/app/sitemap.ts
      to: packages/web-core/src/routes.ts
      via: "Canonical sitemap projection"
      pattern: "projectSitemap"
---

<objective>
Build the static-first public shell and its distinct security/indexing boundary.

Purpose: Implement D-06, D-07, D-22, D-24, D-25, D-61, D-65, D-69, D-78, and WEB-08 for the public origin.
Output: Localized shell, rendering-aware CSP, navigation, robots, sitemap, and authored 404.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/web-core/src/routes.ts
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@packages/design-tokens/src/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the static public security and locale boundary</name>
  <files>apps/web/next.config.ts, apps/web/proxy.ts, apps/web/src/app/[locale]/layout.tsx</files>
  <read_first>apps/web scaffold; 03-RESEARCH.md Static Public Content and CSP open question; Next 16 `proxy.ts` guidance in research; canonical route manifest; 03-UI-SPEC.md public shell/accessibility</read_first>
  <action>Configure PT-BR and English locale segments using `generateStaticParams`/`setRequestLocale`, explicit locale metadata inputs, self-hosted fonts, and static eligibility. Implement public CSP/header contract with no third-party scripts, auth cookies, optional analytics, frame embedding, object sources, or unsafe external form actions. Preserve static generation: do not add request nonce/`headers()` to static routes. Run a report-only production-build CSP probe and promote only directives proven compatible; document violations as blocking, never weaken directives silently. Render skip link, one main landmark/H1 handoff, canonical manifest navigation, compact full-screen mobile menu, search/locale controls, boundary notices, and footer.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web test -- --run -t "public shell|public CSP"</automated></verify>
  <acceptance_criteria>Both locales statically generate; no request API makes public routes dynamic; headers contain no auth cookie/third-party allowance; keyboard/mobile shell exposes every pillar.</acceptance_criteria>
  <done>The public origin has a cacheable, secure, complete localized shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project indexing and authored public failure navigation</name>
  <files>apps/web/src/app/robots.ts, apps/web/src/app/sitemap.ts, apps/web/src/app/[locale]/not-found.tsx</files>
  <read_first>canonical route/indexing projections; 03-CONTEXT.md D-25 and D-78; 03-UI-SPEC.md route manifest/error/accessibility contracts</read_first>
  <behavior>Sitemap contains exactly canonical public routes/locales; robots excludes private/preview/internal/obsolete classes; unknown public route renders localized 404 with safe recovery and no redirect.</behavior>
  <action>Generate sitemap language alternates and robots policy only from web-core projections. Author the localized 404 using the manifest error identity, redacted diagnostics, safe destinations, locale preservation, visible H1, focus target, and no automatic redirect. Seed tests that remove/add one manifest route and attempt to index account/admin/scenario/internal search.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web test -- --run -t "sitemap|robots|404"</automated></verify>
  <acceptance_criteria>Both-direction parity holds; forbidden route classes never index; 404 remains explicit, localized, keyboard operable, and recoverable.</acceptance_criteria>
  <done>Public indexing and not-found behavior cannot drift from route authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet request → public static routes | Locale/path input and browser content policy cross into a production artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02 | Tampering | public content execution | mitigate | No third-party/raw executable content and strict static CSP. |
| T-03-05 | Information disclosure | robots/sitemap | mitigate | Manifest-owned canonical public-only projection. |
| T-03-09 | Denial of service | heavy media | mitigate | Shell/CTA/content render before media; static generation. |
| T-03-SC | Tampering | package installs | mitigate | Locked approved dependencies only. |
</threat_model>

## Artifacts this phase produces

- Files: modified `apps/web/next.config.ts`; new `apps/web/proxy.ts`, `src/app/[locale]/layout.tsx`, `src/app/robots.ts`, `src/app/sitemap.ts`, `src/app/[locale]/not-found.tsx`.
- Symbols/routes/CLI: `routing`, `generateStaticParams`, `PublicLocaleLayout`, `publicHeaderContract`, `robots`, `sitemap`; routes `/[locale]/*` shell and authored 404; CSP report-only probe uses existing test command and no new CLI flag.

<verification>
Build public app, inspect static route output/headers, and run WEB-08 plus route/indexing focused gates.
</verification>

<success_criteria>
The public app is an independently secure static-first surface whose shell and indexing derive from one route authority.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-15-SUMMARY.md` when done.</output>
