---
phase: 03-complete-web-experience
plan: "26"
type: tdd
wave: 7
depends_on: ["03-10", "03-13", "03-18"]
files_modified:
  - packages/web-features/src/preview-machine.ts
  - packages/web-features/src/preview-machine.test.ts
  - packages/web-features/src/preview-workflows.tsx
  - packages/web-features/src/index.ts
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Every sensitive account/admin journey follows input→validation→review→proportional confirmation→authority boundary→cancel/failure/no-change receipt."
    - "Illegal transitions, ambiguous actions, expired/offline/stale conditions, and missing consent fail closed while safe local work is preserved."
  artifacts:
    - path: packages/web-features/src/preview-machine.ts
      provides: "Reusable closed XState preview workflow"
      exports: ["createPreviewWorkflowMachine"]
    - path: packages/web-features/src/preview-workflows.tsx
      provides: "Accessible review/confirmation/receipt renderer"
  key_links:
    - from: packages/web-features/src/preview-machine.ts
      to: packages/web-core/src/index.ts
      via: "FutureAuthorityPort contract"
      pattern: "FutureAuthorityPort|NoChangeReceipt"
---

<objective>
Implement the reusable closed state machine for every Phase 4-dependent account/admin journey.

Purpose: Enforce D-03, D-26, D-48 through D-53, D-57, and D-59 consistently without boolean-state drift.
Output: Guarded XState machine, typed state projections, and accessible workflow composition.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/feature-shell/src/features/preview-workflows.tsx
@packages/feature-shell/src/machines
@packages/web-preview/src/no-change-adapter.ts
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the guarded preview workflow statechart</name>
  <files>packages/web-features/src/preview-machine.ts, packages/web-features/src/preview-machine.test.ts, packages/web-features/src/index.ts</files>
  <read_first>existing feature-shell preview workflows and XState patterns; generated command/receipt; FutureAuthorityPort; 03-UI-SPEC.md account/admin workflow and complete state contract; W10-W16 scenarios</read_first>
  <behavior>
    - Legal states cover editing, validating, validation-error, reviewing, reauth-preview, confirming, issuing, offline, stale, expired-session, partial-failure, cancelled, and complete.
    - Only complete emits schema-valid no-change receipt; cancelled emits cancellation; no state emits authority success.
    - Missing purpose/consent/required fields/role/desktop viewport or ambiguous stale input blocks confirmation.
  </behavior>
  <action>RED: define transition/guard/output tables for every account/admin action family. GREEN: implement `createPreviewWorkflowMachine` with typed context/events/actors, deterministic clock/correlation injection, FutureAuthorityPort actor, AbortSignal cancellation, proportional confirmation metadata, safe-work preservation policy, and final output unions. REFACTOR shared guards and selectors. Do not persist actor internals or use Server Actions/fetch/cookies as authority.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-features test -- --run -t "preview workflow machine"</automated></verify>
  <acceptance_criteria>All legal transitions and every invalid/abort/offline/stale/consent/viewport mutation are covered; complete output always has remoteStateChanged false.</acceptance_criteria>
  <done>Sensitive previews have one exhaustive no-authority workflow truth.</done>
</task>

<task type="auto">
  <name>Task 2: Render accessible review, failure, cancellation, and receipts</name>
  <files>packages/web-features/src/preview-workflows.tsx, packages/web-features/src/index.ts</files>
  <read_first>preview machine state projections; 03-UI-SPEC.md copy/state/destructive action/accessibility contracts; design-system workflow/receipt components</read_first>
  <action>Implement `PreviewWorkflow` with labeled fields/error summary, review diff, purpose/impact, simulated reauthentication, object-specific confirmation, retry/cancel, assertive safety/integrity failures, polite progress/status, and NoChangeReceipt/immutable no-change ledger. Repeat preview boundary at sensitive confirmation; localize PT-BR/en; preserve focus and safe input; never say success/mutated/submitted when only preview completed.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-features test -- --run -t "preview workflow accessibility"</automated></verify>
  <acceptance_criteria>Keyboard/screen-reader/error-summary/focus/live-region tests pass; all terminal copy states no remote change and names Phase 4 authority.</acceptance_criteria>
  <done>Every account/admin plan can compose a finished truthful sensitive journey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User review/confirmation → preview authority | UI friction must not be confused with real reauthentication or mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing/Elevation | workflow terminal state | mitigate | Closed machine outputs only cancellation/failure/no-change. |
| T-03-09 | Denial of service | failure/recovery UX | mitigate | Preserve explicitly safe work and deterministic retry/cancel paths. |
| T-03-SC | Tampering | packages | mitigate | Existing locked XState/design dependencies only. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-features/src/preview-machine.ts`, `preview-machine.test.ts`, `preview-workflows.tsx`; modified `src/index.ts`.
- Types/fields: `PreviewWorkflowContext{action,fields,review,purpose,impact,role,consent,viewport,safeDraft,receipt}`, `PreviewWorkflowEvent`, `PreviewWorkflowOutput`, `PreviewStateProjection`.
- Symbols/routes/CLI: `createPreviewWorkflowMachine`, `previewWorkflowStates`, `PreviewWorkflow`, `PreviewReview`, `PreviewConfirmation`, `PreviewFailure`, `PreviewReceipt`; no route/CLI flag.

<verification>
Run machine transition/property tests, accessibility interaction tests, and fixture-boundary architecture guard.
</verification>

<success_criteria>
Every Phase 4-dependent UI can be complete and stateful while structurally unable to mutate authority.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-26-SUMMARY.md` when done.</output>
