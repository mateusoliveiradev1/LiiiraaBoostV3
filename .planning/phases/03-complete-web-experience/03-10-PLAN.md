---
phase: 03-complete-web-experience
plan: "10"
type: tdd
wave: 3
depends_on: ["03-09"]
files_modified:
  - packages/contracts-ts/src/validation.ts
  - packages/contracts-ts/src/validation.test.ts
  - packages/contracts-ts/src/index.ts
  - crates/contracts-rust/src/validation.rs
  - crates/contracts-rust/src/lib.rs
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Untrusted web documents validate before generated transport deserialization or application mapping."
    - "TypeScript and Rust return equivalent bounded, redacted acceptance/rejection results."
  artifacts:
    - path: packages/contracts-ts/src/validation.ts
      provides: "Cached public validateWebDocument boundary"
      exports: ["validateWebDocument"]
    - path: crates/contracts-rust/src/validation.rs
      provides: "Rust validate_web_document boundary"
  key_links:
    - from: packages/contracts-ts/src/validation.ts
      to: contracts/generated/web/v1/web-document.schema.json
      via: "Cached Ajv schema"
      pattern: "web-document\\.schema"
---

<objective>
Runtime-validate every generated web document before it enters trusted TypeScript or Rust code.

Purpose: Enforce D-37, D-76, and the generated-contract boundary with redacted parity.
Output: Cached validators, public exports, and shared valid/invalid parity tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/contracts-ts/src/validation.ts
@packages/contracts-ts/src/validation.test.ts
@crates/contracts-rust/src/validation.rs
@contracts/generated/web/v1/web-document.schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-language web document validation parity</name>
  <files>packages/contracts-ts/src/validation.ts, packages/contracts-ts/src/validation.test.ts, packages/contracts-ts/src/index.ts, crates/contracts-rust/src/validation.rs, crates/contracts-rust/src/lib.rs</files>
  <read_first>packages/contracts-ts/src/validation.ts and validation.test.ts desktop patterns; crates/contracts-rust/src/validation.rs and lib.rs; generated web schema and models; ADR-0004 redaction boundary</read_first>
  <behavior>
    - Valid route, content, release, no-change receipt, screenshot provenance, and admin audit documents are accepted identically.
    - Unknown fields, invalid enum/literal, missing approval/integrity, `remoteStateChanged:true`, unsafe URL, oversized IDs, and fixture-as-measured cases are rejected.
    - Errors expose schema path and stable code but never echo values.
  </behavior>
  <action>RED: add the closed valid/invalid matrix to both existing validation suites. GREEN: compile the web schema once in Ajv and jsonschema, expose `validateWebDocument` and `validate_web_document`, deserialize generated transport variants only after schema success, and reuse existing bounded structural error shapes. REFACTOR: share caching/redaction utilities without changing desktop validator behavior. Do not create handwritten DTOs or accept a relaxed fallback schema.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contracts-ts test -- --run -t "web document" && rtk cargo test -p liiiraa-contracts-rust web_document</automated></verify>
  <acceptance_criteria>Both runtimes agree on every vector; no payload values appear in errors; generated types are returned only after schema acceptance.</acceptance_criteria>
  <done>Every untrusted Phase 3 document has one fail-closed cross-language runtime boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON/build content → trusted generated transport | Schema-validity and semantic literals must precede application use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02 | Tampering/Elevation | runtime validation | mitigate | Closed schema, validation-before-deserialization, parity vectors. |
| T-03-04 | Information disclosure | validation errors | mitigate | Structural redaction only; never echo payload values. |
| T-03-SC | Tampering | package installs | mitigate | No package installation. |
</threat_model>

## Artifacts this phase produces

- Files: modified `packages/contracts-ts/src/validation.ts`, `validation.test.ts`, `index.ts`, `crates/contracts-rust/src/validation.rs`, `lib.rs`.
- Symbols/routes/CLI: `validateWebDocument`, `WebDocumentValidationResult`, `validate_web_document`, `WebDocumentValidationError`; no type fields beyond generated models, no route, no CLI flag.

<verification>
Focused TypeScript/Rust tests and the full contract parity gate pass.
</verification>

<success_criteria>
No web document can influence route, content, release, or preview behavior without equivalent runtime validation.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-10-SUMMARY.md` when done.</output>
