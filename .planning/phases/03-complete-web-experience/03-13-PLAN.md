---
phase: 03-complete-web-experience
plan: "13"
type: tdd
wave: 6
depends_on: ["03-12"]
files_modified:
  - contracts/scenarios/web-scenarios.json
  - packages/web-preview/src/scenarios.ts
  - packages/web-preview/src/scenarios.test.ts
  - packages/web-preview/src/no-change-adapter.ts
  - packages/web-preview/src/no-change-adapter.test.ts
  - packages/web-preview/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "W01-W18 are one closed deterministic, bilingual, deeply frozen scenario catalog available only to development/test compositions."
    - "Every Phase 4-dependent command can only cancel, fail safely, or return a generated no-change receipt."
  artifacts:
    - path: contracts/scenarios/web-scenarios.json
      provides: "Canonical W01-W18 scenario manifest"
    - path: packages/web-preview/src/no-change-adapter.ts
      provides: "Closed FutureAuthorityPort fixture adapter"
      exports: ["createWebPreviewAuthority"]
  key_links:
    - from: packages/web-preview/src/scenarios.ts
      to: contracts/scenarios/web-scenarios.json
      via: "Runtime validation and frozen projection"
      pattern: "W01|W18|SIMULATED"
---

<objective>
Create the closed W01-W18 scenario catalog and the only Phase 3 future-authority adapter.

Purpose: Implement D-03, D-04, D-05, D-26, D-48, D-49, D-55, D-57, D-58, and D-59 without fabricating real state.
Output: Deterministic scenarios plus cancellation/failure/no-change authority behavior.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@contracts/scenarios/desktop-scenarios.json
@packages/desktop-simulator/src/scenarios/catalog.ts
@packages/desktop-simulator/src/scenarios/catalog.test.ts
@packages/contracts-ts/src/index.ts
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author and validate the W01-W18 closed catalog</name>
  <files>contracts/scenarios/web-scenarios.json, packages/web-preview/src/scenarios.ts, packages/web-preview/src/scenarios.test.ts</files>
  <read_first>03-UI-SPEC.md Deterministic Scenario Matrix; desktop scenario catalog and tests; canonical `webRoutes`; generated provenance/route contracts</read_first>
  <behavior>
    - Exactly W01-W18 exist in order with frozen clock, locale, route, latency, provenance, version, channel, role, consent, viewport, and terminal state.
    - Required routes/states/axes are complete; unknown IDs and undeclared changes fail.
    - Published composition selection is one build-time ID and no public selector/query input exists.
  </behavior>
  <action>RED: port the catalog completeness, unknown-ID, stable serialization, family/delta, and mutation tests. GREEN: author the exact UI-SPEC W01-W18 records with synthetic redacted values, both shipping locales where required, literal fixture provenance, and no authority success. Expose `getWebScenario` only from the fixture package. Model a build-time `publishedPreviewScenarioId` but reject URL/query/cookie/environment override outside explicit test composition.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-preview test -- --run -t "W01-W18 catalog"</automated></verify>
  <acceptance_criteria>Exactly 18 frozen scenarios pass; unknown/duplicate/missing/undeclared mutations fail; no customer-like or measured production data appears.</acceptance_criteria>
  <done>Every required web state has deterministic fixture evidence without a public scenario switcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the closed no-change authority adapter</name>
  <files>packages/web-preview/src/no-change-adapter.ts, packages/web-preview/src/no-change-adapter.test.ts, packages/web-preview/src/index.ts</files>
  <read_first>generated `FutureAuthorityCommand` and `NoChangeReceipt`; packages/feature-shell interaction-policy no-change pattern; W01-W18 catalog; 03-CONTEXT.md D-03, D-04, D-50 through D-59</read_first>
  <behavior>
    - Valid reviewed commands return deterministic schema-valid receipts with `remoteStateChanged:false` and `nextPhase:4`.
    - Cancellation returns a distinct receipt; abort/offline/failure never becomes success.
    - Auth, session, billing, device, privacy, support, diagnostic, consent, and admin commands cannot emit mutation data.
  </behavior>
  <action>Implement `FutureAuthorityPort` and `createWebPreviewAuthority({scenario,clock,correlationIds})`. Runtime-validate command and receipt, preserve only reviewed/redacted inputs, honor AbortSignal, and deep-freeze results. The adapter must have no fetch, server action, cookie, storage, upload, database, or external API path. Export it only from `@liiiraa/web-preview`, never web-core.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-preview test -- --run -t "no-change authority"</automated></verify>
  <acceptance_criteria>Every action family produces only cancel/failure/no-change; receipt names future authority and correlation; invalid/aborted commands fail closed.</acceptance_criteria>
  <done>Phase 3 can demonstrate complete reviews without any remote authority mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scenario fixture → account/admin UI | Synthetic values can be mistaken for authenticated or customer state. |
| Reviewed command → future authority port | Confirmation must terminate without mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | scenario/receipt | mitigate | Fixture runtime identity, persistent simulated provenance, literal no-change result. |
| T-03-07 | Spoofing | scenario selection | mitigate | Closed IDs, build-time published selection, no public override. |
| T-03-SC | Tampering | package installs | mitigate | No install and no external service. |
</threat_model>

## Artifacts this phase produces

- Files: `contracts/scenarios/web-scenarios.json`, `packages/web-preview/src/scenarios.ts`, `scenarios.test.ts`, `no-change-adapter.ts`, `no-change-adapter.test.ts`; modified `src/index.ts`.
- Types/fields: `WebScenario{id,clock,locale,routeId,latencyMs,provenance,version,channel,role,consent,viewport,terminalState}`, `FutureAuthorityPort`, `CancelledReceipt`; generated command/receipt fields consumed unchanged.
- Symbols/routes/CLI: `WEB_SCENARIO_IDS`, `webScenarioCatalog`, `getWebScenario`, `publishedPreviewScenarioId`, `createWebPreviewAuthority`; scenario IDs W01-W18; no route or CLI flag.

<verification>
Catalog, adapter, contract runtime, and architecture fixture-boundary tests pass.
</verification>

<success_criteria>
All preview state is closed, deterministic, visibly simulated, and structurally incapable of remote mutation.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-13-SUMMARY.md` when done.</output>
