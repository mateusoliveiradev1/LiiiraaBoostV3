---
phase: 03-complete-web-experience
plan: "03"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts-source/src/main.tsp
  - packages/contracts-source/src/web.tsp
  - tooling/contract-generation/src/check-drift.test.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Critical web routes, content evidence, releases, and preview receipts have one language-neutral closed contract source."
    - "The contract makes an authoritative mutation or approved artifact unrepresentable without explicit authority and approval fields."
  artifacts:
    - path: packages/contracts-source/src/web.tsp
      provides: "Canonical web transport/document models"
      contains: "model NoChangeReceipt"
    - path: packages/contracts-source/src/main.tsp
      provides: "Canonical import of the web contract library"
  key_links:
    - from: packages/contracts-source/src/main.tsp
      to: packages/contracts-source/src/web.tsp
      via: "TypeSpec import"
      pattern: "import \"./web\\.tsp\""
---

<objective>
Define the language-neutral contract for route identity, compiled content, release integrity, and no-change preview boundaries.

Purpose: Implement D-03, D-08, D-14, D-29, D-32, D-37, D-39, D-49, D-52, D-58, D-59, D-76, and D-77 without handwritten duplicate DTOs.
Output: A closed TypeSpec web library with failing-then-passing semantic drift tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/contracts-source/src/main.tsp
@packages/contracts-source/src/envelope.tsp
@packages/contracts-source/src/provenance.tsp
@tooling/contract-generation/src/check-drift.test.ts
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and author the closed web document contract</name>
  <files>packages/contracts-source/src/main.tsp, packages/contracts-source/src/web.tsp, tooling/contract-generation/src/check-drift.test.ts</files>
  <read_first>packages/contracts-source/src/main.tsp; envelope.tsp; provenance.tsp; tooling/contract-generation/src/check-drift.test.ts; 03-RESEARCH.md Patterns 1, 3, 4, and 5; 03-CONTEXT.md D-03, D-08, D-14, D-29, D-32, D-37, D-39, D-52, D-58, D-59, D-76, D-77</read_first>
  <behavior>
    - `WebRouteRecord` closes route ID, surface, shell, pathname template, locale policy, indexing policy, owner, scenario requirement, security boundary, and allowed safe-context keys.
    - `ContentRecord` closes locale, version/channel, owner, review date, validation state, evidence links, indexing, and stale treatment.
    - `ReleaseRecord` separates human release metadata, distribution approval, official origin/artifact, integrity evidence, and availability.
    - `NoChangeReceipt` requires `remoteStateChanged: false`, simulated provenance, future authority, reviewed action, timestamp, correlation ID, and Phase 4.
  </behavior>
  <action>RED: add semantic assertions for the named models and forbidden widening. GREEN: author `web.tsp`, import it from `main.tsp`, reuse the existing version/provenance vocabulary, close every object and enum, bound strings/arrays, and keep release availability separate from metadata. Define `WebSurface`, `WebShell`, `IndexingPolicy`, `ValidationState`, `CapabilityAvailability`, `SafeContextKey`, `WebRouteRecord`, `ContentRecord`, `ClaimEvidence`, `ScreenshotProvenance`, `ReleaseArtifactEvidence`, `ReleaseRecord`, `FutureAuthorityCommand`, `NoChangeReceipt`, and `AdminAuditEvent`. Do not add HTTP operations, auth/session/payment/device/admin mutation authority, generic script fields, or a development artifact URL.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation test -- --run -t "web contract source"</automated></verify>
  <acceptance_criteria>TypeSpec compiles warning-free; all named models are closed and bounded; mutations that widen provenance, `remoteStateChanged`, origin, route surface, or approval fields fail.</acceptance_criteria>
  <done>The canonical source expresses every critical Phase 3 boundary without granting Phase 4/10 authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository documents → generated transports | Schema weakness could legitimize fabricated authority or artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | `NoChangeReceipt`/provenance | mitigate | Literal false mutation field, closed simulated provenance, named future authority. |
| T-03-08 | Tampering/Spoofing | `ReleaseRecord` | mitigate | Separate approval, official origin, artifact, and integrity evidence fields. |
| T-03-SC | Tampering | package installs | mitigate | No package installation in this plan. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/contracts-source/src/web.tsp`; modified `packages/contracts-source/src/main.tsp`, `tooling/contract-generation/src/check-drift.test.ts`.
- Types/fields: `WebSurface`, `WebShell`, `IndexingPolicy`, `ValidationState`, `CapabilityAvailability`, `SafeContextKey`, `WebRouteRecord{id,surface,shell,pathnameTemplate,localePolicy,indexing,owner,scenarioRequirement,securityBoundary,safeContextKeys}`, `ContentRecord{id,routeId,locale,version,channel,owner,lastReviewedAt,validationState,evidence,indexing,staleTreatment}`, `ClaimEvidence{source,provenance,scope,applicableVersion,validationState,unproven}`, `ScreenshotProvenance{version,locale,scenarioId,viewport,captureCommand,sourceCommit,checksum,crop,reviewState}`, `ReleaseArtifactEvidence{publisher,sha256,sizeBytes,signatureState,origin}`, `ReleaseRecord{channel,version,compatibility,manifest,artifactEvidence,publicDistributionApproved,officialArtifact}`, `FutureAuthorityCommand`, `NoChangeReceipt{receiptVersion,authority,requestedAction,reviewedInputs,reviewedAt,correlationId,provenance,remoteStateChanged,nextPhase}`, `AdminAuditEvent`.
- Routes/CLI: no route or CLI flag; the contract intentionally defines identities, not handlers.

<verification>
Focused source tests pass and generation remains the next explicit dependency.
</verification>

<success_criteria>
The canonical source can generate both language transports while making Phase 3 authority and distribution boundaries explicit.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-03-SUMMARY.md` when done.</output>
