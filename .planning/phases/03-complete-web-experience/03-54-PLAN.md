---
phase: 03-complete-web-experience
plan: "54"
type: tdd
wave: 31
depends_on: ["03-53"]
files_modified:
  - packages/design-system/src/primitives.tsx
  - packages/design-system/src/evidence.tsx
  - packages/design-system/src/design-system.test.tsx
  - packages/web-features/src/shells.tsx
  - packages/web-features/src/web.css
  - packages/web-features/src/components.test.tsx
autonomous: true
gap_closure: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Shared controls, status, rows, panels, and shell regions express the complete approved state matrix with familiar accessible behavior."
    - "Preview truth is quiet neutral/cyan context, never purple fixture chrome or repeated implementation prose."
    - "Components expose tonal hierarchy and one focal region instead of nested identical cards or page-wide divider grids."
  artifacts:
    - path: packages/design-system/src/primitives.tsx
      provides: "Authored state-complete controls and material primitives"
    - path: packages/design-system/src/evidence.tsx
      provides: "Human-readable semantic status/provenance projection"
    - path: packages/web-features/src/shells.tsx
      provides: "Shared web shell, navigation, preview-band, workspace, and row contracts"
  key_links:
    - from: packages/web-features/src/shells.tsx
      to: packages/design-system/src/index.ts
      via: "public-root component imports only"
      pattern: "@liiiraa/design-system"
    - from: packages/web-features/src/web.css
      to: packages/design-tokens/src/tokens.css
      via: "Cobalt Ignition Bay custom properties"
      pattern: "--lb-accent-electric|--lb-panel-raised"
---

<objective>
Build the shared component/material vocabulary that all three revised shells require per D-03, D-04, D-05, D-13, D-14, D-20, D-21, D-48, D-49, D-56, D-57, D-58, D-59, D-63, D-67, and D-68.

Purpose: Prevent each app from recreating inconsistent or rejected fixture/card treatments.
Output: Tested accessible primitives, semantic status projection, shell/workspace structures, and shared CSS states.
</objective>

<execution_requirements>
Invoke Impeccable in product register for control familiarity and state completeness. Preserve React Aria semantics and project public exports. Ban gradient text, default glass, ghost cards, nested/identical cards, over-rounding, side-stripe card accents, raw enum labels, purple fixture badges, and fake metrics.
</execution_requirements>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-53-SUMMARY.md
@packages/design-system/src/primitives.tsx
@packages/design-system/src/evidence.tsx
@packages/web-features/src/shells.tsx
@packages/web-features/src/web.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the control, material, and semantic state vocabulary</name>
  <files>packages/design-system/src/primitives.tsx, packages/design-system/src/evidence.tsx, packages/design-system/src/design-system.test.tsx</files>
  <read_first>
    - .planning/phases/03-complete-web-experience/03-UI-SPEC.md
    - packages/design-system/src/primitives.tsx
    - packages/design-system/src/evidence.tsx
    - packages/design-system/src/design-system.test.tsx
  </read_first>
  <behavior>
    - Test 1: primary controls expose default, hover, focus-visible, pressed, disabled, and loading contracts without width shift or color-only state.
    - Test 2: status projection renders localized human language plus icon/pattern for success, warning, critical, preview, unavailable, stale, offline, and error states.
    - Test 3: focal panel, tonal panel, row, table, empty, skeleton, and disclosure primitives expose semantic landmarks and do not nest identical containers.
    - Test 4: forced colors removes authored glow/fill dependence and reduced motion preserves immediately visible content.
  </behavior>
  <action>RED: extend component tests around the Section 13 state matrix and accessible names. GREEN: refine existing primitives/evidence rather than creating a parallel component kit; model E0-E3 material roles, stable loading geometry, semantic statuses, useful empty/skeleton regions, and explicit disclosure. Translate transport/scenario values at the presentation boundary. Preserve typed no-authority truth and do not imply session, payment, device, support, diagnostic, admin, or release authority.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/design-system exec vitest run src/design-system.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every interactive state has an accessible, token-backed representation.
    - Preview and unavailable are distinguishable without purple or color alone.
    - No component invents authority or duplicates transport schemas.
  </acceptance_criteria>
  <done>The design system supplies a complete, familiar, and truthful product vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author shared web shell and workspace composition primitives</name>
  <files>packages/web-features/src/shells.tsx, packages/web-features/src/web.css, packages/web-features/src/components.test.tsx</files>
  <read_first>
    - packages/web-features/src/shells.tsx
    - packages/web-features/src/web.css
    - packages/web-features/src/components.test.tsx
    - packages/design-system/src/index.ts
  </read_first>
  <behavior>
    - Test 1: branded topbar, quiet 40px preview band, desktop rail, mobile current-task disclosure, focal workspace, contextual panel, semantic rows, and responsive table are composable without app-specific authority.
    - Test 2: exactly one current destination uses aria-current, cobalt edge, raised fill, icon, and label; inactive state has no chroma.
    - Test 3: product canvases support 7/5 and 8/4 layouts, useful empty guidance, max field width, and progressive row detail without ordinary horizontal page scroll.
    - Test 4: raw scenario/phase/adapter/manifest strings and experimental purple presentation are absent from ordinary shared chrome.
  </behavior>
  <action>RED: write shared shell/structure assertions before refactoring. GREEN: evolve the existing exported shells and CSS into the UI-SPEC inventory, preserving route-provided hrefs, native anchors, skip-link behavior, one narrow client navigation boundary, and account/admin authority separation. Give components explicit class/data hooks for geometric browser assertions. Use tonal hierarchy and whitespace rather than thin dividers as the whole composition; allow row dividers only inside semantic lists/tables.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/web-features exec vitest run src/components.test.tsx -t "shell|navigation|preview|workspace|state"</automated>
  </verify>
  <acceptance_criteria>
    - Shared exports cover the revised shell/material inventory without a second design system.
    - Active, focus, disabled, loading, empty, error, and preview behavior is testable.
    - Styling consumes only approved tokens and respects forced-colors/reduced-motion.
  </acceptance_criteria>
  <done>All surfaces can compose the revised visual language from one accessible shared foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| scenario/contract values to visible status | Internal deterministic values become user-facing meaning. |
| shared component events to app routing/workflows | Generic controls invoke app-owned safe destinations and machines. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-54-01 | Spoofing | preview/status chrome | mitigate | Typed semantic mapping and explicit no-authority labels prevent authoritative appearance. |
| T-03-54-02 | Elevation of Privilege | shared actions | mitigate | Components accept app-owned handlers/hrefs and add no mutation authority. |
| T-03-54-03 | Information Disclosure | raw fixture values | mitigate | Presentation mapping exposes bounded localized labels, not raw enums or diagnostics. |
| T-03-SC | Tampering | package supply chain | accept | No package installation or dependency change. |
</threat_model>

<verification>Run design-system and web-features tests plus package checks.</verification>
<success_criteria>
- The Section 12/13 inventory exists through existing package public roots.
- Shared CSS no longer encodes the rejected purple/thin-grid/tiny-type language.
- Authority, accessibility, and responsive boundaries remain explicit.
</success_criteria>
<output>Create `.planning/phases/03-complete-web-experience/03-54-SUMMARY.md` when done.</output>
