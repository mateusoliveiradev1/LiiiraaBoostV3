---
phase: 03-complete-web-experience
plan: "19"
type: tdd
wave: 8
depends_on: ["03-12", "03-14"]
files_modified:
  - packages/web-core/src/content-admission.ts
  - packages/web-core/src/content-admission.test.ts
  - packages/web-core/src/search.ts
  - packages/web-core/src/search.test.ts
  - packages/web-core/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03]
must_haves:
  truths:
    - "Only schema-valid, bilingual, current, evidenced, route-owned repository content can become public or searchable."
    - "Search respects locale, version, availability, indexing, technical identifiers, and error codes without leaking private/preview content."
  artifacts:
    - path: packages/web-core/src/content-admission.ts
      provides: "Fail-closed repository content admission pipeline"
      exports: ["admitContentBundle"]
    - path: packages/web-core/src/search.ts
      provides: "Deterministic public-only MiniSearch index/query"
      exports: ["buildPublicSearchIndex", "searchPublicContent"]
  key_links:
    - from: packages/web-core/src/content-admission.ts
      to: packages/contracts-ts/src/validation.ts
      via: "Runtime validation before admission"
      pattern: "validateWebDocument"
---

<objective>
Build the repository content admission and public-only search engine.

Purpose: Implement D-06, D-14, D-23, D-31, D-32, D-70, D-74, D-76, D-78, D-80, D-82, and D-83 as build-time logic.
Output: Deterministic validated bilingual content bundle and locale/version-aware search index.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/web-core/src/routes.ts
@packages/contracts-ts/src/validation.ts
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit only complete truthful repository content</name>
  <files>packages/web-core/src/content-admission.ts, packages/web-core/src/content-admission.test.ts</files>
  <read_first>generated ContentRecord/ClaimEvidence/ScreenshotProvenance; runtime validator; canonical routes; 03-CONTEXT.md D-06, D-14, D-32, D-70, D-74, D-76, D-80, D-82, D-83; 03-RESEARCH.md Content Is Compiled Evidence</read_first>
  <behavior>
    - Valid PT-BR/en route-owned records with metadata, evidence, review date, indexing, and assets admit deterministically.
    - Missing/stale locale, metadata, warning, evidence, screenshot sidecar, route owner, or review date fails publication.
    - Expired/lost evidence removes actionable claims, labels stale history, and points to valid content.
  </behavior>
  <action>Implement `admitContentBundle(records,{clock,routeManifest,assetIndex})` after runtime validation. Require exact locale parity for public copy/metadata/errors/legal/docs/releases/critical notices, material claim evidence, route ownership, review dates, indexing policy, screenshot provenance, and content-specific social metadata. Reject raw executable MDX/HTML, generic mutation scripts, arbitrary registry/PowerShell recipes, unsupported origin URLs, and mutable CMS input. Return stable codes and a deterministic build/content bundle ID; never default-fallback missing locale in production.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-core test -- --run -t "content admission"</automated></verify>
  <acceptance_criteria>Every required omission/staleness/evidence/route/locale mutation fails; accepted bundle order/hash is deterministic; payload values are not leaked in errors.</acceptance_criteria>
  <done>Repository content becomes executable evidence rather than unchecked copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic public-only search</name>
  <files>packages/web-core/src/search.ts, packages/web-core/src/search.test.ts, packages/web-core/src/index.ts</files>
  <read_first>admitted content API; canonical indexing projection; MiniSearch official pattern in 03-RESEARCH.md; 03-UI-SPEC.md public search contract</read_first>
  <behavior>
    - Search covers admitted product/capability/compatibility/plan/docs/release/support records and filters locale/version/domain/risk/availability.
    - PT-BR diacritics, technical identifiers, versions, hashes, and error-code punctuation normalize predictably.
    - Account/admin/scenario/noindex/internal/obsolete records never enter index or results.
  </behavior>
  <action>Create the MiniSearch index only from `admitContentBundle` canonical-public projection. Define stored fields `routeId,locale,version,domain,risk,availability,validationState`; boost title/identifiers/error codes; support explicit submitted query, prefix/fuzzy behavior, stable tie ordering, filters, and authored suggestions. Preserve meaningful punctuation. Return typed no-result state without silently changing query/filter. Export through web-core.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-core test -- --run -t "public-only search"</automated></verify>
  <acceptance_criteria>Expected bilingual/technical queries resolve stably; all leak mutations fail; no private/scenario record appears in serialized index.</acceptance_criteria>
  <done>The public ecosystem has truthful, deterministic, leak-resistant discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository MDX/records/assets → public build/search | Trusted authors can still introduce executable, stale, unsupported, or private content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02 | Tampering/Elevation | content admission | mitigate | Generated schema, no raw executable content, evidence/review/route gates. |
| T-03-05 | Information disclosure | search index | mitigate | Admitted canonical-public projection and forbidden-record tests. |
| T-03-07 | Information disclosure | locale/cache | mitigate | Explicit locale/version fields and no fallback/private mixing. |
| T-03-SC | Tampering | MiniSearch | mitigate | Exact approved lock identity; no install. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-core/src/content-admission.ts`, `content-admission.test.ts`, `search.ts`, `search.test.ts`; modified `src/index.ts`.
- Types/fields: `ContentAdmissionInput{records,clock,routeManifest,assetIndex}`, `AdmittedContentBundle{schemaVersion,buildId,contentId,records,searchableRouteIds}`, `ContentAdmissionError`, `SearchDocument{routeId,locale,version,domain,risk,availability,validationState,title,summary,body,identifiers,errorCodes}`, `SearchFilters`, `SearchResult`.
- Symbols/routes/CLI: `admitContentBundle`, `buildPublicSearchIndex`, `searchPublicContent`; no route/CLI flag.

<verification>
Run focused admission/search tests, WEB-01/WEB-02 quick gates, and serialized-index leak scan.
</verification>

<success_criteria>
Only complete truthful public records reach rendering/search and missing/stale evidence fails closed.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-19-SUMMARY.md` when done.</output>
