---
phase: 03-complete-web-experience
plan: "17"
type: execute
wave: 8
depends_on: ["03-11", "03-12", "03-14"]
files_modified:
  - apps/admin/next.config.ts
  - apps/admin/proxy.ts
  - apps/admin/src/app/[locale]/layout.tsx
  - apps/admin/src/app/[locale]/not-found.tsx
  - apps/admin/src/app/[locale]/error.tsx
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Admin has a distinct strict CSP, origin, noindex/cookie/access-policy boundary and no public/account navigation link."
    - "Role identity and preview provenance persist in a dense operational shell."
  artifacts:
    - path: apps/admin/proxy.ts
      provides: "Isolated admin request/header/access boundary"
    - path: apps/admin/src/app/[locale]/layout.tsx
      provides: "Role-scoped AdminShell"
  key_links:
    - from: apps/admin/src/app/[locale]/layout.tsx
      to: apps/admin/src/index.ts
      via: "Disconnected fixture composition"
      pattern: "ADMIN_WEB_COMPOSITION"
---

<objective>
Build the isolated admin security policy, operational shell, and authored failures.

Purpose: Implement D-25, D-26, D-54, D-55, D-56, D-60, D-66, D-67, D-68, and WEB-08 before admin workflows.
Output: Strict per-request policy, access-boundary placeholder, role shell, viewport guard region, and authored 403/404/500 states.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/admin/src/index.ts
@packages/web-core/src/routes.ts
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce isolated admin headers and access boundary</name>
  <files>apps/admin/next.config.ts, apps/admin/proxy.ts</files>
  <read_first>admin scaffold; 03-CONTEXT.md D-54; 03-RESEARCH.md per-surface header contract; canonical safe links; account proxy pattern</read_first>
  <behavior>Admin uses a distinct origin and unpredictable nonce; CSP denies framing/object/base/external form; robots noindex; no public/account cookie is accepted; access-policy boundary is explicit but cannot authorize.</behavior>
  <action>Implement strict nonce CSP, `frame-ancestors 'none'`, `base-uri 'none'`, `form-action 'self'`, no-referrer, denied unused permissions, noindex/nofollow/noarchive, and distinct `admin.localhost` test origin. Add an `AdminAccessBoundary` result that can show preview role scenarios but always reports `authoritativeAccessConnected:false`; it must not parse or issue credentials. Reject public/account cookie names and arbitrary return URLs.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/admin test -- --run -t "admin security boundary"</automated></verify>
  <acceptance_criteria>Headers are stricter/distinct from other apps; nonce is random; no credential/session authority exists; cross-surface cookies/context fail.</acceptance_criteria>
  <done>Admin has an isolated security boundary without pretending role authorization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement role shell and authored admin failures</name>
  <files>apps/admin/src/app/[locale]/layout.tsx, apps/admin/src/app/[locale]/not-found.tsx, apps/admin/src/app/[locale]/error.tsx</files>
  <read_first>03-UI-SPEC.md admin shell, role, state, responsive, copy, accessibility; canonical admin routes; design-system public root; ADMIN_WEB_COMPOSITION</read_first>
  <action>Render AdminShell with skip link, main/H1 focus handoff, persistent role/provenance rail, and role-filtered Support, Operations, Security, Audit navigation—never an omnipotent view. Include a semantic viewport-gate region that preserves safe mobile review and announces high-risk desktop requirement below 960px. Author distinct localized 403, 404, and 500 states with redacted correlation IDs and safe recovery, without linking admin from public/account.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/admin test -- --run -t "admin shell|403|404|500"</automated></verify>
  <acceptance_criteria>Role navigation is scoped; preview/access markers persist; mobile review remains usable; high-risk region is blocked below 960px; authored failures preserve locale.</acceptance_criteria>
  <done>The admin preview has a complete isolated operational shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet/browser → isolated admin | Operational UI must not create privilege or leak cross-surface state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing/Elevation | role/access preview | mitigate | Literal disconnected access plus role/provenance rail. |
| T-03-04 | Information disclosure | cookies/context | mitigate | Reject other-surface cookies and unsafe return state. |
| T-03-06 | Clickjacking | admin CSP | mitigate | `frame-ancestors 'none'` and strict nonce CSP. |
| T-03-SC | Tampering | package installs | mitigate | Locked approved dependencies only. |
</threat_model>

## Artifacts this phase produces

- Files: modified `apps/admin/next.config.ts`; new `apps/admin/proxy.ts`, `src/app/[locale]/layout.tsx`, `not-found.tsx`, `error.tsx`.
- Types/fields: `AdminAccessBoundaryResult{role,authoritativeAccessConnected,reason}`.
- Symbols/routes/CLI: `adminHeaderContract`, `createAdminRequestNonce`, `AdminAccessBoundary`, `AdminLocaleLayout`, `AdminNotFound`, `AdminError`; routes `/[locale]/*`, authored 403/404/500; no CLI flag.

<verification>
Build/start admin independently, compare headers/origins/cookies against both other apps, and run WEB-08.
</verification>

<success_criteria>
Admin is a separately deployable, unlinked, noindex, role-scoped preview with the strictest surface policy.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-17-SUMMARY.md` when done.</output>
