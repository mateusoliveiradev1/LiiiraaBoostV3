---
phase: 03-complete-web-experience
plan: "53"
type: tdd
wave: 30
depends_on: ["03-52"]
files_modified:
  - packages/design-tokens/src/index.ts
  - packages/design-tokens/src/tokens.css
  - packages/design-tokens/src/tokens.test.ts
  - apps/web/public/fonts/saira-semi-condensed-variable.woff2
  - apps/web/public/fonts/OFL-Saira-Semi-Condensed.txt
  - apps/web/public/fonts/OFL-Manrope.txt
  - apps/web/public/fonts/OFL-JetBrains-Mono.txt
autonomous: true
gap_closure: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Every web surface consumes the exact Cobalt Ignition Bay spacing, radius, type, material, and OKLCH color contract."
    - "Public display type uses locally admitted Saira Semi Condensed at weight 600 only; missing or unverifiable font/license evidence stops execution."
    - "Every primary CTA state meets 4.5:1 after OKLCH-to-sRGB conversion, and forced-colors mappings remain explicit."
  artifacts:
    - path: packages/design-tokens/src/tokens.css
      provides: "Canonical Cobalt Ignition Bay CSS variables and local font faces"
    - path: packages/design-tokens/src/tokens.test.ts
      provides: "Exact-token, allowed-scale, font-license, and contrast gates"
    - path: apps/web/public/fonts/saira-semi-condensed-variable.woff2
      provides: "Locally served approved public display font"
  key_links:
    - from: packages/design-tokens/src/index.ts
      to: packages/design-tokens/src/tokens.css
      via: "one exported typed token authority mirrors CSS custom properties"
      pattern: "accentElectric|radiusStage|fontDisplay"
    - from: packages/design-tokens/src/tokens.css
      to: apps/web/public/fonts/saira-semi-condensed-variable.woff2
      via: "local @font-face URL"
      pattern: "saira-semi-condensed-variable\\.woff2"
---

<objective>
Replace the rejected low-energy token baseline with the exact approved Cobalt Ignition Bay foundation per D-18, D-19, D-20, D-21, D-68, D-81, D-84, and D-86.

Purpose: Make the revised visual direction enforceable before any surface composes it.
Output: Exact typed/CSS tokens, verified local fonts and licenses, and executable contrast/scale tests.
</objective>

<execution_requirements>
Invoke Impeccable's brand register for public typography and product register for account/admin type. Do not add a package, registry block, remote font, generic condensed fallback, gradient text, glass default, decorative grid, off-scale value, or a third font weight.
</execution_requirements>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-UAT.md
@.planning/phases/03-complete-web-experience/03-52-SUMMARY.md
@packages/design-tokens/src/index.ts
@packages/design-tokens/src/tokens.css
@packages/design-tokens/src/tokens.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit the exact local font set and licenses</name>
  <files>apps/web/public/fonts/saira-semi-condensed-variable.woff2, apps/web/public/fonts/OFL-Saira-Semi-Condensed.txt, apps/web/public/fonts/OFL-Manrope.txt, apps/web/public/fonts/OFL-JetBrains-Mono.txt, packages/design-tokens/src/tokens.test.ts</files>
  <read_first>
    - .planning/phases/03-complete-web-experience/03-UI-SPEC.md
    - packages/design-tokens/src/tokens.test.ts
    - apps/desktop/public/fonts/OFL-Manrope.txt
    - apps/desktop/public/fonts/OFL-JetBrains-Mono.txt
  </read_first>
  <behavior>
    - Test 1: all three local WOFF2 identities used by web CSS have a same-directory license file and deterministic non-empty bytes.
    - Test 2: Saira Semi Condensed is admitted only from an official upstream source under its verified OFL license and only the variable WOFF2 needed by public display type is accepted.
    - Test 3: the test fails when the Saira file, license, family identity, or approved weight range is missing or changed.
  </behavior>
  <action>RED: extend the existing font-integrity tests before adding files and confirm they fail for the absent Saira asset/license. GREEN: retrieve the exact Saira Semi Condensed variable WOFF2 and OFL text from the official upstream font repository, record the verified family identity in the test, and copy the already-approved Manrope and JetBrains Mono OFL texts beside the web font assets. Do not synthesize, convert, subset, rename an unrelated file, or use a remote URL. If exact provenance and licensing cannot be verified, stop for design review as required by the UI-SPEC rather than using a fallback.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/design-tokens exec vitest run src/tokens.test.ts -t "font|license|Saira"</automated>
  </verify>
  <acceptance_criteria>
    - Saira, Manrope, and JetBrains Mono web assets have locally reviewable license evidence.
    - Saira is the exact approved family and is not used as an account/admin control font.
    - No package or registry dependency is introduced.
  </acceptance_criteria>
  <done>The public display font is locally and legally admissible, or execution has stopped for the required design review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode exact tokens, scales, materials, and contrast pairs</name>
  <files>packages/design-tokens/src/index.ts, packages/design-tokens/src/tokens.css, packages/design-tokens/src/tokens.test.ts</files>
  <read_first>
    - .planning/phases/03-complete-web-experience/03-UI-SPEC.md
    - packages/design-tokens/src/index.ts
    - packages/design-tokens/src/tokens.css
    - packages/design-tokens/src/tokens.test.ts
  </read_first>
  <behavior>
    - Test 1: spacing is exactly 4/8/16/24/32/48/64 and radii are exactly 6/10/14/999.
    - Test 2: public type is 14/17/36-48/52-76, documentation type is 14/16/24/40, product type is 14/16/24/32, and only weights 400/600 exist.
    - Test 3: every Section 8 OKLCH token equals the approved value, with no experimental purple token used for preview chrome.
    - Test 4: CTA default, hover, pressed, disabled, and forced-color pairs meet their exact state contract; computed sRGB contrast is at least 4.5:1.
    - Test 5: E0-E3, glow caps, z-index roles, focus geometry, and reduced-motion/forced-color fallbacks are represented without off-scale aliases.
    - Test 6: motion is set-closed to 100/160/200/220/360/480ms, the two approved cubic-bezier curves plus linear, 8px maximum translation, 0.985 minimum entrance scale, 80ms maximum stage delay, and the prohibited-motion set.
  </behavior>
  <action>RED: write exact equality, set-closure, and computed-contrast tests from Sections 5-8 and the complete Section 17 motion contract. GREEN: replace legacy aliases with the approved named values, add `--lb-font-display` for local Saira, retain Manrope body and JetBrains data roles, expose typed mirrors from `index.ts`, and define only the six approved motion durations/easings plus the 8px, 0.985, and 80ms caps. Encode the prohibited set—bounce, elastic spring, parallax, scan line, particles, count-up, pulsing CTA, animated grid, infinite ambient loop—and reduced-motion removal of translation, scale, stagger, and glow animation with immediate or at most 100ms opacity/tone. Keep compatibility aliases only when an existing consumer needs them, but map them directly to the new authority and test that they cannot preserve timid cobalt, tiny typography, universal 28px display, excess radii, purple preview treatment, or off-contract motion. Use dark canvas labels on electric-cobalt CTAs exactly as specified.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/design-tokens test</automated>
  </verify>
  <acceptance_criteria>
    - All exact UI-SPEC token values and allowed sets are executable assertions.
    - Token exports and CSS custom properties agree.
    - Every required contrast pair passes after real color-space conversion.
    - Existing account/admin/runtime consumers retain build-compatible aliases without retaining the rejected appearance.
    - Motion tokens are the exact closed duration set 100/160/200/220/360/480ms with only linear, `cubic-bezier(0.2, 0, 0, 1)`, and `cubic-bezier(0.16, 1, 0.3, 1)` assigned to their approved roles.
  </acceptance_criteria>
  <done>The revised visual contract is one tested token authority for all downstream surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| upstream font artifact to repository | External binary and license enter the trusted local asset set. |
| token source to rendered controls | Color/type values determine accessibility and status legibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-53-01 | Tampering | Saira WOFF2/license | mitigate | Admit only official upstream bytes with deterministic identity and license tests. |
| T-03-53-02 | Information Disclosure | font loading | mitigate | Serve repository-local static files; no third-party font request or tracking. |
| T-03-53-03 | Denial of Service | illegible token pair | mitigate | Compute all CTA/state contrasts and block sub-4.5:1 pairs. |
| T-03-SC | Tampering | package supply chain | accept | No package-manager install or dependency change occurs. |
</threat_model>

<verification>Run the design-token package test and check; later browser plans prove rendered use.</verification>
<success_criteria>
- Exact revised tokens, scales, materials, and fonts are committed.
- Saira provenance/license is verified or the executor stopped for design review.
- No rejected visual token can pass the package tests.
</success_criteria>
<output>Create `.planning/phases/03-complete-web-experience/03-53-SUMMARY.md` when done.</output>
