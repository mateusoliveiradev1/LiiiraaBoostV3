---
phase: 03-complete-web-experience
plan: "48"
type: execute
wave: 27
depends_on: ["03-44"]
files_modified:
  - packages/web-core/src/routes.ts
  - packages/web-core/src/routes.test.ts
  - packages/web-core/src/index.ts
  - packages/web-features/src/shells.tsx
  - packages/web-features/src/components.test.tsx
autonomous: true
gap_closure: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Changing language preserves the matched canonical route and all path parameters instead of returning to a surface root."
    - "The shared locale affordance visibly renders 🇧🇷 Português or 🇺🇸 English and exposes a textual accessible language name, never a flag alone."
    - "Shared navigation exposes one unambiguous current route with `aria-current=page`, supporting task orientation per D-02, D-22, D-24, D-47, and D-56."
  artifacts:
    - path: packages/web-core/src/routes.ts
      provides: "Fail-closed current-route locale projection"
      exports: ["resolveLocalizedCurrentRoute"]
    - path: packages/web-features/src/shells.tsx
      provides: "Accessible flag-plus-language LocaleSwitcher and current-page navigation semantics"
      exports: ["LocaleSwitcher"]
  key_links:
    - from: packages/web-features/src/shells.tsx
      to: packages/web-core/src/routes.ts
      via: "surface clients supply a canonical route-preserving href to LocaleSwitcher"
      pattern: "LocaleSwitcher|resolveLocalizedCurrentRoute"
    - from: packages/web-core/src/routes.ts
      to: packages/web-core/src/routes.test.ts
      via: "public/docs/release/account/admin dynamic-route matrix"
      pattern: "resolveLocalizedCurrentRoute"
---

<objective>
Create the shared, fail-closed navigation foundation that every surface will use for active-route state and route-preserving bilingual switching.

Purpose: Fix the repeated cross-surface navigation and language-switch failure once at the canonical route/component layer, while preserving separate shells and origins.
Output: Tested route projection and an accessible visible flag-plus-language control for the three authored shell plans.
</objective>

<execution_requirements>
Apply Impeccable product guidance: use familiar navigation affordances, explicit current-location state, restrained neutral/cobalt styling hooks, complete keyboard semantics, and no ornamental replacement for standard links/disclosures.
</execution_requirements>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-CONTEXT.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@packages/web-core/src/routes.ts
@packages/web-features/src/shells.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project the current canonical route into the target locale</name>
  <files>packages/web-core/src/routes.ts, packages/web-core/src/routes.test.ts, packages/web-core/src/index.ts</files>
  <read_first>
    - packages/web-core/src/routes.ts
    - packages/web-core/src/routes.test.ts
    - packages/web-core/src/index.ts
    - .planning/phases/03-complete-web-experience/03-UAT.md
  </read_first>
  <behavior>
    - Test 1: Home, documentation, release, account, and admin paths switch only the locale while retaining the same route ID and every dynamic path parameter.
    - Test 2: account Profile remains Profile, an admin support case remains the same case, and documentation/release version/channel identities remain unchanged.
    - Test 3: malformed, foreign-boundary, unknown, query-bearing, fragment-bearing, or locale-invalid path input fails with the existing bounded route-result vocabulary and never falls back to a root.
  </behavior>
  <action>Write the route matrix first. Add `resolveLocalizedCurrentRoute` as a pure helper that composes `matchWebRoute` and `routeHref`: accept pathname, expected security boundary, and target locale; reuse only the matched canonical parameters; replace locale; return a typed `WebRouteResult&lt;string&gt;`. Export it through the package root. Do not parse or preserve arbitrary query values, origins, credentials, or fragments; app clients may retain only their already-validated local role state. This implements D-02, D-06, D-08, and D-24.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/web-core exec vitest run src/routes.test.ts -t "locale|localized current route"</automated>
  </verify>
  <acceptance_criteria>
    - The test matrix is RED before the helper exists and GREEN afterward.
    - Every supported route family preserves identity and path parameters across PT-BR/English.
    - Invalid or cross-boundary input fails closed without an invented destination.
  </acceptance_criteria>
  <done>One canonical helper can preserve the active public, account, or admin route while changing only locale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared accessible locale and current-navigation primitives</name>
  <files>packages/web-features/src/shells.tsx, packages/web-features/src/components.test.tsx</files>
  <read_first>
    - packages/web-features/src/shells.tsx
    - packages/web-features/src/components.test.tsx
    - packages/web-features/src/web.css
    - .planning/phases/03-complete-web-experience/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: target `pt-BR` visibly renders `🇧🇷 Português`; target `en` visibly renders `🇺🇸 English`.
    - Test 2: the flag is decorative/hidden from assistive technology while the link accessible name contains the full textual target language.
    - Test 3: shared navigation emits `aria-current="page"` and `data-current="page"` on exactly one active link and none on inactive links.
  </behavior>
  <action>Write component tests first. Add an authored `LocaleSwitcher` link that accepts canonical `href`, source locale, and target locale, renders the target flag with `aria-hidden`, renders the localized textual language name visibly, and uses a full textual accessible switch action. Update the shared navigation list to place `aria-current` and selected-state data on the active anchor rather than relying only on hidden suffix text. Retain at least 44×44 targets, visible focus, forced-colors semantics, and ordinary anchor behavior per D-61, D-66, D-67, and D-68.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/web-features exec vitest run src/components.test.tsx -t "locale|navigation|shell"</automated>
  </verify>
  <acceptance_criteria>
    - Both locale variants expose visible flag plus language text and an explicit accessible text name.
    - No flag-only control exists.
    - Exactly one current navigation link is programmatically and visually identifiable.
  </acceptance_criteria>
  <done>Every surface can consume one accessible locale affordance and current-page navigation contract without sharing shells or authority.</done>
</task>

</tasks>

## Artifacts this phase produces

- `resolveLocalizedCurrentRoute` in the canonical route package.
- `LocaleSwitcher` with 🇧🇷 Português / 🇺🇸 English visible labels.
- Shared `aria-current` and selected-state navigation semantics.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| current URL to canonical route projection | Untrusted pathname input is matched before a localized link is produced. |
| rendered flag to accessible language identity | Decorative emoji must not replace textual language semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-48-01 | Spoofing | locale destination | mitigate | Match expected surface boundary and rebuild only from the canonical route/parameters. |
| T-03-48-02 | Information Disclosure | preserved URL state | mitigate | Preserve path parameters only; reject arbitrary query, fragment, credential, or foreign-origin data. |
| T-03-48-03 | Denial of Service | compact navigation accessibility | mitigate | Native links/disclosures, 44px targets, keyboard focus, textual names, and 400% reflow contract. |
| T-03-SC | Tampering | package supply chain | accept | No package installation or dependency change. |
</threat_model>

<verification>
Run `rtk pnpm --filter @liiiraa/web-core test`, `rtk pnpm --filter @liiiraa/web-features test`, and `rtk pnpm --filter @liiiraa/web-features check`.
</verification>

<success_criteria>
- All route families preserve active path identity across locales.
- Invalid inputs fail closed.
- Locale controls are never flag-only.
- Active navigation has one explicit accessible current page.
- No origin, shell, cookie, scenario, or authority boundary is merged.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-48-SUMMARY.md` when done.</output>
