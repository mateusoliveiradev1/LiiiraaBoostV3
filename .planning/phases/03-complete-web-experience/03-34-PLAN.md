---
phase: 03-complete-web-experience
plan: "34"
type: execute
wave: 16
depends_on: ["03-32"]
files_modified:
  - apps/admin/src/admin-errors.ts
  - apps/admin/src/admin-preview-model.ts
  - apps/admin/src/app/[locale]/[[...workspace]]/page.tsx
  - apps/admin/src/admin-shell.test.ts
autonomous: true
gap_closure: true
requirements: [WEB-08]
must_haves:
  truths:
    - "The canonical admin 410 URL renders a localized authored gone outcome instead of falling through to admin 404."
    - "Admin 403/404/410/500 remain distinct, redacted, same-origin, role-safe, and explicitly disconnected from authoritative access."
    - "Unknown admin URLs and cross-role workflow attempts retain their existing 404 and 403 behavior."
  artifacts:
    - path: apps/admin/src/admin-errors.ts
      provides: "Closed bilingual 403/404/410/500 admin failure model"
      exports: ["AdminFailureKind", "createAdminFailureModel"]
    - path: apps/admin/src/admin-preview-model.ts
      provides: "Disjoint admin workflow/error route classification"
    - path: apps/admin/src/app/[locale]/[[...workspace]]/page.tsx
      provides: "Manifest-aware admin workflow/error dispatch"
  key_links:
    - from: apps/admin/src/app/[locale]/[[...workspace]]/page.tsx
      to: packages/web-core/src/routes.ts
      via: "matchWebRoute at the admin-origin boundary"
      pattern: "securityBoundary: 'admin-origin'"
    - from: apps/admin/src/app/[locale]/[[...workspace]]/page.tsx
      to: apps/admin/src/admin-failure-view.tsx
      via: "410 failure model rendered before role-workspace admission"
      pattern: "AdminFailureView"
---

<objective>
Close the admin half of the D-25 route gap by admitting the canonical admin 410 identity and rendering a localized, redacted, recoverable outcome.

Purpose: Admin already owns authored 403/404/500 behavior, but its catch-all excludes `admin-error-410`; this plan completes the closed error matrix while preserving origin, role, consent, and authority isolation.
Output: Exhaustive admin failure kind, error-route guard, catch-all 410 dispatch, and focused regression proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-VERIFICATION.md
@.planning/phases/03-complete-web-experience/03-CONTEXT.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-17-SUMMARY.md
@.planning/phases/03-complete-web-experience/03-28-SUMMARY.md
@packages/web-core/src/routes.ts
@apps/admin/src/admin-errors.ts
@apps/admin/src/admin-preview-model.ts
@apps/admin/src/admin-failure-view.tsx
@apps/admin/src/app/[locale]/[[...workspace]]/page.tsx

<interfaces>
Existing admin model:
- `AdminFailureKind = '403' | '404' | '500'` and `AdminFailureView` renders any member of that union.
- `ADMIN_ENTRY_ROUTE_IDS` and `isAdminPreviewRoute(routeId)` admit only seven role workspaces.
- `adminRoleCanAccess(role, routeId)` applies only to `AdminPreviewRoute` and must remain unchanged for workflow authorization previews.

Create and consume these closed interfaces:
- `ADMIN_ERROR_ROUTE_IDS = ['admin-error-404', 'admin-error-403', 'admin-error-410', 'admin-error-500']`.
- `AdminErrorRoute`, `isAdminErrorRoute(routeId)`, and `adminFailureKindForRoute(routeId)`.
- `AdminFailureKind = '403' | '404' | '410' | '500'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify admin 410 and exhaustive error-route admission</name>
  <files>apps/admin/src/admin-shell.test.ts, apps/admin/src/admin-errors.ts, apps/admin/src/admin-preview-model.ts</files>
  <read_first>apps/admin/src/admin-shell.test.ts; apps/admin/src/admin-errors.ts; apps/admin/src/admin-preview-model.ts; packages/web-core/src/routes.ts; 03-CONTEXT.md D-06, D-25, D-26, D-54 through D-60; 03-UI-SPEC.md State Contract and Admin preview workflow</read_first>
  <behavior>
    - The four canonical admin error IDs map exactly to 403, 404, 410, and 500 and never enter the role-workspace union.
    - Admin 410 has distinct PT-BR and English title/affected/detail/safe-state copy, preserves historical context, and links only to the localized admin role workspace.
    - All four kinds use bounded opaque correlations; unsafe digests remain redacted.
    - Existing cross-role 403 logic and unknown-route 404 logic retain their current results.
  </behavior>
  <action>Use RED→GREEN TDD. Add failing 410/exhaustiveness/locale/recovery/redaction expectations to `admin-shell.test.ts` and commit RED as `test(03-34): cover complete admin error routing`. Then add the 410 kind/copy and the disjoint canonical error-route guard/mapping; commit GREEN as `feat(03-34): close admin error contract`. Implement D-25 without changing `ADMIN_ROLE_ROUTE_ACCESS`, accepting credentials, exposing diagnostic data, or connecting administrative authority per D-54/D-57/D-59.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/admin test -- --run src/admin-shell.test.ts</automated>
  </verify>
  <acceptance_criteria>The RED test fails because 410 is absent; GREEN passes with four exhaustive kinds, unique bilingual 410 semantics, same-origin recovery, opaque redaction, and unchanged role-access matrices.</acceptance_criteria>
  <done>The admin failure model and route classifier cover the complete D-25 matrix without broadening role authority.</done>
</task>

<task type="auto">
  <name>Task 2: Render admin 410 before workspace and 404 guards</name>
  <files>apps/admin/src/app/[locale]/[[...workspace]]/page.tsx, apps/admin/src/admin-shell.test.ts</files>
  <read_first>apps/admin/src/app/[locale]/[[...workspace]]/page.tsx; apps/admin/src/admin-failure-view.tsx; apps/admin/src/app/[locale]/errors/403/page.tsx; apps/admin/src/app/[locale]/errors/500/page.tsx; apps/admin/src/app/admin-shell.css</read_first>
  <action>Refactor the catch-all resolution into a discriminated workflow/error result. After locale and canonical admin-origin matching, dispatch `admin-error-410` through `createAdminFailureModel('410', locale)` and `AdminFailureView` before role-workspace access checks; continue applying `adminRoleCanAccess` only to workflow routes. Generate localized noindex metadata for the gone state. Preserve the exact URL, preview/role provenance, separate admin origin, strict CSP, cookie rejection, and no automatic redirect per D-25/D-54. Unknown IDs must still call `notFound()` and cross-role workflow IDs must still render the existing 403 outcome.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/admin verify</automated>
  </verify>
  <acceptance_criteria>`/pt-BR/errors/410` and `/en/errors/410` build and render a unique 410 H1/status/copy/recovery outcome; unrelated invalid routes remain 404; cross-role workflow attempts remain 403; admin CSP, noindex, viewport, consent, audit, and disconnected-authority suites stay green.</acceptance_criteria>
  <done>The canonical admin 410 route is executable and cannot collapse into 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Request pathname/locale → admin catch-all | Untrusted paths must resolve only to canonical error identities, canonical role workspaces, or the genuine 404 boundary. |
| Failure model → operational UI | The state must explain recovery without leaking customer, diagnostic, credential, role, or internal request data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-GC-D1 | Information Disclosure | admin 410 model/view | mitigate | Bounded static copy, opaque correlation validation, redaction tests, and admin-only recovery target. |
| T-03-GC-D2 | Elevation of Privilege | catch-all dispatch | mitigate | Error routes bypass neither role checks nor authority; they render a non-authoritative failure and no operational data. |
| T-03-GC-D3 | Spoofing | admin recovery link | mitigate | Use canonical `admin-role` through `routeHref`; no public/account link or arbitrary return URL. |
| T-03-SC | Tampering | dependency graph | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
Run the focused TDD test through RED/GREEN, then `rtk pnpm --filter @liiiraa/admin verify`. Confirm the optimized route table retains the admin catch-all and source contains no new credential parsing, cross-surface cookie acceptance, fetch mutation, upload, or redirect channel.
</verification>

<success_criteria>
- Admin 403/404/410/500 form a closed, distinct, bilingual, redacted failure set.
- The canonical 410 identity renders before workspace/404 fallback logic.
- Existing role, consent, audit, viewport, origin, CSP, cookie, and no-change boundaries are preserved.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-34-SUMMARY.md` when done.</output>
