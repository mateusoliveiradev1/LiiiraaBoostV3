---
phase: 03-complete-web-experience
plan: "24"
type: tdd
wave: 10
depends_on: ["03-10", "03-12", "03-14", "03-19", "03-22"]
files_modified:
  - packages/web-core/src/releases.ts
  - packages/web-core/src/releases.test.ts
  - packages/web-core/src/index.ts
autonomous: true
requirements: [WEB-03]
must_haves:
  truths:
    - "Release metadata may exist while artifact availability is blocked; Phase 3 published data always blocks."
    - "Missing approval or any signature/hash/publisher/artifact/manifest disagreement has no bypass branch."
  artifacts:
    - path: packages/web-core/src/releases.ts
      provides: "Fail-closed release, integrity, and download decision engine"
      exports: ["decideDownload", "verifyReleaseIntegrity", "selectReleaseChannel"]
  key_links:
    - from: packages/web-core/src/releases.ts
      to: packages/contracts-ts/src/validation.ts
      via: "Validated ReleaseRecord before decision"
      pattern: "validateWebDocument|publicDistributionApproved"
---

<objective>
Implement the fail-closed release and download decision engine.

Purpose: Satisfy WEB-03 and D-35 through D-46 without exposing Phase 2 development-signed artifacts or claiming Phase 10 distribution approval.
Output: Channel selection, integrity comparison, availability decisions, verification steps, and no-bypass tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/contracts-ts/src/validation.ts
@quality/evidence/phase-02/artifacts/signed-desktop-package.json
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decide channel, integrity, and public availability without bypass</name>
  <files>packages/web-core/src/releases.ts, packages/web-core/src/releases.test.ts, packages/web-core/src/index.ts</files>
  <read_first>generated ReleaseRecord/ReleaseArtifactEvidence; Phase 2 signing evidence classification; 03-CONTEXT.md D-35 through D-46; 03-RESEARCH.md Fail-Closed Release Decision and Pitfall 1; UI-SPEC W07/W08</read_first>
  <behavior>
    - Stable defaults; Beta requires explicit opt-in; Experimental is separated with risk/audience/support/update policy.
    - No approval, no official artifact, unavailable artifact, or any integrity disagreement returns blocking decision.
    - Phase 2 development/self-signed/local/CI artifacts are rejected from official/public decisions.
    - Only a future fully approved official record can produce available decision; no `continue anyway`.
  </behavior>
  <action>Implement `selectReleaseChannel`, `verifyReleaseIntegrity`, and `decideDownload(validatedRecord)` as exhaustive discriminated unions. Compare manifest/artifact channel, version, architecture, Windows lifecycle/compatibility, publisher, SHA-256, size, signature state, origin, provenance, and approval. Require controlled official origin; classify history supported/unsafe/unavailable without encouraging downgrade. Produce independent Authenticode/hash/size/version/compatibility verification steps and post-download guidance only for available future result. Hard-reject paths/names/trust classes from Phase 2 development artifacts. The published Phase 3 fixture must resolve `distribution-not-approved`.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-core test -- --run -t "fail-closed release decision"</automated></verify>
  <acceptance_criteria>Every mismatch/absence/development/bypass mutation blocks; published fixture blocks for approval; exhaustive switch has no permissive default; available case requires all future approval facts.</acceptance_criteria>
  <done>WEB-03’s complete fail-closed eligibility journey is implemented without violating the locked distribution boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Release/manifest/artifact evidence → public download | Inconsistent or development artifacts must never be presented as official. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-08 | Spoofing/Tampering | download decision | mitigate | Exhaustive agreement, official origin, public approval, dev-artifact rejection, no bypass. |
| T-03-04 | Information disclosure | analytics/linking | mitigate | Decision engine has no account/device analytics input. |
| T-03-SC | Tampering | package installs | mitigate | No new packages. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-core/src/releases.ts`, `releases.test.ts`; modified `src/index.ts`.
- Types/fields: `ReleaseChannelSelection`, `IntegrityDisagreement{field,manifestValueClass,artifactValueClass}`, `DownloadDecision` variants `blocked|available`, `VerificationStep`, `HistoricalReleaseState`.
- Symbols/routes/CLI: `selectReleaseChannel`, `verifyReleaseIntegrity`, `decideDownload`, `createVerificationSteps`; no route/CLI flag.

<verification>
Run WEB-03 state-machine/release gate tests and scan public decisions for development artifact identities.
</verification>

<success_criteria>
Phase 3 proves the complete eligibility decision and exposes no installer; a future public-trust-approved record can become downloadable only when every trust and integrity fact agrees.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-24-SUMMARY.md` when done.</output>
