---
phase: 03-complete-web-experience
plan: "12"
type: tdd
wave: 5
depends_on: ["03-10", "03-11"]
files_modified:
  - packages/web-core/src/routes.ts
  - packages/web-core/src/routes.test.ts
  - packages/web-core/src/content.ts
  - packages/web-core/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "One canonical manifest owns every public, docs, release, account, admin, and authored error route."
    - "Navigation, breadcrumbs, sitemap, redirects, desktop links, indexing, ownership, and safe cross-origin context are projections—not parallel lists."
  artifacts:
    - path: packages/web-core/src/routes.ts
      provides: "Frozen `webRoutes` manifest and fail-closed route/link projections"
      exports: ["webRoutes", "routeHref", "matchWebRoute", "projectNavigation", "projectSitemap", "createBoundaryLink"]
    - path: packages/web-core/src/content.ts
      provides: "Typed content identity and route ownership helpers"
  key_links:
    - from: packages/web-core/src/routes.ts
      to: packages/contracts-ts/src/index.ts
      via: "Generated WebRouteRecord and runtime validator"
      pattern: "WebRouteRecord|validateWebDocument"
---

<objective>
Build the canonical executable route, ownership, and safe-context authority.

Purpose: Implement D-02, D-08, D-22, D-24, D-25, D-29, D-54, and D-78 before any page inventory is authored.
Output: Frozen route manifest, all consumer projections, safe cross-origin links, and bidirectional omission tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/desktop/src/routes.tsx
@packages/contracts-ts/src/index.ts
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one complete route and link authority</name>
  <files>packages/web-core/src/routes.ts, packages/web-core/src/routes.test.ts, packages/web-core/src/content.ts, packages/web-core/src/index.ts</files>
  <read_first>apps/desktop/src/routes.tsx typed/frozen resolver pattern; generated WebRouteRecord; 03-UI-SPEC.md minimum route families and navigation contracts; 03-CONTEXT.md D-02, D-08, D-22, D-24, D-25, D-29, D-54, D-78; 03-PATTERNS.md routes analog</read_first>
  <behavior>
    - Manifest contains every required family and authored 404/403/410/500 identity with exact surface, shell, indexing, locale, owner, scenario, security boundary, and safe context.
    - Unknown route/parameter/context/origin returns a typed failure.
    - Public/account/admin projections never leak private, noindex, scenario, diagnostic, session, or arbitrary return URL state.
    - Missing, extra, renamed, duplicated, or unowned consumer entries fail bidirectionally.
  </behavior>
  <action>RED: specify the complete route IDs and mutation matrix. GREEN: implement a deeply frozen `webRoutes` array validated through generated contracts; define public Home/Product/Evidence/Compatibility/Plans/Search/Support/Status/Policies/Disclosure; docs task/version/article/reference/troubleshooting/history; releases index/channel/version/integrity/download/install; account sign-in/overview/profile/security/subscription/invoices/device/downloads/privacy/support; admin role/support/operations/security/diagnostics/audit; and authored error identities. Implement `routeHref`, `matchWebRoute`, navigation/breadcrumb/sitemap/redirect/desktop-link/indexing projections, and `createBoundaryLink` that accepts only enumerated `locale`, `version`, `channel`, `section`, `requestedDestination`, and manifest route return IDs. Never accept arbitrary URL/host/scheme or duplicate pathname lists.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-core test -- --run -t "canonical web route manifest"</automated></verify>
  <acceptance_criteria>All required families exist once; every projection derives from `webRoutes`; safe-context allowlist is exact; every seeded drift/redirect/leak mutation fails with a stable code.</acceptance_criteria>
  <done>Every route and boundary transition has one typed, testable authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Route/context input → navigation | User-controlled paths and return intents can cross origins or leak state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03 | Spoofing | return path/origin | mitigate | Route-ID resolution and exact safe-context allowlist. |
| T-03-04 | Information disclosure | cross-origin links | mitigate | Exclude sessions, diagnostics, consent, forms, and arbitrary URLs. |
| T-03-05 | Information disclosure | indexing projection | mitigate | Canonical public-only projection and omission mutations. |
| T-03-SC | Tampering | package installs | mitigate | No install; generated contracts and approved workspace roots only. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-core/src/routes.ts`, `routes.test.ts`, `content.ts`; modified `src/index.ts`.
- Types/fields: `WebRouteId`, `WebRouteMatch`, `WebRouteErrorCode`, `WebRouteResult`, `BoundaryLink`, `BoundaryLinkError`, `ContentIdentity`; generated fields are consumed unchanged.
- Symbols/routes/CLI: `webRoutes`, `routeHref`, `matchWebRoute`, `projectNavigation`, `projectBreadcrumbs`, `projectSitemap`, `projectRedirects`, `projectDesktopLinks`, `projectIndexing`, `createBoundaryLink`; route IDs/families enumerated in the task action; no CLI flag.

<verification>
Focused route tests, package check, and architecture tests pass.
</verification>

<success_criteria>
No menu, sitemap, redirect, desktop link, test, or indexing consumer needs a handwritten route list.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-12-SUMMARY.md` when done.</output>
