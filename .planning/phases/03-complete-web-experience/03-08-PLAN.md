---
phase: 03-complete-web-experience
plan: "08"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-04", "03-05"]
files_modified:
  - apps/admin/package.json
  - apps/admin/tsconfig.json
  - apps/admin/next.config.ts
  - apps/admin/src/index.ts
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Admin is a separate fixture-classified deployable artifact with no ordinary public/account navigation edge."
    - "Its scaffold exposes no authentication, diagnostic, consent, or administrative mutation authority."
  artifacts:
    - path: apps/admin/package.json
      provides: "@liiiraa/admin isolated preview app"
    - path: apps/admin/src/index.ts
      provides: "Fixture/disconnected admin composition identity"
  key_links:
    - from: apps/admin/package.json
      to: packages/web-preview/package.json
      via: "Explicit fixture adapter dependency"
      pattern: "@liiiraa/web-preview"
---

<objective>
Scaffold the isolated administrative preview application.

Purpose: Make D-54 and WEB-08 independent deployment boundaries explicit before any role UI exists.
Output: Exact-pinned admin app manifest, strict config, standalone build config, and disconnected fixture marker.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-DEPENDENCY-APPROVAL.md
@.planning/phases/03-complete-web-experience/03-PATTERNS.md
@packages/web-core/package.json
@packages/web-features/package.json
@packages/web-preview/package.json
@apps/desktop/package.json
@packages/desktop-simulator/package.json
@architecture/module-boundaries.json
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the admin preview build contract</name>
  <files>apps/admin/package.json, apps/admin/tsconfig.json, apps/admin/next.config.ts, apps/admin/src/index.ts</files>
  <read_first>03-DEPENDENCY-APPROVAL.md; 03-CONTEXT.md D-54 through D-60; 03-PATTERNS.md app-package, public-root, and production/fixture sibling patterns; 03-RESEARCH.md admin preview and separate-origin guidance; 03-UI-SPEC.md admin register; packages/web-core/package.json; packages/web-features/package.json; packages/web-preview/package.json; apps/desktop/package.json; apps/desktop/tsconfig.json; apps/desktop/src/index.ts; packages/desktop-simulator/package.json; architecture/module-boundaries.json reserved admin/web-preview records</read_first>
  <action>Create private ESM `@liiiraa/admin` with exact approved Next/React/next-intl identities and workspace dependencies on design plus the already-created web-core, web-features, and web-preview public roots. Define terminating `dev`, `build`, `start`, `check`, `test`, `verify`. Configure standalone, typed routes, and unique build ID prefix; export `ADMIN_WEB_COMPOSITION = {runtimeClass:'fixture',surface:'admin',authorityConnected:false,ordinaryNavigationLinked:false}`. Do not add real access policy, auth/session, diagnostics, upload, customer data, or mutation dependencies. Verify only the static scaffold before Plan 03-11 performs lifecycle-disabled installation and package checking.</action>
  <verify><automated>rtk node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('apps/admin/package.json','utf8'));const exact={next:'16.2.12',react:'19.2.8','react-dom':'19.2.8','next-intl':'4.13.4'};for(const [n,v] of Object.entries(exact))if(j.dependencies?.[n]!==v)process.exit(1);for(const n of ['@liiiraa/web-core','@liiiraa/web-features','@liiiraa/web-preview'])if(j.dependencies?.[n]!=='workspace:*')process.exit(1);if(!['dev','build','start','check','test','verify'].every(x=>typeof j.scripts?.[x]==='string'))process.exit(1);for(const f of ['apps/admin/tsconfig.json','apps/admin/next.config.ts','apps/admin/src/index.ts'])if(!fs.existsSync(f))process.exit(1);const c=fs.readFileSync('apps/admin/next.config.ts','utf8');if(!/standalone/.test(c)||!/typedRoutes/.test(c))process.exit(1)"</automated></verify>
  <acceptance_criteria>The admin artifact has its own package/build identity, literal disconnected authority and navigation flags, and no real sensitive dependency.</acceptance_criteria>
  <done>The isolated admin surface has a truthful independently deployable preview identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin preview → future privileged authority | Operational appearance must not grant or imply privileged access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing/Elevation | admin composition | mitigate | Fixture/disconnected literals and no ordinary navigation link. |
| T-03-06 | Clickjacking | admin deployment | mitigate | Dedicated CSP/access policy is required by Plan 03-17. |
| T-03-SC | Tampering | Next.js packages | mitigate | Exact approved pins; lock resolution deferred to Plan 03-11. |
</threat_model>

## Artifacts this phase produces

- Files: `apps/admin/package.json`, `apps/admin/tsconfig.json`, `apps/admin/next.config.ts`, `apps/admin/src/index.ts`.
- Symbols/routes/CLI: `ADMIN_WEB_COMPOSITION{runtimeClass,surface,authorityConnected,ordinaryNavigationLinked}`; package scripts `dev`, `build`, `start`, `check`, `test`, `verify`; config keys `output`, `typedRoutes`, build ID prefix; no route/CLI flag.

<verification>
Static manifest/config/source assertions pass, including explicit fixture dependencies and app isolation; installed checking begins in Plan 03-11.
</verification>

<success_criteria>
Admin can later expose role previews while remaining artifact-, origin-, and authority-isolated.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-08-SUMMARY.md` when done.</output>
