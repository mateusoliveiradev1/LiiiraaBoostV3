---
phase: 03-complete-web-experience
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - architecture/module-boundaries.json
  - tooling/architecture-tests/src/check-workspace.test.ts
autonomous: true
requirements: [WEB-08]
must_haves:
  truths:
    - "The seven future web roots have unique owners, public roots, layers, runtime classes, and reserved status before manifests exist."
    - "A public production composition cannot depend on the fixture preview adapter, and admin can never become a hidden public route."
  artifacts:
    - path: architecture/module-boundaries.json
      provides: "Reserved public/account/admin and web package ownership records"
    - path: tooling/architecture-tests/src/check-workspace.test.ts
      provides: "Phase 3 reservation and isolation mutation tests"
  key_links:
    - from: tooling/architecture-tests/src/check-workspace.test.ts
      to: architecture/module-boundaries.json
      via: "Canonical in-memory reservation projection"
      pattern: "Phase 3 web reservation|PRODUCTION_TO_FIXTURE|DEEP_IMPORT"
---

<objective>
Establish executable module and deployment ownership before any web application is scaffolded.

Purpose: Make D-07 and D-54 architectural facts rather than naming conventions.
Output: Reserved module records and mutation tests for legal direction, fixture isolation, deep imports, cycles, and separate compositions.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@architecture/module-boundaries.json
@architecture/decisions/0003-module-ownership-and-direction.md
@architecture/decisions/0004-truth-provenance-and-fixture-boundary.md
@tooling/architecture-tests/src/check-workspace.test.ts
@.planning/phases/03-complete-web-experience/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reserve the Phase 3 module graph</name>
  <files>architecture/module-boundaries.json, tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>architecture/module-boundaries.json; tooling/architecture-tests/src/check-workspace.test.ts Phase 2 reservation pattern; ADR-0003; ADR-0004; 03-PATTERNS.md module activation section</read_first>
  <behavior>
    - The exact roots `packages/web-core`, `packages/web-preview`, `packages/web-features`, `tooling/web-evidence`, `apps/web`, `apps/account`, and `apps/admin` each resolve to one reserved owner/public root.
    - `web-preview`, `apps/account`, and `apps/admin` are fixture-classified; `apps/web` is production.
    - None is discoverable before its manifest exists.
  </behavior>
  <action>Add the seven reserved module records per D-07, D-54, and the research responsibility map: `web-core` application/production; `web-preview` adapter/fixture; `web-features` feature/production; `web-evidence` tooling/tooling; `web-app` composition/production; `account-app` and `admin-app` composition/fixture. Add focused tests using canonical metadata and live discovery; do not create directories or manifests in this task.</action>
  <verify><automated>rtk pnpm test:architecture -- --run -t "Phase 3 web reservation"</automated></verify>
  <acceptance_criteria>Seven exact records exist once, remain reserved and undiscoverable, and match the documented layer/runtime map.</acceptance_criteria>
  <done>The repository has one executable ownership authority before web roots appear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject web isolation and fixture-boundary violations</name>
  <files>tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>tooling/architecture-tests/src/check-workspace.test.ts Phase 2 ownership mutations; tooling/architecture-tests/src/policy.ts; architecture/module-boundaries.json; 03-RESEARCH.md Anti-Patterns</read_first>
  <behavior>
    - Legal web-core/design/web-feature/composition edges pass.
    - Public-to-web-preview, public-to-account, public-to-admin, account-to-admin, deep imports, duplicate ownership, and cycles fail with stable diagnostics.
  </behavior>
  <action>Create an in-memory Phase 3 activated graph derived from the canonical records. Add one mutation per forbidden edge and retain all Phase 1/2 tests. Explicitly assert that a route group cannot satisfy the admin deployment boundary and that fixture apps do not become production authority merely because they are independently buildable.</action>
  <verify><automated>rtk pnpm test:architecture -- --run -t "Phase 3 web isolation"</automated></verify>
  <acceptance_criteria>Every negative mutation fails only for its intended reason, the legal graph passes, and no existing architectural rule is weakened.</acceptance_criteria>
  <done>Cross-surface and production-to-fixture violations fail before application work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public composition → preview adapters | Fixture data must never enter the public production artifact. |
| Public/account → admin | Visual consistency must not collapse deployment or privilege boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing/Elevation | module graph | mitigate | Runtime classes plus production-to-fixture and cross-composition mutation tests. |
| T-03-04 | Information disclosure | cross-surface imports | mitigate | Public roots, deep-import refusal, and unique ownership. |
| T-03-SC | Tampering | npm installs | mitigate | This plan installs nothing and leaves roots reserved. |
</threat_model>

## Artifacts this phase produces

- Files: modified `architecture/module-boundaries.json`, `tooling/architecture-tests/src/check-workspace.test.ts`.
- Symbols/routes/CLI: module IDs `web-core`, `web-preview`, `web-features`, `web-evidence`, `web-app`, `account-app`, `admin-app`; test suites `Phase 3 web reservation`, `Phase 3 web isolation`; no route or CLI flag.

<verification>
Run both focused suites and the full architecture gate.
</verification>

<success_criteria>
Every Phase 3 root has a pre-declared owner and illegal surface/fixture edges are rejected before activation.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-02-SUMMARY.md` when done.</output>
