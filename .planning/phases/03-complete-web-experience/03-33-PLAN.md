---
phase: 03-complete-web-experience
plan: "33"
type: execute
wave: 16
depends_on: ["03-32"]
files_modified:
  - apps/account/src/account-errors.ts
  - apps/account/src/account-preview-model.ts
  - apps/account/src/account-failure-view.tsx
  - apps/account/src/app/[locale]/[[...responsibility]]/page.tsx
  - apps/account/src/account-shell.test.ts
autonomous: true
gap_closure: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Canonical account 403, 410, and 500 URLs render distinct localized authored outcomes instead of falling through to account 404."
    - "Every account failure preserves locale, exposes safe same-origin recovery, redacts diagnostics, and keeps Phase 4 authority disconnected."
    - "Unknown account URLs still resolve through the genuine localized 404 boundary."
  artifacts:
    - path: apps/account/src/account-errors.ts
      provides: "Closed bilingual 403/404/410/500 account failure model"
      exports: ["createAccountFailureModel", "redactedAccountCorrelationId"]
    - path: apps/account/src/account-failure-view.tsx
      provides: "Shared semantic account failure presentation"
    - path: apps/account/src/app/[locale]/[[...responsibility]]/page.tsx
      provides: "Manifest-aware account workflow/error dispatch"
  key_links:
    - from: apps/account/src/app/[locale]/[[...responsibility]]/page.tsx
      to: apps/account/src/account-preview-model.ts
      via: "separate preview-route and error-route guards after matchWebRoute"
      pattern: "isAccount(Error|Preview)Route"
    - from: apps/account/src/app/[locale]/[[...responsibility]]/page.tsx
      to: apps/account/src/account-errors.ts
      via: "canonical error route ID to AccountFailureKind mapping"
      pattern: "createAccountFailureModel"
---

<objective>
Close the account half of the D-25 route gap by making all four canonical account error identities executable, distinct, localized, redacted, and recoverable.

Purpose: The canonical manifest already declares account 403/404/410/500, but the catch-all admits only responsibility routes; this plan makes the route contract truthful without adding session or mutation authority.
Output: Closed account error model, explicit route classification, reusable failure view, catch-all dispatch, and focused regression tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-VERIFICATION.md
@.planning/phases/03-complete-web-experience/03-CONTEXT.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/phases/03-complete-web-experience/03-16-SUMMARY.md
@.planning/phases/03-complete-web-experience/03-27-SUMMARY.md
@packages/web-core/src/routes.ts
@apps/account/src/account-errors.ts
@apps/account/src/account-preview-model.ts
@apps/account/src/app/[locale]/[[...responsibility]]/page.tsx

<interfaces>
Existing account model:
- `ACCOUNT_ENTRY_ROUTE_IDS` and `isAccountPreviewRoute(routeId)` admit only the ten responsibility routes.
- `createAccountFailureModel(kind, locale, digest?)` currently accepts only `404 | 500`.
- `matchWebRoute({pathname, securityBoundary: 'account-origin'})` already resolves `account-error-403`, `account-error-410`, and `account-error-500` from the canonical manifest.

Create and consume these closed interfaces:
- `ACCOUNT_ERROR_ROUTE_IDS = ['account-error-404', 'account-error-403', 'account-error-410', 'account-error-500']`.
- `AccountErrorRoute`, `isAccountErrorRoute(routeId)`, and `accountFailureKindForRoute(routeId)`.
- `AccountFailureKind = '403' | '404' | '410' | '500'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the closed account error contract before implementation</name>
  <files>apps/account/src/account-shell.test.ts, apps/account/src/account-errors.ts, apps/account/src/account-preview-model.ts</files>
  <read_first>apps/account/src/account-shell.test.ts; apps/account/src/account-errors.ts; apps/account/src/account-preview-model.ts; packages/web-core/src/routes.ts; 03-CONTEXT.md D-06, D-25, D-26, D-49; 03-UI-SPEC.md State Contract and Copywriting Contract</read_first>
  <behavior>
    - All four canonical account error route IDs map one-to-one to 403, 404, 410, and 500.
    - PT-BR and English provide distinct titles, affected-capability detail, safe-state/recovery copy, and same-origin overview/support destinations for every status.
    - 410 preserves useful historical context and names a current safe destination; 403 explains denied responsibility; 500 remains recoverable; 404 remains the unknown-route boundary.
    - Correlation IDs accept only bounded opaque digests and reduce stack paths, email-like values, request data, and arbitrary text to `REDACTED`.
  </behavior>
  <action>Use RED→GREEN TDD. First extend `account-shell.test.ts` with the four-route mapping, locale parity, unique status/title, recovery, and redaction expectations and commit the failing test as `test(03-33): cover complete account error routing`. Then extend `AccountFailureKind`, bilingual frozen copy, and route ID typing/guards until the focused test passes; commit GREEN as `feat(03-33): close account error contract`. Keep responsibility routes and error routes as separate unions so an error identity can never enter `AccountPreviewPage`. Implement D-25 exactly and preserve D-06/D-26/D-49; do not introduce cookies, request-state scenario selection, real session behavior, or Phase 4 effects.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/account test -- --run src/account-shell.test.ts</automated>
  </verify>
  <acceptance_criteria>The RED commit fails because 403/410 coverage and route guards are absent; the GREEN commit passes with four exhaustive kinds, two-locale parity, opaque redaction, and no change to the ten responsibility route identities.</acceptance_criteria>
  <done>The account owns one closed, typed, bilingual, redacted model for every D-25 failure class.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch canonical account failures before the 404 fallback</name>
  <files>apps/account/src/account-failure-view.tsx, apps/account/src/app/[locale]/[[...responsibility]]/page.tsx, apps/account/src/account-shell.test.ts</files>
  <read_first>apps/account/src/app/[locale]/[[...responsibility]]/page.tsx; apps/account/src/app/[locale]/not-found.tsx; apps/account/src/app/[locale]/error.tsx; apps/admin/src/admin-failure-view.tsx; apps/account/src/app/account-shell.css; apps/account/src/account-shell.test.ts</read_first>
  <action>Create a server-safe `AccountFailureView` following the established admin view pattern: one visible H1, semantic status code, affected/detail/safe-work regions, same-origin recovery actions, redacted correlation text, `role="alert"` only for 500, and existing `.account-failure*` styles so reduced motion, forced colors, focus, and reflow remain intact. Refactor the account catch-all resolution into a discriminated workflow/error result. After canonical `matchWebRoute`, render `AccountPreviewPage` only for responsibility IDs, render the shared failure view for account 403/410/500 (and any canonical 404 path not owned by the special route), and call `notFound()` only for unknown/invalid locale paths. Generate localized noindex metadata from the corresponding failure model. Keep the URL unchanged and do not redirect, per D-25.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/account verify</automated>
  </verify>
  <acceptance_criteria>`/pt-BR/errors/403`, `/en/errors/410`, and `/pt-BR/errors/500` build as executable account routes with their own status/H1/copy/recovery; an undeclared account path still reaches localized 404; account CSP, noindex, preview provenance, and disconnected authority tests remain green.</acceptance_criteria>
  <done>Canonical account error routes can no longer collapse into 404 and remain safe across both locales and accessibility modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Request pathname/locale → account catch-all | Untrusted route input chooses either a known preview responsibility, a known authored failure, or the genuine 404 boundary. |
| Runtime digest → visible account diagnostics | Error details must remain useful without exposing stack paths, identifiers, credentials, form values, or Phase 4 state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-GC-A1 | Information Disclosure | `account-errors.ts` | mitigate | Closed status copy plus bounded opaque digest validation; tests reject stack/path/email/request leakage. |
| T-03-GC-A2 | Spoofing | account error routes | mitigate | Resolve only canonical account-origin route IDs and keep persistent disconnected-preview provenance. |
| T-03-GC-A3 | Tampering | catch-all dispatch | mitigate | Separate exhaustive workflow/error guards; unknown IDs fail through real 404 and cannot select arbitrary UI. |
| T-03-SC | Tampering | dependency graph | accept | No package installation or dependency change occurs in this plan. |
</threat_model>

<verification>
Run the focused TDD test after RED and GREEN, then `rtk pnpm --filter @liiiraa/account verify`. Confirm the optimized route table contains the account catch-all and that source contains no new cookie, fetch mutation, redirect, arbitrary URL, or authority connection.
</verification>

<success_criteria>
- Account 403, 404, 410, and 500 have distinct localized models and visible semantic outcomes.
- Canonical error IDs are dispatched before the unknown-route fallback.
- Diagnostics remain redacted and recovery remains same-origin and keyboard accessible.
- Account deployment policy, noindex behavior, responsive/forced-colors/reduced-motion behavior, and Phase 4 no-change boundary are unchanged.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-33-SUMMARY.md` when done.</output>
