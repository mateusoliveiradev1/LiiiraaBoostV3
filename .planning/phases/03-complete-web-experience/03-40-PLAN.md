---
phase: 03-complete-web-experience
plan: "40"
type: execute
wave: 22
depends_on: ["03-39"]
files_modified:
  - apps/admin/package.json
  - apps/admin/proxy.ts
  - apps/admin/src/proxy.ts
  - apps/admin/src/admin-runtime.ts
  - apps/admin/src/admin-security.test.ts
autonomous: true
gap_closure: true
requirements: [WEB-08]
must_haves:
  truths:
    - "Local UAT reaches the isolated admin app on a dedicated exact origin and enters through /[locale]/admin."
    - "Rejected document navigation receives authored localized HTML while every rejected request remains fail-closed before React."
    - "Rejected non-document requests retain bounded JSON, no-store/security headers, and never forward."
    - "The origin fix preserves independent deployment, CSP/cookie isolation, NodeNext, and Next build compatibility."
  artifacts:
    - path: apps/admin/package.json
      provides: "Dedicated admin.localhost local development command"
    - path: apps/admin/src/admin-runtime.ts
      provides: "Bounded local/production admin origin policy and localized denial copy"
    - path: apps/admin/src/proxy.ts
      provides: "Exact-origin admission and request-intent-aware denial"
    - path: apps/admin/src/admin-security.test.ts
      provides: "Mutation-resistant admin admission and denial tests"
  key_links:
    - from: apps/admin/package.json
      to: apps/admin/src/admin-runtime.ts
      via: "dev hostname and port equal the safe local exact-origin default"
      pattern: "admin.localhost"
    - from: apps/admin/proxy.ts
      to: apps/admin/src/proxy.ts
      via: "Next proxy entry delegates every request to one fail-closed admission policy"
      pattern: "proxy"
---

<objective>
Repair only the isolated admin admission path: dedicated local origin, canonical locale/admin entry, and authored localized browser denial that remains fail-closed.

Purpose: Close the security/routing half of the admin UAT diagnosis without mixing it with visual redesign, golden replacement, regression, or approval. This implements D-59, D-60, D-61, D-62, D-63, and D-64 without adding Phase 4 authority.
Output: A tested exact-origin admin proxy and dedicated local UAT command.
</objective>

<execution_requirements>
Invoke and apply `$impeccable` before editing, using its denial-state copy, accessibility, localization, and recovery guidance while security admission remains the controlling contract.
</execution_requirements>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-complete-web-experience/03-UAT.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
@.planning/debug/phase-03-admin-root-interface.md
@apps/admin/package.json
@apps/admin/src/admin-runtime.ts
@apps/admin/src/proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align the dedicated admin origin and canonical entry</name>
  <files>apps/admin/package.json, apps/admin/src/admin-runtime.ts, apps/admin/src/admin-security.test.ts</files>
  <behavior>
    - Test 1: local admin development binds `admin.localhost:3002` and the bounded local default is exactly `http://admin.localhost:3002`.
    - Test 2: the admitted UAT path is `/pt-BR/admin` or `/en/admin`; the bare locale path cannot mask an earlier origin failure.
    - Test 3: production still requires an explicit isolated origin and no localhost suffix/wildcard is admitted.
  </behavior>
  <action>Write failing exact-origin and canonical-entry tests before changing the runtime. Bind the admin development command to the dedicated hostname/port while retaining the approved Next development mode. Make the bounded safe local default match that command exactly, keep production explicit, and identify `/[locale]/admin` as the UAT entry. Preserve NodeNext, Next proxy/build behavior, CSP, cookie isolation, and independent deployability per D-59 through D-62.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/admin exec vitest run src/admin-security.test.ts -t "origin|canonical|localhost"</automated>
  </verify>
  <done>Admin UAT uses one dedicated exact origin and canonical localized entry without weakening production admission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return safe localized browser denial without forwarding</name>
  <files>apps/admin/proxy.ts, apps/admin/src/proxy.ts, apps/admin/src/admin-runtime.ts, apps/admin/src/admin-security.test.ts</files>
  <behavior>
    - Test 1: an origin-rejected document request returns escaped static PT-BR or English HTML, status 403, recovery link to the canonical entry, and no application data.
    - Test 2: rejected non-document requests retain bounded JSON with equivalent reason semantics.
    - Test 3: every rejection carries no-store/security headers and no rejection branch calls `NextResponse.next()`.
  </behavior>
  <action>Add RED mutations for request intent, locale parsing, escaping, status, headers, response shape, and forwarding. Implement a bounded localized denial record and static HTML document for browser navigation; do not render React or invoke preview adapters on rejection. Preserve JSON for non-document clients, exact-origin equality, request identifiers when already available, and all CSP/cookie/security headers. Keep root proxy delegation thin and prove no rejected branch forwards per D-63 and D-64.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/admin exec vitest run src/admin-security.test.ts &amp;&amp; rtk pnpm --filter @liiiraa/admin build</automated>
  </verify>
  <done>Browser and API clients receive appropriate localized fail-closed denial, and admitted requests alone can reach admin React.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser to admin proxy | Untrusted origin, host, locale, path, headers, and request intent are admitted or rejected before React. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-40-01 | Spoofing | exact-origin admission | mitigate | Bind one dedicated hostname and compare exact origin; reject generic localhost, suffix, and wildcard matches. |
| T-03-40-02 | Tampering | denial response | mitigate | Escape bounded localized copy, preserve no-store/security headers, and test response shape. |
| T-03-40-03 | Information Disclosure | browser denial | mitigate | Return no React, fixture, role, or application data before admission. |
| T-03-40-04 | Elevation of Privilege | proxy forwarding | mitigate | Assert every rejected branch terminates and never invokes `NextResponse.next()`. |
| T-03-SC | Tampering | package supply chain | accept | This plan installs no package and must leave dependency declarations and lockfiles unchanged. |
</threat_model>

<verification>
Run focused security tests and the independent admin build. Do not capture or promote visual evidence in this plan.
</verification>

<success_criteria>
- Dedicated local and explicit production origins remain exact-match isolated.
- Canonical localized admin entry is unambiguous.
- Browser denial is authored, localized, escaped, accessible, and fail-closed.
- Non-document denial remains bounded JSON.
- No rejected request reaches React or preview data.
</success_criteria>

<output>
Create `.planning/phases/03-complete-web-experience/03-40-SUMMARY.md` when done.
</output>
