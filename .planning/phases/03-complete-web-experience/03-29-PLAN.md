---
phase: 03-complete-web-experience
plan: "29"
type: execute
wave: 12
depends_on: ["03-20", "03-23", "03-25"]
files_modified:
  - tooling/web-evidence/src/capture-desktop.ts
  - tooling/web-evidence/src/capture-desktop.test.ts
  - tooling/web-evidence/capture-manifest.json
  - apps/web/public/product/desktop-home.pt-BR.webp
  - apps/web/public/product/desktop-home.pt-BR.json
  - apps/web/public/product/desktop-home.en.webp
  - apps/web/public/product/desktop-home.en.json
autonomous: true
requirements: [WEB-01, WEB-02]
must_haves:
  truths:
    - "Every published real-product image is captured from the executable desktop app in a deterministic scenario with complete provenance."
    - "Cropping/framing cannot change values, states, copy, or provenance; stale captures fail when relevant product source changes."
  artifacts:
    - path: tooling/web-evidence/capture-manifest.json
      provides: "Capture/version/locale/scenario/viewport/source/checksum/invalidation authority"
    - path: apps/web/public/product/desktop-home.pt-BR.json
      provides: "PT-BR image provenance sidecar"
  key_links:
    - from: apps/web/public/product/desktop-home.pt-BR.webp
      to: apps/web/public/product/desktop-home.pt-BR.json
      via: "SHA-256 and capture metadata"
      pattern: "sha256|scenarioId|sourceCommit"
---

<objective>
Create and execute the deterministic real desktop screenshot pipeline.

Purpose: Implement D-16, D-17, D-64, D-79, and D-81 so the public site proves the real desktop product instead of publishing mockups.
Output: Capture tool/tests/manifest and bilingual real-product assets with validated sidecars.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/desktop/playwright.config.ts
@contracts/scenarios/desktop-scenarios.json
@packages/contracts-ts/src/validation.ts
@apps/web/src/features/home.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic capture/provenance tool</name>
  <files>tooling/web-evidence/src/capture-desktop.ts, tooling/web-evidence/src/capture-desktop.test.ts, tooling/web-evidence/capture-manifest.json</files>
  <read_first>03-CONTEXT.md D-16, D-17, D-81; generated ScreenshotProvenance; apps/desktop Playwright/scenario/capture patterns; 03-UI-SPEC.md screenshot matrix; web content admission asset checks</read_first>
  <behavior>
    - Capture accepts only executable desktop URL/process, canonical scenario, shipping locale, approved viewport, source commit/build ID, output file and sidecar.
    - Output/sidecar checksum, dimensions, crop coordinates, fonts/browser/OS, command, review and invalidation inputs are deterministic.
    - Generated/mockup/source-tree/non-executable/missing-provenance/stale/tampered input fails.
  </behavior>
  <action>Implement `capture-desktop` tooling over existing Playwright/desktop lifecycle. Validate emitted ScreenshotProvenance, hash image and source inputs, record desktop version/locale/scenario/viewport/capture command/source commit/build ID/checksum/crop/review/invalidation paths, and allow only non-destructive crop/frame coordinates. Add `--check` and `--capture` modes; `--check` must never regenerate. Pin Windows/Chromium/fonts environment and freeze animations/clock. Reject visual probes, generated images, CSS mockups, or modified in-image text/values.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence test -- --run -t "desktop capture provenance"</automated></verify>
  <acceptance_criteria>Positive exact capture fixture validates; every source/tamper/stale/provenance mutation fails; tool writes only allowlisted image/sidecar paths.</acceptance_criteria>
  <done>Real desktop images are reproducible build inputs with tamper-evident provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and bind the approved Home product assets</name>
  <files>apps/web/public/product/desktop-home.pt-BR.webp, apps/web/public/product/desktop-home.pt-BR.json, apps/web/public/product/desktop-home.en.webp, apps/web/public/product/desktop-home.en.json</files>
  <read_first>capture manifest/tool; desktop executable/canonical scenario; Home RealProductStage; UI-SPEC W01/W02; content admission asset policy</read_first>
  <action>Build/run the actual desktop app in its canonical deterministic Home scenario and capture PT-BR and English at approved source viewport. Produce WebP with explicit dimensions and sidecars; do not alter in-app copy, values, states, or provenance. Bind assets to Home/docs social/content records and ensure the visible full unmodified image remains available. If executable capture cannot run, fail with an owned missing-evidence result—never substitute a mockup.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence verify -- --capture-manifest tooling/web-evidence/capture-manifest.json --check</automated></verify>
  <acceptance_criteria>Both images originate from executable desktop, sidecar hashes match, locale/scenario/version/viewport/source are exact, Home references only admitted assets.</acceptance_criteria>
  <done>The public proof stage uses real deterministic product captures in both shipping locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Desktop executable → public image asset | Capture/editing can fabricate product state or lose provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing/Tampering | product screenshots | mitigate | Executable-only capture, sidecar schema/hash/source invalidation, non-destructive crop. |
| T-03-11 | Repudiation | capture provenance | mitigate | Commit/build/command/environment/checksum record. |
| T-03-SC | Tampering | tooling packages | mitigate | Existing locked Playwright only. |
</threat_model>

## Artifacts this phase produces

- Files: capture tool/test/manifest, two WebP assets and two JSON sidecars at exact paths.
- Types/fields: `CaptureRequest{version,locale,scenarioId,viewport,sourceCommit,buildId,outputPath,crop}`, `CaptureResult{imagePath,sidecarPath,sha256,width,height}`, generated `ScreenshotProvenance` fields.
- Symbols/routes/CLI: `captureDesktopProduct`, `verifyDesktopCapture`; flags `--capture`, `--check`, `--capture-manifest`; no route.

<verification>
Run capture provenance suite, check manifest without regeneration, content admission, and W01/W02 asset render checks.
</verification>

<success_criteria>
Every public “real app” image is demonstrably the executable desktop product and changes invalidate stale evidence.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-29-SUMMARY.md` when done.</output>
