---
phase: 03-complete-web-experience
plan: "05"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - packages/web-features/package.json
  - packages/web-features/tsconfig.json
  - packages/web-features/src/index.ts
  - tooling/web-evidence/package.json
  - tooling/web-evidence/tsconfig.json
  - tooling/web-evidence/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Shared semantic UI and release evidence tooling have different package ownership and runtime classes."
    - "The feature package consumes only public production roots; tooling may inspect all artifacts without becoming product runtime."
  artifacts:
    - path: packages/web-features/package.json
      provides: "@liiiraa/web-features UI boundary"
    - path: tooling/web-evidence/package.json
      provides: "@liiiraa/web-evidence verification boundary"
  key_links:
    - from: packages/web-features/package.json
      to: packages/design-system/package.json
      via: "workspace public-root dependency"
      pattern: "@liiiraa/design-system"
---

<objective>
Create the web feature and evidence-tooling package boundaries.

Purpose: Prevent product UI from owning fixture adapters or release-certification logic.
Output: Strict package scaffolds for semantic components and cross-app evidence.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/feature-shell/package.json
@tooling/fixture-guard/package.json
@architecture/module-boundaries.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the production web-features package</name>
  <files>packages/web-features/package.json, packages/web-features/tsconfig.json, packages/web-features/src/index.ts</files>
  <read_first>packages/feature-shell/package.json; packages/feature-shell/tsconfig.json; packages/feature-shell/src/index.ts; packages/design-system/package.json; architecture/module-boundaries.json</read_first>
  <action>Create private ESM `@liiiraa/web-features` with explicit public export and terminating `build`, `check`, `test` scripts. Add only approved workspace/runtime dependencies needed later: `@liiiraa/contracts-ts`, `@liiiraa/design-system`, `@liiiraa/design-tokens`, `@liiiraa/web-core`, React, XState, and TanStack Query at existing lockfile pins. Export `WEB_FEATURE_CONTRACT_VERSION = 1`; do not import web-preview or create UI yet.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-features check</automated></verify>
  <acceptance_criteria>The feature package has no fixture dependency or deep import and uses only already-approved exact identities.</acceptance_criteria>
  <done>Shared semantic components have an enforceable production boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the tooling-only web-evidence package</name>
  <files>tooling/web-evidence/package.json, tooling/web-evidence/tsconfig.json, tooling/web-evidence/src/index.ts</files>
  <read_first>tooling/fixture-guard/package.json; tooling/acceptance-policy/tsconfig.json; architecture/module-boundaries.json; 03-VALIDATION.md Wave 0 Requirements</read_first>
  <action>Create private ESM `@liiiraa/web-evidence` with explicit tooling public root, terminating `build`, `check`, `test`, and `verify` scripts. Depend on approved workspace packages plus existing Vitest, Playwright, and axe pins only. Export `WEB_EVIDENCE_SCHEMA_VERSION = 1`; leave test implementations to Plan 03-13.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence check</automated></verify>
  <acceptance_criteria>The tooling package is discoverable by manifest, has no runtime product export, and adds no new external identity.</acceptance_criteria>
  <done>Cross-surface verification has a bounded tooling owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Product feature runtime → evidence tooling | Certification code must not enter shipped bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | feature dependency graph | mitigate | Production feature package has no fixture edge. |
| T-03-11 | Tampering | evidence tooling identity | mitigate | Separate tooling module and exact public root. |
| T-03-SC | Tampering | package manifests | mitigate | Existing approved identities only; no install. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-features/package.json`, `packages/web-features/tsconfig.json`, `packages/web-features/src/index.ts`, `tooling/web-evidence/package.json`, `tooling/web-evidence/tsconfig.json`, `tooling/web-evidence/src/index.ts`.
- Symbols/routes/CLI: `WEB_FEATURE_CONTRACT_VERSION`, `WEB_EVIDENCE_SCHEMA_VERSION`; package scripts `build`, `check`, `test`, `verify`; no route or CLI flag.

<verification>
Both strict package checks pass and no production package imports the evidence module.
</verification>

<success_criteria>
UI composition and evidence certification can evolve independently without boundary drift.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-05-SUMMARY.md` when done.</output>
