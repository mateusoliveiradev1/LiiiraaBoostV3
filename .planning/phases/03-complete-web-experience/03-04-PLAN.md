---
phase: 03-complete-web-experience
plan: "04"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - packages/web-core/package.json
  - packages/web-core/tsconfig.json
  - packages/web-core/src/index.ts
  - packages/web-preview/package.json
  - packages/web-preview/tsconfig.json
  - packages/web-preview/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Core production contracts and fixture preview adapters have separate package roots and public exports."
    - "Neither package introduces an unapproved external identity."
  artifacts:
    - path: packages/web-core/package.json
      provides: "@liiiraa/web-core package boundary"
    - path: packages/web-preview/package.json
      provides: "@liiiraa/web-preview fixture package boundary"
  key_links:
    - from: packages/web-preview/package.json
      to: packages/web-core/package.json
      via: "workspace dependency only"
      pattern: "@liiiraa/web-core"
---

<objective>
Create the core and preview package boundaries without implementing route or scenario behavior.

Purpose: Give downstream TDD plans stable public roots and keep fixture ownership structurally separate.
Output: Strict ESM package manifests, TypeScript configs, and empty typed public roots.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-PATTERNS.md
@packages/desktop-simulator/package.json
@packages/feature-shell/src/index.ts
@tsconfig.base.json
@architecture/module-boundaries.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the production web-core package</name>
  <files>packages/web-core/package.json, packages/web-core/tsconfig.json, packages/web-core/src/index.ts</files>
  <read_first>03-PATTERNS.md package manifests/public roots and production/fixture sibling patterns; packages/desktop-simulator/package.json; packages/desktop-simulator/tsconfig.json; packages/feature-shell/src/index.ts; architecture/module-boundaries.json web-core reservation</read_first>
  <action>Create private ESM `@liiiraa/web-core` with `build`, `check`, and terminating `test --run` scripts, strict inherited TypeScript config, explicit `.` public export, workspace dependency on `@liiiraa/contracts-ts`, and Vitest at the existing approved version. Export no implementation yet; add a typed `WEB_CORE_CONTRACT_VERSION = 1` marker so discovery and public-root checks have a real symbol. Do not add Next, React, fixture, CMS, server, or network dependencies. This scaffold precedes dependency materialization, so verify only manifest/config/source shape here; do not invoke TypeScript or a package lifecycle until Plan 03-11 installs the approved graph.</action>
  <verify><automated>rtk node -e "const fs=require('fs');const p='packages/web-core/package.json';const j=JSON.parse(fs.readFileSync(p,'utf8'));if(j.name!=='@liiiraa/web-core'||j.private!==true||j.type!=='module'||j.dependencies?.['@liiiraa/contracts-ts']!=='workspace:*'||j.devDependencies?.vitest!=='4.1.10'||!j.exports?.['.']||!['build','check','test'].every(x=>typeof j.scripts?.[x]==='string'))process.exit(1);for(const f of ['packages/web-core/tsconfig.json','packages/web-core/src/index.ts'])if(!fs.existsSync(f))process.exit(1)"</automated></verify>
  <acceptance_criteria>The package is strict, private, ESM, public-root-only, dependency-minimal, and fixture-free.</acceptance_criteria>
  <done>Production web contracts have a stable package boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the fixture-only web-preview package</name>
  <files>packages/web-preview/package.json, packages/web-preview/tsconfig.json, packages/web-preview/src/index.ts</files>
  <read_first>packages/desktop-simulator/package.json; packages/desktop-simulator/tsconfig.json; packages/desktop-simulator/src/index.ts; architecture/module-boundaries.json web-preview reservation</read_first>
  <action>Create private ESM `@liiiraa/web-preview` with the same terminating scripts, strict config, explicit public root, and only workspace dependency on `@liiiraa/web-core`. Export `WEB_PREVIEW_ADAPTER_ID = 'liiiraa-web-preview'` and literal runtime class `fixture`; do not create scenarios, public switches, or production imports yet. Keep verification static before Plan 03-11 materializes workspace links.</action>
  <verify><automated>rtk node -e "const fs=require('fs');const p='packages/web-preview/package.json';const j=JSON.parse(fs.readFileSync(p,'utf8'));if(j.name!=='@liiiraa/web-preview'||j.private!==true||j.type!=='module'||j.dependencies?.['@liiiraa/web-core']!=='workspace:*'||Object.keys(j.dependencies||{}).length!==1||!j.exports?.['.']||!['build','check','test'].every(x=>typeof j.scripts?.[x]==='string'))process.exit(1);for(const f of ['packages/web-preview/tsconfig.json','packages/web-preview/src/index.ts'])if(!fs.existsSync(f))process.exit(1)"</automated></verify>
  <acceptance_criteria>The preview package is fixture-identifiable, depends only on web-core, and has no route/UI/authority behavior.</acceptance_criteria>
  <done>The deterministic adapter has a separately enforceable ownership boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| web-core → web-preview | Production contracts may be implemented by fixtures, but may not import them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | preview package identity | mitigate | Literal fixture runtime class and separate public root. |
| T-03-SC | Tampering | package manifests | mitigate | Existing approved versions and workspace links only; no install. |
</threat_model>

## Artifacts this phase produces

- Files: `packages/web-core/package.json`, `packages/web-core/tsconfig.json`, `packages/web-core/src/index.ts`, `packages/web-preview/package.json`, `packages/web-preview/tsconfig.json`, `packages/web-preview/src/index.ts`.
- Symbols/routes/CLI: `WEB_CORE_CONTRACT_VERSION`, `WEB_PREVIEW_ADAPTER_ID`, `WEB_PREVIEW_RUNTIME_CLASS`; package scripts `build`, `check`, `test`; no route or CLI flag.

<verification>
Static manifest/config/source assertions pass without dependency installation; Plan 03-11 owns the first TypeScript checks and architecture activation.
</verification>

<success_criteria>
Core and fixture ownership can be activated without a future package/API rename.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-04-SUMMARY.md` when done.</output>
