---
phase: 03-complete-web-experience
plan: "36"
type: tdd
wave: 18
depends_on: ["03-35"]
files_modified:
  - tooling/web-evidence/src/verify-phase.ts
  - tooling/web-evidence/src/verify-phase.test.ts
autonomous: true
gap_closure: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Final Phase 3 verification fails when route-reachability evidence is missing, stale, incomplete, redirected, 404-collapsed, unredacted, unrecoverable, or authority-connected."
    - "D-25 cannot pass from the 53-ID route manifest projection alone; it requires 24 current browser-observed public/account/admin error outcomes."
    - "The complete web build, public Turbopack/account/admin development probes, full web verification, and workspace test regression pass without weakening deployment, CSP, accessibility, localization, preview, or distribution boundaries."
  artifacts:
    - path: tooling/web-evidence/src/verify-phase.ts
      provides: "Final Phase 3 verifier with route-reachability evidence consumption"
      exports: ["verifyPhase3", "createRepositoryPhase3Input"]
    - path: tooling/web-evidence/src/verify-phase.test.ts
      provides: "Positive and mutation proof that route enumeration cannot self-approve D-25"
  key_links:
    - from: tooling/web-evidence/src/verify-phase.ts
      to: quality/evidence/phase-03/web/route-reachability.json
      via: "read, hash-bind, and validate browser-observed error outcomes"
      pattern: "route-reachability"
    - from: tooling/web-evidence/src/verify-phase.ts
      to: tooling/web-evidence/src/route-reachability.ts
      via: "shared closed evidence contract and validator"
      pattern: "validateRouteReachabilityEvidence"
    - from: package.json
      to: tooling/web-evidence/src/verify-phase.ts
      via: "existing web:verify order runs Playwright before final phase verifier"
      pattern: "web:verify:phase"
---

<objective>
Make final Phase 3 acceptance consume current browser route-reachability proof and run the complete closure regression.

Purpose: The existing verifier accepts manifest membership as route proof. This plan makes D-25 depend on the observed W17 artifact produced after live multi-app browser tests, while retaining every Phase 3 truth and deferral.
Output: RED→GREEN verifier hardening, omission/tamper mutations, final evidence integration, and full automated closure results.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-complete-web-experience/03-CONTEXT.md
@.planning/phases/03-complete-web-experience/03-VERIFICATION.md
@.planning/phases/03-complete-web-experience/03-30-SUMMARY.md
@.planning/phases/03-complete-web-experience/03-32-SUMMARY.md
@.planning/phases/03-complete-web-experience/03-35-SUMMARY.md
@tooling/web-evidence/src/route-reachability.ts
@quality/evidence/phase-03/web/route-reachability.json
@tooling/web-evidence/src/verify-phase.ts
@tooling/web-evidence/src/verify-phase.test.ts
@package.json

<interfaces>
Existing final input:
- `Phase3VerificationArtifacts.routes` is populated by regex enumeration from `packages/web-core/src/routes.ts` and proves only closed-set declaration.
- `PHASE_3_PROOFS` currently expects every proof owner to be `plan-03-32`.
- `createRepositoryPhase3Input(mode, root)` runs after Playwright in the existing `web:verify` command.

Consume the Plan 03-35 contract:
- Add a typed `routeReachability` artifact built from `RouteReachabilityEvidence`.
- Require the canonical 12 error route IDs crossed with `pt-BR` and `en` (24 observations).
- Bind the proof to the current `routes.ts`, `public.spec.ts`, `account.spec.ts`, and `admin.spec.ts` hashes.
- Preserve existing `routes` closed-set validation as declaration proof, then require reachability as an independent executable proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require executable route outcomes in final Phase 3 acceptance</name>
  <files>tooling/web-evidence/src/verify-phase.test.ts, tooling/web-evidence/src/verify-phase.ts</files>
  <read_first>tooling/web-evidence/src/verify-phase.test.ts; tooling/web-evidence/src/verify-phase.ts; tooling/web-evidence/src/route-reachability.ts; quality/evidence/phase-03/web/route-reachability.json; packages/web-core/src/routes.ts; package.json; 03-VERIFICATION.md structured gaps</read_first>
  <behavior>
    - A complete input with 53 declared routes but no route-reachability artifact fails final verification.
    - A current proof with exactly 24 public/account/admin × 403/404/410/500 × PT-BR/en observations passes and reports the observed-outcome count.
    - Removing or duplicating one observation, changing route/surface/locale/semantic status/response behavior, collapsing 403/410/500 to 404, setting redirected/unredacted/unrecoverable/authority-connected state, or changing a bound source hash fails with stable diagnostics.
    - Repository input recomputes route/spec hashes, checks the proof owner/status/path, and rejects a stale checked-in proof after any owning source changes.
    - Existing requirement, success-criteria, decision, scenario, route declaration, locale, evidence, app artifact, capture, publication, deferred-authority, and root-command mutations remain green as tests and fail closed as verifier inputs.
  </behavior>
  <action>Use a strict RED→GREEN cycle. In RED, extend `completeInput()` with the observed route-outcome contract and add mutations proving that the old 53-route enumeration is insufficient; run the focused test to confirm failure and commit `test(03-36): require executable route reachability`. In GREEN, import the shared Plan 03-35 validator, extend `Phase3VerificationArtifacts` and repository input, and add independent `validateRouteReachability` execution after declared-route validation. Extend `PHASE_3_PROOFS` entries with their exact expected owner so existing proofs remain `plan-03-32` while route reachability is owned by `plan-03-35`. Add the reachability helper/evidence/specs to source/proof integrity as appropriate, recompute current hashes, add stable diagnostics, and expose `routeOutcomes: 24` in successful counts. Commit GREEN as `feat(03-36): bind final acceptance to observed routes`. This closes D-25 and D-84 without weakening D-05/D-06/D-07/D-26/D-54/D-68 or admitting deferred Phase 4/10 authority.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/web-evidence exec vitest run src/verify-phase.test.ts -t "route reachability|final source coverage|omission|recursive gate"</automated>
  </verify>
  <acceptance_criteria>RED fails specifically because executable route evidence is not consumed; GREEN passes positive closure plus missing/stale/tampered/collapsed/redirected/leaking/unrecoverable/authority mutations, and all earlier verifier mutations still pass.</acceptance_criteria>
  <done>The final verifier cannot approve D-25 or Phase 3 from manifest enumeration alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checked-in/generated evidence → final completion claim | Evidence can be missing, stale, fabricated, incomplete, or disconnected from the browser run that supposedly produced it. |
| Route/content diagnostics → verifier input | The verifier must validate redaction without ingesting raw sensitive runtime output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-GC-V1 | Spoofing | final D-25 evidence | mitigate | Require the exact 24 browser-observed outcomes independently of the 53-ID manifest set. |
| T-03-GC-V2 | Tampering | route evidence/source drift | mitigate | Recompute canonical route and spec hashes; reject mismatched owner/status/path/hash or incomplete closed sets. |
| T-03-GC-V3 | Information Disclosure | verifier artifacts | mitigate | Consume bounded observations and SHA-256 content fingerprints only; reject raw diagnostic fields. |
| T-03-GC-V4 | Repudiation | final acceptance | mitigate | Stable mutation diagnostics and exact command/source/proof ownership preserve an auditable failure reason. |
| T-03-SC | Tampering | package graph | accept | No dependency, lockfile, or install change occurs; existing supply-chain gates remain reachable. |
</threat_model>

## Source Coverage Audit

| Source | ID/item | Feature or constraint | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Polished truthful public/account/admin ecosystem with independent deployments | 03-33–03-36 | COVERED | Missing error outcomes become executable and independently evidenced. |
| REQ | WEB-01 | Public product/trust experience, including authored public failures | 03-35, 03-36 | COVERED | Public W17 moves to canonical browser-observed evidence. |
| REQ | WEB-02 | Versioned documentation and contextual links | 03-36 | COVERED | Unchanged behavior is protected by full `web:verify` and final verifier regression. |
| REQ | WEB-03 | Fail-closed release/integrity journey | 03-36 | COVERED | Existing blocked-distribution truth remains a required verifier input and full regression. |
| REQ | WEB-08 | Separate public/account/admin deployment and security policies | 03-33–03-36 | COVERED | Error routes execute inside their existing isolated origins/CSP/noindex/authority boundaries. |
| RESEARCH | — | Distinct redacted V16 error handling | 03-33, 03-34 | COVERED | Four statuses, bilingual copy, opaque correlations, same-origin recovery. |
| RESEARCH | — | W01–W18 browser/route/security evidence | 03-35 | COVERED | All declared error routes are visited in both locales and written as bounded observations. |
| RESEARCH | — | Final gates must reject missing route/content/security evidence | 03-36 | COVERED | Route reachability becomes an independent final artifact with omission/tamper tests. |
| RESEARCH | — | Preserve three apps, static public/dynamic previews, CSP, NodeNext/Turbopack compatibility | 03-33–03-36 | COVERED | No dependency/config weakening; all build/dev/full regression commands are mandatory. |
| CONTEXT | D-25 | Distinct authored 404/403/410/500 with locale, redaction, recovery, and no hidden redirect | 03-33–03-36 | COVERED | Direct implementation plus live evidence and final consumption. |
| CONTEXT | D-06, D-07, D-26, D-54, D-66–D-68, D-84 | Locale parity, separate shells, outage safety, admin isolation, accessibility axes, complete release gate | 03-33–03-36 | COVERED | Explicit task acceptance and full regression preserve each constraint. |
| CONTEXT | D-01–D-24, D-27–D-86 | Previously implemented locked decisions | 03-01–03-32 plus 03-36 regression | COVERED | No gap implementation rewrites or reduces these decisions. |
| CONTEXT | Deferred Phase 4 authority and Phase 10 trusted distribution | Real auth/billing/device/support/diagnostic/admin effects and public installer | — | EXCLUDED | Explicitly deferred; verifier continues rejecting these as Phase 3 evidence. |

<verification>
Execute in this order after GREEN:

1. Focused closure tests:
   - `rtk pnpm --filter @liiiraa/account test -- --run src/account-shell.test.ts`
   - `rtk pnpm --filter @liiiraa/admin test -- --run src/admin-shell.test.ts`
   - `rtk pnpm --filter @liiiraa/web-evidence exec vitest run src/route-reachability.test.ts src/verify-phase.test.ts`
2. All three production builds: `rtk pnpm web:build`.
3. Development route probes, starting each command through a recorded hidden process and terminating only that recorded process after the probe:
   - Public Turbopack: `rtk pnpm --filter @liiiraa/web dev --hostname public.localhost --port 3100`, then `rtk proxy curl.exe --fail --silent --show-error http://public.localhost:3100/pt-BR/errors/403`.
   - Account: `rtk pnpm --filter @liiiraa/account dev --hostname account.localhost --port 3101`, then `rtk proxy curl.exe --fail --silent --show-error http://account.localhost:3101/en/errors/410`.
   - Admin with `LIIIRAA_ADMIN_ORIGIN=http://admin.localhost:3102`: `rtk pnpm --filter @liiiraa/admin dev --hostname admin.localhost --port 3102`, then `rtk proxy curl.exe --fail --silent --show-error http://admin.localhost:3102/pt-BR/errors/410`.
   - Use `rtk proxy powershell -NoProfile` with `Start-Process -WindowStyle Hidden -PassThru` when automating background startup; retain exact PIDs and stop them in `finally`.
4. Full browser/build/final evidence gate: `rtk pnpm web:verify`.
5. Direct final proof: `rtk pnpm web:verify:phase -- --mode final`.
6. Workspace regression: `rtk pnpm test`.

All commands must pass. The regenerated reachability proof must remain byte-deterministic and `rtk git diff --check` must report no whitespace errors.
</verification>

<success_criteria>
- D-25 requires 24 current observed route outcomes; 53 manifest IDs alone cannot pass.
- Account 403/410/500 and admin 410 remain distinct under production builds and development servers.
- All focused tests, three builds, three development route probes, full `web:verify`, direct final verifier, and `pnpm test` pass.
- Public/account/admin isolation, CSP/security policies, preview no-change authority, PT-BR/en parity, accessibility, responsive/reduced-motion/forced-colors behavior, and NodeNext/Turbopack compatibility remain intact.
</success_criteria>

<uat_followup>
After automated re-verification reports no gaps, retain one post-verification UAT item: compare representative public wide/mobile, account wide/reflow, and admin wide/mobile goldens beside the approved Phase 2 desktop captures. Confirm subjective brand coherence, hierarchy, readability, and non-template visual quality in PT-BR and English. This visual comparison does not replace or block the automated route closure in Plans 03-33 through 03-36.
</uat_followup>

<output>Create `.planning/phases/03-complete-web-experience/03-36-SUMMARY.md` when done.</output>
