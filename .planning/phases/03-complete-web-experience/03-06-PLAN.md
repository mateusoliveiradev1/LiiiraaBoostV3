---
phase: 03-complete-web-experience
plan: "06"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-04", "03-05"]
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/src/index.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "The public/docs/releases application is one independently buildable static-first production composition."
    - "Its manifest contains only exact approved web identities and no preview adapter."
  artifacts:
    - path: apps/web/package.json
      provides: "@liiiraa/web independently deployable public app"
    - path: apps/web/next.config.ts
      provides: "Standalone public build baseline"
  key_links:
    - from: apps/web/package.json
      to: .planning/phases/03-complete-web-experience/03-DEPENDENCY-APPROVAL.md
      via: "Exact Next.js dependency pins"
      pattern: "next.*16\\.2\\.12"
---

<objective>
Scaffold the independently deployable public/documentation/release application.

Purpose: Implement the public half of D-07 and WEB-08 without importing preview authority.
Output: Exact-pinned Next.js application manifest, strict config, standalone build config, and public composition marker.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-DEPENDENCY-APPROVAL.md
@packages/web-core/package.json
@packages/web-features/package.json
@apps/desktop/package.json
@.planning/phases/03-complete-web-experience/03-RESEARCH.md
@.planning/phases/03-complete-web-experience/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the public application build contract</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/src/index.ts</files>
  <read_first>03-DEPENDENCY-APPROVAL.md; 03-RESEARCH.md Standard Stack and Static Public Content; 03-UI-SPEC.md public shell and delivery; packages/web-core/package.json; packages/web-features/package.json; apps/desktop/package.json; tsconfig.base.json</read_first>
  <action>Create private ESM `@liiiraa/web` with exact `next@16.2.12`, `react@19.2.8`, `react-dom@19.2.8`, `next-intl@4.13.4`, `minisearch@7.2.0`, `@next/mdx@16.2.12`, and workspace dependencies on the already-created web-core/web-features public roots. Define terminating `dev`, `build`, `start`, `check`, `test`, and `verify` scripts. Configure Next standalone output, typed routes, MDX page extensions, image formats, and no third-party remote-image host. Export `PUBLIC_WEB_COMPOSITION = {runtimeClass:'production', surface:'public'}`. Do not create routes, auth cookies, analytics, preview dependencies, or public artifacts. This plan still precedes installation: perform static manifest/config/source checks only; Plan 03-11 owns dependency materialization and the first TypeScript/Next checks.</action>
  <verify><automated>rtk node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('apps/web/package.json','utf8'));const exact={next:'16.2.12',react:'19.2.8','react-dom':'19.2.8','next-intl':'4.13.4',minisearch:'7.2.0','@next/mdx':'16.2.12'};for(const [n,v] of Object.entries(exact))if(j.dependencies?.[n]!==v)process.exit(1);for(const n of ['@liiiraa/web-core','@liiiraa/web-features'])if(j.dependencies?.[n]!=='workspace:*')process.exit(1);if(j.dependencies?.['@liiiraa/web-preview']||!['dev','build','start','check','test','verify'].every(x=>typeof j.scripts?.[x]==='string'))process.exit(1);for(const f of ['apps/web/tsconfig.json','apps/web/next.config.ts','apps/web/src/index.ts'])if(!fs.existsSync(f))process.exit(1);const c=fs.readFileSync('apps/web/next.config.ts','utf8');if(!/standalone/.test(c)||!/typedRoutes/.test(c))process.exit(1)"</automated></verify>
  <acceptance_criteria>All external versions match approval/research; standalone output enabled; public root is production-classified; no fixture dependency or authority API.</acceptance_criteria>
  <done>The public surface has a stable independent build identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public build → internet | Production artifact must exclude scenarios, secrets, auth cookies, and development installers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Spoofing | public composition | mitigate | Production runtime marker and no web-preview dependency. |
| T-03-08 | Spoofing | public artifact | mitigate | No artifact URL or installer enters scaffold. |
| T-03-SC | Tampering | Next.js packages | mitigate | Exact pins consume Plan 03-01 approval; resolution waits for Plan 03-11. |
</threat_model>

## Artifacts this phase produces

- Files: `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next.config.ts`, `apps/web/src/index.ts`.
- Symbols/routes/CLI: `PUBLIC_WEB_COMPOSITION{runtimeClass,surface}`; package scripts `dev`, `build`, `start`, `check`, `test`, `verify`; Next config keys `output`, `typedRoutes`, `pageExtensions`, `images.formats`; no route or CLI flag.

<verification>
Static assertions prove exact approved identities, workspace edges, scripts, and Next config shape without resolving imports; installed checks begin in Plan 03-11.
</verification>

<success_criteria>
The public surface can later build as a standalone production artifact with no preview authority edge.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-06-SUMMARY.md` when done.</output>
