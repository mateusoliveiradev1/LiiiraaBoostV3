---
phase: 03-complete-web-experience
plan: "14"
type: execute
wave: 7
depends_on: ["03-11", "03-12", "03-13"]
files_modified:
  - tooling/web-evidence/src/route-manifest.test.ts
  - tooling/web-evidence/src/security-boundaries.test.ts
  - tooling/web-evidence/src/release-gate.test.ts
  - tooling/web-evidence/src/content-publication.test.ts
  - tooling/web-evidence/playwright.config.ts
  - quality/features/WEB-01.json
  - quality/features/WEB-02.json
  - quality/features/WEB-03.json
  - quality/features/WEB-08.json
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "Every Phase 3 requirement has a runnable quick command and five-dimension evidence manifest before page implementation fans out."
    - "Empty or missing route, content, release, security, visual, or preview evidence cannot pass."
  artifacts:
    - path: tooling/web-evidence/playwright.config.ts
      provides: "Three-app deterministic browser harness"
    - path: quality/features/WEB-01.json
      provides: "WEB-01 five-dimension planned evidence"
  key_links:
    - from: quality/features/WEB-08.json
      to: tooling/web-evidence/src/security-boundaries.test.ts
      via: "Exact command/file/owner evidence"
      pattern: "WEB-08|security-boundaries"
---

<objective>
Establish the Wave 0 verification/evidence harness before feature implementation.

Purpose: Satisfy the Nyquist strategy and ensure route, truth, release, security, accessibility, and deployment omissions fail immediately.
Output: Four focused gate suites, a three-server Playwright matrix, and four requirement manifests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-web-experience/03-VALIDATION.md
@tooling/fixture-guard/src/fixture-guard.test.ts
@apps/desktop/playwright.config.ts
@quality/features/ux-01.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create omission-resistant web evidence gates</name>
  <files>tooling/web-evidence/src/route-manifest.test.ts, tooling/web-evidence/src/security-boundaries.test.ts, tooling/web-evidence/src/release-gate.test.ts, tooling/web-evidence/src/content-publication.test.ts</files>
  <read_first>03-VALIDATION.md Wave 0; fixture-guard positive/negative artifact patterns; canonical web routes; W01-W18; 03-RESEARCH.md validation architecture and threat patterns</read_first>
  <behavior>Seeded negatives fail for route drift, unsafe context, untranslated/stale content, missing evidence, fixture/private indexing, header/origin/cookie/CSP drift, development artifact exposure, release mismatch, and bypass attempts; empty discovery fails.</behavior>
  <action>Create the four named tests with positive real-workspace checks and isolated mutation matrices. Each accepts `--requirement WEB-0X` through the package lifecycle, reports stable bounded codes, scans explicit build roots only, rejects source-tree roots, and initially fails with `MISSING_*` diagnostics until owning plans create evidence. Do not weaken tests to make the empty phase pass; Wave 0 is allowed to report planned missing artifacts through the lifecycle wrapper.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence test -- --run</automated></verify>
  <acceptance_criteria>Every specified negative is mutation-tested; an empty/omitted consumer fails; source files are not accepted as distributable evidence.</acceptance_criteria>
  <done>Feature plans receive bounded fail-closed gates before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Configure browser axes and requirement evidence manifests</name>
  <files>tooling/web-evidence/playwright.config.ts, quality/features/WEB-01.json, quality/features/WEB-02.json, quality/features/WEB-03.json, quality/features/WEB-08.json</files>
  <read_first>03-UI-SPEC.md W01-W18, screenshot matrix, accessibility/performance acceptance; apps/desktop/playwright.config.ts; quality/features/ux-01.json; architecture/quality-manifest.schema.json</read_first>
  <action>Configure three explicit webServer entries and public/account/admin projects using `public.localhost`, `account.localhost`, `admin.localhost` test origins, pinned Chromium, frozen clock, locale, viewport, reduced-motion, forced-colors, 320px reflow/400% zoom and 200% text axes. Keep quick projects bounded and full W01-W18 matrix final-only. Create one exact quality manifest per requirement with security, privacy, accessibility, performance, recovery dimensions, planned owner IDs, exact commands/files, no wildcard evidence, and no watch commands.</action>
  <verify><automated>rtk pnpm --filter @liiiraa/web-evidence exec playwright test --list && rtk pnpm test:acceptance-policy -- --mode planned</automated></verify>
  <acceptance_criteria>Three server/origin projects enumerate; all 18 scenario IDs and required axes are reachable; four manifests validate with five non-empty dimensions and exact files/commands.</acceptance_criteria>
  <done>The Nyquist and acceptance evidence roots exist before route work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source/workspace → evidence claim | Passing tests must resolve actual artifact and route evidence, not empty suites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-11 | Spoofing/Repudiation | evidence manifests | mitigate | Exact command/file/owner and omission mutation gates. |
| T-03-08 | Spoofing | release evidence | mitigate | Explicit build roots and development-artifact negatives. |
| T-03-SC | Tampering | package installs | mitigate | Uses already locked approved tooling; no install. |
</threat_model>

## Artifacts this phase produces

- Files: four `tooling/web-evidence/src/*.test.ts` files, `tooling/web-evidence/playwright.config.ts`, `quality/features/WEB-01.json`, `WEB-02.json`, `WEB-03.json`, `WEB-08.json`.
- Symbols/routes/CLI: test suites `route manifest`, `content publication`, `security boundaries`, `release gate`; Playwright project IDs for public/account/admin quick/full axes; flags `--requirement WEB-01|WEB-02|WEB-03|WEB-08`; evidence fields per quality schema; no product route.

<verification>
List browser projects, run all mutation suites, validate planned manifests, and run `pnpm web:verify:quick` expecting only explicit owned MISSING diagnostics.
</verification>

<success_criteria>
Every later task has a sub-120-second focused gate, and no requirement can pass through empty or unowned evidence.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-14-SUMMARY.md` when done.</output>
