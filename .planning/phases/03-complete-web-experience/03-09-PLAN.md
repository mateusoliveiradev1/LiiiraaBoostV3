---
phase: 03-complete-web-experience
plan: "09"
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - tooling/contract-generation/src/generate.ts
  - contracts/generated/web/v1/web-document.schema.json
  - contracts/generated/http/openapi.json
  - packages/contracts-ts/src/generated/models.ts
  - crates/contracts-rust/src/generated.rs
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-08]
must_haves:
  truths:
    - "One generation command emits deterministic web JSON Schema plus matching TypeScript and Rust transports."
    - "Existing desktop/shell schemas remain semantically unchanged."
  artifacts:
    - path: contracts/generated/web/v1/web-document.schema.json
      provides: "Standalone JSON Schema 2020-12 web document validator source"
    - path: packages/contracts-ts/src/generated/models.ts
      provides: "Generated TypeScript web transport types"
    - path: crates/contracts-rust/src/generated.rs
      provides: "Generated Rust web transport types"
  key_links:
    - from: packages/contracts-source/src/web.tsp
      to: contracts/generated/web/v1/web-document.schema.json
      via: "Bounded contract generation stage"
      pattern: "WebRouteRecord|ReleaseRecord|NoChangeReceipt"
---

<objective>
Generate deterministic cross-language artifacts from the canonical web contract.

Purpose: Complete the one-source contract promise without handwritten transport repair.
Output: Standalone web schema, OpenAPI components, and matching TypeScript/Rust generated models.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@packages/contracts-source/src/web.tsp
@tooling/contract-generation/src/generate.ts
@tooling/contract-generation/src/check-drift.ts
@architecture/decisions/0002-contract-generation-and-cross-language-parity.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bounded generation and emit exact web artifacts</name>
  <files>tooling/contract-generation/src/generate.ts, contracts/generated/web/v1/web-document.schema.json, contracts/generated/http/openapi.json, packages/contracts-ts/src/generated/models.ts, crates/contracts-rust/src/generated.rs</files>
  <read_first>packages/contracts-source/src/web.tsp; tooling/contract-generation/src/generate.ts OUTPUT_PATHS, REQUIRED_DEFINITIONS, schema/TypeScript/Rust stages; generated desktop schemas; ADR-0002</read_first>
  <action>Add `webDocument` to the owned output registry and require every Plan 03-03 definition. Emit a standalone sealed JSON Schema 2020-12 bundle at `contracts/generated/web/v1/web-document.schema.json`; include the models as OpenAPI components without inventing paths; include them in TypeScript/Rust generators. Keep desktop runtime schemas filtered to their existing definition set so bytes/semantics do not drift merely because web models exist. Run the atomic generator once and accept only exact owned outputs; do not hand-edit generated files.</action>
  <verify><automated>rtk pnpm contracts:generate && rtk pnpm contracts:check</automated></verify>
  <acceptance_criteria>The new schema resolves standalone; TypeScript and Rust contain all web models; OpenAPI paths remain empty; desktop schema hashes/semantic tests remain unchanged; drift gate passes.</acceptance_criteria>
  <done>Canonical web contracts are generated reproducibly for both languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TypeSpec source → generated artifacts | Generator drift or unowned writes could tamper with trusted contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02 | Tampering | contract generator | mitigate | Exact output allowlist, atomic writes, drift and standalone schema checks. |
| T-03-08 | Spoofing | OpenAPI/release models | mitigate | Components only; no invented official HTTP route. |
| T-03-SC | Tampering | package installs | mitigate | No dependency installation. |
</threat_model>

## Artifacts this phase produces

- Files: modified `tooling/contract-generation/src/generate.ts`, `contracts/generated/http/openapi.json`, `packages/contracts-ts/src/generated/models.ts`, `crates/contracts-rust/src/generated.rs`; new `contracts/generated/web/v1/web-document.schema.json`.
- Symbols/routes/CLI: `OUTPUT_PATHS.webDocument`; generated versions of all Plan 03-03 types and fields; no HTTP route; existing `pnpm contracts:generate` emits one additional owned artifact and gains no flag.

<verification>
Generation, drift, compatibility, TypeScript compilation, and Rust compilation pass.
</verification>

<success_criteria>
The repository can reproduce byte-stable web transports and validation schema from TypeSpec alone.
</success_criteria>

<output>Create `.planning/phases/03-complete-web-experience/03-09-SUMMARY.md` when done.</output>
