---
phase: 02-complete-desktop-experience
plan: "06"
type: tdd
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - packages/feature-shell/src/machines/calibration.machine.ts
  - packages/feature-shell/src/machines/calibration.machine.test.ts
  - packages/feature-shell/src/model/calibration.ts
autonomous: true
requirements: [UX-02, UX-03, UX-07, UX-10, UX-11]
must_haves:
  truths:
    - "Per D-01, only trust/privacy and basic inventory gate Home; optional calibration remains resumable."
    - "Per D-02/D-08, a missing optional step blocks only its dependent action, opens the exact step, and preserves return intent without nagging."
    - "Per D-03/D-04, incomplete calibration reopens on Home with progressive trusted/incomplete regions and a dominant Continue action."
    - "Per D-05/D-07, mandatory failure enters safe limited mode and stale/hardware changes reopen only affected steps."
    - "Per D-06, telemetry, cloud AI, and diagnostic sharing are independent off-by-default consents distinct from required local processing."
  artifacts:
    - path: packages/feature-shell/src/machines/calibration.machine.ts
      provides: Guarded resumable calibration and revalidation workflow
  key_links:
    - from: packages/feature-shell/src/machines/calibration.machine.ts
      to: packages/desktop-client/src/experience.ts
      via: typed evidence, state, and return-intent inputs
      pattern: "returnIntent|requiredEvidence|limited"
---

<objective>
Specify and implement the complete calibration, limited-mode, consent, deferral, and partial-revalidation machine for D-01 through D-08.

Purpose: Make calibration safety and progressive Home access deterministic before UI composition.
Output: Tested XState workflow and immutable calibration model.
</objective>

<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-complete-desktop-experience/02-CONTEXT.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@packages/desktop-client/src/experience.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Define calibration guards, consent, deferral, and resume behavior</name>
  <files>packages/feature-shell/src/machines/calibration.machine.test.ts, packages/feature-shell/src/model/calibration.ts</files>
  <read_first>02-CONTEXT.md D-01 through D-08; 02-UI-SPEC.md calibration states/copy; S01/S06/S14/S22 contracts</read_first>
  <behavior>Tests cover the seven steps; required trust+inventory gate; independent off consents; optional defer; exact dependent-action return intent; Home-first resume; limited mode; retry; partial hardware/stale revalidation; preserved unaffected evidence.</behavior>
  <action>Write failing model/machine tests with exhaustive transition tables and persisted snapshots. Include PT-BR/English message IDs rather than concatenated copy and assert that no recommendation is emitted in limited mode.</action>
  <acceptance_criteria>RED fails on missing workflow implementation for every D-01 through D-08 behavior, not on test setup.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "UX-02|calibration"</automated></verify>
  <done>The failing suite fully specifies calibration gates, safe failure, consent, and revalidation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement the calibration state machine</name>
  <files>packages/feature-shell/src/machines/calibration.machine.ts, packages/feature-shell/src/model/calibration.ts</files>
  <read_first>RED suite; XState project guidance in 02-RESEARCH.md; desktop experience contract</read_first>
  <behavior>Machine snapshots restore deterministically; events validate typed payloads; mandatory failure, offline/local, deferred, partial, cancelled, resumed, completed, and revalidation states are reachable and safe.</behavior>
  <action>Implement explicit XState states, guards, actions, selectors, persistence input, and return intents. D-02 opens only the required step; D-03 routes resume to Home; D-05 suppresses recommendations; D-06 initializes each connected consent false; D-07 invalidates only affected evidence; D-08 raises prominence only for an active dependency.</action>
  <acceptance_criteria>All transition/guard tests pass, every state has a safe next action, and serialized progress contains no machine actor internals or sensitive diagnostic values.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "UX-02|calibration"</automated></verify>
  <done>The complete deterministic calibration workflow passes its behavior contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3 REFACTOR: Prove persisted-state migration and invariants</name>
  <files>packages/feature-shell/src/machines/calibration.machine.ts, packages/feature-shell/src/machines/calibration.machine.test.ts</files>
  <read_first>GREEN machine and tests; 02-UI-SPEC.md startup/local-state migration failure state</read_first>
  <behavior>Valid snapshots round-trip; unknown version/corrupt state fails to safe limited startup; invariants prevent Home recommendations without required evidence.</behavior>
  <action>Extract pure selectors/guards, add property-style event sequence tests, snapshot versioning, and fail-closed migration diagnostics while preserving behavior.</action>
  <acceptance_criteria>Round-trip, invalid snapshot, cancellation, and event-sequence invariants pass with deterministic diagnostics.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "snapshot|invariant|revalidation"</automated></verify>
  <done>Calibration persistence and safety invariants remain green after refactor.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted calibration/scenario evidence → workflow authority | Corrupt or stale state could unlock recommendations or consent. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-11 | Tampering/Elevation | calibration snapshot | mitigate | Versioned validation, explicit guards, limited-mode fallback. |
| T-02-12 | Repudiation/Disclosure | consent model | mitigate | Independent explicit choices, default false, no inferred connected consent. |
</threat_model>

## Artifacts this phase produces
- Files: all frontmatter paths.
- Symbols: `CalibrationMachine`, `CalibrationContext`, `CalibrationEvent`, `CalibrationStep`, `CalibrationSnapshot`, `ConnectedConsent`, `selectHomeCalibrationState`, `requiredStepForAction`.
- Fields: `requiredComplete`, `optionalProgress`, `consents`, `evidence`, `invalidatedEvidence`, `returnIntent`, `limitedReason`, `snapshotVersion`.
- CLI flags: none.

<verification>Run focused machine tests and feature-shell typecheck.</verification>
<success_criteria>All D-01 through D-08 behaviors are executable and ready for Home/calibration UI.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-06-SUMMARY.md` when done.</output>
