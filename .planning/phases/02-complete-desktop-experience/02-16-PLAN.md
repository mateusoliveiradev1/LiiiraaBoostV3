---
phase: 02-complete-desktop-experience
plan: "16"
type: execute
wave: 6
depends_on: ["02-15", "02-24"]
files_modified:
  - apps/desktop/tests/packaged/driver.ts
  - apps/desktop/tests/packaged/windows-matrix.json
  - tooling/desktop-evidence/verify-packaged-wave-zero.mjs
  - tooling/desktop-evidence/verify-packaged-wave-zero.test.mjs
autonomous: true
requirements: [UX-01, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The packaged harness distinguishes reviewed Windows 10/11, local self-signed development signing, and manual prerequisites from browser evidence and fails closed when unavailable."
    - "Dry-run behavior consumes generated shell contract fixtures and never claims packaged acceptance."
    - "The environment/reviewed CLI validates exact Windows 10, Windows 11, and development-signing records, including false public-trust/production/distribution flags, before any downstream plan invokes it."
  artifacts:
    - path: apps/desktop/tests/packaged/windows-matrix.json
      provides: Reviewed-image metadata contract with unresolved state
    - path: apps/desktop/tests/packaged/driver.ts
      provides: Bounded tauri-driver adapter
    - path: tooling/desktop-evidence/verify-packaged-wave-zero.test.mjs
      provides: Focused mutation coverage for environment/reviewed and manual/observed CLI modes
  key_links:
    - from: contracts/generated/desktop/v1/shell-message.schema.json
      to: apps/desktop/tests/packaged/driver.ts
      via: generated lifecycle event fixtures
      pattern: "ShellStartupState|ShellWindowState"
    - from: tooling/desktop-evidence/verify-packaged-wave-zero.test.mjs
      to: tooling/desktop-evidence/verify-packaged-wave-zero.mjs
      via: exact CLI contract and omission mutations
      pattern: "environment|require-reviewed|manual|require-observed"
---

<objective>
Create the packaged Windows Wave 0 harness and unresolved prerequisite model.

Purpose: Prepare clean-machine acceptance without confusing browser substitutes or invented environment evidence with packaged proof.
Output: Driver adapter, Windows image matrix, and packaged Wave 0 verifier.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-VALIDATION.md
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@contracts/generated/desktop/v1/shell-message.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure the bounded packaged driver and reviewed-image schema</name>
  <files>apps/desktop/tests/packaged/driver.ts, apps/desktop/tests/packaged/windows-matrix.json</files>
  <read_first>UI-SPEC Desktop E2E and Performance acceptance; VALIDATION manual-only verifications; RESEARCH Environment Availability; generated shell schema</read_first>
  <action>Create a driver adapter around the pinned `tauri-driver` path and matrix records for Windows edition/build/image identity, review status, WebView2 state, development-signing access, runner identity, and evidence path. Default every unreviewed Windows 10/11 row to unresolved. Model cryptographic Authenticode presence, self-signed/untrusted-root chain classification, absent-or-verified-free timestamp status, non-elevation, startup, single instance, tray, deep link, startup timing, and working set checks without executing them in Wave 0. A reviewed signing row must identify `Cert:\CurrentUser\My`, a non-secret thumbprint, code-signing EKU, SHA-256, non-exportable CNG custody, `publicTrust=false`, `smartScreenReputation=false`, `productionReady=false`, and `distributionAllowed=false`; it must not require a paid provider or CI key access.</action>
  <acceptance_criteria>The driver can dry-run, names exact unavailable prerequisites, rejects unsupported or unreviewed runner metadata, and cannot treat a browser result as packaged evidence.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test:wave-zero -- --packaged-schema</automated></verify>
  <done>The packaged driver and reviewed-image contract are bounded and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce unresolved packaged prerequisites in Wave 0</name>
  <files>tooling/desktop-evidence/verify-packaged-wave-zero.test.mjs, tooling/desktop-evidence/verify-packaged-wave-zero.mjs</files>
  <read_first>Task 1 driver/matrix; generated shell message schema; planned/final acceptance semantics</read_first>
  <behavior>Tests first specify three bounded modes: dry-run reports planned for missing Windows 10/11, development signing, NVDA, forced-colors, and scale evidence; `--environment &lt;path&gt; --require-reviewed` accepts only exactly one Windows 10 image record, one Windows 11 image record, and one local development-signing record whose status is reviewed, trust class is self-signed development, public/production/distribution flags are false, timestamp state is either not-applicable or backed by verified official-free evidence, and provenance/observer/timestamp are present while secret-bearing fields are absent; `--manual &lt;path&gt; --require-observed` validates later human records and attachment references without creating observations. Unsupported metadata, extra or missing environment records, browser substitution, malformed timestamps/provenance, unresolved status, CI private-key access, publishable/release-ready claims, and secret-shaped signing fields fail.</behavior>
  <action>Create `verify-packaged-wave-zero.test.mjs` before implementing the new mode. Add focused CLI fixtures and omission/mutation cases for Windows edition/build/image identity, local CNG provider, CurrentUser store, non-secret certificate thumbprint, code-signing EKU, SHA-256, timestamp applicability/evidence, self-signed chain class, custody boundary, false public-trust/SmartScreen/production/distribution flags, Phase 10 deferral, reviewed status, provenance, observer, timestamp, duplicate/extra records, and forbidden certificate/private-key material. Then implement `--environment &lt;path&gt; --require-reviewed` in the verifier with deterministic diagnostics and exact record cardinality. Preserve `--manual &lt;path&gt; --require-observed`, its attachment reachability checks, and the strictly planned-or-failed Wave 0 dry-run behavior.</action>
  <acceptance_criteria>The focused test fails before implementation and then passes; all environment/review mutations produce stable diagnostics; the reviewed mode accepts only exact Windows 10, Windows 11, and non-secret local development-signing evidence with no public-release claim; dry-run claims no packaged pass; manual mode remains fail-closed until supplied observations have resolvable provenance and attachments.</acceptance_criteria>
  <verify><automated>rtk node --test tooling/desktop-evidence/verify-packaged-wave-zero.test.mjs</automated></verify>
  <done>Environment/reviewed, packaged dry-run, and manual/observed modes are automated, mutation-tested, and fail closed before downstream invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runner/image metadata to packaged acceptance | Unreviewed environments could masquerade as supported clean machines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-33 | Spoofing/Repudiation | Windows matrix | mitigate | Exact image provenance, explicit review status, unresolved defaults, no browser substitution. |
| T-02-34 | Spoofing/Elevation | development signing record | mitigate | Exact CurrentUser CNG identity, self-signed trust class, false production/distribution flags, no CI key access, and secret-field rejection. |
</threat_model>

## Artifacts

- Files: packaged driver, Windows matrix, Wave 0 verifier.
- Fields: local CNG/store/thumbprint/EKU/digest identifiers, self-signed development trust class, timestamp applicability, false public-trust/SmartScreen/production/distribution flags, Phase 10 deferral.
- Fields: OS build/image provenance, review status, WebView2/signing/runner state, evidence path.
- Connections: generated shell lifecycle values to driver fixtures.

<verification>
Run the focused verifier tests plus packaged schema and dry-run omission checks; no clean-machine run occurs in this plan.
</verification>

<success_criteria>
Later non-autonomous gates can attach real environment evidence to exact records without retrofitting the harness.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-16-SUMMARY.md` when done.</output>
