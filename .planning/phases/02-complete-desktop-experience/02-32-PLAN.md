---
phase: 02-complete-desktop-experience
plan: "32"
type: tdd
wave: 4
depends_on: ["02-24"]
files_modified:
  - packages/contracts-ts/src/validation.ts
  - packages/contracts-ts/src/validation.test.ts
  - crates/contracts-rust/src/validation.rs
  - crates/contracts-rust/tests/shell_messages.rs
autonomous: true
requirements: [UX-01, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Unknown shell events and commands validate before routing, lifecycle, notification, or preference use in TypeScript and Rust."
    - "Both runtimes return generated typed values and bounded redacted structural errors."
  artifacts:
    - path: packages/contracts-ts/src/validation.ts
      provides: Renderer shell event/command validators
    - path: crates/contracts-rust/src/validation.rs
      provides: Native shell event/command validators
  key_links:
    - from: contracts/generated/desktop/v1/shell-message.schema.json
      to: packages/contracts-ts/src/validation.ts
      via: compiled JSON Schema before dispatch
      pattern: "validateHostToRendererShellEvent|validateRendererToHostShellCommand"
---
<objective>Add fail-closed TypeScript and Rust runtime validation over generated shell transports.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@contracts/generated/desktop/v1/shell-message.schema.json
@packages/contracts-ts/src/validation.ts
@crates/contracts-rust/src/validation.rs
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate shell messages in TypeScript</name>
  <files>packages/contracts-ts/src/validation.ts, packages/contracts-ts/src/validation.test.ts</files>
  <read_first>generated shell schema/models; existing diagnostic validator/error bounds; PATTERNS groups 3-4</read_first>
  <behavior>`validateHostToRendererShellEvent` and `validateRendererToHostShellCommand` accept valid generated values; reject unknown schema IDs, fields, discriminators, risky navigation, unsupported locale, non-opt-in tray, and unapproved categories; errors never contain payload values.</behavior>
  <action>Compile the generated schema once, add public direction-specific validators, and reuse bounded structural error rules. Return generated values only after validation.</action>
  <acceptance_criteria>Valid/rejection vectors pass and errors are deterministic, bounded, and redacted.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/contracts-ts test -- --run -t "shell messages"</automated></verify>
  <done>The renderer boundary validates unknown shell payloads.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Validate shell messages in Rust</name>
  <files>crates/contracts-rust/src/validation.rs, crates/contracts-rust/tests/shell_messages.rs</files>
  <read_first>generated shell schema/models; Rust diagnostic validator; TS vector semantics</read_first>
  <behavior>Rust direction-specific validators agree with TS on every valid/rejection vector and deserialize only after schema validation.</behavior>
  <action>Compile the generated schema once, expose Rust validators returning generated enums, and apply the same bounded redacted error semantics.</action>
  <acceptance_criteria>Cross-language vectors agree and invalid payloads never dispatch.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-contracts-rust --test shell_messages</automated></verify>
  <done>The native boundary validates unknown shell payloads.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer/host JSON to typed runtime | Unknown cross-process data enters trusted code. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-32 | Spoofing/Elevation | shell validators | mitigate | Generated schema, closed discriminators, validation before dispatch, redacted errors. |
</threat_model>
## Artifacts
- Symbols: TypeScript and Rust direction-specific shell validators.
<verification>Run TS and Rust shell-message corpus tests and generation drift checks.</verification>
<success_criteria>Native and renderer bridge plans can consume only validated generated messages.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-32-SUMMARY.md` when done.</output>
