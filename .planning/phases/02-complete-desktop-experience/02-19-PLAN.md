---
phase: 02-complete-desktop-experience
plan: "19"
type: execute
wave: 7
depends_on: ["02-03", "02-04", "02-16", "02-17", "02-18"]
files_modified:
  - tooling/desktop-evidence/verify-wave-zero.mjs
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "One Wave 0 verifier proves exact canonical scenario parity, story axes, packaged rows, twelve manifests, and root command reachability."
    - "Omission mutations target the canonical source and never validate a duplicated S01-S24 list."
  artifacts:
    - path: tooling/desktop-evidence/verify-wave-zero.mjs
      provides: Unified omission-resistant Wave 0 gate
  key_links:
    - from: contracts/scenarios/desktop-scenarios.json
      to: tooling/desktop-evidence/verify-wave-zero.mjs
      via: authoritative scenario and route/state enumeration
      pattern: "desktop-scenarios"
    - from: quality/features/ux-01.json
      to: tooling/desktop-evidence/verify-wave-zero.mjs
      via: exact twelve-manifest discovery
      pattern: "ux-01|ux-12"
---

<objective>
Unify the split Wave 0 artifacts under one canonical parity and omission gate.

Purpose: Preserve complete requirement coverage after splitting browser, packaged, and manifest concerns into bounded plans.
Output: Focused planned-mode Wave 0 verifier.
</objective>

<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@contracts/scenarios/desktop-scenarios.json
@tooling/desktop-evidence/story-manifest.json
@apps/desktop/tests/packaged/windows-matrix.json
@quality/features/ux-01.json
@quality/features/ux-12.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate canonical scenario, story, packaged, and requirement parity</name>
  <files>tooling/desktop-evidence/verify-wave-zero.mjs</files>
  <read_first>canonical scenario catalog; story manifest; packaged matrix; UX-01..12 manifests; root lifecycle scripts</read_first>
  <behavior>The verifier discovers S01-S24 and required route/state pairs only from the canonical catalog, validates story-specific axes, validates packaged prerequisite rows, finds exactly twelve UX manifests, and resolves every planned command/artifact owner.</behavior>
  <action>Implement a single deterministic verifier with structured diagnostics. Treat the scenario catalog as authoritative; compare derived story coverage exactly. Validate root reachability and preserve planned status for unresolved signing, Windows, NVDA, forced-colors, and scaling evidence.</action>
  <acceptance_criteria>The focused check passes on the complete Wave 0 graph and reports no duplicated source of scenario truth.</acceptance_criteria>
  <verify><automated>rtk node tooling/desktop-evidence/verify-wave-zero.mjs --mode planned --smoke</automated></verify>
  <done>Split Wave 0 artifacts form one canonical, root-reachable planned evidence graph.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add omission mutations for every Wave 0 artifact family</name>
  <files>tooling/desktop-evidence/verify-wave-zero.mjs</files>
  <read_first>Task 1 verifier; omission patterns in acceptance policy and required-artifact tooling</read_first>
  <behavior>Removing or renaming a scenario, required route/state, story axis, packaged row, UX manifest, quality dimension, owner, artifact path, or root command produces the intended stable failure.</behavior>
  <action>Add in-memory or temporary-fixture mutations without rewriting source artifacts. Include a drift case where the story manifest disagrees with `desktop-scenarios.json` and require a parity-specific diagnostic.</action>
  <acceptance_criteria>Every mutation fails for its intended reason and the complete graph remains byte-stable across reruns.</acceptance_criteria>
  <verify><automated>rtk node tooling/desktop-evidence/verify-wave-zero.mjs --mode planned --mutations</automated></verify>
  <done>Wave 0 cannot silently omit or duplicate a scenario, axis, prerequisite, requirement, or command.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Split harness artifacts to unified evidence | Independent plans could leave silent coverage gaps. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-36 | Spoofing/Repudiation | Wave 0 verifier | mitigate | Canonical discovery, exact parity, root reachability, omission mutations. |
</threat_model>

## Artifacts
- File: `tooling/desktop-evidence/verify-wave-zero.mjs`.
- CLI flags: `--mode planned`, `--smoke`, `--mutations`.
- Connections: canonical catalog, story axes, packaged prerequisites, twelve quality manifests, root commands.

<verification>Run smoke and mutation modes; run planned acceptance policy as the wave gate.</verification>
<success_criteria>All later feature plans inherit a complete omission-resistant Wave 0 platform.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-19-SUMMARY.md` when done.</output>
