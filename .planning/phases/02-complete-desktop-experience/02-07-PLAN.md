---
phase: 02-complete-desktop-experience
plan: "07"
type: tdd
wave: 8
depends_on: ["02-03", "02-04", "02-19"]
files_modified:
  - packages/feature-shell/src/model/interaction-policy.ts
  - packages/feature-shell/src/model/interaction-policy.test.ts
  - packages/feature-shell/src/model/preferences.ts
  - packages/feature-shell/src/model/preferences.test.ts
autonomous: true
requirements: [UX-05, UX-06, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Command search ranks context then exact match and risky results route to review instead of executing."
    - "Favorites enforce 5 games, 4 metrics, 4 safe actions and never reorder Home priorities."
    - "Activity and feedback use priority, acknowledgement, correlation, retention, and native-notification policy."
    - "D-08 missing optional steps gain prominence only when needed; D-15 receipts create scenario Activity; D-16 abbreviated controls return actionable phase-boundary explanations."
    - "D-18/D-19 defaults are Comfortable density and exit-on-close until explicit tray opt-in."
  artifacts:
    - path: packages/feature-shell/src/model/interaction-policy.ts
      provides: Search, favorite, activity, feedback, receipt, and boundary policies
    - path: packages/feature-shell/src/model/preferences.ts
      provides: Benign locale/scale/motion/density/tray preferences
  key_links:
    - from: packages/feature-shell/src/model/interaction-policy.ts
      to: packages/desktop-client/src/experience.ts
      via: scenario receipts and Activity contracts
      pattern: "noEffect|correlation|phaseBoundary"
---

<objective>
Implement the deterministic interaction-policy kernel for command search, favorites, Activity, feedback, boundary explanations, and preferences, including D-08, D-15, D-16, D-18, and D-19.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-complete-desktop-experience/02-CONTEXT.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@packages/desktop-client/src/experience.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 RED: Specify command, favorite, Activity, and feedback policies</name>
  <files>packages/feature-shell/src/model/interaction-policy.test.ts</files>
  <read_first>02-UI-SPEC.md UX-05 through UX-09; 02-CONTEXT.md D-08/D-15/D-16</read_first>
  <behavior>Cases cover ranking, no-result, safe routing, favorite bounds/reorder, Activity groups/filters/ack/dismiss/retention, inline/toast/Activity/native channels, no-change receipts, and phase-boundary explanations.</behavior>
  <action>Write failing pure-function tests. Assert critical events cannot dismiss unresolved, Windows notifications only map approved actionable categories, D-15 receipt wording and requested-operation list are mandatory, and D-16 names capability, owning phase, documentation/demo action.</action>
  <acceptance_criteria>RED is complete and fails only because the policy functions do not exist.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "UX-05|UX-06|UX-08|UX-09"</automated></verify>
  <done>The interaction-policy behavior matrix is complete and failing for expected reasons.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement interaction and receipt policies</name>
  <files>packages/feature-shell/src/model/interaction-policy.ts, packages/feature-shell/src/model/interaction-policy.test.ts</files>
  <read_first>RED suite; desktop scenario/receipt contracts; UI-SPEC command/favorites/activity copy</read_first>
  <behavior>Pure reducers/selectors return deeply frozen deterministic results; unsafe actions always route to review; scenario receipts append scenario-marked Activity.</behavior>
  <action>Implement local search index ranking, bounded favorite reducer with keyboard moves, prioritized Activity reducer/selectors, feedback-channel mapper, D-08 dependency prominence, D-15 no-change receipt creator, and D-16 boundary explanation creator. Keep notification payloads free of sensitive hardware detail.</action>
  <acceptance_criteria>All interaction-policy tests pass, including limit boundaries, tie ordering, acknowledgement, retention, and safe action routing.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "UX-05|UX-06|UX-08|UX-09"</automated></verify>
  <done>Search, favorites, Activity, feedback, receipt, and boundary policies are green.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Implement and test benign preference defaults</name>
  <files>packages/feature-shell/src/model/preferences.ts, packages/feature-shell/src/model/preferences.test.ts</files>
  <read_first>02-CONTEXT.md D-17 through D-19; 02-UI-SPEC.md UX-12, i18n, window/tray behavior</read_first>
  <behavior>Brazilian Portuguese locale selects PT-BR else English; Comfortable density and System motion default; close exits until tray opt-in; scale/motion/density/status preferences validate and persist benign values only.</behavior>
  <action>Write tests first and implement a versioned preference reducer/validator. Explicitly encode D-18 Comfortable default and D-19 exit-on-close default. Keep language switch available before consent by exposing locale separately from connected settings.</action>
  <acceptance_criteria>Valid preferences round-trip, corrupt values restore safe defaults, Compact never reduces target/type sizes, and no consent or entitlement is persisted here.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/feature-shell test -- --run -t "UX-12|preferences"</automated></verify>
  <done>Locale/appearance/tray defaults are deterministic, validated, and privacy-bounded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Search/deep action → workflow navigation | User intent could bypass review. |
| Activity/notification → OS surface | Sensitive or fabricated state could leave the app. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-13 | Elevation | command policy | mitigate | Risky results route only to full review/confirmation. |
| T-02-14 | Disclosure/Spoofing | feedback mapper | mitigate | Approved categories, redacted payload, scenario markers and correlation IDs. |
</threat_model>

## Artifacts this phase produces
- Files: all frontmatter paths.
- Symbols: `rankCommands`, `favoritesReducer`, `activityReducer`, `mapFeedbackChannel`, `createPreviewReceipt`, `createPhaseBoundaryExplanation`, `preferencesReducer`, `defaultDesktopPreferences`.
- Fields: `query`, `contextScore`, `matchScore`, `kind`, `priority`, `correlationId`, `acknowledged`, `retentionUntil`, `noEffect`, `owningPhase`, `locale`, `scale`, `density`, `motion`, `trayOptIn`.
- CLI flags: none.
<verification>Run focused policy/preference tests and feature-shell typecheck.</verification>
<success_criteria>UX-05/06/08/09/12 logic is deterministic and cannot bypass truth or risk boundaries.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-07-SUMMARY.md` when done.</output>
