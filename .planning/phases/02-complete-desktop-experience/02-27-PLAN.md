---
phase: 02-complete-desktop-experience
plan: "27"
type: execute
wave: 15
depends_on: ["02-16", "02-25", "02-26", "02-33"]
files_modified:
  - apps/desktop/tests/packaged/journeys.ts
  - apps/desktop/tests/packaged/signature.ts
  - apps/desktop/tests/packaged/performance.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The exact signed non-elevated artifact recorded by 02-33 passes required native journeys on reviewed Windows 10/11 images."
    - "Publisher/timestamp/chain, startup, response, activity, and working-set evidence is observed, not inferred."
  artifacts:
    - path: apps/desktop/tests/packaged/journeys.ts
      provides: Twelve clean-machine native journeys
    - path: apps/desktop/tests/packaged/signature.ts
      provides: Authenticode identity verification
    - path: apps/desktop/tests/packaged/performance.ts
      provides: Packaged startup/response/working-set evidence
  key_links:
    - from: quality/evidence/phase-02/artifacts/signed-desktop-package.json
      to: apps/desktop/tests/packaged/journeys.ts
      via: exact staged installer/executable paths, SHA-256 hashes, publisher, timestamp, and signature-chain identity
      pattern: "installerSha256|executableSha256|stagedPath"
---
<objective>Execute development-signed packaged Windows, native lifecycle, truth-boundary, and performance acceptance without implying public release trust.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@apps/desktop/tests/packaged/windows-matrix.json
@quality/evidence/phase-02/environment/windows-10-image.json
@quality/evidence/phase-02/environment/windows-11-image.json
@quality/evidence/phase-02/environment/signing-access.json
@quality/evidence/phase-02/artifacts/signed-desktop-package.json</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement packaged signature and clean-machine journey acceptance</name>
  <files>apps/desktop/tests/packaged/journeys.ts, apps/desktop/tests/packaged/signature.ts</files>
  <read_first>UI-SPEC packaged journeys; reviewed environment/signing records; exact 02-33 signed artifact record; native shell contracts; installer handoff</read_first>
  <behavior>Twelve journeys cover install/launch non-elevated, calibration resume, Home to Improve, command, favorites, game preview, failure/recovery/receipt, restart Activity/tray, expired entitlement, locale/scale/motion/density, single-instance deep link, and production fixture refusal; signature verification distinguishes cryptographic integrity from public trust.</behavior>
  <action>Implement the full clean-machine journeys and signature checks, selecting only reviewed image records and the exact staged installer/executable paths and SHA-256 hashes from `signed-desktop-package.json`. Recompute hashes before installation and verify signature presence, code-signing EKU, SHA-256 digest, certificate thumbprint, expected self-signed/untrusted-root chain class, timestamp applicability, channel, version, Windows compatibility, and false publicTrust/smartScreenReputation/productionReady/distributionAllowed fields. Accept `not-applicable` timestamp only when the record contains no fabricated timestamp evidence. Reject unsigned CI artifacts and never rebuild or select by filename alone.</action>
  <acceptance_criteria>Focused smoke passes only on the exact 02-33 local development artifact and reviewed images; missing or mismatched hashes, unsigned bytes, wrong thumbprint/EKU/digest, tampering, false public-release claims, fabricated timestamp, elevation, or fixture leakage fails.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test:packaged -- --grep "@install-smoke|@native-smoke"</automated></verify>
  <done>Signed clean-machine native journeys are executable and truth-boundary safe.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement packaged performance acceptance</name>
  <files>apps/desktop/tests/packaged/performance.ts</files>
  <read_first>UI-SPEC Performance acceptance; reviewed environment records; exact 02-33 signed artifact record; packaged driver</read_first>
  <behavior>Startup under two seconds, command/route response budgets, 1,000-row Activity responsiveness, hidden/minimized behavior, and working-set thresholds attach artifact/image/run provenance.</behavior>
  <action>Implement repeatable measurements with warm/cold distinction, bounded samples, thresholds, the exact artifact ID/SHA-256/staged path from `signed-desktop-package.json`, reviewed image ID, and failure diagnostics. Reject measurements whose installed bytes or run provenance do not resolve back to that record. Add a focused `@performance-smoke` sample under 120 seconds; run the full sample set only at the plan gate.</action>
  <acceptance_criteria>Measured results are reproducible and linked to the exact 02-33 artifact and reviewed image IDs; missing samples, unresolved paths, or artifact hash drift fail.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test:packaged -- --grep "@performance-smoke"</automated></verify>
  <done>Packaged performance evidence is measured and resolvable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Development-signed installer to reviewed Windows systems | Elevated, unsigned, fixture-tainted, or falsely public-trusted artifacts could be accepted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-28 | Tampering/Elevation | packaged artifact | mitigate | Authenticode integrity, development thumbprint/trust class, artifact hash, non-elevation, reviewed images, fixture refusal, and false public-release flags. |
</threat_model>
## Artifacts
- Files: packaged journeys, signature, performance specs.
- Focused tags: `@install-smoke`, `@native-smoke`, `@performance-smoke`.
<verification>Run the full signed Windows 10/11 journey, signature, native lifecycle, and performance matrices.</verification>
<success_criteria>Packaged/native acceptance is complete on reviewed environments with real cryptographic development signing and no public-distribution claim.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-27-SUMMARY.md` when done.</output>
