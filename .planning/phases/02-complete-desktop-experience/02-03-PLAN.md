---
phase: 02-complete-desktop-experience
plan: "03"
type: tdd
wave: 5
depends_on: ["02-31"]
files_modified:
  - packages/desktop-client/src/experience.ts
  - packages/desktop-client/src/experience.test.ts
  - packages/desktop-client/src/index.ts
  - packages/desktop-simulator/src/scenarios/catalog.ts
  - packages/desktop-simulator/src/scenarios/families.ts
  - packages/desktop-simulator/src/scenarios/deltas.ts
  - packages/desktop-simulator/src/scenarios/catalog.test.ts
  - packages/desktop-simulator/src/index.ts
  - contracts/scenarios/desktop-scenarios.json
autonomous: true
requirements: [UX-02, UX-03, UX-04, UX-07, UX-08, UX-09, UX-11, UX-12]
must_haves:
  truths:
    - "S01-S24 are immutable, stable, complete, and each mutates a focused condition inside a recognizable scenario family per D-09 through D-12."
    - "Every scenario declares seed, clock, locale, hardware fixture, latency, provenance, required routes/states, and no-effect boundary."
    - "Operational states are closed and exhaustive; UI code never invents ad-hoc booleans."
  artifacts:
    - path: contracts/scenarios/desktop-scenarios.json
      provides: S01-S24 evidence and route/state manifest
    - path: packages/desktop-client/src/experience.ts
      provides: Closed experience-state application contract
  key_links:
    - from: packages/desktop-simulator/src/scenarios/catalog.ts
      to: packages/desktop-client/src/experience.ts
      via: validated frozen scenario projections
      pattern: "OperationalState|DesktopScenario"
---

<objective>
Build the deterministic S01-S24 scenario/evidence contract before any dependent route expansion, honoring D-09 through D-12 and D-20.

Purpose: Make every route and screenshot a projection of one stable truth model, never an isolated mock.
Output: Closed operational state contracts, family/delta scenario catalog, and executable coverage manifest.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-CONTEXT.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-VALIDATION.md
@packages/desktop-client/src/truth.ts
@packages/desktop-client/src/client.ts
@packages/desktop-simulator/src/scenarios.ts
@packages/desktop-simulator/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify closed experience states and S01-S24 coverage</name>
  <files>packages/desktop-client/src/experience.test.ts, packages/desktop-simulator/src/scenarios/catalog.test.ts, contracts/scenarios/desktop-scenarios.json</files>
  <read_first>02-UI-SPEC.md sections 8 and 18; 02-CONTEXT.md D-09 through D-12 and D-20; existing desktop-client truth/conformance tests</read_first>
  <behavior>Tests fail until all authored states are exhaustive and exactly S01-S24 each provide stable identity, seed, frozen clock, locale, family, delta, hardware/game/profile identity, latency, provenance, required route/state pairs, and preview boundary.</behavior>
  <action>Write behavior-first tests and the declarative manifest. S01 uses the mid-range Intel/NVIDIA competitive setup, fictional anchor game, partially complete evidence, one Verified ready operation, one Advanced review operation, and one excluded insufficient-evidence operation. Assert real games appear only in discovery scenarios with explicit integration qualification.</action>
  <acceptance_criteria>RED fails because production types/catalog are absent, not because of syntax or fixture errors; counts and required state pairs are non-vacuous.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop-simulator test -- --run -t "S01-S24|operational state"</automated></verify>
  <done>Behavior-first tests prove the full scenario/state contract is absent for the expected reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement immutable experience contracts and scenario catalog</name>
  <files>packages/desktop-client/src/experience.ts, packages/desktop-client/src/index.ts, packages/desktop-simulator/src/scenarios/catalog.ts, packages/desktop-simulator/src/scenarios/families.ts, packages/desktop-simulator/src/index.ts</files>
  <read_first>Task 1 tests/manifest; packages/desktop-client/src/truth.ts; packages/desktop-simulator/src/scenarios.ts; ADR 0004</read_first>
  <behavior>Every manifest entry maps into a deeply frozen typed scenario; unknown IDs fail closed; production-facing experience values contain no fixture constructor; S01 is the clean-install default per D-20.</behavior>
  <action>Implement branded IDs and closed unions for operational state, evidence freshness/quality, entitlement, calibration progress, activity, receipt, route requirement, and phase-boundary explanation. Export application types from desktop-client and fixture construction only from desktop-simulator. Inject all nondeterminism. Preserve existing standard/unavailable conformance behavior.</action>
  <acceptance_criteria>All 24 scenarios validate and freeze; no duplicate/missing IDs; unknown state/route/provenance rejects; architecture forbids feature/composition production imports of fixture constructors.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop-simulator test -- --run</automated></verify>
  <done>The closed experience model and complete immutable S01-S24 catalog pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 REFACTOR: Normalize families and focused deltas</name>
  <files>packages/desktop-simulator/src/scenarios/families.ts, packages/desktop-simulator/src/scenarios/deltas.ts, packages/desktop-simulator/src/scenarios/catalog.ts, packages/desktop-simulator/src/scenarios/catalog.test.ts</files>
  <read_first>GREEN catalog; 02-CONTEXT.md D-12; 02-UI-SPEC.md S01-S24 matrix</read_first>
  <behavior>Each scenario differs from its family baseline only by declared delta paths; catalog ordering and serialized evidence remain byte-stable.</behavior>
  <action>Extract a small frozen family set and typed focused deltas, add mutation tests for undeclared changes, and keep all scenario IDs/copy/test expectations stable.</action>
  <acceptance_criteria>Family/delta invariants pass, snapshots are stable, and no scenario invents an unrelated PC/game/profile world.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop-simulator test -- --run -t "family|delta|deterministic"</automated></verify>
  <done>Scenario families are normalized, focused, and byte-stable without changing IDs or behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture catalog → application truth | Synthetic data becomes visible in UI projections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05 | Spoofing | scenario catalog | mitigate | Fixture provenance and scenario marker are mandatory fields; production imports remain forbidden. |
| T-02-06 | Tampering/Repudiation | preview receipt/activity | mitigate | Stable scenario/correlation IDs, frozen clock, explicit no-change receipt. |
</threat_model>

## Artifacts this phase produces

- Files: all frontmatter paths.
- Symbols: `DesktopScenarioId`, `DesktopScenario`, `ScenarioFamily`, `ScenarioDelta`, `OperationalState`, `EvidenceState`, `EntitlementState`, `CalibrationProgress`, `PreviewReceipt`, `PhaseBoundaryExplanation`, `DESKTOP_SCENARIOS`, `getDesktopScenario`.
- Fields: `id`, `familyId`, `seed`, `clock`, `locale`, `latencyMs`, `adapterIdentity`, `hardware`, `game`, `profile`, `provenance`, `requiredRoutes`, `requiredStates`, `deltaPaths`, `noEffect`.
- CLI flags: `--scenario`, `--locale`, `--seed` for test-only runners.

<verification>
Execute package tests, adapter conformance, architecture tests, and corpus determinism.
</verification>

<success_criteria>
Every later feature plan consumes one exhaustive scenario/state contract and S01-S24 exists before routes are expanded.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-03-SUMMARY.md` when done.</output>
