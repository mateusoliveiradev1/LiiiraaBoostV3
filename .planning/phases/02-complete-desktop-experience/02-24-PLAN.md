---
phase: 02-complete-desktop-experience
plan: "24"
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/contracts-source/src/main.tsp
  - packages/contracts-source/src/shell.tsp
  - tooling/contract-generation/src/generate.ts
  - contracts/generated/desktop/v1/shell-message.schema.json
  - packages/contracts-ts/src/generated/models.ts
  - crates/contracts-rust/src/generated.rs
autonomous: true
requirements: [UX-01, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Every renderer/native navigation, startup, tray, notification, window-state, locale, close, and preference value has one canonical TypeSpec definition."
    - "TypeScript and Rust transport models and JSON Schema are generated from that source and checked for drift."
    - "Unknown host events and renderer commands are runtime-validated and rejected with bounded redacted errors before use."
  artifacts:
    - path: packages/contracts-source/src/shell.tsp
      provides: Canonical bounded desktop shell message source
    - path: contracts/generated/desktop/v1/shell-message.schema.json
      provides: Runtime schema for both shell message directions
  key_links:
    - from: packages/contracts-source/src/shell.tsp
      to: packages/contracts-ts/src/generated/models.ts
      via: deterministic contract generation
      pattern: "HostToRendererShellEvent|RendererToHostShellCommand"
    - from: packages/contracts-source/src/shell.tsp
      to: crates/contracts-rust/src/generated.rs
      via: typify-backed Rust generation
      pattern: "HostToRendererShellEvent|RendererToHostShellCommand"
---

<objective>
Define the bounded desktop shell protocol once in TypeSpec and generate runtime-validatable TypeScript, Rust, and JSON Schema artifacts.

Purpose: Prevent renderer/native navigation and lifecycle values from becoming independently maintained security boundaries.
Output: Canonical source, deterministic transports, and drift checks ready for Plan 02-32 validators.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@packages/contracts-source/src/main.tsp
@tooling/contract-generation/src/generate.ts
@packages/contracts-ts/src/validation.ts
@crates/contracts-rust/src/validation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify the canonical bounded shell message protocol</name>
  <files>packages/contracts-source/src/main.tsp, packages/contracts-source/src/shell.tsp</files>
  <read_first>02-PATTERNS.md groups 3 and 4; UI-SPEC UX-01, UX-09, window/tray/startup/localization sections; existing TypeSpec envelope/provenance source and validation tests</read_first>
  <behavior>TypeSpec compilation requires `ShellNavigationIntent`, `ShellStartupState`, `ShellTrayPreference`, `ShellCloseContext`, `ShellNotificationPreference`, `ShellNotificationCategory`, `ShellWindowState`, `ShellInstallerIdentity`, `HostToRendererShellEvent`, and `RendererToHostShellCommand` as bounded closed definitions.</behavior>
  <action>Add the bounded TypeSpec namespace and import it from `main.tsp`. Model host-to-renderer and renderer-to-host directions as closed discriminated unions. Include publisher, version, channel, Windows compatibility, startup/splash/failure state, benign navigation, locale, tray opt-in, close context, notification preference/category, and window state.</action>
  <acceptance_criteria>The canonical TypeSpec compiles and contains every named bounded symbol with no second source.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/contracts-source check</automated></verify>
  <done>The cross-process protocol is fully specified from one source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Generate exact schema, TypeScript, and Rust transports</name>
  <files>tooling/contract-generation/src/generate.ts, contracts/generated/desktop/v1/shell-message.schema.json, packages/contracts-ts/src/generated/models.ts, crates/contracts-rust/src/generated.rs</files>
  <read_first>Task 1 TypeSpec/tests; contract generator bounded-path policy; existing generated schema, TS, and Rust artifact conventions</read_first>
  <behavior>One generation run emits the shell schema and both transport languages from the canonical bundle; a check run detects any manual or source drift; the bounded path policy rejects writes outside the owned output set.</behavior>
  <action>Extend `OUTPUT_PATHS`, required definitions, schema roots, TypeScript generation root, and Rust generation input so the named shell symbols are emitted deterministically. Preserve generated headers and read-only policy. Add the shell schema to the exact owned output set and ensure check/drift tests compare byte-normalized outputs.</action>
  <acceptance_criteria>Generation is byte-stable, all three artifacts contain matching closed unions, and modifying either generated transport makes the drift check fail.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/contract-generation test -- --run -t "shell|drift|bounded"</automated></verify>
  <done>Canonical TypeSpec produces exact reviewable TS, Rust, and schema transports with drift protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TypeSpec source to generated transports | Generated TS/Rust/schema artifacts become protocol authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-31 | Tampering | generated shell transports | mitigate | One canonical TypeSpec source, deterministic generation, exact output set, and drift failure. |
| T-02-SC | Tampering | generator dependencies | mitigate | Reuse the already reviewed Phase 1 generator and locked dependencies; no install occurs. |
</threat_model>

## Artifacts

- Source symbols: `ShellNavigationIntent`, `ShellStartupState`, `ShellTrayPreference`, `ShellCloseContext`, `ShellNotificationPreference`, `ShellNotificationCategory`, `ShellWindowState`, `ShellInstallerIdentity`, `HostToRendererShellEvent`, `RendererToHostShellCommand`.
- Generated files: shell JSON Schema, TS models, Rust models.
- Generator behavior: exact owned output list and byte-level drift check.

<verification>
Run TypeSpec compilation and generator drift/path tests.
</verification>

<success_criteria>
Plan 02-32 can add runtime validators over generated shell values with no independently handwritten cross-process DTO.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-24-SUMMARY.md` when done.</output>
