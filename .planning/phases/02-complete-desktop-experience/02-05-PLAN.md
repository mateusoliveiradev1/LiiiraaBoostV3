---
phase: 02-complete-desktop-experience
plan: "05"
type: execute
wave: 8
depends_on: ["02-03", "02-04", "02-19"]
files_modified:
  - packages/design-tokens/src/index.ts
  - packages/design-tokens/src/tokens.css
  - packages/design-tokens/src/tokens.test.ts
  - packages/design-system/src/primitives.tsx
  - packages/design-system/src/evidence.tsx
  - packages/design-system/src/shell.tsx
  - packages/design-system/src/data.tsx
  - packages/design-system/src/index.ts
  - packages/design-system/src/design-system.test.tsx
autonomous: true
requirements: [UX-03, UX-07, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The Pre-Dawn Flight Deck tokens are the only visual constants and enforce the locked scale, typography, cobalt allocation, focus, forced-colors, and motion contract."
    - "Every authored state and provenance kind is expressed with text, icon, and shape/pattern rather than color alone."
    - "React Aria supplies behavior only; all visual APIs remain Liiiraa Boost-owned."
  artifacts:
    - path: packages/design-system/src/index.ts
      provides: Stable Lb primitive, shell, evidence, workflow, and data exports
    - path: packages/design-tokens/src/tokens.css
      provides: Locked authored visual tokens
  key_links:
    - from: packages/design-system/src/evidence.tsx
      to: packages/desktop-client/src/experience.ts
      via: exhaustive projections of provenance and operational state
      pattern: "ProvenanceMark|StatusSignal|OperationalState"
---

<objective>
Implement the bespoke design tokens and accessible component system that every route will use, including D-04 trusted/incomplete distinctions.

Purpose: Prevent route expansion from creating a generic dashboard, one-off colors, or inaccessible state semantics.
Output: Token, primitive, shell, evidence, workflow, and data component public APIs with complete state stories/tests.
</objective>

<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md
@packages/desktop-client/src/truth.ts
@packages/desktop-client/src/experience.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode and test the locked design-token contract</name>
  <files>packages/design-tokens/src/index.ts, packages/design-tokens/src/tokens.css, packages/design-tokens/src/tokens.test.ts</files>
  <read_first>02-UI-SPEC.md sections 2-5, 15-17; 02-PATTERNS.md No Analog Found</read_first>
  <action>Write token contract tests first, then implement the exact spacing, two-weight/four-size typography, self-hosted Manrope/JetBrains Mono declarations, surface/cobalt/semantic palette, radii, focus ring, responsive padding, scale, Comfortable/Compact density, responsive/reduced motion, forced-colors, and status-pattern tokens. Reject off-scale values and opacity-only disabled text.</action>
  <acceptance_criteria>All locked token values and stop conditions are executable; 150% scale, 200% text, forced colors, and reduced motion have explicit token outputs.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/design-tokens test -- --run</automated></verify>
  <done>The token package exposes only contract-compliant deterministic values.</done>
</task>
<task type="auto">
  <name>Task 2: Build authored primitives, shell, evidence, workflow, and data components</name>
  <files>packages/design-system/src/primitives.tsx, packages/design-system/src/evidence.tsx, packages/design-system/src/shell.tsx, packages/design-system/src/data.tsx, packages/design-system/src/index.ts</files>
  <read_first>02-UI-SPEC.md sections 5, 10, 14, 16; design token exports; desktop experience/truth unions</read_first>
  <action>Wrap approved React Aria behavior behind the full `Lb*` primitive API and implement WindowTitleBar, GoalRail, RouteHeader, CriticalStateRail, CommandCenter shell, ContextInspector, ActivityCenter, provenance/freshness/quality/status/metric/risk components, workflow compositions, tables, timelines, topology, and accessible chart wrappers. Render fixture markers persistently and D-04 incomplete/unavailable regions explicitly. Provide keyboard chart cursor plus data-table alternative.</action>
  <acceptance_criteria>All Section 10 components are exported, named, keyboard operable, 44px-target compliant, and exhaustive over their typed state unions; no inherited visual styles or dead icon-only controls remain.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/design-system check</automated></verify>
  <done>The design-system public root provides every required authored component category.</done>
</task>
<task type="auto">
  <name>Task 3: Prove component states and accessibility axes</name>
  <files>packages/design-system/src/design-system.test.tsx</files>
  <read_first>02-UI-SPEC.md Story catalog and accessibility acceptance; Wave 0 story manifest</read_first>
  <action>Add interaction/semantic tests for default, hover, focus-visible, pressed, disabled, loading, error, warning, success, unavailable, fixture, PT-BR, English, pseudo-locale, reduced motion, forced colors, and scale axes. Assert focus return, dialog names, roving tab stops, non-color status text/patterns, and no decorative chart-only information.</action>
  <acceptance_criteria>Component tests and axe checks pass every required axis with zero serious/critical findings and no missing accessible names.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/design-system test -- --run</automated></verify>
  <done>The component system has executable semantic and interaction coverage before feature composition.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Typed application truth → visual projection | State/provenance can be misrepresented or hidden by presentation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09 | Spoofing | evidence components | mitigate | Exhaustive typed projections and mandatory fixture/provenance labels. |
| T-02-10 | Denial of service | dense lists/charts | mitigate | Accessible bounded rendering, hidden/minimized suspension, table alternative. |
</threat_model>

## Artifacts this phase produces
- Files: all frontmatter paths.
- Symbols: all Section 10 `Lb*`, shell, evidence, workflow, and data component names; `designTokens`, `ScaleMode`, `DensityMode`, `MotionMode`.
- Fields: `provenance`, `freshness`, `quality`, `status`, `pattern`, `accessibleLabel`, `scale`, `density`, `motion`, `forcedColors`.
- CLI flags: none.

<verification>Run token tests, design-system tests/check, Storybook interactions, and axe.</verification>
<success_criteria>Every later feature composes one stable bespoke and accessible visual language.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-05-SUMMARY.md` when done.</output>
