---
phase: 02-complete-desktop-experience
plan: "31"
type: execute
wave: 4
depends_on: ["02-14"]
files_modified:
  - pnpm-lock.yaml
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The manifests authorized by the versioned exact Phase 2 approval resolve to one deterministic frozen lockfile with lifecycle scripts denied."
    - "Every direct lock identity and version matches the non-generated 31-item approval artifact and generated evidence."
  artifacts:
    - path: .planning/phases/02-complete-desktop-experience/02-DEPENDENCY-APPROVAL.md
      provides: Versioned authority for the exact 31 Phase 2 identities
    - path: pnpm-lock.yaml
      provides: Frozen reviewed Phase 2 dependency graph
  key_links:
    - from: .planning/phases/02-complete-desktop-experience/02-DEPENDENCY-APPROVAL.md
      to: pnpm-lock.yaml
      via: exact approved identities and versions
      pattern: "react|tauri|storybook|playwright"
---
<objective>Resolve and verify the approved Phase 2 TypeScript lockfile as a bounded workspace step.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-complete-desktop-experience/02-DEPENDENCY-APPROVAL.md
@architecture/dependency-review.md
@packages/design-tokens/package.json
@packages/design-system/package.json
@packages/feature-shell/package.json
@apps/desktop/package.json</context>
<tasks>
<task type="auto">
  <name>Task 1: Resolve the approved dependency graph</name>
  <files>pnpm-lock.yaml</files>
  <read_first>02-DEPENDENCY-APPROVAL.md; generated dependency review; Phase 2 manifests; root package manager policy</read_first>
  <action>Before resolution, prove that the manifests use only exact identities and versions from the versioned 31-item approval artifact and that the artifact matches the generated report from d43fd0e. Resolve only that authorized graph into the lockfile with lifecycle scripts denied and no undeclared direct dependencies.</action>
  <acceptance_criteria>Lockfile resolution is deterministic and contains every approved direct identity/version with no identity or version outside the exact approval artifact.</acceptance_criteria>
  <verify><automated>rtk pnpm install --frozen-lockfile</automated></verify>
  <done>The approved dependency graph is frozen.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Verify lockfile identity and lifecycle policy</name>
  <files>pnpm-lock.yaml</files>
  <read_first>resolved lockfile; dependency verifier; 02-DEPENDENCY-APPROVAL.md; generated dependency review</read_first>
  <behavior>Unknown direct identity/version, lifecycle exception, or unapproved source fails with a stable diagnostic; reruns are byte-stable.</behavior>
  <action>Run the existing supply-chain and manifest/lock parity checks against both the versioned exact approval and generated evidence, preserving the denial of `msw`, the nonexistent single-instance npm plugin, unapproved identities or versions, and unapproved lifecycle scripts.</action>
  <acceptance_criteria>Approval/evidence/manifest/lock identity parity checks pass and lockfile reruns are byte-stable.</acceptance_criteria>
  <verify><automated>rtk node tooling/supply-chain/verify-pins.mjs --check</automated></verify>
  <done>The frozen lockfile is review-equivalent and lifecycle-safe.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Approved manifests to lockfile | Transitive resolution enters the build. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-44 | Tampering | pnpm lockfile | mitigate | Frozen install, exact direct identity parity, lifecycle denial. |
</threat_model>
## Artifacts
- File: `pnpm-lock.yaml`.
<verification>Run frozen install, pin verifier, and package architecture checks.</verification>
<success_criteria>Dependent workspace plans consume one reviewed deterministic lockfile.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-31-SUMMARY.md` when done.</output>
