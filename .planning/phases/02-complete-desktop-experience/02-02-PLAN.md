---
phase: 02-complete-desktop-experience
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - architecture/module-boundaries.json
  - pnpm-workspace.yaml
  - tooling/architecture-tests/src/check-workspace.ts
  - tooling/architecture-tests/src/check-workspace.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The Phase 2 TypeScript package roots are active, uniquely owned, and discoverable before dependencies or feature code are added."
    - "Production features cannot import fixture constructors, deep package paths, unknown roots, or cyclic dependencies."
  artifacts:
    - path: architecture/module-boundaries.json
      provides: Active design-tokens, design-system, feature-shell, and desktop-app ownership
    - path: tooling/architecture-tests/src/check-workspace.test.ts
      provides: Live negative workspace mutations
  key_links:
    - from: pnpm-workspace.yaml
      to: architecture/module-boundaries.json
      via: discovered workspace roots must have one canonical owner
      pattern: "packages/design|packages/feature-shell|apps/desktop"
---

<objective>
Activate the Phase 2 TypeScript workspace and ownership graph without registering a Rust crate that does not yet exist.

Purpose: Establish legal package boundaries before dependency installation, lifecycle wiring, or feature work.
Output: Discoverable package roots and executable negative architecture mutations.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md
@architecture/module-boundaries.json
@architecture/decisions/0003-module-ownership-and-direction.md
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate Phase 2 TypeScript roots and workspace discovery</name>
  <files>architecture/module-boundaries.json, pnpm-workspace.yaml</files>
  <read_first>02-PATTERNS.md groups 1-2, 11, 13, 19, and 25; architecture/module-boundaries.json; pnpm-workspace.yaml</read_first>
  <action>Change only `design-tokens`, `design-system`, `feature-shell`, and `desktop-app` from reserved to active, preserving approved roots, public roots, and layers. Add the four roots to pnpm discovery. Do not edit root `Cargo.toml`: Plan 02-12 creates and registers `apps/desktop/src-tauri` atomically so the Cargo workspace never names a nonexistent member.</action>
  <acceptance_criteria>The four TypeScript roots are discovered exactly once, each has one owner and public root, and the Cargo workspace is unchanged.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "workspace discovery"</automated></verify>
  <done>Phase 2 TypeScript roots are active without breaking Cargo workspace resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce live Phase 2 ownership and fixture boundaries</name>
  <files>tooling/architecture-tests/src/check-workspace.ts, tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>tooling/architecture-tests/src/check-workspace.ts; existing architecture mutation tests; ADR 0003 and ADR 0004</read_first>
  <behavior>Unknown ownership, a production feature import from desktop-simulator, a deep import, and a cycle each fail with stable diagnostics; legal design to feature to composition edges pass.</behavior>
  <action>Add live negative mutations for the four failure modes and keep production fixture imports fail-closed. Preserve existing Phase 1 cases and ensure mutations operate on discovered workspace metadata rather than duplicated root lists.</action>
  <acceptance_criteria>Each negative mutation fails for its intended reason, the legal graph passes, and no Phase 1 ownership rule is weakened.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "Phase 2 ownership"</automated></verify>
  <done>Canonical architecture enforcement covers the live Phase 2 TypeScript graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workspace discovery to canonical ownership | Newly executable roots enter the production dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Elevation/Tampering | module graph | mitigate | Unique ownership, public-root imports, cycle rejection, and production fixture boundary mutations. |
| T-02-SC | Tampering | package installation | mitigate | No installation occurs; Plan 02-14 remains blocked on the approved dependency record. |
</threat_model>

## Artifacts

- Files: `architecture/module-boundaries.json`, `pnpm-workspace.yaml`, workspace checker and tests.
- Symbols: discovered Phase 2 roots and stable mutation diagnostics.
- Connections: pnpm discovery to canonical module ownership.

<verification>
Run the focused workspace tests, then the full architecture suite as the wave gate.
</verification>

<success_criteria>
Dependency installation and package scaffolding can proceed without an invalid Cargo member or an ambiguous TypeScript owner.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-02-SUMMARY.md` when done.</output>
