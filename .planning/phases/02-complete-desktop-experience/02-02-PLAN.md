---
phase: 02-complete-desktop-experience
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tooling/architecture-tests/src/check-workspace.ts
  - tooling/architecture-tests/src/check-workspace.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The four Phase 2 TypeScript roots remain reserved and undiscoverable until their package manifests exist."
    - "Architecture tests define the unique ownership, public-root, fixture, deep-import, and cycle rules that atomic activation must satisfy."
  artifacts:
    - path: tooling/architecture-tests/src/check-workspace.test.ts
      provides: Reservation and Phase 2 ownership mutation contract
  key_links:
    - from: tooling/architecture-tests/src/check-workspace.test.ts
      to: architecture/module-boundaries.json
      via: reservation and ownership assertions consume canonical policy
      pattern: "reserved|Phase 2 ownership"
---

<objective>
Define the Phase 2 TypeScript reservation and ownership enforcement contract without activating package roots that do not yet have manifests.

Purpose: Establish executable legal boundaries before Plan 02-14 atomically creates manifests and activates workspace discovery.
Output: Reservation assertions and executable negative architecture mutations.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md
@architecture/module-boundaries.json
@architecture/decisions/0003-module-ownership-and-direction.md
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce reserved roots before manifests exist</name>
  <files>tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>02-PATTERNS.md groups 1-2, 11, 13, 19, and 25; architecture/module-boundaries.json; pnpm-workspace.yaml; existing live workspace tests</read_first>
  <behavior>The exact four Phase 2 roots remain reserved, are absent from live pnpm discovery while their manifests do not exist, and each retains one declared owner/public root ready for later activation.</behavior>
  <action>Add the focused `Phase 2 reservation` test against the current canonical policy and live discovery adapter. Assert that `design-tokens`, `design-system`, `feature-shell`, and `desktop-app` are not active or discoverable before their package manifests exist. Do not modify `architecture/module-boundaries.json`, `pnpm-workspace.yaml`, or root `Cargo.toml`; Plan 02-14 owns atomic TypeScript activation and Plan 02-12 owns atomic Rust registration.</action>
  <acceptance_criteria>The focused test proves all four roots remain reserved and undiscoverable while preserving their unique declared ownership metadata.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "Phase 2 reservation"</automated></verify>
  <done>Nonexistent package roots cannot enter live workspace discovery prematurely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Phase 2 ownership and fixture mutations</name>
  <files>tooling/architecture-tests/src/check-workspace.ts, tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>tooling/architecture-tests/src/check-workspace.ts; existing architecture mutation tests; ADR 0003 and ADR 0004</read_first>
  <behavior>Unknown ownership, a production feature import from desktop-simulator, a deep import, and a cycle each fail with stable diagnostics against an explicit activated Phase 2 test graph; legal design to feature to composition edges pass.</behavior>
  <action>Add the focused `Phase 2 ownership` mutation cases using an in-memory activated graph derived from canonical ownership metadata, not nonexistent live package roots or a duplicated root list. Keep production fixture imports fail-closed and preserve all Phase 1 cases. Plan 02-14 will rerun these invariants against the live graph after atomic activation.</action>
  <acceptance_criteria>Each negative mutation fails for its intended reason, the legal synthetic activation graph passes, and no Phase 1 ownership rule is weakened.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "Phase 2 ownership"</automated></verify>
  <done>Canonical architecture enforcement is ready to validate the live Phase 2 graph at activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reserved ownership metadata to future workspace discovery | Premature activation could admit nonexistent or ambiguously owned roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Elevation/Tampering | module graph | mitigate | Reservation assertions plus unique ownership, public-root, cycle, and production fixture mutations before activation. |
| T-02-SC | Tampering | package installation | mitigate | No installation occurs; Plan 02-14 remains blocked on the approved dependency record. |
</threat_model>

## Artifacts

- Files: workspace checker implementation and tests.
- Symbols: `Phase 2 reservation`, `Phase 2 ownership`, and stable mutation diagnostics.
- Connections: canonical reserved ownership metadata to the later live activation gate.

<verification>
Run the focused reservation and ownership tests, then the full architecture suite as the wave gate.
</verification>

<success_criteria>
Plan 02-14 can create manifests and activate the TypeScript roots atomically, with executable invariants ready before the transition.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-02-SUMMARY.md` when done.</output>
