---
phase: 02-complete-desktop-experience
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - Cargo.toml
  - Cargo.lock
  - architecture/module-boundaries.json
  - packages/design-tokens/package.json
  - packages/design-tokens/tsconfig.json
  - packages/design-system/package.json
  - packages/design-system/tsconfig.json
  - packages/feature-shell/package.json
  - packages/feature-shell/tsconfig.json
  - apps/desktop/package.json
  - apps/desktop/tsconfig.json
  - apps/desktop/vite.config.ts
  - apps/desktop/src/index.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "All four reserved Phase 2 roots are active, independently discoverable workspaces with legal dependency directions."
    - "The exact approved React/Tauri/UI/test dependencies are owned by the package that uses them."
    - "Root quick/full verification reaches every Phase 2 package without watch mode."
  artifacts:
    - path: apps/desktop/package.json
      provides: Desktop workspace lifecycle and Tauri/Vite entrypoints
    - path: architecture/module-boundaries.json
      provides: Active design, feature, and composition ownership
  key_links:
    - from: pnpm-workspace.yaml
      to: architecture/module-boundaries.json
      via: independent package discovery followed by canonical ownership
      pattern: "packages/design|packages/feature-shell|apps/desktop"
---

<objective>
Activate the Phase 2 monorepo roots, exact dependencies, and terminating lifecycle commands before feature work.

Purpose: Give later plans a legal, strict, testable package graph without bypassing Phase 1 ownership or fixture boundaries.
Output: Active workspaces and empty public entrypoints/configuration ready for Wave 0 test and scenario contracts.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@.planning/phases/02-complete-desktop-experience/02-PATTERNS.md
@architecture/module-boundaries.json
@architecture/decisions/0003-module-ownership-and-direction.md
@package.json
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate Phase 2 ownership and workspace discovery</name>
  <files>architecture/module-boundaries.json, pnpm-workspace.yaml, Cargo.toml, Cargo.lock</files>
  <read_first>02-PATTERNS.md groups 1-2/11/13/19/25; architecture/module-boundaries.json; tooling/architecture-tests/src/check-workspace.ts; Cargo.toml</read_first>
  <action>Change only `design-tokens`, `design-system`, `feature-shell`, and `desktop-app` from reserved to active, preserve their approved roots/public roots/layers, include the real desktop Rust crate as a Cargo member when created by Plan 02-12, and ensure pnpm discovery includes all four roots. Add live negative mutations proving unknown ownership, forbidden feature-to-fixture import, deep import, and cycle rejection.</action>
  <acceptance_criteria>The live graph discovers all Phase 2 packages, all have one owner/public root, legal design→feature→composition edges pass, and a production feature import from `desktop-simulator` fails.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture</automated></verify>
  <done>All Phase 2 roots are active and canonical architecture enforcement covers their live graphs.</done>
</task>

<task type="auto">
  <name>Task 2: Install only approved package-owned dependencies</name>
  <files>packages/design-tokens/package.json, packages/design-tokens/tsconfig.json, packages/design-system/package.json, packages/design-system/tsconfig.json, packages/feature-shell/package.json, packages/feature-shell/tsconfig.json, apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/vite.config.ts, pnpm-lock.yaml</files>
  <read_first>02-RESEARCH.md Standard Stack, Installation Boundaries, Package Legitimacy Audit; architecture/dependency-review.md; tsconfig.base.json; existing workspace package manifests</read_first>
  <action>Create strict package manifests and exact dependencies in their owning packages: tokens own no renderer behavior; design-system owns React Aria/Tailwind/icons/chart behavior; feature-shell owns React/XState/Query/forms and desktop-client; desktop composition owns React DOM/TanStack Router/Vite/Tauri JS and testing composition. Use React Intl/FormatJS per agent discretion and recorded approval. Keep MSW and the nonexistent single-instance npm plugin absent. Generate one frozen lockfile with lifecycle scripts still denied.</action>
  <acceptance_criteria>Frozen install succeeds, package versions match the approved review exactly, each package typechecks from its public root, and no production package depends on the simulator.</acceptance_criteria>
  <verify><automated>rtk pnpm install --frozen-lockfile</automated></verify>
  <done>The approved dependency graph is frozen and package-owned with lifecycle scripts denied.</done>
</task>

<task type="auto">
  <name>Task 3: Add terminating desktop lifecycle and verification entrypoints</name>
  <files>package.json, apps/desktop/package.json, apps/desktop/src/index.ts</files>
  <read_first>root package.json; turbo.json; 02-VALIDATION.md Test Infrastructure and Wave 0 Requirements; apps/desktop/package.json</read_first>
  <action>Add root-reachable non-watch commands for desktop unit, Storybook interaction, Playwright/axe/visual, packaged-driver, scenario coverage, localization, and quality evidence. Keep `verify:quick` bounded and `verify` exhaustive. Export only the `DesktopCompositionBootstrap` composition contract from `apps/desktop/src/index.ts`; concrete adapter selection is explicitly owned and verified by Plan 02-11.</action>
  <acceptance_criteria>Every planned command terminates, Turbo can discover all four packages, `verify:quick` reaches package checks, and no command reports product success before its evidence files exist.</acceptance_criteria>
  <verify><automated>rtk pnpm verify:quick</automated></verify>
  <done>Root and package lifecycle commands terminate and reach the new workspace roots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workspace discovery → canonical ownership | New package roots become executable production code. |
| Package lock → renderer/Rust build | Third-party code enters the desktop distribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Elevation/Tampering | module graph | mitigate | Active roots and negative live mutations preserve design/feature/composition and fixture boundaries. |
| T-02-04 | Tampering | lockfile | mitigate | Exact reviewed versions, frozen install, lifecycle scripts denied. |
| T-02-SC | Tampering | package install | mitigate | Plan depends on the blocking approval record from 02-01. |
</threat_model>

## Artifacts this phase produces

- Files: all frontmatter paths.
- Symbols: package public roots, `DesktopBootstrapContract`.
- Fields: package scripts `check`, `test`, `build`, `test:e2e`, `test:visual`, `test:packaged`; active module statuses.
- CLI flags: `--run`, `--grep`, `--project`, `--identity`, `--scenario`, `--locale`, `--scale`, `--mode` forwarded only through terminating scripts.

<verification>
Run frozen install, architecture tests, package typechecks, and `verify:quick`.
</verification>

<success_criteria>
Wave 0 scenario, test, design, and native-host plans can work in disjoint owned directories.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-02-SUMMARY.md` when done.</output>
