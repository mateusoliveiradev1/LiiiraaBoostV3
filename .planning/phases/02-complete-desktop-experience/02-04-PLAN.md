---
phase: 02-complete-desktop-experience
plan: "04"
type: execute
wave: 6
depends_on: ["02-03", "02-15"]
files_modified:
  - apps/desktop/.storybook/main.ts
  - apps/desktop/.storybook/preview.tsx
  - apps/desktop/playwright.config.ts
  - apps/desktop/tests/browser/fixtures.ts
  - apps/desktop/tests/browser/keyboard.ts
  - apps/desktop/tests/browser/axe.ts
  - tooling/desktop-evidence/story-manifest.json
autonomous: true
requirements: [UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Storybook and Playwright expose every locked viewport, locale, scale, motion, contrast, keyboard, and authored-state axis deterministically."
    - "The story/evidence manifest derives its S01-S24 route/state expectations from the canonical desktop-scenarios catalog rather than duplicating them."
  artifacts:
    - path: tooling/desktop-evidence/story-manifest.json
      provides: Canonical-catalog-linked story axis manifest
    - path: apps/desktop/tests/browser/fixtures.ts
      provides: Frozen browser scenario fixture
  key_links:
    - from: contracts/scenarios/desktop-scenarios.json
      to: tooling/desktop-evidence/story-manifest.json
      via: exact route/state parity derivation
      pattern: "S01|S24|requiredRoutes"
---

<objective>
Create the deterministic component and browser Wave 0 harness with exact parity to the canonical S01-S24 catalog.

Purpose: Give route and component work bounded tests while preventing a second drifting scenario list.
Output: Storybook, Playwright, keyboard, axe, fixture, and story-manifest infrastructure.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-VALIDATION.md
@contracts/scenarios/desktop-scenarios.json
@apps/desktop/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure deterministic component and browser harnesses</name>
  <files>apps/desktop/.storybook/main.ts, apps/desktop/.storybook/preview.tsx, apps/desktop/playwright.config.ts, apps/desktop/tests/browser/fixtures.ts, apps/desktop/tests/browser/keyboard.ts, apps/desktop/tests/browser/axe.ts</files>
  <read_first>02-UI-SPEC.md sections 15-19; 02-VALIDATION.md Test Infrastructure and Wave 0; desktop lifecycle scripts; canonical scenario catalog</read_first>
  <action>Configure Storybook React-Vite and Playwright projects for 1440x900, 1280x800, 960x700, and 760x600; PT-BR, English, and pseudo locale; 100, 125, and 150 percent scale; responsive/reduced motion; normal/forced colors. Freeze clock, IDs, seed, latency, animations, and chart data. Provide keyboard-only and axe fixtures and reject serious/critical axe violations.</action>
  <acceptance_criteria>Both harnesses list projects and exit without watch mode, decorators expose every locked axis, and fixture data is frozen and scenario-marked.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test:wave-zero -- --browser-smoke</automated></verify>
  <done>Deterministic Storybook, browser, axe, keyboard, and screenshot harnesses are runnable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive the story evidence manifest from the canonical scenario catalog</name>
  <files>tooling/desktop-evidence/story-manifest.json</files>
  <read_first>contracts/scenarios/desktop-scenarios.json; UI-SPEC story catalog; browser fixture axes</read_first>
  <behavior>Every S01-S24 required route/state pair appears exactly once through derivation or exact validation; missing, extra, renamed, or duplicate scenario/route/state entries fail with stable diagnostics; visual axes remain authored additions.</behavior>
  <action>Generate or validate the story manifest directly against `desktop-scenarios.json`. Keep only story-specific viewport, locale, scale, motion, and forced-colors axes as authored fields. Do not maintain a duplicate scenario ID or route/state list. Add parity mutations to the focused Wave 0 check.</action>
  <acceptance_criteria>The manifest has exact scenario/route/state parity with the canonical catalog and fails on either-side drift.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test:wave-zero -- --story-parity</automated></verify>
  <done>Story/evidence omission checks are anchored to one canonical S01-S24 source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scenario catalog to browser/story evidence | Duplicated or synthetic coverage could falsely certify route/state completeness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07 | Spoofing/Repudiation | story manifest | mitigate | Direct derivation or exact parity validation against the canonical scenario catalog. |
| T-02-08 | Information disclosure | screenshots/logs | mitigate | Synthetic frozen scenarios only and persistent fixture provenance. |
</threat_model>

## Artifacts

- Files: Storybook/Playwright config, browser fixtures, keyboard/axe helpers, story manifest.
- Symbols: `desktopTest`, `freezeDesktopScenario`, `runKeyboardJourney`, `expectNoAxeViolations`.
- Connections: canonical scenario route/state requirements to evidence stories.

<verification>
Run browser Wave 0 smoke/parity checks; run the complete empty harness matrix as the wave gate.
</verification>

<success_criteria>
All browser/component implementation plans receive bounded deterministic checks with omission-resistant canonical scenario parity.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-04-SUMMARY.md` when done.</output>
