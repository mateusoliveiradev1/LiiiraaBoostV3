---
phase: 02-complete-desktop-experience
plan: "15"
type: execute
wave: 5
depends_on: ["02-31"]
files_modified:
  - package.json
  - apps/desktop/package.json
  - packages/design-tokens/tsconfig.json
  - packages/design-system/tsconfig.json
  - packages/feature-shell/tsconfig.json
  - apps/desktop/tsconfig.json
  - apps/desktop/vite.config.ts
  - apps/desktop/src/index.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Every desktop unit, story, browser, packaged, localization, evidence, quick, and final command terminates."
    - "Root commands remain fail-closed until their owned evidence artifacts exist."
  artifacts:
    - path: package.json
      provides: Root-reachable desktop verification lifecycle
    - path: apps/desktop/src/index.ts
      provides: Empty composition bootstrap contract
  key_links:
    - from: package.json
      to: apps/desktop/package.json
      via: terminating root scripts
      pattern: "verify:quick|verify|test:e2e|test:packaged"
---

<objective>
Add strict compiler/Vite configuration, terminating desktop lifecycle commands, and the empty composition entrypoint after dependencies are installed.

Purpose: Give every later task a bounded command without coupling command wiring to package installation.
Output: Package compiler/build configuration, root/package scripts, and `DesktopCompositionBootstrap`.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-VALIDATION.md
@package.json
@turbo.json
@apps/desktop/package.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure strict package builds and terminating desktop commands</name>
  <files>packages/design-tokens/tsconfig.json, packages/design-system/tsconfig.json, packages/feature-shell/tsconfig.json, apps/desktop/tsconfig.json, apps/desktop/vite.config.ts, apps/desktop/package.json</files>
  <read_first>02-VALIDATION.md Test Infrastructure and Wave 0 Requirements; four package manifests; tsconfig.base.json; existing package script conventions</read_first>
  <action>Create strict package-local TypeScript configurations with legal references/public-root output and configure desktop Vite for the static Tauri renderer and deterministic test composition. Add non-watch commands for desktop unit tests, Storybook interaction, Playwright axe/visual, packaged driver, scenario coverage, localization, and quality evidence. Every command must support a focused filter or smoke mode intended to terminate under 120 seconds.</action>
  <acceptance_criteria>All four packages inherit strict compiler settings, Vite preserves the static Tauri boundary, and each script terminates with focused filtering and fail-closed missing-artifact diagnostics.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop run</automated></verify>
  <done>Desktop package scripts expose bounded and complete verification entrypoints.</done>
</task>

<task type="auto">
  <name>Task 2: Wire root lifecycle and the empty composition contract</name>
  <files>package.json, apps/desktop/src/index.ts</files>
  <read_first>root package.json; turbo.json; Task 1 scripts; architecture public-root rules</read_first>
  <action>Expose the package commands through root-reachable `verify:quick` and exhaustive `verify` graphs without watch mode. Export only `DesktopCompositionBootstrap` from the desktop public root; concrete route, adapter, and native bridge selection remain owned by Plans 02-11 and 02-25.</action>
  <acceptance_criteria>Turbo discovers all four packages, root commands reach the desktop package, quick mode is bounded, and no command reports evidence as passing before its artifact exists.</acceptance_criteria>
  <verify><automated>rtk pnpm verify:quick -- --smoke lifecycle</automated></verify>
  <done>Root lifecycle commands terminate and the composition entrypoint is ready for later wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Root script graph to acceptance evidence | An incomplete command could falsely signal product completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-30 | Spoofing/Repudiation | lifecycle scripts | mitigate | Terminating commands, planned/final distinction, and fail-closed missing artifacts. |
</threat_model>

## Artifacts

- Files: root/package manifests and desktop public entrypoint.
- Symbols: `DesktopCompositionBootstrap`.
- CLI flags: `--run`, `--grep`, `--project`, `--scenario`, `--locale`, `--scale`, `--mode`, `--smoke`.

<verification>
Run lifecycle smoke checks, then root `verify:quick` as the wave gate.
</verification>

<success_criteria>
All subsequent tasks have root-reachable focused checks and complete suites remain reserved for wave/final gates.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-15-SUMMARY.md` when done.</output>
