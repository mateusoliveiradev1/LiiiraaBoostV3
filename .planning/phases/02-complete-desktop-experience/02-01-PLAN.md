---
phase: 02-complete-desktop-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - architecture/dependency-allowlist.json
  - architecture/dependency-review.md
  - tooling/supply-chain/verify-pins.mjs
  - .planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
autonomous: false
requirements: [UX-01, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "No Phase 2 package is installed until every SUS identity in 02-RESEARCH.md has been reviewed by a human."
    - "The Authenticode provider, certificate type, timestamp service, custody model, and CI access path are explicitly selected before signed-package acceptance work."
  artifacts:
    - path: architecture/dependency-review.md
      provides: Phase 2 registry and repository evidence with human disposition
    - path: .planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
      provides: Reviewed Windows signing contract
  key_links:
    - from: architecture/dependency-allowlist.json
      to: tooling/supply-chain/verify-pins.mjs
      via: exact identity verification
      pattern: "phase-02|react|tauri|storybook|playwright"
---

<objective>
Close the two irreversible pre-installation decisions: Phase 2 dependency legitimacy and Windows signing custody.

Purpose: Preserve the Phase 1 supply-chain gate while making UX-01 genuinely signable rather than silently accepting unsigned local output.
Output: Reviewed dependency evidence and a recorded Authenticode decision. No package installation occurs in this plan.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-complete-desktop-experience/02-CONTEXT.md
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@architecture/dependency-allowlist.json
@architecture/dependency-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend exact dependency evidence without installing packages</name>
  <files>architecture/dependency-allowlist.json, architecture/dependency-review.md, tooling/supply-chain/verify-pins.mjs</files>
  <read_first>02-RESEARCH.md lines 114-179; architecture/dependency-allowlist.json; architecture/dependency-review.md; tooling/supply-chain/verify-pins.mjs</read_first>
  <action>Add every approved Phase 2 npm and crates.io identity from the Package Legitimacy Audit, preserve exact versions and official repositories, record SUS/OK/SLOP verdicts, and keep `@tauri-apps/plugin-single-instance` and `msw` excluded. Generate a deterministic Phase 2 review section without running a package manager. Do not weaken the existing 26-pin Phase 1 evidence or lifecycle-script policy.</action>
  <acceptance_criteria>The report identifies every new package, version, registry, repository, license/lifecycle evidence, and verdict; both SLOP identities remain excluded; reruns are byte-stable.</acceptance_criteria>
  <verify><automated>rtk node tooling/supply-chain/verify-pins.mjs --check</automated></verify>
  <done>Exact Phase 2 dependency evidence is reviewable, deterministic, and contains no install side effect.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve dependency pins and choose the Authenticode custody contract</name>
  <files>architecture/dependency-review.md, .planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md</files>
  <read_first>architecture/dependency-review.md; 02-RESEARCH.md Open Questions 1 and Environment Availability; 02-UI-SPEC.md UX-01 and packaged acceptance</read_first>
  <action>Present the SUS rows with official registry links and ask for explicit approval or rejection. Separately ask the user to select the Authenticode provider/certificate identity, timestamp service, secret custody, and protected CI access approach. After the user decides, record the exact choice and non-secret identifiers in `02-SIGNING-DECISION.md`; do not invent credentials or accept unsigned output as satisfying UX-01.</action>
  <acceptance_criteria>The user explicitly approves the exact package set and the signing record names a real provider/certificate path, timestamp service, custody owner, CI retrieval boundary, and revocation/rotation responsibility.</acceptance_criteria>
  <verify><automated>rtk node tooling/supply-chain/verify-pins.mjs --check</automated></verify>
  <done>The install set and signing custody contract are explicitly approved and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry metadata → repository allowlist | External package identity evidence becomes an installation authority. |
| Signing provider → release pipeline | Certificate custody authorizes Windows binaries trusted by users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing/Tampering | dependency allowlist | mitigate | Verify exact registry, version, repository, and lifecycle evidence; require human approval for SUS rows. |
| T-02-02 | Elevation/Information disclosure | signing custody | mitigate | Record protected CI access and non-secret identifiers only; never commit private key material. |
| T-02-SC | Tampering | npm/cargo installs | mitigate | Blocking human checkpoint precedes every Phase 2 install. |
</threat_model>

## Artifacts this phase produces

- Files: `architecture/dependency-allowlist.json`, `architecture/dependency-review.md`, `tooling/supply-chain/verify-pins.mjs`, `02-SIGNING-DECISION.md`.
- New fields: Phase 2 dependency group, verdict, approval timestamp/reference, signing provider, certificate type/identity, timestamp URL, custody boundary, CI retrieval boundary, rotation/revocation owner.
- CLI flags: existing `--check` remains authoritative; no new install flag.
- Symbols: deterministic Phase 2 allowlist/report helpers inside `verify-pins.mjs`.

<verification>
The supply-chain verifier passes without a package installation, and both human decisions are recorded.
</verification>

<success_criteria>
Plan 02-02 may install only explicitly approved identities, and Plan 02-12 has an actionable signing contract.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-01-SUMMARY.md` when done.</output>
