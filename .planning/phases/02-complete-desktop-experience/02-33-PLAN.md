---
phase: 02-complete-desktop-experience
plan: "33"
type: execute
wave: 13
depends_on: ["02-01", "02-16", "02-25"]
files_modified:
  - architecture/signed-desktop-artifact.schema.json
  - tooling/desktop-evidence/package-signed-desktop.mjs
  - tooling/desktop-evidence/package-signed-desktop.test.mjs
  - quality/evidence/phase-02/environment/windows-10-image.json
  - quality/evidence/phase-02/environment/windows-11-image.json
  - quality/evidence/phase-02/environment/signing-access.json
  - quality/evidence/phase-02/artifacts/signed-desktop-package.json
autonomous: false
requirements: [UX-01, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "A terminating pinned Tauri build signs and timestamps both the installer and executable without elevating the UI."
    - "Packaging fails closed until exact reviewed Windows 10, Windows 11, and real non-secret signing-access records pass the Wave 0 environment gate."
    - "One schema-valid artifact record binds publisher, timestamp, signature chain, channel, version, Windows compatibility, staged paths, and exact SHA-256 hashes without certificate or private-key material."
  artifacts:
    - path: tooling/desktop-evidence/package-signed-desktop.mjs
      provides: Fail-closed Tauri build, Authenticode signing, timestamping, verification, hashing, and staging workflow
    - path: architecture/signed-desktop-artifact.schema.json
      provides: Exact signed desktop artifact evidence contract
    - path: quality/evidence/phase-02/artifacts/signed-desktop-package.json
      provides: Signed installer and executable identity, staged paths, metadata, and SHA-256 hashes
  key_links:
    - from: .planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
      to: quality/evidence/phase-02/environment/signing-access.json
      via: approved provider, certificate identity, timestamp service, custody, and access boundary
      pattern: "provider|certificateIdentity|timestampService|custody"
    - from: quality/evidence/phase-02/environment/signing-access.json
      to: tooling/desktop-evidence/package-signed-desktop.mjs
      via: reviewed non-secret signing authority selected before execute mode
      pattern: "reviewed|certificateIdentity|timestampService"
    - from: tooling/desktop-evidence/package-signed-desktop.mjs
      to: quality/evidence/phase-02/artifacts/signed-desktop-package.json
      via: verified signatures and hashes for the exact staged installer and executable
      pattern: "installerSha256|executableSha256|stagedPath"
---

<objective>
Build, Authenticode-sign, timestamp, verify, hash, and stage the exact installable Tauri artifact after the native UI and signing gate are complete.

Purpose: Give manual and packaged acceptance one immutable artifact identity instead of an inferred or ad hoc build.
Output: Reviewed environment/signing records, a tested fail-closed packaging workflow, and the exact signed desktop artifact evidence record.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@apps/desktop/src-tauri/tauri.conf.json
@apps/desktop/package.json
@tooling/desktop-evidence/verify-packaged-wave-zero.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the signed artifact schema and fail-closed dry-run workflow</name>
  <files>architecture/signed-desktop-artifact.schema.json, tooling/desktop-evidence/package-signed-desktop.mjs, tooling/desktop-evidence/package-signed-desktop.test.mjs</files>
  <read_first>02-SIGNING-DECISION.md; Tauri config/package scripts; Plan 02-16 environment/reviewed CLI contract; existing evidence schemas and deterministic verifier patterns</read_first>
  <behavior>Tests first require dry-run to resolve the pinned `@tauri-apps/cli` Tauri build path, expected installer/executable outputs, approved signing-access fields, publisher, timestamp service, channel, version, and Windows compatibility without building or signing. Missing/unreviewed access, signing-decision drift, elevated UI configuration, non-Windows execution, absent output classes, duplicate paths, invalid signature metadata, hash/path mismatch, or secret-shaped fields fail with stable diagnostics. A valid completed record requires separately verified installer and executable publisher, timestamp, signature-chain status, staged path, size, and SHA-256.</behavior>
  <action>Write `package-signed-desktop.test.mjs` first with schema and dry-run mutation fixtures, then define the strict JSON schema and implement `package-signed-desktop.mjs`. Expose `--dry-run` for bounded validation and `--execute` for the post-checkpoint workflow. Resolve the build through the lockfile-pinned desktop package CLI, require the non-elevated Tauri configuration, accept signing authority only through the reviewed non-secret record and approved custody adapter, and reject any evidence or argument containing certificate bytes, private-key material, passwords, PINs, tokens, or secret paths. Keep the test/dry-run command under 120 seconds and side-effect free.</action>
  <acceptance_criteria>The focused test is created before execute mode, fails for the intended missing workflow, then passes every schema/dry-run mutation under 120 seconds without creating an installer, signature, staged artifact, or secret-bearing record.</acceptance_criteria>
  <verify><automated>rtk node --test tooling/desktop-evidence/package-signed-desktop.test.mjs</automated></verify>
  <done>The packaging contract and dry-run are deterministic, bounded, non-elevating, and fail closed before real signing access.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve exact Windows images and confirm actual signing access</name>
  <files>quality/evidence/phase-02/environment/windows-10-image.json, quality/evidence/phase-02/environment/windows-11-image.json, quality/evidence/phase-02/environment/signing-access.json</files>
  <read_first>RESEARCH Environment Availability/Open Questions; packaged Windows matrix; 02-SIGNING-DECISION.md; Task 1 schema/dry-run diagnostics</read_first>
  <action>Require the human to select and review exact supported Windows 10 and Windows 11 image IDs/builds, runner provenance, WebView2 baseline, snapshot/reset method, and support status. Confirm actual access to the approved Authenticode provider/certificate identity and reachable timestamp service through the recorded custody boundary. Record status, provenance, observer, and timestamp plus non-secret identities only. Leave any unresolved fact unresolved and do not place certificate/private-key material or access secrets in the repository or evidence.</action>
  <acceptance_criteria>Exactly three environment records name real reviewed Windows 10, Windows 11, and signing prerequisites with provenance, observer, timestamp, and no secret material; unresolved signing access blocks Task 3.</acceptance_criteria>
  <verify><automated>rtk node tooling/desktop-evidence/verify-packaged-wave-zero.mjs --environment quality/evidence/phase-02/environment --require-reviewed</automated></verify>
  <done>The exact environment and signing authority are reviewed, or packaging remains blocked.</done>
</task>

<task type="auto">
  <name>Task 3: Build, sign, timestamp, verify, hash, and stage the Tauri package</name>
  <files>tooling/desktop-evidence/package-signed-desktop.mjs, quality/evidence/phase-02/artifacts/signed-desktop-package.json</files>
  <read_first>Task 1 workflow/schema; three reviewed environment records; approved signing decision; pinned desktop package and Tauri configuration</read_first>
  <action>Run execute mode only after Task 2 passes. Invoke the lockfile-pinned Tauri package/build path and preserve the non-elevated renderer/host boundary. Use the approved custody adapter and actual signing-access record to Authenticode-sign and RFC 3161 timestamp both the installable package and the shipped executable. Verify the expected publisher, timestamp, complete signature chain, channel, version, and Windows compatibility metadata from the produced binaries; reject unsigned, partially signed, mismatched, expired/revoked, untrusted, or unstaged outputs. Stage the exact artifacts under a bounded Phase 2 evidence directory, compute SHA-256 and size from the staged bytes, and write `signed-desktop-package.json` with build/signing provenance and paths relative to the repository. Never copy or serialize certificate/private-key material, secrets, provider tokens, passwords, PINs, or secret locations.</action>
  <acceptance_criteria>The workflow terminates with one schema-valid record whose installer and executable staged paths resolve, whose recomputed SHA-256 hashes match, whose publisher/timestamp/signature chain and channel/version/compatibility checks pass, and whose evidence contains no secret material. Any unresolved access or verification failure produces no accepted artifact record.</acceptance_criteria>
  <verify><automated>rtk node tooling/desktop-evidence/package-signed-desktop.mjs --execute --environment quality/evidence/phase-02/environment --output quality/evidence/phase-02/artifacts/signed-desktop-package.json</automated></verify>
  <done>The exact signed installable artifact is staged and immutably identified for manual and packaged acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signing custody to build workflow | External certificate authority and protected key access authorize distributable Windows code. |
| Build outputs to evidence record | Unverified, substituted, or rehashed bytes could masquerade as the reviewed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-45 | Spoofing/Tampering | signed desktop package | mitigate | Verify publisher, timestamp, complete chain, metadata, staged bytes, and exact SHA-256 before recording acceptance identity. |
| T-02-46 | Information disclosure | signing access/evidence | mitigate | Non-secret identifiers only; protected custody adapter; reject secret-shaped fields and never serialize key material. |
| T-02-47 | Elevation | Tauri build configuration | mitigate | Assert the renderer/UI remains non-elevated and reject elevated manifests/configuration before package execution. |
</threat_model>

<verification>
Run the focused schema/dry-run tests under 120 seconds, validate the exact reviewed environment records, then execute the post-checkpoint build/sign/timestamp/hash/stage workflow and independently recompute both staged hashes.
</verification>

<success_criteria>
Plans 02-26 and 02-27 consume one exact reviewed, signed, timestamped, non-elevated Tauri artifact record rather than selecting or building their own package.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-33-SUMMARY.md` when done.</output>
