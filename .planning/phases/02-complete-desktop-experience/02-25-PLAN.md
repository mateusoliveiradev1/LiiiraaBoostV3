---
phase: 02-complete-desktop-experience
plan: "25"
type: execute
wave: 12
depends_on: ["02-21", "02-23", "02-32"]
files_modified:
  - apps/desktop/src/native/shell-bridge.ts
  - apps/desktop/src/native/shell-bridge.test.ts
  - apps/desktop/src/features/startup.tsx
  - apps/desktop/src/features/startup.test.tsx
  - apps/desktop/src/features/installer-handoff.tsx
  - apps/desktop/src/app.tsx
  - apps/desktop/src/app.test.tsx
autonomous: true
requirements: [UX-01, UX-04, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The renderer subscribes to every generated native navigation, startup, tray, notification, window-state, and installer-identity event after runtime validation."
    - "Locale, tray opt-in, close context, notification preference, and window commands return to the host as validated generated commands."
    - "Signed-installer publisher/version/channel/compatibility handoff, splash, first launch, and every startup failure are user-visible."
  artifacts:
    - path: apps/desktop/src/native/shell-bridge.ts
      provides: Validated bidirectional renderer/native bridge
    - path: apps/desktop/src/features/startup.tsx
      provides: Splash, first-launch, and startup-failure UI
    - path: apps/desktop/src/features/installer-handoff.tsx
      provides: Signed installer identity and compatibility handoff
  key_links:
    - from: packages/contracts-ts/src/validation.ts
      to: apps/desktop/src/native/shell-bridge.ts
      via: validate before route, state, preference, or command dispatch
      pattern: "validateHostToRendererShellEvent|validateRendererToHostShellCommand"
    - from: apps/desktop/src/native/shell-bridge.ts
      to: apps/desktop/src/routes.tsx
      via: validated navigation event subscription
      pattern: "navigation"
---
<objective>Wire the generated native shell protocol into visible renderer navigation, preferences, installer, splash, and startup-failure flows.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@packages/contracts-ts/src/validation.ts
@apps/desktop/src/routes.tsx
@apps/desktop/src/preferences.tsx
@apps/desktop/src/app.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the validated bidirectional shell bridge</name>
  <files>apps/desktop/src/native/shell-bridge.ts, apps/desktop/src/native/shell-bridge.test.ts</files>
  <read_first>generated TS shell models/validators; Tauri listen/invoke package boundary; native event/command registrations; route/preference APIs</read_first>
  <behavior>Validated navigation/startup/tray/notification/window/installer events dispatch to typed renderer handlers; locale, tray, close, notification, and window commands validate before invoke; invalid payloads emit bounded diagnostics and cause no state change.</behavior>
  <action>Build one owned bridge using generated types and public validators. Subscribe to every host event, validate `unknown`, and then dispatch to routes/features. Construct every renderer command with the generated union, validate it, and only then invoke the host. Provide explicit unlisten/disposal and deduplicate event registration.</action>
  <acceptance_criteria>Every protocol variant has a handler/command test, invalid values never reach app state or native invoke, and no handwritten DTO mirrors the protocol.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test -- --run -t "shell bridge"</automated></verify>
  <done>The renderer/native bridge is complete, validated, and disposable.</done>
</task>
<task type="auto">
  <name>Task 2: Build installer identity, splash, first-launch, and startup-failure surfaces</name>
  <files>apps/desktop/src/features/startup.tsx, apps/desktop/src/features/startup.test.tsx, apps/desktop/src/features/installer-handoff.tsx</files>
  <read_first>UI-SPEC UX-01 signed installer handoff/startup states; generated startup/installer identity; localization catalogs</read_first>
  <action>Render publisher, version, channel, supported Windows compatibility, signature/update identity, and safe continuation before/at first launch. Implement splash and visible states for initializing, ready, missing WebView2, damaged install, unsupported build, migration failure, update-in-progress, signature-invalid, rollback, and safe mode. Every failure gets localized reason, safe action, documentation/support route, and no fabricated success.</action>
  <acceptance_criteria>All generated startup and installer identity variants have localized accessible UI, exact publisher/version/channel/compatibility fields, and safe actions.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test -- --run -t "startup|installer handoff"</automated></verify>
  <done>UX-01 installer and startup handoffs are visible rather than native-only.</done>
</task>
<task type="auto">
  <name>Task 3: Wire native events and preferences into application composition</name>
  <files>apps/desktop/src/app.tsx, apps/desktop/src/app.test.tsx</files>
  <read_first>Task 1 bridge; Task 2 surfaces; route tree; preference provider; shell composition</read_first>
  <action>Mount/dispose the bridge at the application root, connect validated navigation to the typed router, connect startup/tray/notification/window events to visible features/Activity, and send locale, tray opt-in, close context, notification, and window preferences to the host. Ensure the splash precedes the full route shell and startup failures remain operable.</action>
  <acceptance_criteria>Every native event produces a user-visible tested effect, every preference reaches a validated host command, and listener cleanup prevents duplicate behavior.</acceptance_criteria>
  <verify><automated>rtk pnpm --filter @liiiraa/desktop test -- --run -t "native composition smoke"</automated></verify>
  <done>Native lifecycle and renderer UX are wired end to end.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Tauri event/invoke transport to renderer state | Unknown cross-process data could navigate or change preferences. |
| Installer/startup metadata to user trust | Unsigned or incompatible artifacts could be represented as safe. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-39 | Tampering/Elevation | shell bridge | mitigate | Generated closed unions, runtime validation before dispatch/invoke, bounded errors. |
| T-02-40 | Spoofing | installer/startup UI | mitigate | Generated identity fields, signature state, explicit compatibility, no success inference. |
</threat_model>
## Artifacts
- Symbols: `createShellBridge`, `StartupSurface`, `InstallerHandoff`.
- Connections: every generated host event and renderer command to a concrete app consumer/publisher.
<verification>Run focused bridge/startup/composition tests; run desktop package check and native integration smoke as wave gate.</verification>
<success_criteria>The real Tauri host behavior is visible and controllable through one validated renderer bridge.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-25-SUMMARY.md` when done.</output>
