---
phase: 02-complete-desktop-experience
plan: "14"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - architecture/module-boundaries.json
  - pnpm-workspace.yaml
  - packages/design-tokens/package.json
  - packages/design-system/package.json
  - packages/feature-shell/package.json
  - apps/desktop/package.json
  - tooling/architecture-tests/src/check-workspace.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The four package manifests are created in the same task that activates their canonical owners and pnpm workspace discovery."
    - "Only the human-approved Phase 2 dependency identities and exact versions enter the lockfile."
    - "Dependencies are owned by the narrowest package and production packages do not depend on desktop-simulator."
  artifacts:
    - path: apps/desktop/package.json
      provides: Reviewed desktop composition dependencies
    - path: architecture/module-boundaries.json
      provides: Active uniquely owned Phase 2 TypeScript roots
    - path: pnpm-workspace.yaml
      provides: Live discovery for the four manifest-backed roots
  key_links:
    - from: architecture/dependency-review.md
      to: apps/desktop/package.json
      via: exact approved registry identities and versions
      pattern: "react|tauri|storybook|playwright"
---

<objective>
Create the four TypeScript manifests and atomically activate their canonical ownership and workspace discovery using only the dependency set approved in Plan 02-01.

Purpose: Ensure no workspace root becomes active before its manifest exists while giving lockfile resolution exact package-owned dependencies.
Output: Manifest-backed active roots with executable discovery, ownership, and identity checks.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@architecture/dependency-review.md
@architecture/module-boundaries.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create manifests and atomically activate live ownership</name>
  <files>architecture/module-boundaries.json, pnpm-workspace.yaml, packages/design-tokens/package.json, packages/design-system/package.json, packages/feature-shell/package.json, apps/desktop/package.json, tooling/architecture-tests/src/check-workspace.test.ts</files>
  <read_first>02-RESEARCH.md Standard Stack, Installation Boundaries, and Package Legitimacy Audit; approved dependency review; existing workspace manifests; architecture/module-boundaries.json; pnpm-workspace.yaml; Plan 02-02 reservation and ownership tests</read_first>
  <behavior>The focused `workspace discovery`, `package ownership`, and `approved manifest identities` cases exist; each of the four manifest-backed roots is discovered exactly once, active, uniquely owned, and constrained to its approved public root and direct identities.</behavior>
  <action>Create the four package manifests with exact approved identities and legal design-to-feature-to-composition dependencies. In the same task, change only `design-tokens`, `design-system`, `feature-shell`, and `desktop-app` from reserved to active and add their manifest-backed roots to pnpm discovery. Extend the architecture test file with the exact three focused cases named in behavior, consuming live workspace metadata and the approved dependency review. Keep `msw`, the nonexistent single-instance npm plugin, and production desktop-simulator dependencies absent. Do not edit root `Cargo.toml`.</action>
  <acceptance_criteria>All four manifests exist before live discovery is evaluated; the roots are discovered exactly once, each has one active owner/public root, direct identities are approved, and the Cargo workspace is unchanged.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "workspace discovery|package ownership"</automated></verify>
  <done>Manifest creation, canonical ownership activation, and pnpm discovery form one valid workspace transition.</done>
</task>

<task type="auto">
  <name>Task 2: Verify manifest ownership and approved direct identities</name>
  <files>packages/design-tokens/package.json, packages/design-system/package.json, packages/feature-shell/package.json, apps/desktop/package.json</files>
  <read_first>Task 1 manifests; architecture/dependency-review.md; module ownership rules</read_first>
  <action>Check every direct identity/version against the approved review, ensure dependency ownership follows design to feature to composition direction, and reject simulator production dependencies or lifecycle exceptions. Leave lockfile resolution to Plan 02-31.</action>
  <acceptance_criteria>All direct dependencies match approval and package ownership before lockfile mutation.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "approved manifest identities"</automated></verify>
  <done>The approved direct dependency graph is ready for deterministic lockfile resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registry and lockfile to renderer build | Third-party code enters the desktop distribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | Tampering | pnpm lockfile | mitigate | Exact reviewed versions, frozen install, and denied lifecycle scripts. |
| T-02-SC | Tampering | package installation | mitigate | Plan depends on the blocking approval and identity review in 02-01. |
</threat_model>

## Artifacts

- Files: the four package manifest/config pairs and desktop Vite config.
- Fields: exact dependency versions, package exports, strict compiler options.
- Connections: approved dependency evidence to lockfile identities.

<verification>
Run focused manifest identity/ownership checks and the architecture suite.
</verification>

<success_criteria>
Plan 02-31 can resolve a deterministic lockfile from exact package-owned dependencies.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-14-SUMMARY.md` when done.</output>
