---
phase: 02-complete-desktop-experience
plan: "14"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/design-tokens/package.json
  - packages/design-tokens/tsconfig.json
  - packages/design-system/package.json
  - packages/design-system/tsconfig.json
  - packages/feature-shell/package.json
  - packages/feature-shell/tsconfig.json
  - apps/desktop/package.json
  - apps/desktop/tsconfig.json
  - apps/desktop/vite.config.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "Only the human-approved Phase 2 dependency identities and exact versions enter the lockfile."
    - "Dependencies are owned by the narrowest package and production packages do not depend on desktop-simulator."
  artifacts:
    - path: apps/desktop/package.json
      provides: Reviewed desktop composition dependencies
    - path: apps/desktop/package.json
      provides: Desktop composition dependency boundary
  key_links:
    - from: architecture/dependency-review.md
      to: apps/desktop/package.json
      via: exact approved registry identities and versions
      pattern: "react|tauri|storybook|playwright"
---

<objective>
Scaffold the four TypeScript packages and install only the dependency set approved in Plan 02-01.

Purpose: Give later contract, test, design, and feature plans strict package-owned dependencies without mixing lifecycle orchestration into installation.
Output: Strict manifests, compiler configurations, and Vite configuration ready for lockfile resolution.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@architecture/dependency-review.md
@architecture/module-boundaries.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold strict package-owned manifests and compiler boundaries</name>
  <files>packages/design-tokens/package.json, packages/design-tokens/tsconfig.json, packages/design-system/package.json, packages/design-system/tsconfig.json, packages/feature-shell/package.json, packages/feature-shell/tsconfig.json, apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/vite.config.ts</files>
  <read_first>02-RESEARCH.md Standard Stack, Installation Boundaries, and Package Legitimacy Audit; approved dependency review; existing workspace package manifests; tsconfig.base.json</read_first>
  <action>Create strict package manifests and exact dependencies in their owning packages: tokens own no renderer behavior; design-system owns React Aria, Tailwind, icons, and chart behavior; feature-shell owns React, XState, Query, forms, and desktop-client; desktop composition owns React DOM, TanStack Router, Vite, Tauri JS, and test composition. Use React Intl/FormatJS as recorded by the approved review. Keep `msw` and the nonexistent single-instance npm plugin absent, and preserve public-root-only exports.</action>
  <acceptance_criteria>All four packages have strict configs, exact approved identities, legal dependency direction, and no production dependency on desktop-simulator.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "package ownership"</automated></verify>
  <done>Package manifests encode the approved ownership and dependency graph.</done>
</task>

<task type="auto">
  <name>Task 2: Verify manifest ownership and approved direct identities</name>
  <files>packages/design-tokens/package.json, packages/design-system/package.json, packages/feature-shell/package.json, apps/desktop/package.json</files>
  <read_first>Task 1 manifests; architecture/dependency-review.md; module ownership rules</read_first>
  <action>Check every direct identity/version against the approved review, ensure dependency ownership follows design to feature to composition direction, and reject simulator production dependencies or lifecycle exceptions. Leave lockfile resolution to Plan 02-31.</action>
  <acceptance_criteria>All direct dependencies match approval and package ownership before lockfile mutation.</acceptance_criteria>
  <verify><automated>rtk pnpm test:architecture -- --run -t "approved manifest identities"</automated></verify>
  <done>The approved direct dependency graph is ready for deterministic lockfile resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registry and lockfile to renderer build | Third-party code enters the desktop distribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | Tampering | pnpm lockfile | mitigate | Exact reviewed versions, frozen install, and denied lifecycle scripts. |
| T-02-SC | Tampering | package installation | mitigate | Plan depends on the blocking approval and identity review in 02-01. |
</threat_model>

## Artifacts

- Files: the four package manifest/config pairs and desktop Vite config.
- Fields: exact dependency versions, package exports, strict compiler options.
- Connections: approved dependency evidence to lockfile identities.

<verification>
Run focused manifest identity/ownership checks and the architecture suite.
</verification>

<success_criteria>
Plan 02-31 can resolve a deterministic lockfile from exact package-owned dependencies.
</success_criteria>

<output>Create `.planning/phases/02-complete-desktop-experience/02-14-SUMMARY.md` when done.</output>
