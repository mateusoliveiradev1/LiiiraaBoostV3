---
phase: 02-complete-desktop-experience
plan: "12"
type: execute
wave: 5
depends_on: ["02-31", "02-32"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/build.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/tests/shell_contract.rs
autonomous: true
requirements: [UX-01, UX-07, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The desktop Rust crate is created and registered in Cargo atomically, so the workspace never names a nonexistent member."
    - "The host is non-elevated, deny-by-default, fixture-refusing, and exposes no generic mutation RPC."
    - "Native commands and events use generated shell transports and validate unknown values before dispatch."
  artifacts:
    - path: apps/desktop/src-tauri/Cargo.toml
      provides: Real Tauri desktop crate with generated contract dependency
    - path: apps/desktop/src-tauri/capabilities/default.json
      provides: Least-privilege capability boundary
  key_links:
    - from: Cargo.toml
      to: apps/desktop/src-tauri/Cargo.toml
      via: same-task workspace registration
      pattern: "apps/desktop/src-tauri"
    - from: crates/contracts-rust/src/generated.rs
      to: apps/desktop/src-tauri/src/main.rs
      via: generated validated shell envelopes
      pattern: "HostToRendererShellEvent|RendererToHostShellCommand"
---
<objective>Create and register the real least-privilege Tauri host atomically, consuming generated shell contracts.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@.planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
@Cargo.toml
@crates/contracts-rust/src/lib.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create and register the Rust crate in one workspace change</name>
  <files>Cargo.toml, Cargo.lock, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/build.rs, apps/desktop/src-tauri/src/main.rs</files>
  <read_first>root Cargo workspace; generated Rust shell contracts/validators; signing decision; Tauri pinned dependencies</read_first>
  <behavior>`cargo metadata` resolves before and after the task; `liiiraa-desktop` depends on `liiiraa-contracts-rust`; host startup refuses production fixture mode and registers bounded shell dispatch only.</behavior>
  <action>Create the crate, add exact approved Rust dependencies, register `apps/desktop/src-tauri` in root `Cargo.toml` in the same task, and update the lockfile. Build a non-elevated Tauri entrypoint that accepts/emits only generated messages after runtime validation. Register no optimizer, privileged service, generic file, registry, service, script, or mutation command.</action>
  <acceptance_criteria>`cargo metadata` and focused host tests resolve the package; no intermediate or final workspace references a missing member.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- host_boundary</automated></verify>
  <done>The desktop crate exists, is registered, and consumes generated validated transports.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Lock least-privilege capabilities and signed bundle identity</name>
  <files>apps/desktop/src-tauri/capabilities/default.json, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/tests/shell_contract.rs</files>
  <read_first>UI-SPEC UX-01; RESEARCH Security Domain; signing decision; Task 1 host</read_first>
  <behavior>Tests reject capability broadening, generic system mutation methods, elevation assumptions, unsigned release identity, and production fixture mode.</behavior>
  <action>Define minimal Tauri capabilities, CSP, bundle publisher/version/channel/compatibility metadata, signed updater identity, 1280x800 initial and 760x600 minimum configuration, and bounded startup states. Add contract tests over config and command/event registration.</action>
  <acceptance_criteria>Capability broadening/mutation additions fail, signed identity fields are present, and the host remains non-elevated.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- capabilities</automated></verify>
  <done>The host configuration is least-privilege and buildable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer to Tauri host | Untrusted renderer requests native behavior. |
| Cargo workspace to desktop crate | A native executable becomes a workspace authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-24 | Elevation/Tampering | capabilities and commands | mitigate | Deny-by-default operation-specific permissions and generated validated messages. |
| T-02-SC | Tampering | Cargo dependencies | mitigate | Exact approved crates and atomic workspace registration. |
</threat_model>
## Artifacts
- Symbols: native `ShellContract` dispatch over generated envelopes.
- Fields: capability permissions, bundle publisher/version/channel/compatibility, updater identity.
<verification>Run focused Rust host/capability tests, cargo metadata/check/clippy/fmt as wave gate.</verification>
<success_criteria>The real non-elevated Windows host is buildable without Cargo ordering defects or handwritten cross-process DTOs.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-12-SUMMARY.md` when done.</output>
