---
phase: 02-complete-desktop-experience
plan: "12"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/build.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/src/window.rs
  - apps/desktop/src-tauri/src/navigation.rs
  - apps/desktop/src-tauri/src/tray.rs
  - apps/desktop/src-tauri/src/notifications.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/tauri.test.conf.json
  - apps/desktop/src-tauri/tests/shell_contract.rs
autonomous: true
requirements: [UX-01, UX-07, UX-09, UX-10, UX-11, UX-12]
must_haves:
  truths:
    - "The real Tauri 2 shell starts non-elevated, deny-by-default, with no generic registry/file/service/script RPC."
    - "Native window, single instance, allowlisted deep links, tray, notification, updater/signature failure, and WebView2 startup states follow the UI-SPEC."
    - "D-19 close exits unless explicit tray opt-in; recovery preview close offers only keep-running/stay."
    - "Native notifications are emitted only for the UX-09 actionable policy categories."
  artifacts:
    - path: apps/desktop/src-tauri/capabilities/default.json
      provides: Least-privilege Tauri capabilities
    - path: apps/desktop/src-tauri/src/main.rs
      provides: Native shell/plugin composition
  key_links:
    - from: apps/desktop/src-tauri/src/navigation.rs
      to: apps/desktop/src/routes.tsx
      via: allowlisted navigation intent event
      pattern: "NavigationIntent"
---

<objective>
Build the real installable Tauri shell and native Windows behavior, implementing UX-01 and D-19 without granting optimization authority.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@.planning/phases/02-complete-desktop-experience/02-RESEARCH.md
@.planning/phases/02-complete-desktop-experience/02-SIGNING-DECISION.md
@Cargo.toml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and implement least-privilege native shell contracts</name>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/build.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/capabilities/default.json, apps/desktop/src-tauri/tests/shell_contract.rs</files>
  <read_first>UI-SPEC UX-01/window behavior; RESEARCH Security Domain/Tauri capabilities; root Cargo workspace; signing decision</read_first>
  <behavior>Tests assert non-elevation assumptions, capability allowlist, absence of generic system mutation methods, single-instance plugin registration order, production fixture refusal configuration, and bounded error events.</behavior>
  <action>Write Rust contract tests first, then create Tauri 2 host with exact approved core/plugins, minimal capability file, production/test configs, CSP, bounded initialization states, and no privileged service/optimizer command. Register single-instance first and keep the WebView process non-elevated.</action>
  <acceptance_criteria>Rust tests reject capability broadening and mutation/RPC command additions; app configuration identifies a real signed bundle path and separate test scenario mode.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract</automated></verify>
  <done>The native host and capability boundary are executable, least-privilege, and mutation-free.</done>
</task>
<task type="auto">
  <name>Task 2: Implement native window, single-instance, deep-link, and close behavior</name>
  <files>apps/desktop/src-tauri/src/window.rs, apps/desktop/src-tauri/src/navigation.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/tauri.test.conf.json</files>
  <read_first>UI-SPEC window behavior/title bar/responsive widths/keyboard; D-19; Tauri official patterns captured in RESEARCH.md</read_first>
  <action>Implement 1280×800 centered first open, 760×600 minimum, work-area-clamped window-state restore, native minimize/maximize/close/snap/Alt+Space semantics, drag exclusions, double-click maximize, single-instance focus, and allowlisted benign deep-link navigation intents. Per D-19, close exits by default and hides to tray only after explicit preference; recovery-preview close exposes keep-running/stay rather than termination.</action>
  <acceptance_criteria>Window behavior matches the contract, second launch focuses one instance, malformed/risky deep links reject, and close behavior follows preference/recovery state exactly.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- window_navigation</automated></verify>
  <done>Native window/navigation lifecycle is deterministic and preserves Windows behavior.</done>
</task>
<task type="auto">
  <name>Task 3: Implement tray, notification, update, and startup failure bridges</name>
  <files>apps/desktop/src-tauri/src/tray.rs, apps/desktop/src-tauri/src/notifications.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/tauri.conf.json</files>
  <read_first>UI-SPEC tray contract, UX-09, startup/update states; interaction feedback policy; D-19</read_first>
  <action>Implement the exact tray menu/order/accessible tooltip and native event bridge, plus allowlisted notifications only for recovery required, chosen restart deadline, profile restore failure, signed update action, and account security. Surface missing WebView2, damaged install, unsupported build, migration failure, update-in-progress, signature-invalid, rollback, and safe-mode startup states. Do not include sensitive hardware data or claim update success.</action>
  <acceptance_criteria>Tray appears only after opt-in or safety workflow, notifications honor approved categories/preferences/Focus Assist, and every startup/update failure has a safe UI event/action.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- tray_notification_startup</automated></verify>
  <done>Tray, notification, and startup/update native bridges satisfy the locked policy.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| WebView renderer → Tauri host | Untrusted renderer requests native behavior. |
| Deep link/second instance → active app | External arguments enter navigation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-24 | Elevation/Tampering | capabilities | mitigate | Deny-by-default operation-specific permissions; no mutation commands. |
| T-02-25 | Tampering | deep links | mitigate | Allowlisted routes/benign IDs, full validation, no privileged intent. |
| T-02-26 | Disclosure | native notifications | mitigate | Approved categories and redacted content only. |
</threat_model>

## Artifacts this phase produces
- Files: all frontmatter paths.
- Symbols: Rust `main`, `configure_window`, `parse_navigation_intent`, `build_tray_menu`, `map_native_notification`, `ShellContract`.
- Fields: Tauri capability permissions, bundle/signing/update config, window dimensions/state, navigation intent, tray preference/state, notification category.
- CLI flags: Tauri build/test configuration flags exposed through package scripts only.
<verification>Run Rust shell tests, cargo check/clippy/fmt, and Tauri config validation.</verification>
<success_criteria>The real non-elevated Windows shell is buildable and exposes only safe native behavior.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-12-SUMMARY.md` when done.</output>
