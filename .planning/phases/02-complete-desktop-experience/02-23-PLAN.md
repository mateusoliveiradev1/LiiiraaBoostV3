---
phase: 02-complete-desktop-experience
plan: "23"
type: execute
wave: 7
depends_on: ["02-22", "02-32"]
files_modified:
  - apps/desktop/src-tauri/src/tray.rs
  - apps/desktop/src-tauri/src/notifications.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/tests/shell_contract.rs
autonomous: true
requirements: [UX-01, UX-07, UX-09, UX-11, UX-12]
must_haves:
  truths:
    - "Tray, notifications, startup, update, and failure events consume/emits generated validated messages."
    - "Tray appears only after opt-in or safety workflow; notifications use approved redacted categories."
  artifacts:
    - path: apps/desktop/src-tauri/src/tray.rs
      provides: Generated-contract tray bridge
    - path: apps/desktop/src-tauri/src/notifications.rs
      provides: Generated-contract notification/startup bridge
  key_links:
    - from: crates/contracts-rust/src/generated.rs
      to: apps/desktop/src-tauri/src/notifications.rs
      via: generated startup and notification values
      pattern: "ShellStartupState|ShellNotificationCategory"
---
<objective>Implement tray, notifications, startup, update, and failure bridges over generated shell contracts.</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-complete-desktop-experience/02-UI-SPEC.md
@packages/feature-shell/src/model/interaction-policy.ts
@crates/contracts-rust/src/generated.rs
@apps/desktop/src-tauri/src/main.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement validated tray lifecycle and actions</name>
  <files>apps/desktop/src-tauri/src/tray.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/tests/shell_contract.rs</files>
  <read_first>UI-SPEC tray contract; D-19; generated tray preference/action messages; current main registration</read_first>
  <behavior>Tray menu/order/tooltip are exact; it appears only after validated opt-in or active safety workflow; actions emit generated events; unknown actions reject.</behavior>
  <action>Implement the tray menu and main registration using generated messages. Validate renderer preferences before changing tray state and never infer opt-in from close behavior.</action>
  <acceptance_criteria>Tray visibility/actions match policy and malformed messages have no side effects.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- tray</automated></verify>
  <done>Tray behavior is generated-contract-bound and D-19 compliant.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement validated notification and startup failure events</name>
  <files>apps/desktop/src-tauri/src/notifications.rs, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/tests/shell_contract.rs</files>
  <read_first>UI-SPEC UX-09/startup/update states; feedback policy; generated startup/notification/installer identity messages</read_first>
  <behavior>Only recovery required, chosen restart deadline, profile restore failure, signed update action, and account security notify; missing WebView2, damaged install, unsupported build, migration failure, update-in-progress, signature-invalid, rollback, and safe mode emit generated startup events with safe actions.</behavior>
  <action>Implement redacted allowlisted notifications and startup/update/failure event emission. Consume validated locale/notification preferences and generated installer identity; honor Focus Assist and never claim update success.</action>
  <acceptance_criteria>Approved categories/preferences work, sensitive hardware data never leaves the app, and each startup failure emits a safe generated state/action.</acceptance_criteria>
  <verify><automated>rtk cargo test -p liiiraa-desktop --test shell_contract -- notification_startup</automated></verify>
  <done>Native tray, notification, and startup bridges are complete and validated.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Host lifecycle to OS notification/tray surfaces | Sensitive or fabricated state could leave the app. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-26 | Disclosure/Spoofing | notifications/startup | mitigate | Generated allowlisted categories, redaction, validated preferences, explicit failure states. |
</threat_model>
## Artifacts
- Symbols: validated tray, notification, and startup event handlers.
- Connections: generated shell protocol to OS surfaces.
<verification>Run focused tray/notification/startup tests; run complete Rust shell suite as wave gate.</verification>
<success_criteria>All native host events required by the renderer bridge exist as validated generated values.</success_criteria>
<output>Create `.planning/phases/02-complete-desktop-experience/02-23-SUMMARY.md` when done.</output>
