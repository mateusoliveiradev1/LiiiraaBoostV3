---
quick_id: 260805-m3y
slug: preparar-staging-gratuito-corrigir-rende
status: planned
mode: quick
phase: quick-260805-m3y
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
created: 2026-08-05
files_modified:
  - apps/api/staging.render.yaml
  - apps/api/src/staging/container-contract.test.ts
  - .planning/STATE.md
---

# Preparar staging gratuito e alinhar a branch principal

<objective>
Corrigir o contrato de staging para a conta e o plano gratuitos realmente escolhidos, preservar o deploy imutável via GHCR e tornar `main` a branch principal usada pelos workflows do GitHub Actions.

Purpose: desbloquear a criação do staging sem custo e sem executar Docker localmente.
Output: manifesto Render e teste coerentes, branch `main` publicada e estado GSD registrado.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir o contrato gratuito do Render</name>
  <files>apps/api/staging.render.yaml, apps/api/src/staging/container-contract.test.ts</files>
  <action>Alterar o plano do serviço para `free` e apontar a imagem imutável para `ghcr.io/mateusoliveiradev1/liiiraa-boost-api`. Atualizar o teste do contrato para exigir explicitamente o plano gratuito e o proprietário real, preservando `autoDeploy: false`, o digest fornecido pelo CI, health check e isolamento de staging. Não iniciar, consultar ou usar Docker local.</action>
  <verify>
    <automated>rtk pnpm --filter @liiiraa/api test -- --run container-contract</automated>
  </verify>
  <done>Manifesto e teste concordam com Render Free e com o pacote GHCR do repositório real; o teste direcionado passa sem daemon local.</done>
</task>

<task type="auto">
  <name>Task 2: Publicar e tornar main a branch principal</name>
  <files>.planning/STATE.md</files>
  <action>Criar `main` no mesmo histórico de `master`, commitar somente os arquivos desta quick task, enviar `main` para `origin` e configurar `main` como branch padrão do repositório GitHub. Preservar `master` e todos os arquivos não rastreados do usuário. Não disparar deploy pago nem usar Docker local.</action>
  <verify>
    <automated>rtk git status --short --branch; rtk git ls-remote --heads origin main; rtk gh repo view mateusoliveiradev1/LiiiraaBoostV3 --json defaultBranchRef</automated>
  </verify>
  <done>`origin/main` existe, aponta para o commit preparado e é a branch padrão; `master` continua disponível.</done>
</task>

</tasks>

<success_criteria>
- O Render está configurado com `plan: free`.
- A imagem referencia `ghcr.io/mateusoliveiradev1/liiiraa-boost-api` por digest.
- O teste daemon-free do contrato passa.
- `main` existe local e remotamente e é a branch padrão do GitHub.
- Nenhum Docker local é iniciado ou consultado.
- Arquivos não rastreados preexistentes permanecem intactos e fora dos commits.
</success_criteria>

<output>
Ao concluir, criar `260805-m3y-SUMMARY.md`, atualizar `.planning/STATE.md` e registrar os commits e verificações executadas.
</output>
